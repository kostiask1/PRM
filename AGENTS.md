# AGENTS.md

Коротка практична карта проєкту для AI-агентів. Проєкт містить українські тексти; файли `js`, `jsx`, `html`, `css`, `scss`, `json`, `md` очікуються у UTF-8.

## Project Overview

- Назва з `package.json`: `dnd-session-manager`.
- Призначення: локальний менеджер кампаній і сесій D&D з кампаніями, сесіями, сценами, нотатками, персонажами, NPC, локаціями, енкаунтерами, бестіарієм, заклинаннями, зображеннями, дайсами й опційною Gemini AI-допомогою.
- Архітектура: React 18 + Vite frontend, Express backend, локальне файлове сховище. Зовнішньої БД немає.
- Основні залежності:
  - Frontend: `react`, `react-dom`, `react-list`, `react-markdown`.
  - Backend: `express`, `multer`, `dotenv`, `@google/generative-ai`.
  - Tooling: `vite`, `eslint`, `sass`, `concurrently`.
- AI потребує `GEMINI_API_KEY` у `.env`.

## Architecture Map

- `src/main.tsx` - strict TypeScript frontend entry point, монтує app і реєструє service worker.
- `src/App.tsx` - strict TypeScript кореневий React-компонент: завантажує кампанії/налаштування, компонує глобальні modal/message/dice hosts і вузький Editor mention-picker runtime bridge.
- `src/app/ui/MentionPickerModalHost.tsx` - app-owned selector/API/modal orchestration for the global mention-picker request; it stays inside `EditorMentionPickerRuntimeProvider` while editor promise/selection behavior remains feature-owned.
- `src/app/routing/MainContent.tsx` - strict TypeScript композиція маршрутів; нові екрани підключаються з `src/pages/*`.
- `src/app/model/appStore.ts` - strict TypeScript app-owned global-store composition and sole stateful store/selector/modal/navigation owner: state/listeners, thunk dispatch, `useSyncExternalStore`, modal resolvers, router adapter, and browser navigation. App/root consumers and app-owned runtime hosts import it through `src/app/model/index.js`.
- `src/shared/model/` - global action contracts/creators, action and state types, and lower-level reducer policies only. It must never contain a store, selectors, modal resolvers, router adapter, browser navigation, or an app-store compatibility facade/runtime port.
- `src/shared/api/httpClient.ts` - спільний HTTP transport для `/api/...`; consumers import it through `src/shared/api/index.ts`.
- `src/entities/*/api` та `src/features/*/api` - API-клієнти, що належать відповідному домену/use case.
- `src/entities/reference/api/referenceApi.ts` - reads for conditions, diseases, variant rules, skills, and senses; `entities/spell/api` owns spell endpoints only.
- `src/features/campaign/index.js` plus `index.d.ts` - minimal runtime/type public entry for campaign feature policies; external slices must not import `campaignStateUtils.ts` directly.
- `src/features/rules-reference/model.js` plus `model.d.ts` - Node-safe public entry for rules-reference parsing, preview loading, and input resolution; browser consumers use the slice root entry.
- Compatibility facade `src/api.js` видалено; кожен consumer імпортує API client з domain owner.
- `server/server.js` - Express entry point, монтує routes, віддає `dist/`.
- `server/http/requestValidation.js` plus `server/modules/*/http/*RequestSchemas.js` - shared validation mechanics and domain-owned HTTP mutation schemas.
- `server/storage.js` - основний файловий storage layer, нормалізація шляхів, JSON read/write, кампанії, сесії, entities, image refs, imports/exports, AI history.
- `server/modules/ai/infrastructure/bestiaryAiHistoryMigration.js` - canonical, retryable, non-destructive migration of legacy Bestiary AI history.
- `server/routes/*.js` - REST API:
  - `campaigns.js` - campaigns/entities/reorder/move/export.
  - `sessions.js` - session CRUD/reorder.
  - `ai.js` - Gemini, parsed AI operations, history, apply/undo.
  - `bestiary.js` - local bestiary, custom monsters, favorites.
  - `spells.js` - spells/conditions/diseases/rules/skills/senses.
  - `images.js` - image upload/list/move/delete/rename.
  - `backups.js` - full and partial archives/imports.
  - `settings.js` - app settings.
  - `assets.js` - static asset serving.
- `database/` - bundled D&D reference data (`bestiary`, `spells`, conditions, skills, senses, etc.).
- `data/` - local user data. Treat as user-owned runtime data, not source code.
- `tests/run-tests.mjs` - Node-based regression suite.
- `scripts/` - local helper scripts for env, project run, encoding/performance checks, database update/bundling.

## Main Features

### Campaign Workspace

- What: campaigns with story, notes, sessions, PCs, NPCs, locations/factions, graph, import/export.
- Main UI: `src/pages/campaign/ui/CampaignPage.tsx`.
- Main logic: `src/pages/campaign/model/useCampaignView.ts`, `src/entities/campaign/model/CampaignViewModel.ts`, `src/features/campaign/campaignStateUtils.ts`.
- App integration: `src/pages/campaign/model/CampaignPageRuntime.tsx` owns the page runtime contract; `src/app/ui/CampaignPageRuntimeHost.tsx` is the only store adapter and scopes it inside `CampaignRoute` after the active-campaign guard.
- Backend: `server/routes/campaigns.js`, `server/routes/backups.js`, `server/storage.js`.
- Important files: `src/widgets/campaign-entity-card`, `NoteCard.tsx`, `DraggableList.tsx`, `CampaignNotesGraph.tsx`, `GlobalSearchModal.tsx`, `PartialArchiveModal.tsx`.
- Campaign character/NPC/location CRUD frontend ownership is `src/features/campaign-entity`; creation payload cleanup and create/update/delete delegation must go through its client. `submitCreateEntity` receives its narrow `onRefreshEntities` command from the widget owner instead of importing the app store: an awaited custom `onCreate` returns without fallback creation or refresh; the default path awaits creation then invokes exactly one refresh; failed creation propagates without refresh. Do not reintroduce `app/model` or `shared/model` access inside the feature.
- `src/features/campaign-entity/model` is fully TypeScript. Extend entity IDs, payloads, setters, sanitizers, pending saves, reorder/move commands, and scope-modal contracts at this boundary; runtime consumers continue importing through `features/campaign-entity/index.js`.
- Optimistic collection changes, collapse persistence, rename confirmation/application, mention rename callbacks, deletion, and reload-on-error belong to `useCampaignEntityCollection`; debounced writes belong to `useCampaignEntityPersistence`.
- Character/NPC type movement and entity reorder/undo persistence belong to `useCampaignEntityOrdering`; page hooks only supply campaign state, reload callbacks, and user-facing errors.
- Campaign/session NPC and location scope changes must use the pure direction-specific plans plus `executeEntityScopeMove` in `features/campaign-entity/model/scopeMovement.ts`, composed by `useCampaignEntityScopeMovement`, and the backend `campaignEntityScopeCommands` transaction. Preserve string and zero-valued numeric IDs, string-compatible session entity lookup, confirmation before flushing, `throwOnError` pending-save flush before the command, flushed-session filename precedence, explicit cancelled/moved/failed outcomes, import-modal removal only after success, and backend compensation for partial filesystem failures. Do not duplicate the filesystem transaction or compensation in the frontend hook.
- Session/scene/note mutations belong to `features/session-editor`; use `useSessionEditing` for editing, `useSessionPersistence` for debounced writes, and `useSessionHistory` for undo/redo or AI replacement snapshots. Keep encounter creation/linking out of this slice.
- Pure session/scene/note mutation contracts live in `features/session-editor/model/sessionMutations.ts`. Preserve resource IDs, immutable updates, encounter cleanup when deleting linked scenes, virtual-note sanitization on reorder, and dynamic section-collapse keys.
- `src/features/session-editor/model` is fully TypeScript. Pending timer/ref ownership, debounce scheduling, instant flush, discard, and unmount flush stay in `useSessionPersistence.ts`; normalized throw/update-UI policy, one injected save attempt, rename notification, error callback/rethrow behavior, and saving-indicator finalization belong to `sessionPersistence.ts`. Preserve delayed-save ownership by the originating session, clearing the timer/pending payload before execution, no UI or rename callback for `updateUi: false`, rename only for a truthy changed filename, error notification before optional rethrow, saving reset in `finally`, flush-on-unmount behavior, history application suppression, distinct snapshot transitions, and external replacement discarding pending writes.
- Encounter creation/linking and encounter persistence belong to `features/encounter-editor` and backend session application commands. `useEncounterCreation` is only the React dependency adapter; existing-link/create/no-op planning, string-compatible scene lookup, flushed-session filename precedence, injected creation execution, runtime result validation, state replacement, navigation, and handled flush/API/result failures belong in `model/encounterCreation.ts`. Preserve cancellation before flush, immediate existing-link navigation without name/API calls, `throwOnError` pending-save flush, server-generated string/number IDs, idempotent scene linking, and request-name errors remaining with the requesting UI boundary. Page hooks must not read/modify/write whole sessions to save an encounter.
- Backend scene-encounter creation lives in `server/modules/session/application/createSceneEncounter.js` behind exactly the `createSceneEncounterCommand` and `findById` exports. `findById` retains `(items || []).find`, string-compatible first-match lookup, candidate-ID-before-requested-ID coercion for every visited row, falsy-collection recovery, and native failures for truthy malformed collections, null/sparse rows, Symbols, or custom coercion. The command destructures and reads the complete request before `repository.read`, then reads array-only scenes, rejects a missing scene with the exact status-404 error before encounter access/ID creation/write, and reads array-only encounters. Preserve the null/undefined link guard's two reads plus the lookup read. Existing links return the exact encounter identity and a fresh session spread with the requested filename without ID creation, name coercion, or persistence. Creation calls `repository.createId()` before `String(name || "").trim() || "Encounter"`, preserves generated IDs including numeric zero, appends a fresh encounter with an empty monsters array, links every string-compatible duplicate scene ID without mutating source collections, retains sparse scene holes and encounter spread semantics, writes exactly `(campaignSlug, fileName, nextSession)`, and returns the repository's saved-session identity. Keep repository method receiver binding, getter order, and sequential uncompensated failure behavior unchanged.
- Adding a Bestiary monster to an existing encounter must use `sessionApi.addEncounterMonster` and the backend `addEncounterMonster` application command. The modal may discover campaigns/sessions/encounters, but must not read-modify-write a whole session; the command validates the participant object, preserves existing monsters, matches encounter IDs by string value, and returns the focused monster/encounter/session result. `AddMonsterToEncounterModalContent` receives its narrow `AddMonsterToEncounterModalRuntime` from the Monster Stat Block widget rather than importing `app/model` or `shared/model`: preserve mounted-only loading errors, API success -> reload -> `closeModal(true)` order inside the same try block, add-failure notification -> unlock order, and the host's click-time filtered campaign snapshot.
- `src/features/encounter-editor/model` is fully TypeScript. Preserve server-generated encounter IDs, existing-link navigation without duplicate creation, pending-session flush before creation, debounced encounter writes, unmount flushing, custom-only participant refresh, pending-save sync guards, and selected-instance preservation.
- Encounter participant source loading and custom Bestiary refreshes belong to `useEncounterParticipantSynchronization`; typed merge rules belong to `participantSynchronization.ts`. Preserve `instanceId`, local display name/current HP, and never rewrite official monsters or character participants during custom refresh.
- `participantSynchronization.ts` exposes only participant-name normalization, entity image-map construction, and the synchronization command. Source normalization, payload extraction, identity-name derivation, and individual merge mechanics are private implementation details.
- Session CRUD, rename, deletion, ordering, encounter linking, and encounter persistence must go through backend session application commands and `SessionRepository`. `server/routes/sessions.js` only maps HTTP; do not add filesystem paths or JSON manipulation back to it.
- Campaign lifecycle, rename/reference updates, deletion, export, image checks, ordering, bulk entity replacement, and character/NPC movement must go through campaign application commands and repository ports. `server/routes/campaigns.js` is HTTP-only; preserve campaign/entity IDs and `createdAt`.
- Backend character/NPC/location CRUD and replacement policies live in `server/modules/campaign/application/campaignEntityCommands.js` behind its unchanged `CAMPAIGN_ENTITY_TYPES`, `createCampaignEntityCommands`, and `getEntityDisplayName` exports. Keep entity-type validation before payload access and preserve the exact status-400 messages. The entity-move HTTP transport must validate the source `req.params.type` against `CAMPAIGN_ENTITY_TYPES` before reading or validating `req.body`; only a recognized source type reaches body validation. The campaign command remains the sole owner of target-type validation, valid `characters <-> npc` pair validation, and their exact status-400 errors. Location display uses lazy truthy `name -> title`; character/NPC display composes truthy first/last names before that fallback, retains template-literal coercion, and trims only the final text. Creation sanitizes the lazy location `name` or person `firstName -> name`, then executes `toSlug -> ensureUniqueSlug -> payload projection -> write`. Location/person defaults precede payload spread, generated IDs follow the spread and always win, location name fallback follows ID creation, and person level retains the `level === "" ? "" : level || 1` read policy before payload spread. Updates strip mention-control fields, preserve the stored ID/slug, write before optional mention replacement, and reread using the repository-returned slug. Bulk replacement accepts arrays only, normalizes every requested slug twice through lazy `slug -> name -> firstName`, deletes stale current entities sequentially before request-order sequential writes, overrides `order` with the original zero-based index, skips falsy normalized slugs, and performs one final list read; do not add compensation for partial failures. Preserve repository result identity, getter/call order, caller input immutability, and route/repository separation.
- Campaign entity filesystem listing remains in `server/storage.js` behind the unchanged `listEntities` export and `fileCampaignEntityRepository` adapter; keep order/name/comparator/entry-reader helpers private. It must ensure the campaign/type directory, read directory entries once in returned order, ignore non-directories without reading their names, test each directory's `info.json`, skip missing files, read accepted entries sequentially, spread the saved value, and force the entry name as `slug`. Sorting happens only after all reads and mutates the collected array. Order projection remains `Number.isFinite(Number(order)) ? Number(order) : 0`, including its second read/coercion only for finite values. Equal-order names retain lazy truthy `name -> trimmed firstName/lastName composition -> slug -> ""`, final `String`, and `localeCompare`; stable equal keys preserve filesystem order. Preserve entry getter order, primitive/null spread behavior, UTF-8 collation, and fail-fast filesystem/coercion boundaries. Do not move traversal or sort policy into routes or campaign commands.
- Campaign mention normalization, bracket replacement, recursive projection, and persistence orchestration live in `server/modules/campaign/infrastructure/campaignMentionReferences.js`; `server/storage.js` only composes its dependencies and keeps the unchanged `updateCampaignMentionReferences` export for `fileCampaignEntityRepository`. The module exposes exactly `createCampaignMentionReferenceUpdater`, `normalizeMentionName`, `replaceBracketedMentionNames`, and `replaceMentionsInValue`; keep focused persistence helpers private. Preserve the guard's short-circuit order: normalized old name, raw-trimmed new name, a second old-name normalization, then normalized new-name equality. Invalid/equal renames remain filesystem-lazy undefined exits. Mention matching uses falsy-empty string coercion, trim, whitespace collapse, and lowercase; replacement inserts the trimmed/collapsed display name only into nonempty nonnested bracket matches. Recursive projection preserves sparse-array holes, enumerable own string-key object traversal, fresh array/object identities, getter execution, prototype/symbol loss, primitive/function identity, and circular failure. Persistence remains strictly sequential and uncompensated: optional metadata, `ENTITY_TYPES` and listed entities, then listed sessions with a path rebuild and reread. Compare `JSON.stringify(next)` before `JSON.stringify(current)`, suppress unchanged writes, keep entity write arguments unchanged, and stop later work on the first failure without rolling back earlier writes.
- Custom Bestiary search, source reads, mutations, and favorites go through `server/modules/bestiary/application/bestiaryCommands.js` and `BestiaryRepository` behind exactly `CUSTOM_SOURCE`, `buildReplacementCustomMonster`, `createBestiaryCommands`, `findCustomMonsterIndex`, `normalizeSource`, and `resolveLegendaryGroups`; keep source/search, modifier, replacement/image, duplicate, favorite, selection, and orchestration helpers private. Custom updates must resolve the target and eagerly normalize its old name/ID before inspecting payload mode. Replacement mode requires a truthy object `payload.monster`, intentionally reads that property three times, accepts arrays, JSON-clones the selected value, trims the name, forces CUSTOM, and inherits the prior truthy image only when the clone lacks an own `imageUrl`. Reject empty/duplicate normalized names before writes; exclude the current index without reading its name. Normalize HP before writing. Only a normalized rename reads and unconditionally rewrites favorites; update only matching normalized old-name CUSTOM rows and preserve unmatched identity. Select the persisted replacement by normalized new name or return the pre-HP-normalized candidate. Image mode reads a falsy `monster` once, distinguishes exact null from a twice-read trimmed/coerced `imageUrl`, writes the prior spread with nullable image, then selects persisted output by truthy previous ID before name or returns the local row. Preserve repository order, partial failures, getter/coercion timing, and storage-owned persistence normalization. Search must evaluate both name and type predicates for every indexed monster before combining them, then delegate ordering to shared `sortByNameQuery`. Source reads normalize before exact lowercase-all detection, preserve direct-file before aggregate fallback, and keep projection getter order.
- Generic backend name-query ranking lives in `server/modules/search/application/searchResults.js` behind exactly the `sortByNameQuery` export. Bestiary and reference application commands may depend on this infrastructure-free policy; application modules must never import utilities from `server/routes`. A falsy query remains a lazy undefined-returning no-op. A truthy query delegates to the collection's own `sort`, mutates it, and returns undefined. Ranking lowercases nullable names through `item.name?.toLowerCase() || ""`, then prioritizes exact strict matches, prefix-only matches, shorter lowered names, and finally `localeCompare`, with stable ties and raw unnormalized query behavior. Preserve getter/method order, UTF-8 casing/collation, custom method results, and malformed type/Symbol failures.
- Custom Bestiary persistence normalization remains in `server/storage.js` behind `normalizeCustomBestiaryMonster` and `writeCustomBestiaryMonsters`; repository/application layers must not duplicate it. Entry normalization is array-only: trim nonempty string entries, accept truthy object entries (arrays included), retain existing array-valued `entries`, otherwise use truthy `text → description → content` with the historical second read and `String` coercion, spread original fields, override only trimmed `name` from `name → title`, and keep only nonempty entry arrays. Monster normalization spreads the source before reevaluating falsy-ID generation and `name → title`, forces `CUSTOM`, then recursively strips mention brackets. Clone only truthy object non-array HP; a valid dice formula overrides stored average, otherwise an own average is base-10 parsed and clamped to at least one with invalid values becoming one. Wrap truthy non-array spellcasting. Normalize `trait`, `action`, `bonus`, `reaction`, and `legendary` in that order; skip only `undefined` values and delete families that normalize empty. The writer filters malformed/non-object and nameless monsters, regenerates duplicate IDs, sorts by localized name, writes the standard source metadata, returns the normalized array, and must not mutate caller input.
- Cross-runtime Bestiary modifier, dice, HP, clone, array, and mention policies live in `shared/bestiaryUtils.cjs` behind exactly `applyArrayMod`, `calculateDiceFormulaAverage`, `clone`, `normalizeCustomMonsterHpAverage`, `stripMentionBrackets`, and `toArray`; keep all focused helpers private. `clone` remains undefined passthrough plus a JSON round trip, and `toArray` preserves array identity while mapping nullish values to a fresh empty array. Array modifiers must normalize the target before reading and JSON-cloning items, inspect `mode` through the ordered append/prepend/unique/insert/replace/remove chain, retain unknown-mode side effects, use JSON-string uniqueness, clamp insertion through `Number(index) || 0`, preserve unmatched entry identity, read `replace` per source row, and use nullish `names -> items` removal inputs. Entry identity remains case-insensitive `String` comparison over lazy truthy object `name -> id -> title`. Dice parsing keeps falsy-to-empty coercion, whitespace removal, minus-to-`+-` tokenization, exact integer/die grammar, default-one falsy die counts, finite die dimensions, identical high/low keep averaging, early invalid-part rejection, final floor/minimum one, and constant overflow to `Infinity`. HP normalization retains repeated getter reads, unchanged identity for ineligible/invalid input, root then HP spreading, and average override. Mention cleanup remains a one-pass bracket replacement with sparse-array mapping and recursive enumerable own string-key object projection; getters execute, prototypes/symbols are dropped, primitives/functions pass through, and circular inputs fail.
- Bestiary reads and indexing remain in `server/storage.js` behind the unchanged `getBestiaryIndex`, `readCustomBestiary`, and `readCustomBestiaryMonsters` exports; keep their focused discovery, envelope, source, insertion, and projection helpers private. Custom data loads before official discovery. Missing official data returns the custom-only index; `all.json` is authoritative when present; otherwise accepted files are processed sequentially in `readdir` order after `isFile`, exact lowercase `.json`, and exact control-file exclusions. Direct arrays precede truthy `monster -> monsters -> results -> []`. Metadata source precedes the filename source; filename fallback removes one leading case-insensitive `bestiary-`. Indexing skips only falsy names, normalizes source to uppercase, keeps the first key position while later values overwrite it, and appends CUSTOM last. Custom reads preserve truthy non-array envelope fields, map arrays only, force `CUSTOM` without persistence normalization, and return a fresh empty monster array when the file is absent. Preserve lazy evaluation, sequential failure boundaries, caller immutability, and the storage/repository/HTTP separation.
- Bundled Bestiary discovery remains in `BestiaryRepository`; source fallback and legendary-group resolution remain in `bestiaryCommands`. Legendary modifier routing uses the closed append/prepend/append-if-missing/insert/replace/remove array-mode list, delegates array behavior to shared `applyArrayMod`, deletes only for exact string `remove`, skips malformed rows, and clones `setProp` values after reading the property key. Preserve outer `Object.entries`, `toArray`, property, and modifier order; array classification reads mode once before shared dispatch, while set/unknown classification reads it twice. Copy resolution normalizes index keys, checks cycles before direct cloning, preserves exact cycle/missing-base errors, recursively resolves the base before child overlay, skips only `_copy`/`_mod` during overlay, applies `_copy._mod` afterward, and removes both metadata fields. Direct groups clone and retain a truthy raw source; duplicate normalized keys keep the last indexed group. Keep filesystem paths and direct JSON/storage operations out of `server/routes/bestiary.js`.
- Settings reads/patches use `SettingsRepository` and `settingsCommands`; keep `server/routes/settings.js` HTTP-only and preserve storage normalization/default recovery.
- Settings persistence policy remains in `server/storage.js` behind the unchanged `readSettings` and `updateSettings` exports; keep its normalization, prompt-map, default, rewrite, and recovery helpers private. Normalize only exact `uk`, `dark`, and `grid` enum values; parse encounter columns in base 10 with fallback two and clamp one through four; coerce simplified notes with `Boolean`; coerce AI prompts through falsy-empty strings except that an exactly `undefined` image prompt receives the default; normalize ignored sources; and treat only exact `false` as disabling auto-apply or search debounce. Campaign prompt maps accept only truthy object non-arrays, preserve `Object.entries` order, trim/coerce keys, coerce falsy prompts to empty strings, discard empty keys, and let later normalized-key values overwrite earlier ones. Missing settings write the frozen defaults and return a shallow clone. Existing settings compare saved and normalized objects through ordered `JSON.stringify`, rewrite only when different, and recover any guarded read/normalize/compare/rewrite failure by writing defaults; recovery-write failures propagate. Updates preserve read/recovery -> current spread -> patch spread -> normalize -> write -> return order. Do not duplicate this policy in `SettingsRepository`, commands, or routes.
- Image upload naming/directories, gallery queries, token reads, subcategories, rename/move/delete, and reference propagation use `ImageRepository`/`imageCommands`. Keep filesystem/path logic in the adapter and preserve UTF-8 filename decoding plus collision suffixes.
- Campaign image existence, campaign-image directory renames, and campaign-wide image-slug reference propagation remain in `server/storage.js`. `campaignHasImages` uses `path.basename(String(slug || ""))`, returns before filesystem access for an empty safe slug, short-circuits a missing root, and traverses `fs.readdir(..., { withFileTypes: true })` depth-first in returned order; a file returns immediately, directories recurse, other entry kinds are ignored, and failures propagate. `replaceImageSlugReferences` keeps its raw falsy/equal guard, exact encoded `/api/images/{slug}/` segments, one JSON serialization, original identity when absent, serialized split/join replacement including keys, and JSON clone/failure semantics when present. Campaign rename executes campaign-directory rename -> optional old-image existence/new-image collision check -> image-directory rename -> reference propagation. Collision checking occurs only when the old image directory exists. Reference propagation remains sequential across listed campaigns and within each campaign uses metadata -> `ENTITY_TYPES`/entity order -> sessions -> AI history; present JSON files are read once, unchanged values are not written, changed entities write through `writeEntity` using the normalized slug, and any failure keeps all prior renames/writes without compensation. Preserve the existing storage exports and do not move filesystem traversal into routes.
- Spell and rules-reference reads use `ReferenceRepository`/`referenceCommands`. Preserve aggregate/index fallback, source labels, search ordering, disease fallback, and XPHB/XDMG-over-PHB/DMG precedence; keep `server/routes/spells.js` path-free.
- `server/modules/reference/application/referenceCommands.js` is a policy-oriented application boundary behind `createReferenceCommands`; keep source priority, named-record normalization, spell discovery/search, named-file reads, condition/status aggregation, disease fallback, and aggregate/index source selection in focused private helpers. Preserve the compatibility exports `getSourcePriority`, `normalizeNamedReferenceRecords`, and `pickPreferredRecord`. XPHB/XDMG outrank PHB/DMG, which outrank other sources; equal/lower candidates retain the first record and its map position. Names are string-coerced, trimmed, deduplicated case-insensitively, and locale-sorted; core truthy defaults precede optional extra-field spreading, while original truthy `entries` always wins after that spread. Spell queries lowercase without trimming, level compares by `String`, aggregate `exists` remains authoritative, indexed files load concurrently in index order, aggregate sources deduplicate and sort while indexed source keys preserve insertion order, and indexed lookup stays case-sensitive with the exact 404 error. Keep condition/status kinds, disease file fallback order, named extra fields, HTTP route delegation, and repository call order unchanged.
- Full/partial backup exports and imports use `BackupRepository`/`backupCommands`. Preserve gzip/UTF-8 payloads, campaign-only append behavior, replace-by-ID/wipe strategies, and archive response headers; keep persistence orchestration out of `server/routes/backups.js`.
- Campaign archive-image restoration lives in `server/modules/backups/infrastructure/archiveImageRestoration.js` behind exactly the `createCampaignArchiveImageRestorer` export. `server/storage.js` composes `IMAGES_DIR`, `ensureDir`, and `fs.writeFile`; full/partial import call sites and partial-import accounting remain unchanged. Backup/archive HTTP schemas validate `images` only as an optional array container; they intentionally do not validate individual image rows. Row tolerance, skipping, coercion, path containment, and failure timing remain governed exclusively by this restoration/storage policy. Non-array and empty inputs must return before slug/path access. Build one root through `path.join(imagesDir, path.basename(String(slug || "")))`, resolve it once, and process rows sequentially. Relative paths retain falsy-empty coercion, backslash-to-slash conversion, and leading-slash stripping. Skip empty paths, falsy first `base64` reads, and resolved targets outside the exact root/root-plus-separator boundary; encoded traversal text stays literal and a target resolving to the root remains accepted. Accepted rows execute directory creation before the second `base64` read, `Buffer.from(value, "base64")`, and the write. Do not add filename/extension/base64 prevalidation, deduplication, parallel writes, or compensation. Later duplicates overwrite earlier rows; getter, conversion, directory, and write failures stop later work while retaining earlier writes. Partial imports continue counting the original image-array length, including skipped rows.
- Static asset directories are resolved in `modules/assets/infrastructure`; routes may mount `express.static` but must not construct filesystem paths.
- Image gallery UI and state (`ImageGallery`, `ImageTargetSettings`, `ImageDropzone`, `ImageAssetField`, `useImageGallery`) belong to `features/images`. External code imports them only through `features/images/index.js`; do not recreate legacy `components/Image*` or `hooks/useImageGallery` paths.
- `src/features/images/model` is fully TypeScript. Keep gallery categories, content scopes, image locations, readonly assets, drag/drop payloads, selection groups, and storage statistics owned by the image feature. Normalize nullable HTTP results and narrow parsed drag data before applying mutations; do not move modal/UI composition into the model.
- Public `useImageGallery.ts` is a stable controller facade over private `useImageGalleryLocation.ts`, `useImageGalleryLoading.ts`, `useImageGallerySelection.ts`, and `useImageGalleryMutations.ts`; do not export those focused hooks or merge their ownership back into the facade. Location owns selected path/search scope/debounce and ignored-source derivation; loading owns campaigns, images, folders, official-token metadata, storage stats, initialization, and reload callbacks; selection owns readonly guards, selected Sets, range/single-click delegation, and grouped movable items; mutations owns upload/create/rename/move/delete, keyboard execution, drag state, localized errors, and reload sequencing. Preserve initialization-before-reload effect order, selection clearing on every location/scope/open transition, full anchor clearing only where it existed, fire-and-forget success reloads, loading-finally behavior, and the exact public return keys.
- `features/images/model/ImageGalleryRuntime.tsx` is the narrow Images global bridge. Its provider and types are public through `features/images/index.js`, but its Context hook remains private. `app/ui/ImageGalleryRuntimeHost.tsx` alone selects live search-debounce/campaign/ignored-source inputs and maps alert, confirmation, and prompt commands; `App` wraps the whole UI tree so every mounted or closed gallery, dropzone, asset field, and injected AI gallery has the same runtime. Keep ignore-source derivation and debounce policy in `useImageGalleryLocation`, bulk-delete result normalization in `useImageGalleryMutations`, and prompt guards/rename ordering in the private gallery item; do not restore `app/model` or `shared/model` imports inside Images.
- `features/images` has full strict TypeScript implementation coverage; `index.js` and `imageGalleryConfig.js` are runtime compatibility barrels with typed owners. URL/target presets, gallery-URL parsing, and `ImageAssetField` valid/missing/empty state, preview gating, context-menu effects, and gallery-selection narrowing live in `model/imageAssetField.ts`; URL parsing must resolve against an optional base before splitting, require the exact case-sensitive `api/images` prefix plus a filename, split before safe per-segment decoding, retain malformed escapes verbatim, allow decoded slashes in subcategories, and exclude the final filename. `useImageAssetFieldController.ts` owns URL-change error reset, location memoization, preview/gallery state, DOM event execution, and nullable callback delegation. `ImageAssetField.tsx` remains the public thin shell, while `ImageAssetFieldView.tsx` owns private valid/missing/empty/preview/gallery compositions and must not enter the public API. Preserve error reset only when `imageUrl` changes, retained preview-open state while invalid/empty, preview visibility only for valid images, no prevent/stop effects when context replacement is disabled, stop-propagation for image/clear/choose clicks, always-close gallery selection with callback only for a truthy URL, missing-image clear regardless of `showClearButton`, and upload/gallery location reuse. Upload naming/source/result policies live in `model/imageUpload.ts`; folder navigation in `model/imageTargetSettings.ts`; path/history/deduplication/row projection and global-search result navigation plans live in `model/imageGalleryPresentation.ts`. Opening a global result is disabled in selection mode and for non-global/unknown-category items; otherwise preserve `general`/empty-path fallbacks, clear search, return to local scope, clear the current selection, let the normal location effect record history, and defer selection by exact name plus normalized path key. Validated drop, move, selection, and drag plans live in `model/imageGalleryInteraction.ts`; nullable subcategory/local/scoped/database loading in `model/imageGalleryLoading.ts`. Keep React hooks as effect/state orchestration over these policies. `ImageGalleryItem.tsx`, `ImageGallerySections.tsx`, and `ImageAssetFieldView.tsx` are private compositions and must not enter the public API. Preserve `general` fallback, character/NPC presets, object-URL cleanup, image-only drops, original extensions, upload locking, static/async subfolders, create paths, readonly official tokens, multi-selection/ranges, drag/drop validation, grouped cross-location moves, navigation history, search scopes, virtual rows, previews, context replacement, and nullable callbacks. The `react-list` ambient declaration belongs in `src/types`, not in a feature slice.
- Image-gallery deduplication keys must resolve `source` and `category` with falsy fallback, resolve `subcategory` with nullish fallback, then append image path, URL, and lowercased name using the existing null separators. An explicit empty subcategory represents the root and must remain distinct from an omitted/null subcategory that inherits the current folder.
- Scoped image-gallery searches use the pure query projection in `model/imageGalleryLoading.ts`: `all` clears source/category/subcategory, `source` retains only the selected source, and local or unrecognized scopes retain the full selected path. Preserve search/category allowlists/ignored sources unchanged, keep `databaseTokens` on its dedicated recursive loader, and normalize null or non-array scoped responses to an empty image list.
- Image-gallery Shift ranges use the ordered folder-then-image projection in `model/imageGalleryInteraction.ts`. Preserve forward/reverse `slice` bounds, the original `lastIndex` anchor, readonly gaps without terminating the range, type-specific folder/image sets, additive cloning of prior selections, fresh-range replacement, and immutability of caller-owned Sets.
- Image-gallery plain single selection uses the focused readonly, selected-item, action, Set-projection, and anchor policies in `model/imageGalleryInteraction.ts`. Preserve `null` for readonly items without cloning state; clicking the sole selected item must clear both Sets and reset `lastIndex`; clicking an item inside a multi-selection must narrow to that one item rather than toggle it off; folder/image names remain type-separated even when equal; every clear/select result owns new Sets and must not mutate caller state.
- Image-gallery folder icons/state and nested-path projection live in `model/imageGalleryPresentation.ts`; drag-over, drop-target, and rename-result plans live in `model/imageGalleryInteraction.ts`. `ImageGalleryFolderItem` must only execute these policies and compose its private visual/name parts. Preserve bestiary-icon precedence, `npc`/`players` icons, selected/drag/file badges, readonly single-click/drag/checkbox/rename guards, readonly-allowed double-click navigation, repeated-target drag-over suppression, nested-path readonly evaluation at drop time, and non-empty string rename acceptance without trimming or same-name rejection.
- Gallery keyboard routing uses `getGalleryKeyboardPlan` in `model/imageGalleryInteraction.ts`; the hook only maps a real `HTMLElement` to its exact tag name and executes the plan. Preserve closed-gallery and uppercase `INPUT`/`TEXTAREA` guards only, Delete delegation without preventing default, Backspace prevention even at the empty root, no root setter call, truthy-path parent navigation through split/filter/pop, and current behavior for `SELECT`, contenteditable, lowercase synthetic tag names, and unrelated keys.
- Gallery history shortcuts use `getGalleryHistoryKeyboardPlan` in `model/imageGalleryPresentation.ts`; `ImageGallery.tsx` only derives editable-target state and executes the returned plan. Closed galleries and descendants matching `input`, `textarea`, `select`, or `[contenteditable='true']` must not handle history keys. Preserve exact modifier-sensitive shortcuts: plain `Backspace` and `Alt+ArrowLeft` navigate back, `Alt+ArrowRight` navigates forward, key casing is exact, Ctrl/Meta/Shift or extra Alt combinations are rejected, unavailable directions do not prevent default, and accepted navigation prevents default exactly once before delegation.
- Gallery search placeholder, clear visibility/value, ordered scope controls, active variants, next-scope actions, icons, and localization keys come from `getGallerySearchPresentation` in `model/imageGalleryPresentation.ts`. `ImageGallerySearch` only composes its private input and scope-button controls. Preserve truthy raw-query Clear visibility including whitespace, Clear resetting only the query to `""`, repeated active-scope clicks returning to `local`, inactive clicks selecting the requested scope, exact `general` versus campaign source titles, optional database-token control, and control order `source → databaseTokens (when allowed) → all`.
- Gallery storage rows, selection count/action visibility, and upload visibility come from `getGalleryStatsAndActionsPresentation` in `model/imageGalleryPresentation.ts`; `ImageGalleryStatsAndActions` only composes private stats, selection-action, and upload controls. Preserve ordered total/category rows, nullable `?? 0` fallback without clamping or finite-number normalization, count as the direct filename/subfolder size sum, action visibility from the independent `hasSelection` flag, readonly folders hiding only upload, both Move/Delete sharing the same count, and file-input delegation for every non-null `FileList` including an empty list.
- Gallery-grid drag-over/drop behavior uses `getGalleryGridDropTarget`, `getGalleryGridDragOverPlan`, and `getGalleryGridDropPlan` in `model/imageGalleryInteraction.ts`; UI callbacks only execute DOM effects or delegate the original event. Drag-over always prevents default. Search results explicitly clear overlay state and reject drops without stopping propagation. Same-location comparison is strict across slug/category/subcategory, so `undefined` and `""` remain different. Same-location and readonly drag-over return `nextDraggingOver: null` and must not call the setter. Mutable different/absent sources show the overlay. Local drops, including readonly targets, delegate the exact target and let `handleDrop` own prevent/stop/cleanup.
- Public `ImageGallery.tsx` is a thin open-state shell over private `useImageGalleryUiController.ts` and `ImageGalleryView.tsx`; neither private file enters the feature API. The UI controller owns only modal-local history, preview, pending selection/highlight, responsive columns, move-dialog state, scroll refs/effects, deduplicated rows, and global-result transitions over the domain `useImageGallery` controller. The private view owns sidebar, tabs, toolbar, virtual rows, modal shell, and dialogs. Preserve mounted controller state while closed, Escape preview-before-selection priority, capture-phase prevent/stop effects, double animation-frame selection scrolling, 2800ms highlight clearing, pending-selection scroll-top suppression, history application suppression, 400-row virtualization threshold, 170px estimator fallback, and unchanged public props.
- Gallery Escape decisions use `getGalleryEscapePlan` in `model/imageGalleryInteraction.ts`; non-Escape and empty state are no-ops, preview wins over selection, and handled actions prevent default plus stop propagation. Responsive columns use `getGalleryColumnCount` in `model/imageGalleryPresentation.ts`; preserve `parseFloat(columnGap) || 16`, 120px minimum item width, floor division, and minimum one column without extra finite-number normalization.
- Subcategory renames use `getGallerySubcategoryRenamePlan` in `model/imageGalleryInteraction.ts`; the hook owns only API execution, subcategory-then-image reloads, optional selected-path application, and localized error dispatch. Reject whitespace-only new names and exact same names, but do not trim accepted names or normalize slash prefixes. Build old/new paths with the selected truthy prefix and replace the selected path only for exact `selectedSub === oldName`.
- Gallery bulk deletion uses selection-summary, confirmation, result-normalization, and grouped payload policies in `model/imageGalleryInteraction.ts`; non-empty folder inspection lives in `model/imageGalleryLoading.ts`. The hook owns only loading/error lifecycle, localized confirmation composition, parallel delete execution, success-only selection clearing, and image/subcategory/storage reloads. Count only safe filenames and folders, inspect selected folders and each folder's image/subcategory queries in parallel, treat non-array responses as empty, preserve image-group payload order before the folder payload, and enable content extraction only when a selected folder is actually non-empty and the user opted in.
- Generic `Button`, `Checkbox`, `Icon`, `MultiSelect`, `Select`, `TextInput`, `Tooltip`, `Panel`, `Notification`, and `Switch` live in `shared/ui`; import them only from `shared/ui/index.js`. Use `TextInput` for plain native text-input behavior. Rich `Input` and `EditableField` remain app-composed editor controls and must not be moved into shared without first removing their injected runtime/entity dependencies. `classNames` lives in `shared/lib` and is imported through its public index; the legacy utility path is retired.
- `src/shared/ui` has no JavaScript component/model implementations remaining; `index.js` is the runtime compatibility barrel and `index.d.ts` is its type facade. New shared primitives and controller models must be TypeScript, and external slices must not deep-import their implementation files.
- Shared `Tooltip` and `Notification` are strict TypeScript presentation primitives. Tooltip registry, nesting, timer coordination, and viewport-position derivation live in the Node-safe `shared/ui/tooltipModel.ts`; keep DOM effects and portal rendering in `Tooltip.tsx`. Preserve delayed open/close behavior, drag-mode cancellation, anchored positioning, notification duration, and cleanup semantics; external consumers still import components only through `shared/ui/index.js`.
- Shared `Icon`, `Button`, and `Checkbox` are strict TypeScript controls. Button size normalization and appearance derivation live in the Node-safe `shared/ui/buttonModel.ts`; keep render/ref/event behavior in `Button.tsx`. Preserve the closed icon-name catalog, `Button.SIZES` runtime API, `small` legacy size normalization and invalid-value fallback, native button/input attribute forwarding, disabled-button guard, click propagation prevention, controlled checkbox inversion, and tooltip wrapping. Do not expose type-only named values from `shared/ui/index.d.ts` unless `shared/ui/index.js` has the matching runtime export.
- Shared `TextInput` is a strict TypeScript native-input primitive. Preserve native `input` props, forwarded refs, caller class composition after the base `Input` class, and its public runtime/type exports. Plain fields in `AiApiKeyPanel`, `CreateCampaignModalContent`, `DiceCalculator`, `MonsterFieldEditModal`, and `PlayerQuestionsModalContent` must not regain a dependency on the app-coupled editor `Input`.
- Shared `Panel`, `CollapseToggleButton`, and `Switch` are strict TypeScript primitives. Preserve Panel's section wrapper, collapse rotation override/default semantics and size-specific icon sizing, Button event delegation, Switch controlled inversion, disabled guard, native input attribute forwarding, propagation handling, and optional label/description rendering. External consumers import all three only through `shared/ui/index.js`.
- Shared `ListCard` and `SearchHighlight` are strict TypeScript presentation boundaries. Link/button modifier-click planning lives in Node-safe `shared/ui/listCardModel.ts`; regex-safe case-insensitive text splitting lives in Node-safe `shared/ui/searchHighlightModel.ts`. Preserve plain-link prevent/default navigation delegation, modifier-open behavior, non-link callbacks, article drag-attribute forwarding, active/actions rendering, nullish passthrough, original casing, and public imports through `shared/ui/index.js`.
- Shared `Select` is a strict TypeScript controller. Option fallback, compatibility change-event construction, viewport-clamped portal placement, and selected-option scroll targeting live in Node-safe `shared/ui/selectModel.ts`; shared outside-click/resize/scroll listener ownership lives in `shared/ui/useDropdownPortalLifecycle.ts`; refs, portal rendering, and animation-frame scheduling stay in the owning control. Preserve strict value equality, first-option fallback, `{ target: { value } }` callbacks, upward-opening threshold, viewport gaps, minimum/max heights, disabled guards, and public imports through `shared/ui/index.js`.
- Shared `usePointerDownOutsideDismissal` is a strict TypeScript public UI lifecycle. It owns only the open-gated document `pointerdown` listener and cleanup for a supplied container ref and state setter; consumers retain state, refs, rendering, and commands and import it through `shared/ui/index.js`. Preserve the `event.target instanceof Node` guard, inside-container no-op, outside/non-Node close, and do not merge it into `useDropdownPortalLifecycle` or change that hook's `mousedown` portal behavior.
- Shared `MultiSelect` is a strict TypeScript controller. String-normalized selection identity, option-order output, override/all/none/count label projection, option selected/active projection, single/toggle/delegate click plans, active-filter scroll targeting, and its 340px portal-height policy live in Node-safe `shared/ui/multiSelectModel.ts`; shared viewport clamping lives in `shared/ui/dropdownPortalModel.ts`. `MultiSelect.tsx` owns state, portal lifecycle, and action orchestration, while private `MultiSelectDropdown.tsx` owns dropdown/actions/options rendering and must not enter the public API. Preserve select-all/clear actions, zero-option all-label precedence, truthy label fallbacks, delegated source-filter clicks, checkbox-only toggles, menu close semantics, zero/all scroll-to-top behavior, disabled guards, and public imports through `shared/ui/index.js`.
- Shared `DraggableList` is a strict generic TypeScript controller. Default key precedence, the five-pixel Euclidean start threshold, bounded immutable reorder, key-based final-order comparison, finish-plan projection, and finish callback sequencing live in Node-safe `shared/ui/draggableListModel.ts`; pointer listeners, hit-testing, preview animation, ref/state cleanup, click suppression, body drag modes, and custom-event dispatch stay in `DraggableList.tsx`. Preserve eligible-item counting, handle-only initiation, interactive/contenteditable exclusions, original-list snapshots, return-to-origin suppression, truthy-only cross-list details with pointer coordinates/source-list ID, callback order `onReorder -> custom drop -> onDrop`, `onReorder`/`onDrop` only after a real reorder, custom drop even without a local reorder, and public imports through `shared/ui/index.js`.
- Generic note-state helpers live in `shared/lib` and must be imported through its public index. Character/location card models live in `entities/campaign/model` and are exposed only by `entities/campaign/index.js`; their former `models/*CardModel` paths are retired.
- `CharacterCard`, `LocationCard`, and their create-button/modal compositions live in the fully strict TypeScript `widgets/campaign-entity-card` slice. Browser consumers import them only through `widgets/campaign-entity-card/index.js`; Node/model tests use the JSON-free `model.js` facade. Card-data/collapse derivation, display-name normalization, AI field/note highlights, note ignore updates, draft construction, and draft validation live in `model/campaignEntityCard.ts`; shared note composition is private widget UI. Submit behavior stays in `features/campaign-entity`, and pure entity/note mutation stays in `entities/campaign`. `ui/CampaignEntityCreationRuntime.tsx` provides only creation-error notification and entity refresh commands; expose its provider/types through the widget root, keep its hook private, and let `app/ui/CampaignEntityCreationRuntimeHost.tsx` map the app actions. Preserve modal cards as non-collapsible, empty/virtual trailing notes, simplified-note rendering, note reorder sanitization with tracked undo, name-blur baseline advancement only after successful rename, campaign/session/encounter callback compatibility, character/NPC image targets, location image behavior, invalid-draft notice/return, custom-create-without-refresh, default-create refresh, error notice, and finally unlock. Do not import private widget paths or recreate legacy card/create-button components.
- Campaign-entity modal card composition is app-owned. `App` supplies the stable public `CharacterCard` and `LocationCard` component symbols to `CampaignEntityModalProvider`, which forwards the required slots through its private content composition to `CampaignEntityModalCard`; the modal widget must not regain a sibling import from `widgets/campaign-entity-card`. Preserve parent-resolver-first delegation and short-circuiting, campaign/scope guards, entity initialization/reset and mutation state, the location-versus-character plan branch, the exact planned React key, callback adapters, and the existing modal-card flags (`collapsed: false`, `onToggleCollapse={null}`, `viewMode="modal"`, hidden delete button and header).
- `SpellCardModel` lives in `entities/spell/model` and is exported through `entities/spell/index.js`. The fully strict rich `SpellCard` composition is private UI at `widgets/spells-browser/ui/SpellCard.tsx`, owned by its sole consumer in `widgets/spells-browser`; the redundant `widgets/spell-card` slice and its public barrels are retired. Preserve localized model labels, rich-content render options, inline dice/spell parsing, source formatting, query highlighting, classes, higher-level entries, and every existing rendering contract; do not recreate `widgets/spell-card`, `components/SpellCard`, or `models/SpellCardModel`.
- `MonsterStatBlockModel` lives in `entities/bestiary/model` and must be imported through `entities/bestiary/index.js`; its former legacy model path is retired. Damage defense formatting narrows string/array input first, then uses focused descriptor value/composition helpers; preserve first-truthy `resist -> immune -> vulnerable -> conditionImmune` precedence, array joining, optional pre-note/note spacing, empty-entry filtering, and comma-separated output.
- Monster type normalization and Bestiary search matching live in `entities/bestiary/model` and are exposed through `entities/bestiary/index.js`; do not recreate `src/utils/bestiary.js`. `bestiarySearch.ts` owns query normalization, legacy/structured type projection, `choose` formatting, tag projection, and the combined search text. Preserve falsy/trimmed-empty query match-all behavior, case-insensitive matching, field order `name -> type -> tags`, cross-field substring matches, string types, structured `{ type, tags }`, slash-joined `choose` values, array-only tags with native `join` coercion, and safe empty output for malformed/null structured types.
- Bestiary widget composition is page-owned. `EncounterPage` supplies the stable public `AiAssistantPanel` and `MonsterStatBlock` symbols plus its module-scope configured `EncounterMonsterEditorModal` and `EncounterAiResponseModal` components to `BestiaryBrowser`; the browser forwards the assistant/stat-block slots plus those existing editor/response contracts to private `BestiaryContent`. Both direct Bestiary response paths must reuse the configured response symbol, and every Bestiary editor path must reuse the configured editor symbol. Neither Bestiary widget file may regain a sibling-widget import for the assistant, stat block, editor, or response modal. Preserve the overlay's closed-state null guard, the assistant's unconditional mount at its existing position while the Bestiary is open, the stat block's detail-presentation guard, component identity, prop forwarding, and the browser/content ownership of existing state and callbacks.
- Encounter participant identity, HP derivation, and instance creation live in `entities/encounter/model` and are exposed through `entities/encounter/index.js`; do not recreate `src/utils/encounters.js`.
- Campaign graph projection and layout live in `pages/campaign/model`; page UI and Node-based tests consume the Node-safe `pages/campaign/graph.js` entrypoint. Flow-node collision and minimap sizing share `getCampaignGraphFlowNodeSize`: preserve direct node type before nested graph-node type, measured -> direct -> style -> type-default dimension precedence, positive finite numeric acceptance, `parseFloat`-compatible strings, and independent width/height fallback. Pure Flow-node presentation in `campaignGraphPresentation.ts` owns current-position → layout-position → fresh-zero fallback, type size/color/label fallback, selection, focus muting, openability, visibility, type class, and sequential placement of only newly added nodes; preserve input immutability and unaffected node identity. Recursive graph text collection preserves depth-first order, shared accumulator identity, root `text` fallback, indexed/dotted paths, strings only, whitespace rejection, and nested `_...` plus known-field exclusion. Bracket mentions preserve string/marker guards, regex order, duplicates, whitespace normalization, and empty/malformed/newline rejection. Note projection preserves null/virtual/empty skips before commands, regular title precedence, simplified title suppression, Markdown-stripped text fallback, numbered fallback labels, simplified empty aliases, and add-node → contains-edge → mention-queue order. Session-detail lookup preserves missing/empty guards, Map exact-key lookup, first exact array `fileName` match, record direct-key lookup, and null fallback. Graph node/edge registries preserve first-node-wins identity, one-time label/meta/search normalization, alias insertion order, invalid/self-edge rejection, `localeCompare` canonicalization only for related edges, in-place duplicate count/source accumulation, queued mention resolution after all aliases, unresolved reuse, propagated-mention related suppression, cloned final node records, orphan-edge filtering, one degree increment per retained edge endpoint, and exact stats. Session projection in `campaignGraph.ts` is composed from focused session, scoped-NPC/location, scene, and note helpers; preserve deterministic fallback filenames/IDs, list/detail merge precedence, parent/scope/scene navigation metadata, virtual/empty-note filtering, simplified-note labels, mention propagation, result → NPC → location → note → scene command order, scene-sequence edges, four-field scene-summary order, all-text detail order, and `scene.texts` precedence as the mention source. There are no encounter graph nodes in the current contract. The former `src/utils/campaignGraph*` paths are retired.
- `src/pages/campaign/model` is fully TypeScript. Keep campaign loading, history/keyboard routing, graph-note editing, archive flows, AI refresh, and entity-feature callback composition at this boundary. Campaign keyboard routing is split into editable/app-history permission and exact key-action mapping; character drag/drop projection is split into kind-aware source normalization, exact type normalization, legacy ID narrowing, and complete-candidate validation. Preserve guard-before-key mapping, native undo/redo in ordinary inputs and textareas, app history only for opted-in editable targets, exact-case `KeyZ`/`KeyY`, Shift affecting only `KeyZ`, and no handling for unrelated keys. Editable-target detection accepts exact uppercase `INPUT`/`TEXTAREA` or truthy `isContentEditable`. Drag/drop accepts only the exact `campaign-character` kind and `characters|npc` source/target types, permits same-type requests, and deliberately retains the legacy typeof-only ID contract including empty strings, `0`, `NaN`, and infinities. Notes list/graph visibility, active view variants, collapse patches, view-mode auto-expand, ordered hash resolution/navigation, and entity AI-ignore update plans also belong to `campaignPagePresentation.ts`; browser UI only executes these plans. Hash markers retain note → character → NPC → location precedence; navigation executes list-mode selection before section expansion, while decoding/timers/scrolling stay in UI. AI-ignore planning uses strict ID identity, accepts numeric zero, rejects only `undefined` before lookup, selects only the declared NPC/location collection, preserves source identity/data, and returns an immutable updated entity or explicit no-op. The private `pages/campaign/ui/components/CampaignNotesSection.tsx` owns campaign-note list controls plus toolbar/content composition and must not enter the page public API. Preserve list rendering for an empty uncollapsed section so its virtual note remains creatable, collapse refusal without note content, list-only bulk collapse, mutually exclusive list/graph content, active mode styling while collapsed, exact `{ isNotesCollapsed }` persistence patches, and graph navigation through the campaign slug. The hook only adapts `KeyboardEvent`, prevents default for handled actions, and invokes undo/redo. Graph-note target validation, immutable campaign/session/scene note updates, optimistic local session updates, focused `{ data }` API patches, and explicit saved/missing-session/failed outcomes belong to `campaignGraphNoteSave.ts`; planning validates string/number note IDs plus record updates once, then dispatches through campaign/session/scene readers. Preserve update object identity, truthy session filenames, numeric ID `0`, the legacy unknown-type scene fallback, string-compatible session/scene note identity, campaign-note materialization through the shared upsert policy, existing-only session/scene note updates, and local-before-API ordering. Graph projection/layout remain pure behind the Node-safe `pages/campaign/graph.js` facade. Flow size normalization accepts positive finite numbers or `parseFloat`-compatible strings in measured → direct → style → type-default order. Collision resolution ignores hidden peers, normalizes each coordinate independently, clamps finite negative margins to zero, defaults non-finite margins to 16, selects the deterministic nearest free candidate, and never mutates input nodes. Layout accepts non-null IDs including zero, string-normalizes and sorts nodes, normalizes scalar/object edge endpoints, rejects self/orphan edges, retains seeded fixed-tick simulation, pins the campaign node to zero, and runs final deterministic collision cleanup. Preserve mention propagation, unresolved nodes, and simplified-note handling. Entity persistence, ordering, rename, and scope movement remain owned by `features/campaign-entity`.
- Campaign view state/sync/delete/AI/session-creation/rename orchestration belongs to the Node-safe `pages/campaign/model/campaignViewOrchestration.ts` boundary; `useCampaignView.ts` only supplies prompts plus React/runtime/API/navigation effects. Preserve truthy state fallbacks, caller-owned note/entity identity, sync-version and campaign-scope guards, entities-before-sessions reload order, entity-only image sync, fail-open image checks before delete confirmation, cancellation silence, image movement only for campaigns that have images, navigation only after successful deletion, explicit empty AI entity-type selections, nullable entity normalization, and campaign reload dispatch after AI handling. Session creation cancels only for prompt `null`, preserves string identity, maps other prompt values to the empty-name backend date fallback, treats nullable API results as failures, and applies append → navigation → reload after success. Rename rejects non-string, empty, and unchanged prompt values without frontend trimming, sends exactly `{ name }`, treats nullable results as failures, and applies reload before replace-navigation. Keep deterministic plans and injected executors directly regression-testable without React.
- AI attachment validation, diff construction, model-option loading, and response-resource helpers live in `features/ai/model` and are exposed through `features/ai/index.js`. Attachment identity in `aiAttachments.ts` uses private URL/name/size narrowing plus one shared metadata-key policy. Preserve non-empty string URL precedence for image keys, exact `name:size` fallback, `:` for nullish descriptors, string-only names, numeric truthy size stringification (so `0` and `NaN` produce an empty segment while negative/infinite numbers remain stringified), and URL-independent file keys; malformed non-string URLs must fall back to the metadata key. In `aiResponseHelpers.ts`, keep resource-ID narrowing, monster-name fallback, token identity/URL construction, single-resource image injection, collection replacement, and encounter-route matching in private policies. Preserve null resource-list fallback, null-vs-array resource filtering, string-normalized truthy IDs, first-truthy `after.name -> before.name -> resource.name` selection followed by string validation, explicit non-empty string `imageUrl` precedence, trimmed and URL-encoded `source/originalBestiaryName|name` token paths, image injection only into added custom monsters without a truthy image, existing summary precedence, original entry identity when nothing changes, exact Bestiary route handling, and exact campaign/session/encounter matching only for truthy encounter IDs. History change-summary formatting belongs to `historyChangeSummary.ts`: preserve server counter precedence, `Number(total)` truthiness semantics, ordered `added -> deleted -> modified` parts with `+/-/~` prefixes, raw resource-count fallback without kind/ID deduplication, an empty result when neither total nor resources exists, and localization of only the `Changes` label. The former `src/utils/aiAttachments`, `aiDiff`, `aiModels`, and `aiResponseHelpers` paths are retired.
- Reference cache loaders/normalizers, content tokens, tag preprocessing, source metadata/filtering, and spell metadata live in `entities/reference/model`. The five entity-owned cache modules are `conditions.ts`, `diseases.ts`, `senses.ts`, `skills.ts`, and `variantRules.ts`; they remain public through `entities/reference/index.js`. The JSON-free `entities/reference/model.js` stays a minimal Node-facing facade and must not mirror unused browser exports. Their former `src/services` and `src/utils` paths are retired.
- `src/entities/reference/model` is fully TypeScript. Keep cache maps, nullable API handling, token match indexes, parser fallbacks, and source normalization explicit. Reference preview loading and input resolution compose sibling Bestiary/spell owners, so `referencePreview.ts` and `referenceResolvers.ts` belong to `features/rules-reference/model` and are exposed to Node consumers through `features/rules-reference/model.js`; do not move that orchestration back into an entity. Maintain matching declaration facades, and do not make Node consumers load `sourceNames.ts` or its JSON catalog.
- Generic search highlighting lives in `shared/ui`. Mention boundary/picker behavior lives in `features/editor/model` and is exposed through the Node-safe `features/editor/model.js` entrypoint. Do not recreate the retired `src/utils` tree.
- `src/features/editor/model` is fully TypeScript. Collapsed-selection mention lookup belongs to `mentionSelectionPolicy.ts`; `mentionEditor.ts` owns zero-width boundary-node creation and the Space mutation. Preserve collapsed range-only handling, ordered Mention -> Text -> Element candidate resolution because `MentionNode` subclasses `TextNode`, mention anchors only at their end, text anchors only at offset `0` or within the one-character zero-width boundary, previous-sibling/previous-child mention requirements, the existing boundary rewrite/caret placement and new-node fallback, and the selected/cancelled picker result union. Keep `features/editor/model.js` as the Node-safe compatibility entrypoint and do not deep-import the private selection policy.
- Pure campaign entity name matching lives in `entities/campaign/model`; HTTP-backed resolution lives in `entities/campaign/api/resolveEntityByName.ts`. Theme behavior lives in `features/settings/model`; localization lives in `shared/lib`; realtime synchronization lives in `app/realtime`. Import through their public entrypoints and do not recreate the retired `src/services` tree.
- Global action contracts, action creators, action/state types, and lower-level reducer policies live in `shared/model` and are imported through `shared/model/index.js`; the actual stateful external store, selectors, modal coordination, and navigation commands are owned solely by `app/model`. Lower slices receive narrow injected runtimes rather than a global store facade. Do not recreate `src/actions`, `src/store`, or `shared/model/appStore.ts`.
- Application-shell settings projection, editable-target checks, mention-option normalization, campaign-completion planning, and unknown-error narrowing live in `app/model/appShellPresentation.ts`. Keep HTTP/store/modal/effect orchestration in `App.tsx`; do not rebuild these policies inside UI callbacks. `src`, `app`, `pages`, `widgets`, `features`, `entities`, and `shared` contain no JSX implementations; new React implementations must be TSX.
- Realtime synchronization is strict TypeScript in `app/realtime/realtimeSync.ts`. Preserve stable per-tab client IDs, WebSocket reconnect backoff, runtime event narrowing, entity refreshes, and campaign reload dispatch; consumers import through `app/realtime/index.js`.
- Backend realtime classification in `server/realtime.js` must remain a private policy pipeline behind the unchanged `realtimeMiddleware` and `setupRealtime` exports. Preserve `req.originalUrl || req.url` event-path selection, URL-path segment parsing without decoding campaign/session/entity segments, decoded AI `campaign` query values, monotonic post-read event versions, and exact null-filled event shape. Top-level settings, Bestiary/custom-Bestiary, images, AI, and campaigns classification uses the private descriptor map; campaign sessions and entities precede broad nested checks, and nested `images` precedes `import`. Middleware eligibility continues to require `req.originalUrl` beginning with `/api/`, rejects only `GET|HEAD|OPTIONS` and status codes `>= 400`, and evaluates after response finish. Notifications exclude only the exact source client ID and retain WebSocket handshake/frame behavior. Keep classification, gating, client storage, and transport helpers private.
- Recursive rich-content rendering lives in `features/rich-content`; browser composition owners import the callable `createRichContentRenderers` factory through `features/rich-content/index.js`, while Node/model tests use the JSON-free `features/rich-content/model.js` facade. The slice has a full strict TypeScript implementation: `model/richContentPresentation.ts` owns pure token render plans and safe recursive-shape normalization, while private `ui/RichContentRenderer.tsx` composes Markdown through injected structural `RollDice` and `RulesLink` slots. The raw `parseRollsAndSpells` and `renderRecursiveContent` functions are private and must not be exported or deep-imported. Token extraction/tag preprocessing stays in `entities/reference`; use its JSON-free `model.js` facade from Node-safe feature models. Bracketed `renderMentionText` moved unchanged to `features/entity-link` and is not rich-content ownership. Preserve token-handler priority, recharge interactivity when other rolls are disabled, `disablePlainRolls`, creature source fallback, condition/status distinction, PB/spell-level formula labels, notes-reference stripping, escaped Markdown, and deterministic React keys. Normalize untrusted list/section/table shapes before rendering. External code must not import rich-content model/UI implementation paths, run the factory during render, or recreate `src/renderers`.
- TypeScript adoption is contract-first. `tsconfig.json` is strict/no-emit, and `npm run typecheck` is required after changing `.ts`/`.tsx` files. Convert stable identifiers, events, API payloads, AI operation types, and repository ports independently; do not combine type conversion with domain ownership moves.
- Runtime validators remain authoritative for filesystem, HTTP, archive, and Gemini input. TypeScript types complement but never replace validation of untrusted data.
- Shared workflow identifiers live in `shared/model/contracts.ts`. Typed navigation implementation lives in `shared/lib/navigation.ts` but runtime consumers continue importing functions through `shared/lib/index.js`.
- AI operation types live beside the runtime schema in `server/modules/ai/domain/aiPayloadContracts.ts`; `aiPayloadSchemas.d.ts` describes the CommonJS validator. Keep the type union synchronized with the validator, but never remove runtime validation for Gemini output.
- `server/aiPatchService.js` applies validated operations through an explicit dispatch outcome. Keep custom-monster operations batched separately, execute all other operations sequentially with shared client-ID and encounter-link state, resolve pending scene links before removing unlinked created encounters, and persist campaign metadata before session data. Preserve invalid-operation silence, session-read failure fallback, note scope classification, session-over-campaign-over-standalone-Bestiary result precedence, warnings, and changed-monster output when extending the coordinator.
- AI note operations in `server/aiPatchService.js` must prepare one target context, reuse the per-type campaign-entity cache, resolve direct campaign/session/scene targets or scoped entity identity, dispatch only the closed append/update/delete note set, and immediately persist campaign-scoped entity notes while session-owned notes wait for the final session write. Keep client-ID scope/identity resolution, simplified-note normalization, missing-target silence, cache replacement after entity writes, and prototype-safe `Map` dispatch for untrusted operation/entity names.
- Custom-Bestiary AI operations in `server/aiPatchService.js` run as one isolated batch. Materialize missing stored IDs before taking the `before` snapshot, resolve targets by exact ID before case-insensitive name fallback, and dispatch create/update/delete through the closed prototype-safe handler map. Preserve create-time removal of caller IDs, same-name replacement at the tail, update ID retention/name fallback, delete exclusion from `changedMonsters`, silent invalid/missing targets, operation order, the unconditional final normalized Bestiary write, and standalone-Bestiary updated-result precedence.
- Scene AI mutations in `server/aiPatchService.js` use focused create/update/delete commands behind a closed handler map. Keep missing-session warnings, existing-only update/delete behavior, immutable scene-list replacement on delete, partial-content acceptance and empty-create warnings, stable IDs/collapse state, existing-first image URLs, normalized texts/notes/NPCs, client-ID registration, and pending encounter-link queuing. Encounter-disabled create must clear both encounter fields; encounter-disabled update must retain the existing `encounterId` while leaving an incoming `encounterClientId` available to the established unresolved-link warning path.
- Scene text projection and create eligibility in `server/aiPatchService.js` use the closed `summary`, `goal`, `stakes`, and `location` field set. A truthy object `texts` source takes precedence over flat scene fields; otherwise flat fields apply. Preserve own-field-only updates, per-field existing fallback, scalar coercion, and partial creates. Eligibility independently accepts normalized text, note title/text, NPC name/description, encounter ID, or image URL; malformed non-array note/NPC collections are empty, inherited text fields are ignored, numeric/boolean scalar text remains content after coercion, and fully empty creates retain the exact warning.
- Full scene normalization in `server/aiPatchService.js` composes text, note, NPC, identity/UI, encounter, and image policies. Preserve stable existing IDs, generated create IDs, replacement objects on update, existing-only collapse flags and image precedence, omitted valid note/NPC retention, explicit empty-array clearing, simplified note titles, matched collapse state, and `_aiIgnored` reinsertion. NPCs accept strings or object `name|firstName` plus `description|trait`, remove invalid entries, and discard extra fields. Encounter resolution is direct ID, then mapped encounter client ID, then existing ID; pending-link finalization remains authoritative afterward. Malformed stored note/NPC collections normalize to arrays.
- Encounter AI mutations in `server/aiPatchService.js` must pass session and permission eligibility before target/context preparation, then load the Bestiary index before dispatching even delete, missing-target, or unsupported eligible operations. Resolve operation ID before target ID before the focused encounter fallback. Keep create-only client-ID plus predeclared scene-link validation, server-generated IDs, created-ID/client-map registration, immutable delete replacement, update object identity, existing-monster fallback when `patch.monsters` is not an array, normalized participant instances, exact warnings, and final pending-link then orphan cleanup order.
- Encounter participant projection in `server/aiPatchService.js` narrows `monsterName` before `name`, resolves the first insertion-ordered `name|source` Bestiary prefix, spreads the resolved monster before instance overrides, and keeps caller ID before Bestiary ID before generated ID. Preserve per-instance IDs, caller display-name override, original Bestiary name, resolved source precedence, modern object-HP average versus legacy `hit_points`, first truthy AC entry versus legacy `armor_class`, and zero combat defaults for unresolved monsters. Treat nullable/malformed `hp` as eligible for the legacy fallback, and keep clock/random seams deterministic in focused tests.
- Character/NPC AI normalization in `server/aiPatchService.js` uses own-property name detection, canonical-before-alias scalar lookup, existing-value fallback, normalized levels, and presentation-state retention. Preserve `name|fullName|title|firstName|first_name|lastName|last_name`, `race|species`, `class|role`, `motivation|goal`, `description|bio|backstory`, and `trait|personality|quirk` precedence; explicit name-part clearing, omitted-field retention, existing IDs, collapsed/image state, simplified notes, matched note IDs/collapse, and `_aiIgnored` reinsertion order. Non-array stored notes are malformed input and must recover to the established empty placeholder rather than reaching array operations.
- Location/faction AI normalization in `server/aiPatchService.js` keeps `name|title` sanitization and `description|summary|text` coercion domain-specific while sharing private entity note, ID, collapse, and image policies with characters/NPCs. Preserve first-own canonical-before-alias selection, explicit empty clearing, omitted-field retention, existing IDs and UI/image state, simplified notes, matched note collapse, `_aiIgnored` order, campaign/session scope routing, and duplicate replacement during scope movement. Malformed non-array location notes recover to the standard placeholder; inherited aliases never become persisted fields.
- Typed global workflow action modules live beside `shared/model/actions.js`; the JavaScript action barrel remains the runtime-compatible public facade. Keep each workflow focused and preserve action type strings and payload normalization during conversion.
- Modal, rules-reference, mention-picker, dice, and message-box workflows already have focused TypeScript action modules. New workflow actions should follow this pattern instead of growing the compatibility barrel.
- All global action creators now originate in typed workflow modules; `shared/model/actions.js` is re-export compatibility only. `appStoreTypes.ts` defines the composed state/action/dispatch contracts; `src/app/model/appStore.ts` implements the sole strict store API. The retired shared facade/runtime must not return.
- UI-settings action normalization belongs to the closed typed policy table in `shared/model/appStateActions.ts`; use `normalizeUiSettingsPatch` when validating trusted application patches and keep `setUiSettingsAction` as the stable `ui/setSettings` envelope. Add a policy entry and focused coverage for every new UI field. Preserve own-property-only updates, theme/view fallbacks, grid-column parsing and 1–4 clamping, truthy simplified notes, string prompt coercion, record-only campaign prompt maps, uppercase/deduplicated/sorted sources, and false-only disabling for default-enabled flags. Unknown and inherited keys must not enter store state.
- Typed lower-level reducer sections live in `shared/model`; `src/app/model/appStateReducer.ts` is the ordered app-owned top-level composition of settings/sync, workflow, then navigation policies. Keep each extraction behavior-identical and covered by the existing store/workflow tests.
- Store transitions are split across `settingsSyncReducer.ts`, `navigationStateReducer.ts`, and the workflow reducer family; `src/app/model/appStore.ts` solely owns stateful infrastructure and invokes its local `appStateReducer.ts`, while `shared/model` retains only effect-free action/type/reducer policies. `workflowReducer.ts` is an ordered first-match composer over the private modal, mention-picker, dice, message-box, and rules-reference reducers. `navigationStateReducer.ts` is likewise an ordered composer over private version, route, and active-resource reducers; route lookup/identity projection lives in `navigationStateModel.ts`. Preserve `undefined` for unmatched actions, exact current-state identity for invalid/duplicate/no-op workflow and navigation transitions, independent dice request/result fields, branch truncation after history rewind, finite index normalization/clamping, campaign lookup by strict slug, string-compatible session/encounter route IDs, clearing stale descendants when campaign/session ownership disappears, monotonic refresh/reload versions, and unchanged sibling-slice identity. Add new workflow/navigation domains as focused reducers instead of growing either composer into a switch.
- Every backend repository port has a colocated TypeScript declaration. Keep required-method runtime guards; declarations describe trusted internal calls but do not validate filesystem, HTTP, archive, or AI input.
- Shared HTTP transport is TypeScript and exported through `shared/api/index.ts`. Domain clients must keep importing the public barrel and should specify focused payload/result types as they migrate.
- Frontend API clients are TypeScript modules exported through their owning entity/feature barrels. Extend contracts at the owning boundary; do not recreate response shapes in widgets or unrelated features. Runtime validation is still required for untrusted responses and imported files.
- AI model discovery, Gemini API-key refresh, and history workflows are strict TypeScript consumers of the AI client contracts. API-key saving uses `saveGeminiApiKeyAndRefreshModels`: trim and reject empty keys before persistence, save exactly once, retry model discovery up to five times with 500 ms delays, treat a null/permanently failing model refresh as non-fatal after a successful save, report only the terminal refresh error, and select the server default or first returned model. Preserve dependency injection in tests, explicit handling of nullable transport results, and history resource, restore mode, and retry payload ownership in `features/ai`.
- Frontend AI operations use the discriminated union in `src/features/ai/model/operationContracts.ts`. Do not replace it with generic operation records; runtime Gemini payloads must still pass the server validator before these trusted frontend types apply.
- AI diff public composition and types are exposed by `src/features/ai/model/aiDiff.ts`; private contracts, snapshot/identity policies, LCS line projection, and session/custom-Bestiary expansion live in `aiDiffContracts.ts`, `aiDiffSnapshot.ts`, `aiLineDiff.ts`, and `aiDiffExpansion.ts` and must not be imported outside the feature model. Line transition classification delegates cursor availability, current-line equality, and LCS look-ahead removal policy to private helpers. Preserve context only when both current lines exist and match exactly, addition when the old side is exhausted, removal when the new side is exhausted, removal-first `>=` LCS ties, added-before-later-old-match and removed-before-later-new-match transitions, Unicode text verbatim from pretty JSON, and exact old/new numbering. Also preserve strict record narrowing, zero-valued IDs, `id → slug → derived name → index` pairing, before-key then after-only ordering, after-side `listIndex` with `null` for deletion, `parentResourceId` plus derived IDs, name/summary/known-array/extra-data/extra-top-level session ordering, unchanged-resource fallback, and the 200,000-cell full-replacement guard. Add regression coverage when changing identity, expansion, or line classification.
- Granular array expansion delegates each keyed item to private suffix and diff constructors; session expansion delegates snapshot normalization, known scalar fields, known arrays, and uncovered data/top-level fields to focused private policies. Keep those helpers unexported and retain the original resource when expansion is inapplicable or produces no changes. Undefined sides normalize to `null`; names are derived from the after item before the before item, trimmed, and fall back to the stable pairing key; array additions carry their after index while deletions carry `null`; custom-Bestiary expansion reuses the same mechanics.
- AI attachment allowlists/identity, prompt-target builders, and context-list mutation contracts are owned by their TypeScript modules under `src/features/ai/model`. Keep browser `File`/`Blob` handling at this feature boundary and preserve immutable context updates and `_aiIgnored` note filtering.
- AI attachment controls receive validation notices only through the typed `AiAttachmentAlertRuntimeProvider` supplied by `App`; their private runtime hook must not become a feature-root export. Preserve native input clearing, no-file/no-slot exits, asynchronous selection preparation, functional attachment merges before non-awaited error notification, gallery behavior, and stable module-scope composition factories. `features/ai` must not import `app/model` or `shared/model`; generation lifecycle request IDs are feature-owned numbers rather than global modal IDs.
- AI token estimates are deliberately approximate and owned by `src/features/ai/model/tokenEstimation.ts`. `buildAiTokenEstimateContext` owns Bestiary/campaign/session/encounter context projection; `estimateAiAttachmentTokens` owns fixed per-image and byte-based file costs; `buildAiTokenEstimate` only composes mode, request shape, text cost, and totals. Preserve mode precedence, session-owned entity-list precedence over fallback lists, context inclusion filters, ignored-content filtering, selected-session compaction, the four-bytes-per-file-token formula, fixed image cost, and separate text/image/file totals. Update focused tests if estimation inputs or formulas change.
- Campaign token-estimate projection composes private header, optional-note, and entity-list policies; entity display-name precedence composes one private truthy-value resolver. Keep these helpers unexported. Preserve `sessionData.name || sessionName || ""`, omission of the `notes` property when campaign notes are disabled, header-only output when context is off, empty session arrays overriding non-empty fallback collections, NPCs using the character key resolver, context `included: false` and explicit item exclusions, `firstName/first_name` then `lastName/last_name` joining before `name`/`title`, and original truthy rather than nullish fallback behavior.
- `src/features/ai/model` is fully TypeScript. Context loaders depend on injected API functions, retain cancellation guards, and rely on unchanged context-list objects preserving reference identity. Character/NPC and location context-list identities belong to `contextIdentity.ts` and are reused by widget presentation through the feature public barrel; preserve trimmed `slug → id → full name → name → title` character precedence, trimmed `slug → id → name` location precedence, zero-valued numeric IDs, modern/legacy first/last-name fields, empty keys for unidentified entities, and separate context maps rather than scope prefixes. Do not duplicate key fallbacks in widgets or move HTTP calls/widget state into these model modules.
- Encounter participant, session view, and spell-card domain contracts are owned by their TypeScript entity models. Preserve participant IDs, session encounter ID comparison, spell source data variants, and translation callback behavior when extending them.
- Rules-reference navigation, theme application, and Bestiary search normalization are typed at their owning feature/entity boundaries. Rules-link controls receive navigation and error reporting through the public `RulesReferenceRuntimeProvider` from `App`; Sidebar forwards its typed navigation request through its injected runtime, and `features/rules-reference` must not import `app/model` or `shared/model`. Keep theme writes limited to `data-theme`, and keep search normalization tolerant of string and structured monster types.
- `src/entities/bestiary/model` is fully TypeScript. Preserve legacy and 5eTools-compatible HP/AC/speed/save/type/defense variants in `MonsterStatBlockModel`; do not narrow official/custom monster input to only one source schema.
- `CampaignViewModel` and `SessionViewModel` live in their respective entity `model` segments and are imported through entity public indexes. `idsEqual` lives in `shared/lib`. Do not recreate the retired `src/models` directory.
- `src/entities/campaign/model` is fully TypeScript. Keep entity lookup and card/view contracts owned there, preserve note IDs and virtual-note materialization, and import runtime values through `entities/campaign/index.js`; the declaration facade must re-export owning types rather than duplicate them.
- Generic `useDebounce` lives in `shared/lib` and is imported through its public index; do not recreate `hooks/useDebounce`.
- Campaign screen orchestration lives in `pages/campaign`; import `CampaignPage` only through `pages/campaign/index.js`. Former `components/CampaignView` and `hooks/useCampaignView` paths are retired.
- Session screen orchestration is fully strict TypeScript under `pages/session`; import `SessionPage` only through `pages/session/index.js`. `SessionPage.tsx` composes focused private header, notes, scoped-entity resolver/modals, scenes, checklist, scope-import loading/empty/items, hash navigation, outside-click lifecycle, and floating-action boundaries; scene/todo implementations under `pages/session/ui/components` are private TSX. Former `components/SessionView`, `hooks/useSessionView`, and page-level JSX implementations are retired.
- `src/pages/session/model` is fully TypeScript. Keep checklist progress, page callback composition, and cross-feature setter adapters in `useSessionView.ts`; loaded-session normalization plus pure keyboard/editable-target routing, realtime-sync resource/pending decisions, note/section/scene-note presentation, session-data fallback, rename planning/execution, encounter quick-access projection, scope-import type/copy, and scene-note bulk-collapse plans belong in `sessionPagePresentation.ts`. Loaded sessions without `data` preserve identity; data-bearing sessions immutably normalize notes, scenes, NPCs, and locations while retaining unknown fields. Scene-note presentation owns virtual rows, content-aware collapse, bulk direction/labels, and list/action visibility. Rename rejects non-string, empty, and unchanged values without trimming and delegates the raw name to the existing session editing/persistence owner. Editable targets are exact uppercase `INPUT`/`TEXTAREA` or truthy contenteditable. Sync requires a truthy version plus compatible campaign/session scope; AI discards pending writes and reloads, while session/import/entity/image resources reload only without pending writes. Session-scoped NPC/location normalization, display-name rules, and ID recovery belong in `sessionEntityModel.ts`; display candidates retain truthy selection before final trimming, NPC full names precede name/title/fallback, IDs retain truthy strings or finite numbers including zero, and invalid IDs use the exact type-specific timestamp prefix. Strip internal fields except the explicit `_aiIgnored` flag, validate images/notes at runtime, and normalize card-note fields before rendering. Preserve modal blocking for shortcuts, native editor undo unless app history is requested, AI sync pending-save discard, first-scene encounter labels, hash-driven note expansion, virtual trailing notes, scope IDs, session-data identity, hidden-footer modal close behavior, parent entity-link fallback, and session-scoped modal CRUD. Editing/history/persistence remain in `features/session-editor`, encounter creation in `features/encounter-editor`, and scope movement in `features/campaign-entity`.
- Session page app state arrives only through its page-owned `SessionPageRuntime`, mapped by `app/ui/SessionPageRuntimeHost.tsx` inside `SessionRoute` after the active-campaign guard. Keep the Context hook private to `pages/session`; expose only the provider/types through the page root. The runtime carries live active campaign/session/sync state plus narrow navigation, active-session selection, campaign reload, entity refresh, confirmation, prompt, and message commands. Do not inject raw store state or dispatch into the page, or move session loading/normalization, sync decisions, editing/history/persistence, scope movement, encounter creation, keyboard/hash/header behavior, or quick-access navigation into the host.
- Encounter screen orchestration is fully strict TypeScript under `pages/encounter`; import `EncounterPage` only through `pages/encounter/index.js`. `EncounterPage.tsx` composes focused header, participant-row, monster-detail, character-picker, Bestiary, notification, and page-private Bestiary AI edit/draft modal boundaries. Former `components/EncounterView`, `hooks/useEncounterView`, and page-level JSX implementations are retired. Do not recreate the retired `src/hooks` directory.
- `src/pages/encounter/model` is fully TypeScript. Keep page-state normalization, undo/redo coordination, import/export events, dice-result routing, participant selection, grid identity/HP/keyboard/sync/AI-update policies, and encounter-page callbacks at this boundary. `encounterPagePresentation.ts` owns exact-case editable targets, KeyZ/KeyY history policies, string-compatible load lookup plus retry/not-found/loaded execution, validated import projection, truthy/duplicate dice-result processing, route-matched HP application, monster-drop planning/execution, none/update state plans, generated-monster result application, and local/global draft restore effects. Preserve active-session dispatch before load planning, the 300ms retry, first-participant selection, optional history reset, imported name fallback, per-monster clock/random instance IDs, processed-before-context dice semantics, numeric result ID `0` rejection, minimum rolled HP of one, JSON monster-order comparison, reorder-ref retention for no-op drops, undo/redo-clear/state/selection/persistence effect order, default immediate persistence/undo, and explicit push/persist/debounce/preferred-ID overrides. AI result callbacks preserve draft-mode before draft-entry order, source-image enrichment, target instance IDs, local returned-session updates, global undo silence, and exact resource-ID filtering; restore requests always finalize through the model lifecycle executor. Navigation gives Escape-with-Bestiary priority before editable blocking; sync accepts only truthy-version, non-pending, compatible sessions/AI/import events. Monster row stats retain nullish AC/HP precedence and numeric zero. Pure initiative/CR derivation belongs in `encounterViewMetrics.ts`; preserve numeric passthrough plus fractional, structured, invalid, and decimal CR behavior. Custom-monster field-save and AI-generation planning/persistence/request lifecycles belong in `features/ai-edit-monster`; page UI owns the encounter-specific edit/draft shell, API-effect adapters, and encounter updates. Persistence and custom participant synchronization remain owned by `features/encounter-editor`.
- Encounter grouped-grid projection, representative IDs, available-character filtering, character double-select modal routing, monster select/focus routing, add-character projection, rename planning, navigation/history execution, player-create lifecycle, and collection normalization also belong to `encounterPagePresentation.ts`. Preserve official-monster grouping by normalized original/name plus source, local-override isolation, select-before-focus order, handled-before-shortcut-action order, campaign refresh before nullable create-result validation, add-before-form-reset, unconditional create finalization, raw non-empty rename values, and public page imports only through `pages/encounter/index.js`. Private combat/detail/header/overlay components must remain private UI.
- The embedded spells browser is fully strict TypeScript under `widgets/spells-browser`. Browser consumers import it through `widgets/spells-browser/index.js`; Node/model tests use its JSON-free `model.js` facade. App/page owners inject the stable public component into configured rules-reference content; `widgets/rules-reference-modal` must not import it directly. Spell/reference identity, nullable source/spell/settings normalization, class/school options, source/level/class/school/basic/detailed filtering, level/name sorting, sort transitions, initial-selection fallback, source-filter validity, and error extraction live in `model/spellsBrowser.ts`; controls, list/detail composition, and the sole-consumer `ui/SpellCard.tsx` stay private UI. `SpellCard.tsx` configures its `RulesLink` component and rich-content renderer set exactly once at module scope; never recreate either configured symbol during render. Preserve campaign-over-global ignored sources, campaign/settings persistence, `all` source filtering, `none → desc → asc` sorting, explicit `name|source` selection, displayed-list then all-list fallback, virtualized scroll restoration, mobile list/detail scrolling, active-selection callbacks, optional insert/double-click behavior, injected rich-content options, and all spell-card rendering behavior. Do not import private paths or recreate `components/Spells` or the retired `widgets/spell-card` slice.
- Rich monster rendering lives in `widgets/monster-stat-block` and is imported through its public index; pure monster derivation stays in `entities/bestiary`. The widget is fully strict TypeScript: browser consumers use `widgets/monster-stat-block/index.js`, while Node/model tests use its JSON-free `model.js` facade. App/page owners inject the stable public component into Bestiary, AI-response, encounter-detail, and configured rules-reference compositions; those sibling widgets must not import it directly. Source-variant normalization for spell lists, structured spellcasting, senses, action/content arrays, changed-field markers, token sources/upload results, mutation identity, and drag payloads belongs in `model/monsterStatBlockPresentation.ts`; private token, header, ability, stats, spellcasting, action, and content composition stays under `ui`. Private `monsterStatBlockRichContent.ts` configures one stable module-scope `RulesLink` component and rich-content renderer set for every stat-block section; never duplicate those factories inside render paths. Preserve controlled and API-owned favorite modes, injected versus global encounter actions, both injected token callbacks `(monster, url)` and custom-bestiary persistence, official local/external token fallbacks, drag MIME payloads, legacy and 5eTools spellcasting shapes, sense links, dice rolls, creature-source fallback, AI highlight classes, and grid/single layout. Do not import private widget paths or recreate `components/MonsterStatBlock`.
- The reusable Bestiary browser is fully strict TypeScript under `widgets/bestiary-browser`. Browser consumers import `BestiaryBrowser` only through `widgets/bestiary-browser/index.js`; Node/model tests use the JSON-free `widgets/bestiary-browser/model.js` facade. Canonical monster/source identity, reference resolution, nullable response/source/sync/AI normalization, legendary-group enrichment, favorites, filtering, CR sorting, realtime sync, selection/refresh and initial-selection scroll plans, list-row presentation/action planning, custom import/merge, field-editor start/edit payloads, AI instructions and AI-edit start/error/finalization policies, draft metadata preservation, AI draft-restore start/result plans, and update selection live in `model/bestiaryBrowser.ts`. Keep API/effect orchestration in `BestiaryBrowser.tsx`; app-global preferences, campaign state, raw sync events, campaign/settings replacement, messages, and confirmations arrive only through its widget-owned `BestiaryBrowserRuntime`, mapped by `app/ui/BestiaryBrowserRuntimeHost.tsx`. Private row actions/content, toolbar, virtual list, details, and AI assistant composition stay in `BestiaryContent.tsx`. Field-editor start planning rejects a missing target or falsy name; normalized custom sources retain the same object as original/draft in `edit` mode without resolving a token. Official monsters retain the original object, enter `create-based`, clone a `CUSTOM` draft, preserve every truthy string image verbatim, and lazily delegate fallback URL derivation to the injected entity-owned `MonsterStatBlockModel.localTokenSrc`; the UI executes mode → original → draft setters in that order. Restore start planning rejects missing IDs and active restores, preserves the exact resource-ID list, and deep-clones an undo snapshot only for apply. Result planning keeps `response || original entry`, emits no update without `updated`, resolves apply selection from the restored entry plus resource IDs, returns the apply snapshot only when the resulting custom-monster list differs, and never creates a snapshot or selected-name option for undo; both update modes keep `trackUndo: false`. The UI executor owns Bestiary transport, busy/error/finally lifecycle, and exact entry → optional undo snapshot → custom update effect order. AI-edit start planning treats a missing target or target without a truthy name as a silent skip, requires trimmed instructions outside create-based mode, preserves target and attachment-array identity, and owns the exact custom-monster payload flags, Bestiary path, selected-model truthy fallback, language, custom-Bestiary scope, and null context. Abort errors produce no UI message; other failures keep a non-empty `Error.message` or use the localized fallback. Clear an AI-edit controller only when it is still the active request, and preserve result application before successful editor reset. Initial-selection scroll planning requires the opt-in flag, truthy reference and selected names, and normalized reference matching; it preserves the raw `${source || ""}:${name}` once-key, uses canonical monster identity to find the first displayed index, and rejects missing or already-scrolled targets. The effect records the key before scheduling one animation frame, cancels that frame on cleanup without resetting the key, and resets the key only when the initial name/source inputs change. List-row presentation owns selected/favorite/custom flags, toggle target, primary action/title, token source, and CR fallback. Preserve select-callback precedence over add, selected-row click clearing, exact monster identity for unselected clicks, explicit string `imageUrl` precedence including `""`, non-string token fallback, and falsy CR display as `--`. Double-click and Plus execute the same primary action; action buttons stop propagation before delegation, Edit remains universal, and AI/Delete remain custom-only. Sync-event parsing is the runtime boundary before planning: reject non-record roots, require an exact string `resource`, accept only primitive string/number `version`, and narrow optional monster fields to primitive strings or `undefined`. Do not trim, coerce, finite-check, or truthy-check parser values; preserve empty/whitespace strings, numeric `0`, `NaN`, and infinities exactly, and keep optional keys present with `undefined`. Realtime sync planning then applies truthy-version gating and the exact case-sensitive `bestiary|custom-bestiary|ai` allowlist. Every accepted event refreshes favorites; only custom/AI events reload monsters; only a truthy monster name creates a pending selection and suppresses auto-selection; preserve the raw name and `monsterSource || "CUSTOM"` fallback. The UI effect executes the plan, normalizes nullable favorites to `[]`, logs refresh errors without blocking reload/selection effects, and increments the reload token functionally. Preserve official-first auto-selection, explicit source matching, campaign/global ignored sources, `CUSTOM` identity, legendary action enrichment, fractional CR sorting, case-insensitive import replacement, undo snapshots, token-image fallback, draft resource-level apply/undo, mobile scroll behavior, stable ReactList keys, and runtime narrowing of imported JSON and external envelopes. Do not import private widget model/UI paths or move this composed browser into an entity/feature layer.
- `BestiaryBrowser` receives the required configured `BestiaryAiModals`, `AiAssistantPanel`, `MonsterStatBlock`, `ResponseModal`, and `MonsterEditorModal` contracts from its sole page-level composition owner, `EncounterPage`; it must not import sibling widgets or the page implementation directly. The AI-modal slot is a widget-owned callable structural contract. This dependency injection changes ownership only: keep browser state and handlers, `MonsterEditorModal` → `MonsterAiActionModal` → `BestiaryAiModals` order, `ResponseModal` forwarding, render placement, forwarded props, and nullable response/editing behavior unchanged.
- Bestiary AI draft restore lifecycle is model-owned by `executeAiDraftRestore` and supersedes the earlier UI-ownership wording for this flow. A null start must read no injected dependency. Preserve busy=true before guarded work, exclusive apply/undo routing with exact entry and `{ resourceIds }`, nullable result planning, entry → optional undo snapshot → custom update effect order, one original-error path for transport/planning/effect failures, short-circuiting after a failed effect, and busy=false in `finally` even when the error callback throws. `BestiaryBrowser.tsx` only builds the start plan and injects API, state, undo, update, and localized-alert adapters.
- Custom-monster deletion in `widgets/bestiary-browser` uses model-owned eligibility, official-first list replacement, and favorite-removal policies. Require a truthy name and normalized `CUSTOM` source, but preserve the exact untrimmed name for confirmation and transport. Keep confirmation before cloning the undo snapshot; on success preserve undo → auto-selection suppression → selected ref clear → selected state clear → list replacement → favorite removal order. Append API arrays unchanged after current official monsters, treat non-arrays as empty, and remove favorites by exact case-sensitive name plus normalized custom source only. The UI retains confirmation, API/error handling, refs, and React state effects.
- Bestiary detail presentation uses `getBestiaryDetailPresentation` from the widget model. Null selection returns no plan and must not resolve localized add-title text. Preserve selected monster and callback identity, normalize nullable insert/add/delete actions to `undefined`, use the same add callback for name-click and encounter-picker actions, resolve the localized add title lazily only when add exists, derive favorites through canonical identity, and expose deletion only for normalized custom sources. `BestiaryContent.tsx` retains the private container/ref, Insert button, stat-block composition, favorite/AI/edit forwarding, and search highlighting.
- Bestiary CR parsing/sorting in `model/bestiaryBrowser.ts` uses focused private value/text/scalar/fraction/direction/comparator policies behind the existing model facade. Preserve structured-versus-scalar CR extraction, exact numeric passthrough including `NaN`/infinities, nullish `"0"`, `parseFloat(...) || 0`, slash splitting/defaults, truthy numeric denominators, invalid-numerator `NaN`, and ignored extra slash parts. Sorting must clone for every mode, keep `none` order, negate only CR differences for descending, retain ascending `localeCompare` ties in both directions, and preserve input/item identity.
- Bestiary filtering in `model/bestiaryBrowser.ts` uses private selected-source membership, exact source-filter, favorite-gate, matcher-selection, and per-monster policies behind `filterBestiaryMonsters`. Preserve case/whitespace-normalized source membership, empty-selection match-none behavior, the literal lowercase `"all"` bypass only, normalized exact-source comparison otherwise, favorite checks only when enabled, detailed/simple matcher selection after preceding gates, exact search forwarding, input order, and returned monster identity.
- Custom Bestiary update planning in `model/bestiaryBrowser.ts` separates updated-envelope narrowing, list normalization, generated/requested selection, and undo eligibility behind `getCustomBestiaryUpdatePlan`. Treat only a record with an array-valued `monsters` property as an updated list, including an empty array; filter malformed monster entries at this runtime boundary; prefer the first valid generated monster over `selectedName`; replace a generated draft with the returned custom monster when their normalized names match; otherwise preserve generated identity. Requested selection remains truthy-name gated, and undo tracking requires an updated array plus `trackUndo !== false`.
- Bestiary source-list normalization in `model/bestiaryBrowser.ts` uses private record-candidate and string-narrowing policies behind `getBestiarySourceCodes`. Accept only a root array; pass direct strings through unchanged; resolve records with nullish `value -> source -> id -> name` precedence; and accept the selected candidate only when it is a string. Preserve non-string candidate blocking, empty-string removal, whitespace strings, duplicates, and input order; do not trim, uppercase, deduplicate, stringify, or fall through after a non-nullish candidate.
- Legendary-group enrichment in `model/bestiaryBrowser.ts` uses private record narrowing plus name/source fallback policies behind `enrichMonstersWithLegendaryGroups`. Only record-shaped `legendaryGroup` values may override monster identity; string overrides, including `""`, remain valid. Invalid/missing names fall back to the exact monster name; invalid/missing sources use `String(monster.source ?? "")`. Lookup identity remains trim/case normalized. Unmatched monsters retain object identity; matched monsters are copied and their `lairActions`/`regionalEffects` are replaced exactly, including explicit `undefined` from the group.
- Canonical Bestiary identity in `model/bestiaryBrowser.ts` uses private normalized-name eligibility/equality and normalized-source equality behind `isSameMonsterIdentity`. Normalize and read both names first; reject an empty normalized left name; and read/compare sources only after names match. Names and sources remain trim/case normalized through the shared coercing helpers, so both missing sources compare equal and numeric/string-equivalent sources can match. Preserve symmetry for valid names and all selection, favorite, list-index, and refresh consumers.
- Bestiary row-plan aggregation in `model/bestiaryBrowser.ts` composes private CR fallback, favorite-title, next-selection, primary-action/title, and token-source projections behind `getBestiaryMonsterRowPresentation`. Keep canonical selected/favorite/custom flags, `select -> add -> null` action precedence, selected-row clearing, original monster identity for unselected rows, exact localized title keys, explicit string `imageUrl` precedence including `""`, non-string fallback tokens, and falsy CR display as `--`.
- Custom Bestiary refresh selection in `model/bestiaryBrowser.ts` composes private pending-name and current-custom recovery behind `getCustomRefreshSelection`. Pending recovery always runs first, uses normalized name only, intentionally ignores the pending reference source, skips official candidates, and returns the first matching custom object. Current fallback is allowed only for normalized `CUSTOM` selections and uses canonical name/source identity; otherwise return `null`. Preserve collection object identity throughout.
- Dice formula parsing, rolling, and probability utilities live in `shared/lib`. `diceExpressionParser.js` is the private shared recursive-descent driver for both roll-stat and probability-distribution evaluation; keep value creation, unary negation, multiplication, and signed addition injected from `dice.js` rather than duplicating grammar control flow. In `dice.js`, input normalization/stat parsing, invalid-result creation, exact-one-d20 critical resolution, legacy/advanced presentation, final result projection, random roll collection, plain-d20 tracking, stable keep-index selection, plain/kept stat projection, probability request validation, limited evaluation, sorted outcome projection, probability summary construction, and operator/dice/number token readers are focused helpers; keep `rollDiceFormula`, `rollDiceTerm`, `getDiceProbabilityDistribution`, and `tokenize` as thin orchestration. Token readers return a private `{ token, length }` cursor contract; preserve operator-before-dice-before-number precedence, exact matched-length advancement, shorthand and zero-count dice falling back to one die, leading-zero numeric parsing, case-insensitive dice syntax, global whitespace removal before tokenization, and whole-formula failure for unknown or partially consumed input. Also preserve unary `+/-`, multiplication-before-addition precedence, parentheses, complete token consumption, null probability results for invalid/resource-limited formulas, `0` probability-limit options falling back to defaults, valid empty distributions with zero summaries, probability aggregation and numeric outcome ordering, original-case formatted probability formulas, invalid roll results without `expressionBreakdown`, original-case advanced formulas, legacy aggregated formulas, floored roll averages, probability-weighted distribution averages, the legacy roll-only lone-`-` fallback to negative-zero stats with a `-1` modifier breakdown, exact-one-plain-d20 natural 1/20 overriding the computed total even inside mixed/multiplied formulas, no critical tracking for kept d20 groups, original breakdown order, stable index-order tie selection, dropped-roll markers, approximate kept-die roll averages, number/dice atoms, keep-high/low/zero/over-count limits, and distinct roll versus distribution failure results. Interactive dice UI lives in `features/dice` and is imported through `features/dice/index.js`; do not export/deep-import the private parser or recreate former dice component/`utils/dice` paths.
- `features/dice` has no JavaScript UI/model implementations remaining; `index.js` is its runtime compatibility barrel and `index.d.ts` is the typed facade. Roll request decoding, shortcut detection, formula/history transformations, context wrapping, recharge classification, breakdown labels, probability labels, and bar widths live in `features/dice/model.ts`; DOM listeners, timers, animation frames, injected runtime commands, portals, and rendering stay in TSX UI. `App` supplies the stable request command through `DiceRequestRuntimeProvider`; `app/ui/DiceCalculatorHost.tsx` alone selects the live `rollRequest` and injects the result-publish callback. Preserve multiplication-glyph normalization, truthy-context compatibility, request-ID deduplication, two-frame roll animation, player-question auto-close, outside-click exceptions, ten-entry history, probability limits, and public imports through `features/dice/index.js`.
- Rules link/preview behavior, sibling-entity preview loading/input resolution, and rules-modal navigation commands live in `features/rules-reference`. Rules modal composition lives in `widgets/rules-reference-modal`; its public runtime API exposes `createRulesReferenceModalContentComponent` plus the public `RulesReferenceModalHost`, not the raw content/view renderers. `MainContent` and `EncounterPage` configure stable module-scope rules content by injecting public `MonsterStatBlock` and `SpellsBrowser` symbols through lower-layer structural slot contracts; `App` passes those same stable symbols directly to the one global host. Private `RulesReferenceModalView.tsx` also configures its `RulesLink` component and rich-content renderer set once at module scope. Raw content and view components remain private, factories must never run during render, and the rules-reference slice must not regain sibling-widget imports. Preserve all seven tabs, `forceTab` and back/forward history semantics, detailed-search match indicators, official/custom Bestiary aggregation, token fallback, source-aware selection, scroll-to-selection, insertion tags, empty selected-item detail output, selected-Bestiary-only stat rendering, and spells-tab-only browser rendering with the exact callback projection and fresh render options. The tab catalog, source-qualified spell/creature identity, search matching, response-list normalization, and inline-tag construction remain strict TypeScript policies exposed to Node code through `widgets/rules-reference-modal/model.js`; do not import private `model/*` or `ui/*` files.
- Composed monster editing with reference insertion lives in `widgets/monster-editor-modal`, whose public runtime API exposes a configuration factory instead of the raw renderer. `MainContent` and `EncounterPage` create stable module-scope monster editors from their configured rules content and reuse those symbols in every direct, Bestiary, and AI-response path. Its lower-layer structural content slot accepts only the selection callback; public editor props exclude that slot. Preserve the fresh `{ ...selection }` adapter projection, outer-props-before-injected-slot precedence, rule-picker-only rendering while `ruleInsertTarget` exists, nested modal props/order, and delegation into `features/edit-monster`; the feature must not import widgets. Successful saves use the feature-owned `NamedMonsterData` guarantee, so keep name normalization in `prepareMonsterDraftForSave` and do not add consumer-side casts.
- `features/rules-reference` is fully strict TypeScript behind its JavaScript public barrel and typed facade. `model/referencePreview.ts` and `model/referenceResolvers.ts` own orchestration across reference, spell, and Bestiary entities; `model/rulesLink.ts` owns reference parsing, canonical names, resolver/loader tables, navigation targets, creature CR/AC/HP metadata, tagged-tooltip normalization, and text/roll descriptors. Node consumers use the JSON-free `features/rules-reference/model.js` facade; browser composition owners import `createRulesLinkComponent` through `features/rules-reference/index.js` and inject the structural `RollDice` slot. Raw `RulesLink` is private and must not be exported, deep-imported, or configured during render. Browser-only source/spell formatters remain TSX-injected. Keep recursive list/section/table rendering, image error handling, localized alerts, stale async-load guards, and interactive dice composition in UI. Preserve spell source identity, creature source fallback, status-to-conditions routing, skill ability uppercase display, recharge thresholds, hit/damage/dice formulas, dynamic formula labels, and public-entry imports.
- `features/edit-monster` is fully strict TypeScript behind its JavaScript public barrel and typed facade. Monster field/JSON normalization, schema-preserving field updates, dice-average derivation, speed parsing/formatting, action mutations, Ctrl/Cmd+K detection, rule-tag insertion, JSON object validation, name validation, cloning, and source restoration live in the Node-safe `features/edit-monster/model.ts`. Keep React state, modal composition, localized errors, textarea selections, and the injected rules-reference component in TSX UI. Preserve legacy `desc` actions, 5eTools `entries`, structured type choices/tags, scalar and structured speed, array/scalar senses and languages, original source on save, Cyrillic `л` shortcut support, and imports through `features/edit-monster/index.js`.
- Generic `ListCard`, `CollapseToggleButton`, and `DraggableList` live in `shared/ui` and must be imported through its public index; their former `components/common` paths are retired.
- Entity-link behavior, bracketed mention rendering, identity, contexts, and generic entity modal resolution live in `features/entity-link`. UI consumers import the unchanged `renderMentionText` through `index.js`; its raw `EntityLink` component is a private implementation detail and must not be exported or deep-imported. Node-only/model consumers may use the model-only public `model.js` entrypoint. Campaign-specific entity modal rendering lives in `widgets/campaign-entity-modal`; browser consumers use its `index.js` and Node/model consumers use `model.js`. Its strict TypeScript policy owns modal-entity slug validation, character/location card projection, mention-name comparison, private-field/note sanitization, and campaign-scope ownership. Preserve `renderMentionText` falsy coercion, bracket splitting, trimming, numeric React keys, and exact link children; also preserve parent resolver precedence, refusal to handle session-scoped entities, refresh dispatch after update/delete/confirmed rename, rename cancellation semantics, `_updateMentionReferences` plus `_mentionOldName`, forced expanded modal cards, and close-after-delete. Do not import private `model/*`/`ui/*` paths or recreate former `components/common/Entity*` paths.
- `src/features/entity-link/model` is fully TypeScript. Contracts live in `entityLinkContracts.ts`; pure normalization/comparison lives in `entityLinkIdentityPolicy.ts`; `buildEntityLinkModalTargetPlan` owns current/modal recursion suppression and scope-preserving target projection; `entityLinkModalUtils.ts` resolves scoped-then-campaign targets and dispatches only open plans. Preserve zero-valued IDs, explicit-scope then `_scope`/entity-scope fallback, exact type and compatible-scope requirements, `id → slug → normalized display name` match fallback, async or sync scoped resolvers, and campaign lookup only after an empty scoped result. Keep `features/entity-link/model.js` as the model compatibility entrypoint and do not deep-import these private policies.
- `src/features/entity-link/ui` is fully strict TypeScript. Context scope memoization, inline-link `preventDefault`/propagation stopping, modal state, resolver-provided React content, and generic entity-modal props use the entity-link model contracts. Generic location/NPC/character modal presentation belongs to `entityLinkModalUtils.ts`; preserve nullable route slugs, no lookup without campaign/name context, current-entity/current-modal suppression, injected resolver precedence, resolved scope in modal/nested context, location modal styling, and public imports through `features/entity-link/index.js`.
- Generic modal UI belongs to `shared/ui`: browser consumers import `Modal` through `shared/ui/index.js`, while its Node-safe policies/types stay in private `modalModel.ts`. Private `useModalController.ts` owns input/checkbox state, refs, focus, current-state keyboard listeners, active-element blur, and event routing; private `ModalView.tsx` owns overlay/card/header/standard-body/footer composition. The synchronized `SHARED_MODAL_PUBLIC_API_PATTERN` must protect all four implementation paths for both JavaScript and TypeScript consumers. Do not export or deep-import the private model/controller/view, add store ownership to the shared primitive, or recreate the deleted `features/modal` slice. The dead imperative `Modal.createApi`/`createModalApi` contract was removed and must not be restored.
- Store-backed global message rendering belongs to `app/ui/MessageBoxHost.tsx`; `App.tsx` composes that host over the shared `Modal`. Lower layers must not import the app host. Preserve custom-content Enter prevention without confirmation, alert close-through-confirm behavior, overlay-only close, close-button cancel requirement, blur-before-callback ordering, input focus/selection, checkbox transformation including keyboard confirmation with the latest value, cancel-disabled buttons/close actions, message-box cancel actions, and shared-modal imports through `shared/ui/index.js`.
- AI attachment behavior and shared AI model-option rendering live in `features/ai/ui`. Raw `AiAttachmentControls` and `AiPromptComposer` renderers are private behind `createAiAttachmentControlsComponent` and `createAiPromptComposerComponent`; their public contracts are callable structural slots that must not import sibling-feature types. `widgets/ai-assistant/ui/aiAssistantUiComposition.ts` configures the stable assistant controls/composer once from public `ImageGallery` and `EditableField` symbols, while `MonsterAiEditModal.tsx` configures its encounter controls once at module scope. Never call either factory during render or restore an AI feature sibling import. Keep model options as immediate `Select` children by calling `renderAiModelOptions`; do not wrap them in a component because the custom Select inspects its direct children. Keep `features/ai/index.js` model-safe for Node consumers and do not recreate `components/ai/AiAttachmentControls`.
- `features/ai-edit-monster` is fully strict TypeScript behind its JavaScript public barrel and typed facade. Mode-specific edit titles, target labels, prompt placeholders, submit labels, field-save plans/persistence, generation plans/payloads, and abort-aware request execution live in the Node-safe `features/ai-edit-monster/model.ts`; `MonsterAiActionModal` remains feature-owned. Cross-feature edit/draft modal composition lives privately under `pages/encounter/ui/components`: `EncounterPage` owns one stable `EncounterBestiaryAiModals` symbol, uses it directly for encounter editing, and injects it into `BestiaryBrowser` through the widget-owned callable structural slot contract. Do not move that shell back into the feature or let the Bestiary import it directly. Preserve edit-before-draft order, null guards, the `general` campaign scope, model/attachment controls, request cancellation while generation is active, silent `AbortError`, non-abort error propagation, unconditional lifecycle finalization, optional attachments, draft resource-level apply/undo callbacks, and public imports through `features/ai-edit-monster/index.js`.
- Node/model consumers that must avoid TSX use `features/ai-edit-monster/model.js`; browser consumers continue through the feature index. Field-edit plans preserve the original encounter target and create-based `CUSTOM` draft fallback. Field-save execution preserves local update → close and persistent save → refresh → encounter update → close order, while failures remain open and route only to `onError`. Draft saves use campaign scope only for local mode, accept nullable local results into state, ignore nullable global results, and reject falsy history IDs including numeric zero; restore plans preserve exact resource IDs and busy guards.
- AI response composition lives in `widgets/ai-response-modal`, whose public runtime API exposes `createAiResponseModalComponent` rather than the raw renderer. `MainContent` and `EncounterPage` configure stable module-scope response components by injecting the public `CharacterCard`, `LocationCard`, and `MonsterStatBlock` symbols plus their owner-configured `MonsterEditorModal` through lower-layer structural slot contracts. Feature-level history/draft dialogs, `AiAssistantPanel`, and Bestiary consumers receive only the configured `AiResponseModalComponent`; never call the factory during render or pass the raw renderer across a slice boundary. The raw renderer must remain private, must not regain sibling-widget imports, and must preserve `useState` -> `useAiResponseDraftController` -> generated-prompt null-guard order, one character card, one location card, two monster stat blocks, `AiResponseModalView` before the one monster editor, and the existing conditional props/callbacks. Former `components/ai/AiResponseModal` is retired.
- AI response preview classification, encounter participant identity, nested session/note/custom-Bestiary draft resolution, and immutable draft replacement live in `widgets/ai-response-modal/model`. Preserve nested-list matching priority (`listIndex`, then `id`, then `instanceId`, then structural equality). Draft text/state synchronization and save/apply/undo orchestration belong to the private typed draft controller; modal shell/details/diff switching/draft-editor composition belongs to `AiResponseModalView.tsx`. Keep these policies out of the preview render tree and do not recreate a JavaScript modal implementation.
- AI assistant orchestration and its image-prompt picker live in the fully strict TypeScript `widgets/ai-assistant` slice; browser consumers import the panel through the JavaScript compatibility barrel `index.js`, while Node/model consumers use `model.js`. `AiAssistantPanel` requires an owner-injected `ResponseModal`: `MainContent` supplies its stable module-scope `MainContentAiResponseModal` for route assistants, and the Bestiary path forwards the stable module-scope `EncounterAiResponseModal` through `BestiaryBrowser`/`BestiaryContent`. The assistant must not regain a sibling import from `widgets/ai-response-modal`. `AiAssistantPanel.tsx` owns only store/effect/request orchestration over private typed context, history, image-prompt, and view boundaries; its prop contract is implementation-owned and re-exported by `index.d.ts`. Preserve the `MainContent` route key and conditional mount, all assistant state/hooks/controllers, forwarding of the supplied response component into the existing history-dialog contract, the Bestiary assistant's unconditional mount and props, and the existing nullable response behavior. The injected-localization presentation model owns response previews/dates, generated-monster prompt cleanup, history request/options/context/detail/title/state projection, campaign entity display names/keys, and scene/image-prompt labels. Generated-monster cleanup uses a three-state outside/inside/escaped JSON-string scanner plus separate structural depth projection; braces inside Unicode strings and escaped quotes/backslashes must never close the snapshot, while missing labels, braces, or a complete object return the original trimmed prompt. Preserve first-truthy-before-trim compatibility for request and entity aliases, lazy `Untitled` translation, explicit retry history instructions, localized mode/on-off labels, parsing-gated generation options, disabled/empty context summaries, Markdown-free response titles, nullable/numeric route IDs, session filename validation/fallback, attachment payload fields, nullable history commands, and runtime narrowing of campaign notes and retry errors. The image-prompt model owns detail/selection mode, context-instruction requirements, generation eligibility, stable fallback keys, custom-monster metadata, and scene list labels; private TSX compositions own the modal, details form, target columns, attachments, and selection callbacks. Preserve context-mode non-empty request validation, loading locks, direct `renderAiModelOptions` children, Bestiary image/no-image columns, campaign scene hiding, session-qualified scene labels, targetless continuation, and injected target builders. Do not import private `model/*` or `ui/*` paths. The legacy `src/components/ai` directory is retired; AI feature UI remains under `features/ai/ui`.
- AI assistant route/state projection stays Node-safe in `widgets/ai-assistant/model/assistantContext.ts`. `getAiAssistantRouteState` is the public composition boundary; private route, prompt, and target projectors must remain unexported. Preserve falsy campaign/session/encounter semantics (`||` prompt fallbacks, empty session as campaign mode, numeric encounter `0` as non-encounter), Bestiary campaign/history forcing with `general` asset ownership, original session/encounter passthrough, and generation defaults. Context data precedence is Bestiary empty data, then encounter, then campaign with injected collections, then session `data`; conflicting runtime flags must retain that order. Do not move store reads, React state, or HTTP calls into this model.
- Session-only scene and todo UI lives privately under `pages/session/ui/components`; do not expose it from the page public API or recreate `src/components/session`.
- Campaign notes graph and partial archive UI are private under `pages/campaign/ui/components`. Global campaign search lives in `widgets/campaign-search`; browser consumers import its modal through `index.js`, while Node/model consumers use `model.js`. Its strict TypeScript model owns deep searchable-text projection, campaign/session index construction, nullable API collection normalization, four-filter behavior, result limiting, snippets, highlight terms, navigation targets, and dependency-injected load execution. Preserve ordered recursive array/record projection, scalar/nullish behavior, nested `_...`/`imageUrl` exclusion, first/last-name → name → title → localized untitled precedence, collapsed-whitespace case-insensitive snippet positioning with the 70/180 window and exact ellipses, campaign-first in-memory entity precedence, four-way parallel source acquisition, complete ordered session hydration, fail-fast source/detail errors, exact error/finalization effect order, and suppression of every post-await effect after cancellation. `ui/CampaignSearchRuntime.tsx` exposes only a provider/types contract for the active campaign value and ordinary target navigation; keep its hook private. `app/ui/CampaignSearchRuntimeHost.tsx` is the sole selector/navigation adapter and is mounted only around `MainContentLayout`'s `<Outlet />`, so it remains live across Campaign/Session routes without covering Sidebar, root modals, or the AI assistant. The private controller owns runtime/effect/query/filter orchestration; the private View owns modal/filter/state composition; target navigation and delayed hash scrolling/cleanup remain browser-owned. Also preserve malformed-campaign narrowing, mention-aware Markdown rendering outside `code`/`pre`, non-empty filter selection, the 80-result cap, stable scene/note fallback IDs and hashes, lazy scene-title translation, and URL hash cleanup. Do not import private `model/*` or `ui/*` paths or recreate `src/components/campaign`.
- The campaign route composition is strict TSX in `pages/campaign/ui/CampaignPage.tsx` and is imported only through `pages/campaign/index.js`. Weak active-campaign values, session filenames, hash targets, cross-list character/NPC drops, card-note inputs, entity keys, and section visibility/collapse state narrow through `model/campaignPagePresentation.ts`. Keep header, sessions, description, notes, entity-list, and archive/search dialog composition split into focused private components; do not put entity mutation, reorder persistence, graph projection, or global-search indexing back into the route component. Preserve empty-section expansion, stored collapse state only when data exists, list/graph note switching, official session filename validation, AI-ignore toggles, cross-list identity, partial-archive busy locking, and typed public page exports.
- The private campaign graph is strict TSX in `pages/campaign/ui/components/CampaignNotesGraph.tsx`. Pure filter/search visibility, source labels, edge and Flow-node presentation, node-card/detail-text decisions, detail activation, topology-key and stable/fresh projection decisions, new-node collision projection, minimap geometry, topology-fit eligibility, connection presentation/action planning, open-target resolution/routing, and note-save request narrowing belong to `pages/campaign/model/campaignGraphPresentation.ts`; React Flow node/edge shapes, state, layout triggers, drag events, viewport effects, debounced note editing, translation, callbacks, and private toolbar/canvas/details composition remain in the controller. Relation presentation is table-driven; preserve contains/related/sequence type, dash, marker, mention-animation, count-label, and idle/focused/muted opacity contracts. Detail activation accepts non-interactive pointer targets plus Enter/Space only; recursive Markdown mention rendering must preserve `code`/`pre` element identity. Canvas error/loading/empty states remain independent private compositions. Initial topology fitting requires a flow instance, a non-empty complete matching topology, no manual positions, and no previous fit; fresh projection is used for campaign changes, empty flow state, or topology changes without manual positions. Scheduling/cancellation remains in React. Connection plans preserve opposite-node identity, relation/count/source metadata, session opening for truthy string filenames, and selection fallback. Editable notes must resolve through campaign/session/scene scope without cloning. Open targets retain session-first, then campaign/session-scoped entity, then saveable-note precedence and route through injected effects; note-save requests accept only campaign/session/scene note types plus string/number IDs, including numeric zero, and must preserve the update object. Preserve scene-note hiding with scene filters, matched-node neighbor expansion, saved manual positions, sequential deterministic collision resolution, fit-view behavior, accessible node/edge labels, minimap pointer panning, and entity/session/note opening. Do not recreate a JSX implementation or declaration bridge.
- Partial campaign archive section IDs are a closed contract owned by `entities/campaign/api/campaignApi.ts` and exported through the entity public barrel. `pages/campaign/model/partialArchiveSelection.ts` owns immutable toggle and canonical-order projection; `PartialArchiveModal.tsx` owns only localized labels, file-input behavior, selection UI, and callback composition. Preserve all five sections (`sessions`, `npc`, `locations`, `images`, `aiHistory`), canonical request order, input reset after selection, disabled import/export for busy or empty selection, and server-side runtime normalization of archive input. Do not widen frontend callbacks back to arbitrary `string[]`.
- Partial campaign persistence import in `server/storage.js` must use the fixed command order `sessions → npc → locations → aiHistory → images`, regardless of the normalized section order returned to the caller. Preserve existing-target validation before effects, input-order section deduplication, sequential session/entity writes, ID-based session filename replacement, sanitized fallback filenames, NPC/location slug overwrite behavior, source-to-target image/history reference rewriting, fresh history IDs, existing history retention, raw incoming history/image counters, path-safe image restoration, and partial-progress semantics when a later command fails. Section commands and their mutable import context are private; the result remains exactly `{ ok, imported, sections }`.
- Partial-import NPC/location slug selection remains private in `server/storage.js`; keep nullable property access, raw-slug projection, safe basename selection, fallback-name selection, and orchestration helpers unexported. Read `slug` once, apply `String(value || "").trim()`, and call `path.basename` only for a truthy result. Accept every nonempty basename except exact `.` and `..`; otherwise read lazy truthy `firstName → name → type` and pass the selected value to the unchanged `campaignSlug` sanitizer. Preserve nullish/scalar/array/object/Proxy property behavior, Symbols, BigInts, UTF-8, platform path separators, and coercion failures before image-reference projection or entity persistence. Partial imports do not allocate unique entity slugs: rows write sequentially, a later duplicate overwrites the earlier directory payload, every successful input row still increments its section counter, and prior writes remain after later failures.
- Full campaign persistence import in `server/storage.js` must use its private import context and the exact effect order `replace-existing → meta → sessions → characters → npc → locations → AI history`. Preserve invalid-meta rejection before effects, non-array collection recovery, nullish options recovery, source-slug fallback, basename-normalized forced slugs, unique campaign allocation only without a forced slug, deletion only for forced replace-existing targets whose campaign directory exists, image-reference rewriting, original-or-current `createdAt`, legacy session filename normalization and sequential collision suffixes, falsy session-content fallback, entity slug fallback/collisions, fixed `ENTITY_TYPES` order, recursive exact `slug`/`campaign` history replacement, resource campaign/label projection, exact returned/stored meta, and partial progress when a later write fails. Keep the context, projectors, and restore commands private; `importCampaignBundle` remains the public storage operation.
- Database-bundle spell-class lookup lives in the side-effect-free `scripts/build-database-bundles-policies.mjs` behind exactly the `getSpellClassInfo` export; the executable bundle script imports it and retains all filesystem/CLI behavior. Read/coerce `spell.name` directly through falsy-empty `String`, split at the first pipe, then traverse `Object.keys(spellSources)` in insertion order. For every candidate uppercase the key before rereading/coercing/uppercasing `spell.source`; select the first match, but preserve the later truthy source-key guard so an empty matched key never reads its value. Read only the exact case-sensitive spell-name property through optional source-map access; falsy info returns a fresh empty array before class fields. Spread `info.class || []` completely before reading/spreading `info.classVariant || []`, retaining native iterable failures. Iterate the combined list in order; a truthy `entry?.name` is deliberately read again for `Set.add`. Preserve exact SameValueZero deduplication, raw values, and fresh-array sorting through `a.localeCompare(b)` without coercion, including single non-string values that never invoke the comparator.
- Bestiary `_copy._mod` application in `scripts/materialize-bestiary-copies.mjs` must use the private closed modifier command map. Preserve property insertion order and repeated-mod order; literal `"remove"`; exact unsupported-mod/mode warnings; selected-property precedence over wildcard `replaceTxt`; destructive whole-target wildcard replacement; delegation of all six array modes to `shared/bestiaryUtils.cjs`; cloned `setProp`, skills, and spell values; first spellcasting-block creation even for an empty `addSpells`; truthy-only will/daily/leveled branches; legacy object/array buckets; metadata overwrite except `spells`; additions only on the primary block; replacements/removals across every block; string-coerced replacement/removal equality; reverse duplicate removal; and chained-copy inheritance. Dry runs may resolve and warn in memory but must not alter database bytes.
- Bestiary copy materializer resolution in `scripts/materialize-bestiary-copies.mjs` must remain a focused private pipeline. Preserve direct-array monster lists and truthy `monster → monsters → results → []` precedence even for legacy scalars; `_meta.sources[0].json` before filename source; normalized uppercase sources and lowercase-name index keys; excluded `all.json`, `legendarygroups.json`, and `index.json` plus non-files/directories; filesystem and monster order; last duplicate exact-key winner; exact name/source lookup before first insertion-ordered name fallback; cycle detection before plain cloning and exact error chains; missing-base error text; falsy-only source fill; recursive resolution before child overlay; exclusion/removal of `_copy` and `_mod`; modifier application after overlay; truthy child-name forcing; updated-index visibility for chained copies; verbose resolution order; warning order; dry-run byte immutability; and exact changed-file/materialized/warning summaries.
- AI request-history snapshots in `server/aiHistoryService.js` must remain a private ordered projection pipeline behind `buildAiRequestSnapshot`. Preserve scalar-only trimmed text coercion; explicit type before encounter/session/campaign mode precedence; actual versus requested parse flags; parse-gated generation summaries; truthy model, scene, image-target, and base-prompt summary order; fresh exact disabled-context zeros; object-list `included !== false`; NPC fallback to the character configuration only when the NPC configuration is `undefined`; array-only campaign/session/scene counts; unconditional nonzero session/scene context parts; object-like image targets; stable image/file order; scalar-only attachment fields; and complete omission of empty attachment groups and the attachment envelope. Keep summary/count/target/attachment helpers private and the CommonJS export surface unchanged.
- AI prompt context in `server/modules/ai/application/buildPromptContext.js` must remain a focused private projection pipeline behind `buildPromptContext`. Preserve exact nonblank string-note text, ignored-note/entity removal, trimmed structured-note titles, simplified-title omission, first/last alias precedence before name/title fallback, and the distinct entity-content gates (selected-session NPCs exclude trait-only entries; campaign/current-session entities retain them). Nullish campaign collections stay omitted, present malformed collections normalize to empty arrays, and valid order is stable. Selected sessions remain `conf.included` gated; a nonempty object scene map selects only explicitly included scenes, otherwise every scene uses the defaults; scene text accepts every non-nullish value; encounter links require truthy IDs and compare string values; and any truthy projected section, including an assigned empty array, retains the selected session. A truthy object `currentSession.data` overrides the request session data, but `currentEncounter` is still resolved from the request session. Preserve custom-Bestiary identity, exact `entityTargetScope === "campaign"` suppression, truthy encounter targeting, malformed collection recovery, serialized key order, and the single public export.
- Current-target inference in `server/modules/ai/application/fillCurrentTargetIds.js` must use the private closed operation set and entity-command map. Preserve the original generated envelope, operations array, and operation identities; mutate eligible operations in place; accept only array-valued operations and non-array object entries; trim but do not lowercase `update|delete|updateNote|deleteNote`; lowercase trimmed entity aliases; and check scalar-coerced existing targets in exact `id → slug → name → targetClientId` short-circuit order. Encounter aliases read only a truthy `path.encounter`; scene aliases require a truthy `sceneId`; custom-monster aliases require a truthy target and use one trimmed scalar ID before one trimmed scalar name. Unsupported operations/entities and malformed entries remain untouched, target reads stay lazy behind eligibility, and only `fillCurrentTargetIds` is exported.
- Global image-gallery search in `server/storage.js` must remain a private storage-adapter pipeline behind `searchImageGalleryAssets`. Preserve lowercased trimmed query text, case-sensitive trimmed source/category filters, normalized safe subcategory segments, and selected-category plus category-set intersection. User assets are discovered through directory-only source/category entries and sequential depth-first traversal; only supported image extensions participate; matching covers filename/source/category/full subcategory; and output retains encoded URLs, platform paths, byte sizes, source/category/subcategory labels, mutable/read-write markers, traversal-independent display-name sorting, and stable metadata shapes. Official tokens are included only for empty/general source plus an allowed `tokens` category, receive the selected subcategory only when `category === "tokens"`, preserve their original source as `assetSource`, and are projected into the general/tokens location before the combined sort. Keep traversal/projection helpers private, do not mutate user image data, and preserve the repository result `{ images }`.
- Official Bestiary token listing and image-gallery statistics in `server/storage.js` must remain private read pipelines behind `listBestiaryTokenAssets` and `getImageGalleryStorageStats`. Token paths use safe normalized segments; ignored sources use deduplicated uppercase normalization and apply to the first relative segment; ignored/missing roots return fresh empty envelopes. A truthy query or `recursive` selects sequential depth-first traversal and returns no subcategories; direct mode returns sorted immediate directories plus concurrently projected immediate image files. Preserve supported extensions, query matching against full relative paths, query-only `Name (folder/path)` labels, encoded token URLs, platform paths, exact byte sizes, readonly/source metadata, and mode-specific display/name sorting. Statistics count every file type recursively and sequentially; source uses `path.basename(String(source || "general"))`; selected category/subcategory remain raw strings; explicit array categories retain coercion, order, whitespace, and duplicate sizing before object assignment, while other values trigger directory discovery. Preserve directory-only source/category maps, missing-path zeros, exact result keys, and read-only behavior.
- Image-gallery writes and reference propagation in `server/storage.js` must remain a private adapter pipeline behind `moveImages`, `renameImage`, and `deleteImages`. Decode source/destination slugs before resolving directories, preserve raw categories and falsy-subcategory fallback, return before destination creation for identical directories, and process requested items and recursive directory entries sequentially. Moves skip missing items, track every regular file regardless of extension, retain the existing distinction between encoded standalone filenames and raw nested directory URL segments, rename through `renameWithRetry`, and update references only after the complete move batch succeeds. Reference propagation walks campaigns, metadata, all entity types, and sessions sequentially; metadata/entity URLs use exact equality with ordered chained replacements, while sessions retain serialized global string replacement. Rename keeps its exact missing/collision errors and result shape. Delete remains recursive/forced, and folder extraction delegates nested movement before removing the emptied folder so reference updates and partial-failure behavior stay unchanged. Keep all decomposition helpers private and preserve the storage/repository contracts.
- Application routing and empty-state guide live under `app/routing`. Sidebar/navigation composition lives in `widgets/sidebar`; browser consumers import it through `index.js`, while Node/model consumers use `model.js`. Its strict TypeScript model owns campaign grouping, group merging, reorder payloads, campaign-root toggle navigation, expanded/mobile class projection, keyboard activation, and safe error messages. `ui/SidebarRuntime.tsx` exposes only the provider/types contract; keep its hook private. `app/ui/SidebarRuntimeHost.tsx` is the sole selector/action adapter and wraps both `<Sidebar>` and the root modal tree so delayed settings/player-question modal children receive live runtime state. Preserve active-before-completed ordering, slug-to-index reorder maps, session/encounter-aware campaign selection, `forceTab` Bestiary/Spells links, explicit archive import strategies, mobile close callbacks, fine-pointer hover expansion, modal-owned settings/player-question flows, and close-before-open/navigation ordering. Do not import `app/model` or `shared/model` from any Sidebar file, import private `model/*` or `ui/*` paths externally, or recreate the former root `Sidebar`.
- Theme switching and settings modal UI live in `features/settings/ui` and browser consumers use `features/settings/ui/index.js`. Keep `features/settings/index.js` API/model-safe; former legacy settings UI paths are retired.
- `features/settings` has no JavaScript API/model/UI implementations remaining; its JavaScript indexes are compatibility barrels with typed facades. Theme decisions stay in `model/theme.ts`; settings-modal campaign/source/prompt normalization, scope recovery, selected-value projection, map updates, save payloads, and response narrowing live in `model/settingsModal.ts`. Raw `SettingsModalContent.tsx` and `SettingsModalView.tsx` remain private; the browser runtime entry exposes the controlled `ColorThemeSwitcher` and `createSettingsModalContentComponent`, while its typed facade also exposes `SettingsModalRuntime` and `SettingsModalUiPatch`. `widgets/sidebar/ui/sidebarSettingsComposition.ts` configures `EditableField` once at module scope and derives its live Settings adapter from `SidebarRuntime` inside the delayed modal; do not pass a click-time state snapshot because modal children render later. Private `useSettingsModalController.ts` owns injected-runtime reads, API/effect/save orchestration, notifications, and scope recovery, but must not import `app/model` or `shared/model` directly. `ColorThemeSwitcher` is controlled-only (`theme`, `onToggle`) and must not regain selector, dispatch, or settings-API fallback behavior. Never call the factory during render, export/deep-import the raw UI, or restore a settings-to-editor sibling import. Preserve empty AI-prompt fallback, `undefined`-only image-prompt defaulting, empty ignored-source fallback, false-only auto-apply/search-debounce disabling, fire-and-report simple settings persistence, explicit failure for empty PATCH responses, per-campaign source/prompt overrides, campaign-to-global source fallback, normalized copied sources, UTF-8 prompt values, and imports through `features/settings/index.js` or the browser-only `features/settings/ui/index.js`.
- Repeated campaign-scope `<option>` composition inside settings belongs to the private typed `CampaignScopeOptions.tsx`; source and prompt selectors reuse it while retaining their distinct global options and state. Do not export this implementation detail from the settings UI barrel.
- App-aware `Input`, `EditableField`, and mention-picker modal UI live in `features/editor/ui` and are imported through `features/editor/ui/index.js`. The modal feature must not import editor UI; its simple prompt uses a native styled input to avoid a feature cycle. Do not recreate `src/components/form`.
- `EditableField` entity-link integration is injected by the app-owned `EditableFieldEntityLinkProvider`. Its stable runtime contains the `EntityLinkContext` and `EntityLinkResolverContext` Context objects plus `EntityModal` and `openEntityLinkModal`; it must not contain resolved context values. `EditableField` reads both Context objects locally at each render site so nested campaign/session providers retain precedence. Do not restore an editor-to-entity-link sibling import, read resolver values at `App` root, or replace these Context objects with captured app-level values.
- `Input` and `EditableField` request the global mention-picker modal only through the typed `EditorMentionPickerRuntimeProvider`. `App` maps `openMentionPicker(request)` directly to `openMentionPickerAction(request)`, while `app/ui/MentionPickerModalHost.tsx` owns the global request selector, active-campaign entity reads, validation/options projection, and modal close/cancel/error orchestration. Mount that host inside the editor runtime provider; keep this runtime separate from `EditableFieldEntityLinkProvider`, expose only its provider/types through the browser UI barrel, and keep the Context hook private. `requestMentionSelection` remains the feature-model promise adapter. Preserve Input's target-snapshot -> await -> change -> cursor ordering and EditableField's selected-text immediate path, picker path, focus-before-update order, and callback/promise semantics.
- `features/editor/ui` has full strict TypeScript implementation coverage behind its JavaScript browser barrel and typed facade. Mention filtering/grouping, initial cursor mapping, square-bracket paste detection, markdown text edits, value normalization, and keyboard shortcut decisions live in `editorPresentation.ts`; keep Lexical/DOM/runtime effects in TSX controllers. Preserve Ukrainian-layout shortcut aliases, Ctrl/Cmd formatting and mention commands, tab/list/heading/quote behavior, application-history delegation, external-update tags, formatted clipboard handling, mention boundaries/tooltips, and entity-modal resolution. `Input` and `EditableField` remain app-composed and must not move to `shared` while these dependencies remain.
- Public `Input.tsx` is a thin behavior/ref/effect controller over private `InputView.tsx`; shared private React/event/native-prop contracts live in `inputTypes.ts`. `Input.tsx` must continue re-exporting `InputProps` and `InputValueChangeEvent`, while neither private module enters `features/editor/ui/index.js` or `index.d.ts`; ESLint blocks external deep imports. Raw effect-value projection and class/mention presentation belong to `getInputRawValue` and `getInputClassPresentation` in `editorPresentation.ts`. Preserve repeated `source.value` reads, native array `join(",")`, `String(value ?? "")`, strict textarea class selection, string-only case-sensitive `"["` detection, and class ordering base → caller → mention. The private view preserves textarea `rows={1}` before spread, spread before explicit ref/class/keyboard/paste overrides, input spread before ref/class/type/keyboard/paste, textarea native-prop casting, falsy-title passthrough, and exact Tooltip content/class wrapping. Do not move controller callbacks, timers, refs, selection effects, or store access into the view.
- Editable change-event projection belongs to Node-safe `createEditableFieldChangeEvent` in `features/editor/ui/editorPresentation.ts`; `EditableField.tsx` re-exports the same `EditableFieldChangeEvent` public type and invokes the policy only inside the optional `onChange` call. Preserve falsy/non-object source fallback, object/array source spreading, and repeated lazy target/currentTarget reads: truthiness read, `typeof === "object"` read, then successful-value read. Invalid target uses a fresh object; invalid currentTarget falls back to the already-selected target. Keep outer source spread before explicit currentTarget and target, currentTarget construction before target, each nested spread before the supplied value override, enumerable string/symbol/getter behavior, third-read primitive compatibility, source immutability, and exact getter/spread failure order. Do not reconstruct event-shape narrowing in the Lexical controller.
- Editable click routing belongs to the Node-safe `getEditableClickPlan` policy in `features/editor/ui/editorPresentation.ts`; modifier activation, link targeting, and mention-name fallback remain in its focused policy helpers. Preserve mention lookup before modifier reads, falsy-mention short-circuiting, truthy Ctrl before Meta, activated-mention return before link lookup, and link lookup only on the content path. `EditableField.tsx` owns the original event effects: an activated mention prevents default, stops propagation, then resolves the dataset name and opens the modal; content prevents default only for links, always stops propagation, then optionally delegates `onClick`. Keep DOM target narrowing, original target/event identities, dataset-read timing, exact `|| ""` name fallback, hook dependencies, and thrown lookup/effect/callback boundaries in that order.
- Editable mention-shortcut selection projection belongs to the private `getEditableSelectedMentionText` helper in `EditableField.tsx`; picker-result eligibility belongs to Node-safe `shouldInsertEditableMentionResult` in `editorPresentation.ts`. Preserve prevention before propagation, one selection read, range recognition before collapsed-state access, and text reads/normalization/trim only for non-collapsed ranges. A truthy selected text inserts immediately and returns before opening the picker. Otherwise await the injected picker runtime through the feature policy, read exact `"selected"` status before one truthiness read of name, return on ineligible results, then focus before update and read `result.name` again only inside the update callback. Keep the original selection/result identities, no stale name caching, hook dependencies, and exact selection/picker/focus/update failure boundaries.
- Editable keydown classification belongs to Node-safe `getEditableKeyDownPlan` in `editorPresentation.ts`; its modifier and non-history routing helpers stay private. Preserve the initial raw `ctrlKey || metaKey` short circuit, then eager classifier input reads in `code → ctrlKey → key → metaKey` order, followed by the second dynamic code read for history routing. History overrides the classified action and is distinct from ordinary delegation; falsy/exact `"delegate"` actions delegate, while every other action retains its identity. `EditableField.tsx` owns effects: history delegates without stopping propagation; non-history always stops first, then either delegates the original event or executes the original action/event/editor/mention handler. Do not duplicate classifier/history decisions in the Lexical controller.
- Editable formatted-copy eligibility belongs to Node-safe `getEditableCopyRequest` in `editorPresentation.ts`; timeout replacement belongs to private `replaceEditableCopyResetTimer` in `EditableField.tsx`. Preserve event prevention before propagation stop, editor-ref read before eligibility, raw editor/value truthiness, and exact identities in a fresh request. The controller alone reads Lexical HTML and writes one ClipboardItem with HTML before plain text; copied state changes only after a fulfilled write. Timer replacement must retain the truthy ref check, second ref read for clearing, replacement assignment, 2000ms delay, and callback order `setCopied(false)` before ref nulling. Keep clipboard errors inside the existing catch with the exact message/original error, preserve cleanup and dependencies, and do not duplicate readiness or timer decisions in the handler.
- `LexicalEditableField` is a behavior/effect controller over private same-file `LexicalEditableFieldView`; view decisions belong to Node-safe `getLexicalEditableFieldViewPresentation` in `editorPresentation.ts`. Keep the private view and presentation contract out of editor barrels. Preserve ContentEditable class/attribute/handler order, strict textarea aria and rich-plugin visibility, raw history visibility plus `undefined`/`"true"` data marker, disabled tab index, and fresh null-hover state on leave. Preserve plugin order `EditorContentPlugin → HistoryPlugin when enabled → MarkdownValuePlugin → MarkdownChangePlugin → EditableStatePlugin → ListPlugin then MarkdownShortcutPlugin for textarea`. Editor access, command registration, callback/ref ownership, DOM lookup, modal opening, and hover state effects remain in the controller.
- The exported outer `EditableField` is a behavior/effect controller over private same-file `EditableFieldView`; neither the view nor its focused presentation policies enter editor barrels. `getEditableFieldMarkdownValue`, `isEditableFieldDisabled`, `getEditableFieldTitleContent`, `getEditableFieldTooltipPresentation`, `resolveEditableFieldCampaignSlug`, `getEditableFieldCopyPresentation`, and `getNextEditableMentionTooltipState` own only Node-safe derived decisions. Preserve `value || value === 0` before one `String`, normalization afterward, exact `Boolean(disabled || readOnly)`, primitive-string title trim gating with the original title returned, mention content before field content, independent null anchor fallback, tooltip disabling from resolved content, truthy campaign slug before one lazy zero-argument `parseUrl`, raw `normalizedMarkdownValue && showCopyButton`, copied check/copy icon selection, and strict current-tooltip equality with identity reuse. Title and route fallback remain in their original `useMemo` scopes. Cleanup stays effectful in private `clearEditableCopyResetTimer`, reads a truthy timer ref again for `clearTimeout`, and does not null it. The private root view owns container spread/class/stop/style order, lazy copy Button composition, Tooltip, `LexicalComposer`, `EditorRefPlugin` before `LexicalEditableField`, and trailing `EntityModal`; store/context/editor/modal-opening/clipboard/timer effects and callback ownership remain in the controller.
- Markdown block edits remain in `features/editor/ui/editorPresentation.ts` behind the unchanged `applyInputBlockEdit` export; marker, list, heading, and per-line dispatch helpers stay private. Preserve list dispatch before quote with the corresponding repeated `edit.kind` getter behavior, branch-local `add`/`level` reads, exact `"- "` and `"> "` markers, native `String.prototype.repeat` coercion/failures, `/^#{1,6} /` heading recognition, and exact prepend/remove/replace shift arithmetic. Aggregate selection projection must keep `substring` clamping/swapping, newline endpoint behavior, first-line versus total shifts, sequential per-line execution, and native malformed-value failures. Do not expose the line transformer or move DOM/store effects into this policy.
- Initial input cursor mapping remains in `features/editor/ui/editorPresentation.ts` behind the unchanged `resolveInitialCursorPosition` export; clamp, preview validation, first-entry, mapped-entry, non-empty projection, and preview resolver helpers stay private. Preserve nullish-selection, numeric-selection, numeric-index, then preview precedence; the numeric index getter is intentionally read once for classification and again for projection. Preview resolution must read `rawValue.length` before preview getters, destructure `previewOffset` before `previewToRaw`, require a number plus a real array, return zero for an empty map, project nonpositive offsets from entry zero without upper clamping, return raw length at or beyond map length, and clamp only interior entries. Keep repeated Proxy-array length reads, nullish-only entry fallback, native number coercion/Symbol failures, NaN/fractional property lookup, and undefined-only `rawValue` default behavior. Focus and selection DOM effects remain in `Input.tsx`.
- Initial Input selection eligibility remains Node-safe in `features/editor/ui/editorPresentation.ts` behind `getInitialInputSelectionPosition`; the nullish/type eligibility helper stays private and delegates eligible cursor mapping to `resolveInitialCursorPosition`. Preserve truthy already-applied short-circuiting before selection access, exact `initialSelection == null` before `supportsSelectionRange(type)`, and no raw-value reads for applied, nullish, or unsupported selections. In `Input.tsx`, read `internalRef.current` first and return on a missing node before reading the applied ref; only a numeric position focuses with `{ preventScroll: true }`, applies the same selection endpoint twice, and sets the applied ref after both DOM calls succeed. Keep the layout-effect dependency list `initialSelection`, `rawValue`, then `type`.
- Input mention-bracket edits remain in `features/editor/ui/editorPresentation.ts` behind the unchanged `toggleInputMention` export; selected-marker, surrounding-bounds, surrounding-marker, branch-detection, and three edit-projection helpers stay private. Preserve initial `value.substring(selectionStart, selectionEnd)`, selected-wrapper precedence, ordered and lazy `startsWith("[") -> endsWith("]") -> length >= 2` checks, then ordered and lazy `selectionStart > 0 -> selectionEnd < value.length -> left "[" -> right "]"` checks. Selected wrappers remove two from the end, surrounding wrappers remove one from both positions, and fallback insertion adds one to both positions. Keep native `substring` clamping/swapping, raw cursor arithmetic, exact bracket/whitespace content, concatenation coercion, method/getter order, and malformed-value failures. `Input.tsx` remains responsible for keyboard-event effects and only calls this policy for a non-collapsed selection.
- Input keyboard shortcut dispatch remains in `features/editor/ui/editorPresentation.ts` behind the unchanged `getInputShortcutAction` export; modifier, heading, mapped-action, and normalized-key routing helpers stay private. Preserve object-parameter getter order `ctrlKey -> key -> metaKey -> type`, exact `textarea` gating before lowercasing, one receiver-bound `key.toLowerCase()` call, tab precedence before modifier eligibility, and truthy Ctrl-or-Meta eligibility. Keep the frozen Latin/Ukrainian lookup table and its stable mutable action identities for `k/л`, `b/и`, `i/ш`, `]/ї`, `[/х`, and `q/й`; lookup intentionally uses ordinary inherited properties. Only a falsy lookup reaches exact heading regex `/^[1-6]$/` and then `Number`. Preserve custom normalized-key property/regex/number coercion order, prototype-like keys, native method/Symbol failures, and fresh tab/heading action identities. Keyboard-event prevention, selection reads, and command execution remain in `Input.tsx`.
- Input shortcut execution planning remains Node-safe in `features/editor/ui/editorPresentation.ts` behind `getInputShortcutExecutionPlan`; edit-plan construction plus tab, mention, format, and block routing helpers stay private. Preserve repeated `action.kind` reads and precedence `tab -> mention -> format -> block`, strict `selectionEnd === selectionStart` collapsed-mention detection, no value access for a collapsed mention, exact edit-policy delegation, original block-action identity, branch-local marker/add/level reads, and fresh plan objects. `Input.tsx` must classify first, delegate a null action through the original `onKeyDown` without prevention or selection reads, prevent a handled action exactly once, then project `currentTarget` as `value -> selectionStart ?? 0 -> selectionEnd ?? selectionStart`. It alone launches the picker with `void`, applies one edit using the original event, and owns change, focus, timer, and selection-range effects. Do not reintroduce direct tab/mention/format/block transforms into `handleKeyDown`.
- Raw Input mention insertion remains split between Node-safe `getInputMentionInsertion`/`getInputMentionCursorPosition` policies in `features/editor/ui/editorPresentation.ts` and async effects in `Input.tsx`. Snapshot `currentTarget` through the shared private target projector before requesting/awaiting the picker. In insertion policy, read `result.status` first and return null only for exact `"cancelled"`; otherwise preserve truthy name selection, the second `result.name` read for interpolation, exact `[]`/`[name]` text, prefix-mention-suffix substring order, and native failures. The handler must emit `onChange` with the original event and projected value before calling the cursor policy, so its second status read remains callback-observable. Cursor projection stays raw `cursorStart + (status === "selected" ? mention.length : 1)`. Keep zero-delay scheduling, ref lookup, null-node no-op, focus-before-selection order, and picker rejection propagation in `Input.tsx`.
- Bracket-aware Input paste editing remains Node-safe in `features/editor/ui/editorPresentation.ts` behind `getInputBracketPasteEdit`; `Input.tsx` owns event delegation, range eligibility, prevention, clipboard access, change dispatch, and scheduled DOM effects. Non-textareas must delegate before target or clipboard reads. Textareas project `value -> selectionStart ?? 0 -> selectionEnd ?? selectionStart`, evaluate `isRangeInsideSquareBrackets` once, and delegate unchanged when false. A handled range prevents default before `clipboardData.getData("text/plain")`; the policy then performs one receiver-bound `.replace(/\r\n/g, "\n")`, preserving lone carriage returns, assembles prefix/normalized text/suffix with native `substring` semantics, and independently computes both cursor positions as raw `selectionStart + normalizedText.length`. Keep optional paste delegation receiver-bound to the props object and do not move clipboard/DOM effects into the policy.
- Input bold/italic edits remain in `features/editor/ui/editorPresentation.ts` behind the unchanged `toggleInputFormat` export; italic-conflict, selected-marker, surrounding-start, surrounding-marker, and wrapper-orchestration helpers stay private. Preserve initial `value.substring(selectionStart, selectionEnd)`, selected-wrapper precedence, outside-wrapper fallback, and final insertion. Inside conflict must be computed first through exact `marker === "*" -> startsWith("**") -> !startsWith("***")` checks; selected markers are then evaluated lazily as `startsWith(marker) -> endsWith(marker) -> length >= marker.length * 2`, with conflict consulted only after that predicate. Outside conflict likewise runs first through the exact `substring(start - 2, start)` and conditional `substring(start - 3, start)` reads; then evaluate `start >= marker.length`, strict left/right marker equality, and conflict last. Bold markers skip conflict-only reads. Preserve strict-equality versus string-coercing marker asymmetry, repeated `marker.length` reads, native `substring` clamping/swapping, raw cursor arithmetic, exact text assembly, and malformed-value failures. Keyboard and selection DOM effects remain in `Input.tsx`.
- EditableField shortcut classification remains in `features/editor/ui/editorPresentation.ts` behind the unchanged `getEditableShortcutAction` and `shouldDelegateEditableHistory` exports; delegation, type routing, textarea direct-key, mapped-key, and heading helpers stay private. Preserve object getter order `code -> ctrlKey -> enableHistory -> isDisabled -> key -> metaKey -> type`, the raw `ctrlKey || metaKey` value, history delegation before disabled delegation, and both before type routing. Non-`textarea` values use exact Enter blur before modifier delegation. Textarea routing must invoke receiver-bound `key.toLowerCase()` first, accept normalized tab before exact raw space, then gate on modifier truthiness. Keep the frozen ordinary code table for `KeyB`, `KeyI`, `KeyK`, `BracketRight`, `BracketLeft`, and `KeyQ`; inherited and custom property keys intentionally remain observable, and only a falsy lookup reaches exact raw-key `/^[1-6]$/` plus template coercion. Preserve standalone history-helper falsy-value returns, strict code comparisons, malformed input failures, and all `EditableField.tsx` event prevention, delegation, selection, and command effects.
- Note editing, AI-ignore note controls, and bulk collapse UI live in `features/notes/ui` and are imported through its UI public index. The raw NoteCard renderer is private: external owners import `createNoteCardComponent`, bind the public `EditableField` and `renderMentionText` symbols once at module scope, and render only the returned stable component. `App` configures the live feature-owned `SimplifiedNotesProvider` once around the application tree; every note presentation consumer reads `useSimplifiedNotesEnabled` through the public UI entry rather than selecting the global store. Never call the factory during render, export the raw card, import the private runtime/card files, or reintroduce direct `app/model` or `shared/model` access inside Notes; changing component identity remounts Lexical editors and loses local editor/history state. Former common note-control paths are retired.
- `features/notes/ui` is fully strict TypeScript. Note-card collapse/header/preview decisions, collapsed-card click intent, field-highlight checks, real-versus-virtual note filtering, and bulk-collapse intent live in the Node-safe `features/notes/model.ts`. `noteCardComposition.ts` owns the minimal callable editor/mention slots and public configured-card contract without importing sibling-feature types. `SimplifiedNotesRuntime.tsx` owns the private nullable Context plus public provider/hook contract: `false` is a valid live value and missing provider must fail fast; do not turn the setting into a factory-captured snapshot or a writable feature store. Private `NoteCard.tsx` captures the structural slots and reads the hook before forwarding to private `NoteCardParts.tsx`, which owns classic header, simplified actions/preview, and editable body rendering; neither raw renderer nor parts enter the public barrel. Preserve consumer-props-before-injected-slots precedence, Header → Simplified → Body order, last/virtual note behavior, trimmed collapse eligibility, raw untrimmed preview slicing before mention rendering, ellipsis afterward, title default text mode, body textarea mode, campaign/history forwarding, simplified/classic action visibility, event propagation, and AI-ignore/drag composition outside the card.
- Clipboard feedback, localized status badges, encounter monster insertion UI, campaign creation UI, and player questions UI live in their respective feature slices. `src/components` is retired; do not recreate it.
- Campaign creation UI is strict TypeScript under `features/campaign-create`; create/import/close callbacks belong to its public component contract, while name normalization, API calls, reloads, error messages, modal closure, and navigation remain in app orchestration. Import it only through `features/campaign-create/index.js`.
- Player-question modal UI is strict TypeScript under `features/player-questions`. Dice-factor selection, roll formulas, numeric search normalization, and typed dice-result targeting live in its Node-safe `model.ts`; list virtualization, scrolling animation, and refs remain in UI. `PlayerQuestionsRuntime` owns only the current opaque dice result, already-normalized debounce boolean, and narrow roll-request command; `widgets/sidebar/ui/sidebarPlayerQuestionsComposition.tsx` derives it from the live `SidebarRuntime`. Sidebar must mount `SidebarPlayerQuestionsModalContent` as delayed modal content rather than capture a click-time runtime snapshot, so current dice results reach the rendered modal. Player Questions must not import `app/model` or `shared/model`. Dice-result targeting must first narrow a record with a truthy result ID, then independently validate the exact `playerQuestions` context and an integer total within inclusive `1..questionCount` bounds; invalid context/result data keeps the result ID while returning a null question target. Preserve initial result-ID capture, unknown truthy result-ID identity, standard-die formula distribution, false-only debounce behavior, one-time result processing, two-frame scrolling, and public imports through `features/player-questions/index.js`.
- Clipboard feedback is a typed feature boundary. Keep clipboard writes, success notification state, copy text/message, child content, and context-menu event contracts in `features/clipboard`; consumers import it only through the feature public entrypoint.
- The localized status badge is a typed presentation boundary in `features/status-badge`. Keep its completed-state label and span click contract there; consumers import it only through the feature public entrypoint.
- Cross-domain JSON, download, byte-formatting, and deep-search helpers live in `shared/lib` and are imported through its public index; their former `src/utils` paths are retired.
- Navigation URL/event helpers, DOM target navigation, and undo/redo transitions live in `shared/lib` and are imported through its public index; the former `src/utils/navigation`, `src/utils/domNavigation`, and `src/utils/undoRedo` paths are retired.
- Backend campaign entity CRUD rules live in `server/modules/campaign/application/campaignEntityCommands.js` over the repository port, with filesystem delegation in `server/modules/campaign/infrastructure/fileCampaignEntityRepository.js`.

### Sessions and Scenes

- What: session planning, scenes, scene notes, session-scoped NPC/location entities, encounter links, todo/prep state.
- Main UI: `src/pages/session/ui/SessionPage.tsx`, `src/pages/session/ui/components/*.tsx`.
- Main logic: `src/pages/session/model/useSessionView.ts`, `src/entities/session/model/SessionViewModel.ts`.
- Backend: `server/routes/sessions.js`, `server/storage.js`.
- Business detail: NPC/location can be campaign-scoped or session-scoped; scope changes must preserve IDs/references where intended.

### Encounters

- What: combat encounters linked to session scenes, monsters/characters, HP, initiative, grid/focused views, import/export.
- Main UI: `src/pages/encounter/ui/EncounterPage.tsx`.
- Main logic: `src/pages/encounter/model/useEncounterView.ts`, `src/entities/encounter/model/participants.ts`.
- Backend data path: stored inside session data, not a separate DB.
- Important integrations: `src/widgets/bestiary-browser`, `src/widgets/monster-stat-block`, `src/features/encounter-editor/ui/AddMonsterToEncounterModalContent.tsx`.
- Encounter-target discovery for the add-monster modal is typed in `features/encounter-editor/model/addMonsterTargets.ts`. Preserve completed-campaign filtering, nullable list/detail normalization, sessions-with-encounters filtering, stable campaign/session/encounter target IDs, single-submit locking, reload dispatch, and modal closure after the focused command succeeds. Browser consumers import the modal through `features/encounter-editor/ui/index.js`.

### Bestiary and Custom Monsters

- What: browse local bestiary, favorites, custom creatures, custom monster CRUD/import/export, token image upload.
- Main UI: `src/pages/bestiary`, `src/widgets/bestiary-browser`, `src/widgets/monster-stat-block`.
- Bestiary features: `src/features/edit-monster`, `src/features/ai-edit-monster`.
- Main logic: `src/entities/bestiary/model/MonsterStatBlockModel.ts`, `src/entities/bestiary/model/bestiarySearch.ts`, `server/aiCustomMonsterService.js`.
- Backend: `server/routes/bestiary.js`, `server/storage.js`.
- Data:
  - Official/local reference: `database/bestiary/all.json`, `database/bestiary/legendarygroups.json`, `database/bestiary/tokens/`.
  - Custom monsters: `data/custom-bestiary.json`.
- Important rule: custom monster legendary actions live on the monster object; do not move them to a separate legendary group file.

### Spells and Rules References

- What: spell browser, condition/disease/variant rule/skill/sense references, parsed 5eTools-style tags.
- Main UI: `src/widgets/spells-browser`, `src/features/rules-reference`; the browser privately owns `ui/SpellCard.tsx`.
- Main logic: `src/features/rich-content/ui/RichContentRenderer.tsx`, `src/features/rich-content/model/richContentPresentation.ts`, `src/entities/reference/model/parserTags.ts`, `src/entities/reference/model/contentTokens.ts`, `src/features/rules-reference/model/referencePreview.ts`, `src/features/rules-reference/model/referenceResolvers.ts`.
- Backend: `server/routes/spells.js`.
- Data: `database/spells/all.json`, `database/conditions.json`, `database/diseases.json`, `database/skills.json`, `database/senses.json`, `database/variantrules.json`.

### AI Assistance

- What: prompt UI, context selection, image prompt generation, parsed operations, draft/apply/undo, history and retry.
- Main UI: `src/widgets/ai-assistant`, `src/features/ai/ui/*`, `src/widgets/ai-response-modal`.
- Backend HTTP composition: `server/modules/ai/http/router.js` (`server/routes/ai.js` is a compatibility re-export). Parsed-operation domain contract: `server/modules/ai/domain/aiPayloadSchemas.js` (`server/aiPayloadSchemas.js` is a compatibility re-export).
- Backend AI application modules are being extracted under `server/modules/ai/application`; `buildPromptContext.js` owns ignored-content filtering and Gemini input-context shaping.
- `server/modules/ai/application/buildUserPrompt.js` owns deterministic mode-specific task instructions and final user-prompt assembly behind exactly `buildTaskInstructions` and `buildUserPrompt`; keep task constants, mode builders, the closed dispatcher, and section helpers private. Mode lookup is strict and supports only image, character, custom-monster, NPC, location, encounter, scene, and campaign. Image tasks prefer a truthy target type over a truthy scene ID and intentionally read a truthy `imageTarget.type` twice before generic fallback. NPC/location tasks preserve exact mixed/session/campaign-fallback text, raw scope interpolation, and session guidance only for mixed or exact session scope. Encounter IDs interpolate before the truthy custom-monster append; scene scope guidance applies unless scope is exactly campaign, followed by the truthy encounter-link append. Final assembly must retain destructured input getter order, pretty context JSON first, an image-target section only for exact image mode plus a truthy object target (arrays included, functions/null excluded), task text, then `userInstructions || ""`, with exact labels/newlines. Preserve `toJSON`, undefined, circular/BigInt, raw Symbol interpolation, and unknown-mode behavior; do not move prompt wording into routes, Gemini infrastructure, or configuration.
- `server/modules/ai/application/buildSystemInstruction.js` owns base mode policies and all shared/conditional AI contracts, including language, JSON operations, scope, generation toggles, notes, mentions, encounters, and user base prompts.
- `server/modules/ai/infrastructure/geminiGateway.js` is the Gemini SDK boundary: client/key lifecycle, model configuration, attachment request parts, invocation, and response text retrieval stay there.
- `server/modules/ai/infrastructure/attachmentParts.js` owns attachment MIME/size validation, local image resolution, base64 conversion, text-file prompt parts, and binary inline parts behind exactly `buildFileParts`, `buildImageParts`, `collectImageUrls`, and `resolveLocalImageUrl`; keep focused helpers private. Both attachment types accept arrays only, inspect at most the first four entries, decode at most 10 MiB, normalize Buffer output back to base64, and preserve input order. Image URL collection mutates and returns the supplied output, reads a truthy URL twice, trims after `String`, and checks the four-item threshold after each source row. Local image resolution uses `new URL(String(value || ""), "http://local").pathname` with a second raw-string coercion only on construction failure, splits and removes empty slash segments before one `decodeURIComponent` pass, requires the decoded `api/images/{slug}/{category}/{relative...}` shape, rejects empty/dot/dot-dot/slash/backslash path parts, enforces the resolved image root, and accepts only case-insensitive jpg/jpeg/png/webp/gif extensions. Do not add an origin restriction: external URLs with the exact image pathname remain eligible. Malformed percent escapes propagate. Inline images prefer an allowed trimmed/lowercased declared MIME before filename extension fallback; valid inline data prevents any URL read. URL fallback is trimmed, deduplicated before stat/read, and processed sequentially; stat/missing/non-file/oversized outcomes are skipped, while read failures propagate. File MIME selection prefers a recognized filename extension before a declared allowed MIME. Lowercase `data:` values strip through the first comma; other values are decoded as supplied. Empty decoded buffers and oversized payloads are rejected. Text MIME types produce one UTF-8 prompt part; binary files produce prompt then inline parts. Prompt filenames use falsy `attached-file` fallback, collapse CR/LF runs only, and trim. Preserve getter/coercion order and the unchanged `aiService.__test` compatibility exposure.
- `server/modules/ai/application/parseAiResponse.js` owns outer-fence cleanup, surrounding-prose JSON extraction, recursive escaped-newline normalization, and invalid-JSON shaping behind exactly `extractFirstJsonObject`, `normalizeEscapedNewLines`, `parseAiResponseText`, and `stripOuterJsonFence`; keep scan, projection, structured-parse, and fallback helpers private. Fence cleanup uses `String(text || "").trim()` and one anchored case-insensitive optional-`json` match; consequently other language text remains inside the stripped result, `json` prefixes are consumed, four-backtick wrappers retain one backtick, and partial/trailing fences remain unchanged after outer trim. Object extraction scans from the first `{` by code unit with brace depth, quote state, and one-character escape state; it ignores braces inside strings, returns the first balanced trimmed object, discards trailing prose, and returns the complete trimmed source for missing or unterminated objects. Newline normalization replaces every literal `\n` escape recursively, maps sparse arrays, and projects objects through `for...in` into a fresh plain object, retaining inherited enumerable string keys/getter effects while losing symbols and the original prototype; circular matching structures still fail. Parsed mode keeps extraction, `JSON.parse`, and normalization under the same catch. Plain mode must remain parse/callback-lazy. On failure, call `onParseError` once with the exact label, original text, and thrown value before constructing the fixed error/raw-response object; callback and second raw-text coercion failures propagate. Preserve `aiService.__test` function identities and getter/coercion order.
- `server/modules/ai/application/resolveAiRequest.js` owns effective mode/parsing decisions, entity scope, generation permissions, response language normalization, and requested-model fallback selection through focused private policies behind its unchanged four exports. Language values use falsy-to-empty coercion, trimming, lowercasing, a prototype-safe closed Ukrainian/English alias map, exact `language is required` failure, and the normalized code itself for every unknown label. Model normalization removes one exact lowercase leading `models/` before trimming; selection matches normalized names exactly or returns the supplied default. Character/NPC/location generation is disabled only by strict `false`; encounters use `Boolean`, and custom monsters require both encounter generation and their own truthiness. Session entity scope requires a truthy session, falsy encounter ID, and non-campaign scope; exact `session` remains session and every other eligible value becomes mixed. Custom-monster forces parsing; other parsing is encounter-gated. Disabled explicit encounter types fall back through route context. Image use-key selection precedes prompt selection; parsed supported types precede truthy encounter, session, and campaign fallbacks. Structured JSON requires both effective parsing and a supported final use key. Keep the decision table and result property shape stable; `aiService` must not duplicate these rules.
- `server/modules/ai/application/prepareGenerateAiRequest.js` owns generate-request validation, route-path normalization, settings/base-prompt resolution, auto-apply policy, and route-facing generation flags through an injected settings reader.
- Complete generate workflows belong in application commands such as `server/modules/ai/application/generateBestiaryImagePrompt.js`; commands return `{ status, body }` and receive generation/history persistence dependencies explicitly.
- `server/modules/ai/application/generateCustomMonster.js` owns custom-bestiary normalization, selected-monster context, optional campaign/session context, generation, target-ID completion, validation, failure history, and normal versus encounter-local draft routing through focused private policies behind its single factory export. Preserve the initial Bestiary read; normalize only array-valued monster lists containing an ID that is empty through `asText`; perform one write and use the returned array as the authoritative before/context snapshot. Compact context projection keeps exactly `id`, `name`, `source`, `type`, and `cr` without cleanup. Selected targets require a truthy object (arrays included), match each candidate by scalar-coerced ID then case-insensitive scalar-coerced name (including empty-name matches), fall back to the original target, and expose only exact `create-based` versus default `edit` modes. Campaign context loads only for a truthy non-`bestiary` campaign; campaign and session reads recover independently to null and context append still runs afterward. Keep the generation flags/scope/parsing and both draft envelopes exact. Generated errors save failure history and return before target completion/validation/routing; successful content completes targets against `{ campaign: "bestiary" }`, requires operations, and checks encounter-local eligibility only after validation.
- `server/ai/EncounterLocalMonsterAiFlow.js` owns encounter-local custom-monster operation selection, normalization, session participant replacement, history projection, and draft responses behind the unchanged `EncounterLocalMonsterAiFlow` class and `buildLocalEncounterMonsterSessionChange` export. Preserve eager trimmed/case-insensitive reads of both `entity` and `op`, first eligible `monster|custom-monster|custommonster` create/update selection, object-like arrays, `data -> monster -> payload -> operation` and `patch -> changes -> data` precedence, and the historical three reads of a selected getter on every selector call. Update and create branches deliberately reevaluate selectors; keep their merge and truthy ID/name fallback order exact. Own `originalBestiaryName` or `imageUrl` fields, including `undefined`, block target inheritance. Session changes require raw truthy route/target/session/monster inputs, clone through JSON before lookup, select only the first string-compatible encounter, require an array of monsters, replace every string-compatible target instance, preserve each participant's source/provenance, and retain the established HP precedence and zero/negative clamp behavior. Failure history uses the exact 400 envelope. Successful history construction dynamically dispatches through the current flow/writer properties in `format -> request snapshot -> retry clone -> change summary -> add response` order, keeps the exact draft snapshot/retry/resource shape, and lets normalization, clone, collaborator, and persistence errors propagate.
- `server/modules/ai/application/generateCampaignContent.js` owns campaign/session context loading, custom monster-name context, generation, target completion, mention processing, contract validation, failure history, and campaign flow persistence through private orchestration policies behind its single factory export. Preserve exact effect order: reject a falsy campaign path before reads; read campaign; tolerate only rejected session reads as null; append campaign context; attach filtered current-session data only for mixed scope plus a truthy session; read custom Bestiary only for a truthy encounter or enabled encounter generation; generate; fill parsed targets; apply mentions only to truthy parsed objects (arrays included); persist generated errors before validation; validate other parsed results; then delegate persistence. Bestiary monsters remain array-only, names use `asText`, and empty names/context are omitted. Keep generation and persistence envelopes exact, including payload-vs-prepared parse flags, all base prompts, target path/scene, mixed-scope validation, history status/body, original object identities, and thrown dependency propagation. Do not move storage, Gemini, mention, validation, history, or persistence implementations into this command.
- `server/modules/ai/application/campaignContext.js` owns ignored-content filtering and configured campaign/session context loading through injected entity/session readers behind its unchanged two-export API; `fillCurrentTargetIds.js` owns implicit current-target completion. Context entity keys use lazy truthy `slug -> id -> display name` selection followed by `asText`. Entity filtering always removes truthy `_aiIgnored` rows before interpreting config. Preserve falsy config as select-none, exact `true` as all visible, object/array `included !== false`, truthy scalar inclusion, `items || {}`, entries whose value is anything except strict `false`, empty-item-map select-all, and lazy key reads only for nonempty item maps. Session data projection spreads the source first, then overrides notes/NPCs/locations/scenes with array-only collections; array-valued data properties are deliberately read twice after spread, ignored scenes remain present, and only each scene's notes are filtered. Configured append is a lazy no-op for any falsy target/campaign/config, mutates the supplied target, and executes campaign notes -> characters -> NPCs -> locations -> sessions sequentially. Included character/location config is reread for filtering. Preserve the legacy NPC fallback only when `campaignNpcs === undefined` and characters are included, including repeated getter reads and an effective `true` NPC filter; direct NPC config takes precedence. Sessions are truthiness-checked and then reread for `Object.entries`, skipped unless `conf.included` is truthy, read and pushed sequentially with config identity, and retain prior target mutations when any dependency or malformed value fails. Do not parallelize these reads or add compensation.
- AI history persistence uses the contract in `server/modules/ai/application/ports/aiHistoryRepository.js`; filesystem storage mapping belongs to `server/modules/ai/infrastructure/fileAiHistoryRepository.js`.
- AI history filesystem reads remain in `server/storage.js` behind the unchanged `readAiResponses` export; keep path selection, saved-list projection, normalization, sorting, and guarded reads private. Campaign slugs use falsy-empty string coercion, trim, and `path.basename`; an empty normalized slug returns before filesystem access. Regular campaigns read their campaign `_aiResponses.json`. Bestiary canonicalization belongs to `server/modules/ai/infrastructure/bestiaryAiHistoryMigration.js`: the dedicated root history file always wins; when it is absent, normalize the legacy campaign history, write the canonical file, retain the legacy file, and return the normalized in-memory rows even if that write fails. Deduplicate concurrent migration attempts per slug and retry on a later read while the canonical file is still absent. Canonical Bestiary writes must enter the same per-slug operation queue so a live write cannot be overwritten by an in-flight legacy migration; do not write that path around `writeCanonicalAiResponses`. Malformed legacy reads recover to the normal canonical-path read instead of rewriting. Storage stats use the canonical Bestiary path. Reads keep `readJson`/`updatedAt` stripping, accept direct arrays or the truthy `saved?.responses` envelope fallback, normalize all rows before Boolean filtering, and mutate the resulting list through descending `String(createdAt).localeCompare`. Any guarded canonical read, JSON, envelope mapping, row normalization, timestamp coercion, or comparison failure returns `[]`; ordinary writes still target only `aiResponsesPath(slug)`.
- `server/modules/ai/application/aiHistoryCommands.js` owns entry lookup/not-found behavior, draft resource editing with stable nested IDs, and apply/undo snapshot dispatch behind exactly `createAiHistoryCommands`, `patchDraftAiChanges`, and `preserveExistingIds`; keep alignment, validation, submitted-resource, stored-resource, and assembly helpers private. Stable-ID projection enters array mode only when both operands are arrays, maps the `after` array with sparse holes preserved and named properties dropped, and aligns rows by index. Object mode requires truthy non-array objects, spreads `after` into a fresh plain object before any ID read, preserves an own `before.id` including non-enumerable values, and then recursively processes only `Object.keys(next)` in order. Preserve ordinary inherited `before[key]` lookup, two reads of an accessor `before.id`, symbol copying without recursive processing, prototype loss, after-only nested identity, safe own `__proto__` projection, and aligned circular failure; mismatched shapes return the exact `after` identity. Draft patching validates draft state before submitted-array shape, keeps exact 400 errors, accepts truthy object-like rows including arrays, maps `String(id || "")` to `after ?? null` with last duplicate value winning, and matches existing resource IDs strictly against those string keys. Matched resources are fresh projections, unmatched resources retain identity, `entry.changes` is intentionally read once for stored resources and again before summary construction, and the summary callback runs last. Commands must preserve `get -> patch/summary -> update` or `get -> restore` order, update by `entry.id`, pass `{ resourceIds }` even when undefined, and propagate dependency failures unchanged.
- `server/aiResponseHistoryService.js` owns AI snapshot resource restoration, history-state projection, refreshed-history loading, and updated-target readback behind its unchanged four-export API. Restore resources remain `entry?.changes?.resources || []` without malformed-value normalization; explicit `null` options still throw, and only array `resourceIds` enable partial mode. Requested IDs use `String(id || "")`, drop falsy-normalized values, deduplicate through `Set`, and match raw resource IDs strictly. Reject missing resources and empty selections with the exact status-400 errors. Selected snapshots write sequentially through `resource[snapshotKey] ?? null` with no transaction or compensation; earlier writes survive later failures, and timestamps/history updates begin only after every write succeeds. Exact `"after"` means applied and every other key means undone. Partial restores clone only selected resources, retain unselected identities, rebuild summary from the projected list, and derive root applied/undone/draft state from every resource. Full apply clones every resource but summarizes the original list; full undo retains `entry.changes` by reference and does not rewrite resource metadata. Preserve effect order `writes -> update response -> read responses -> read updated target`, return falsy update results unchanged while using `response || entry` only for readback, prioritize exact `bestiary`, let missing sessions fall back to campaign metadata, force the requested session filename on successful reads, and propagate every storage/read failure unchanged.
- Per-monster custom Bestiary change construction in `server/aiResponseHistoryService.js` uses focused private list, index, match, resource, clone, and ordering policies behind the unchanged `buildCustomMonsterChangeResources` export. Accept only array-valued source lists and only truthy object-like records, including arrays. Preserve repeated ID/name getter reads during filter/map indexing; trimmed truthy IDs; case-insensitive trimmed names; `Map` last-value replacement with first-key insertion order; mapped-object identity tracking; and the ID pass before the unmatched-name pass, including residual duplicate-ID objects. Ordered unions keep before keys before after-only keys. Change detection remains JSON-string comparison over nullish-to-null snapshots, so property order matters and circular/BigInt failures propagate. Resource IDs retain `after ID -> before ID -> normalized name from after || before -> pass key`; labels use the same selected record and fallback key. Snapshots are JSON deep clones, array named properties are lost, unchanged resources are omitted, and final ordering remains stable `localeCompare` by `label || id`. Keep both AI-flow consumers and the service's four-export API unchanged.
- `server/modules/ai/application/generateAiRequest.js` is the top-level generation use case behind exactly the `createGenerateAiRequest` export; keep preparation, prepared-error, workflow-input/selection, and failed-history helpers private, while Express only maps `{ status, body }`. Factory destructuring retains `console.error` as the undefined-only history callback default. Every request calls `historyWriter.getUserInstructions(payload)` before the guarded block, so its failures bypass API-key checks, preparation, and failure persistence. Guarded preparation builds `{ payload, apiKeyConfigured: isApiKeyConfigured(), readSettings }` in that order and awaits `prepareRequest`. A truthy prepared error is read once for eligibility and twice again for status/message, returns immediately, and never selects a workflow or writes history. Selection is exact custom-monster type first, then truthy Bestiary-image flag, then campaign content; all receive one fresh input with exact payload/prepared/history identities. Preserve return-without-`await`: synchronous workflow throws enter recovery, while rejected workflow Promises bypass the coordinator catch. Recovery awaits `historyWriter.saveFailed(payload, error, error.status || 500)`; a truthy entry returns a fresh response with a second status read and `error.message || "AI request failed."`. Falsy history results rethrow the exact original value. Save/status/message/projection failures call `onHistoryError("Failed to save failed AI request", historyError)` before rethrowing the original; a throwing callback replaces it. Preserve primitive/null failures and collaborator method receiver behavior.
- Gemini API-key validation/cache invalidation belongs to `server/modules/ai/application/saveGeminiApiKey.js`; `.env` reading/writing and `process.env` mutation belong to `server/modules/ai/infrastructure/envApiKeyStore.js`.
- Generated entity mention candidate collection, safe text-field wrapping, canonicalization, and nested-bracket cleanup belong to `server/modules/ai/application/mentionProcessing.js`, not HTTP routes.
- Mention candidate collection composes campaign, current-session, included configured-session, generated top-level entity, and generated-operation sources. Preserve character/NPC display-name rules, location/faction aliases, object `data` before object `patch`, non-empty scene config as an allowlist, empty scene config as include-all, scene NPC extraction, exact deduplication, minimum two-character names, and longest-first ordering. Optional collections must be malformed-safe. Recursive wrapping changes only the closed text-key allowlist; identifier and unknown keys remain untouched, existing bracketed links canonicalize without nesting, and browser/HTTP layers must not recreate this policy.
- Frontend feature model: `src/features/ai/model/*`; pure estimation, history and workflow rules do not belong inside `AiAssistantPanel.tsx`.
- AI generation/retry uses explicit lifecycle statuses and request IDs. `generationLifecycle.ts` owns frozen status constants, the frozen idle singleton, start/finish event maps, start projection, exact active-request completion guards, and terminal projection. Preserve unconditional replacement on every start event, including while another request is pending; exact request-ID equality for succeed/fail/cancel; original state identity for stale terminal events; cleared request IDs for every matching terminal transition; reset identity to `initialAiGenerationLifecycle`; and pending status only for generating/retrying, including false for nullish state. Do not reintroduce a shared `loading` boolean for generation, retry, and context loading.
- AI generation transport execution and outcome classification belong to `src/features/ai/model/generationExecution.ts`. Required callbacks cover success, cancellation, failed-history capture, missing API key, and ordinary failure. Preserve failed-history capture before API-key classification, treat `AbortError` only as cancellation, and normalize status/message from unknown errors before presentation. React owns request construction inputs, `AbortController` references, request IDs, lifecycle dispatch, localized fallback copy, and alerts.
- AI generation result interpretation and callback execution belong to `src/features/ai/model/generationResultWorkflow.ts`. Preserve prompt-before-draft-before-update precedence and validate `updated` as a record. Updated-plan construction delegates generated/history analysis, route-shape application, reload, prompt-clear, refresh, close, and notification decisions to focused private policies. Treat only record-shaped `generated` values as generated content; determine session shape only from a record-shaped `updated.data`; apply Bestiary data directly, campaign-shaped data only on campaign screens, and session-shaped data only on non-campaign screens; request reload for every shape mismatch and for directly applied session data with campaign changes; default prompt clearing through `?? true`; refresh only when application is indirect and generated/history entity types exist; and use the custom-monster notification only for the exact request type. Updated effects execute strictly as history → direct apply → campaign reload → prompt clear → notification → entity refresh → dialog close; keep persistence/reload effects separate from presentation effects without snapshotting mutable plan flags before callbacks run. React owns localized notification text and performs plan effects through the executor callbacks.
- Applying updated AI data to application state belongs to `src/features/ai/model/updatedDataWorkflow.ts`. Preserve Bestiary sync metadata and unconditional Bestiary entity refresh, session/encounter activation with string-normalized encounter IDs, campaign merging over the active campaign, reload-only fallback for route-shape mismatches, session filename fallback, and history-derived entity types. Reject arrays/non-record updates. Execute state changes, reload, sync publication, and entity refresh in plan order; do not duplicate entity refresh in the outer generation-result workflow when direct application already owns it.
- AI history retry planning and execution belong to `src/features/ai/model/historyWorkflow.ts`. Block retry while generation is active or the entry is ineligible; delete failed history before regeneration and reflect that deletion immediately; preserve explicit payload types, use the Bestiary fallback only when type is absent, and never parse image retries. Retry execution delegates failed-entry deletion, nullable response normalization, generation transport, success notification, and failure/cancellation classification to focused private policies. Preserve strict delete → deletion callback → generate → success callback ordering, pass the exact retry payload and signal, normalize a non-array delete result to `[]`, accept nullable generation success, treat only object-shaped `AbortError` as cancellation, pass the original failure object through unchanged, and keep callback exceptions inside the same guarded transaction so a throwing success callback becomes a failure outcome. Generated entity types use the ordered `characters -> npc -> locations` collection table; any array, including empty, counts as generated, and history-resource fallback applies only when no generated collection is present. React owns `AbortController`, request IDs, localized feedback, and state updates through the executor callbacks.
- AI history collection and restore decisions belong to `src/features/ai/model/historyState.ts`; React executes the plan using functional state updates so concurrent history entries are preserved. Direct restore first classifies untrusted updated data as invalid, Bestiary, campaign, or session, then applies the explicit Bestiary/campaign/session/encounter route policy. Preserve same-campaign and same-session identity, string-compatible encounter IDs (including `0`), encounter-scoped history rejection outside its active encounter, session-level restore within an encounter, Bestiary campaign identity, record-only session `data`, and reload-only fallback whenever shape or route is incompatible.
- Async AI history commands belong to `src/features/ai/model/historyCommands.ts`; keep confirmation copy and screen-specific effects in the caller. Restore transport selection and nullable-result rejection belong to the command service; confirmation happens before lock acquisition, a ref-backed lock is rechecked after async confirmation to suppress racing restores, and release must run after success, transport failure, empty responses, or throwing restore effects.
- AI-specific presentation boundaries live in `src/features/ai/ui`; keep modal shell, prompt composer, and response-dialog wiring out of `AiAssistantPanel.tsx`.
- Import AI UI through `src/features/ai/ui/index.js`; do not re-export JSX from the model/API `src/features/ai/index.js`, because Node regression tests consume that entry directly.
- `features/ai/ui` has complete strict TypeScript implementation coverage behind its JavaScript browser barrel and typed facade. Toolbar/context/attachment/API-key/token/history presentation rules live in `presentationModel.ts`; toolbar action visibility is projected before composition. Entity actions require parsed output outside encounter screens, with character generation additionally gated by campaign visibility. Encounter actions require parsed output; the encounter toggle is hidden on campaign screens, custom-monster visibility remains caller-controlled, and direct custom-creature creation remains encounter-only. Preserve functional boolean inversion, active/ghost variants, loading disablement, current-encounter versus scene title copy, and encounter-off forcing custom-monster generation false while encounter-on leaves it unchanged. History rows derive fallback title, formatted date, change/state labels, and optional retry visibility in the presentation model, while `AiResponseHistory` owns virtualization and private row composition. Keep row selection on the native button rendered by shared `ListCard`, retry/delete as independent Button actions, callback evaluation order, empty summary/state omission, and missing-entry virtualization fallback. Attachment controls derive action visibility, disabled state, and gallery source in the presentation model, while private UI compositions render actions, image rows, and file rows. Attachment reads must preserve MIME/size/count validation, stable deduplication, gallery URL support, input reset, partial-selection alerts, the independent four-image/four-file caps, and disabled remove actions. Context UI must preserve campaign-list defaults, immutable functional updates, lazy session data, per-scene defaults, and encounter field selection. Keep toolbar orchestration thin and its private action groups in `AiToolbarControls.tsx`; reuse the slice-owned model-option renderer. The injected response-modal contract in `responseModalContracts.ts` is shared with AI monster editing. Keep model/API imports Node-safe, import browser UI through `features/ai/ui/index.js`, and preserve nullable history-entry handling at the injected modal boundary.
- Context list normalization and nested updates belong to `src/features/ai/model/contextConfig.ts`. List normalization keeps complete-container validation, missing-key insertion, and reference preservation in focused private policies. Preserve `included !== false` fallback, record-only item maps, existing own keys including false values, truthy-key insertion as true, duplicate suppression, normalization of partial/invalid containers, and original reference identity only when `included` is boolean, `items` is a record, and no key is added. Nested updates retain JSON-compatible deep-clone semantics, return the original nullable config for an empty/invalid path, replace non-record intermediate nodes, apply a fresh full default scene context only to nodes whose immediate parent key is `scenes`, preserve existing scene records without backfilling defaults, and mutate only the cloned tree.
- Context data fetching, session hydration, and entity-list synchronization belong to `src/features/ai/model/useAiContextData.ts`; inject entity/session API functions instead of importing page clients into the hook. Keep campaign-entity visibility, session-list visibility, active hydration campaign, pending-session selection, tuple loading, cancellation-aware application, tuple merging, and list synchronization in focused private policies. Campaign entities load for context modal/image picker regardless of `useContext`, or for an open panel with context enabled; session lists require context enabled plus any context surface; hydration additionally requires non-Bestiary, an open panel, context enabled, and a truthy campaign slug. Hydrate only included sessions without truthy data, run loads in parallel, report each failure and drop its tuple, use `session.data || {}`, and never apply after cancellation. During merge, truthy existing data wins, current session config wins over fallback config, the first successfully merged duplicate wins once its data is truthy, no-op merges preserve the root reference, and changed merges clone only the root and sessions map. Character/NPC/location synchronization must share the generic list updater and preserve the root when the owned list config is unchanged.
- Token-estimate request shaping, context compaction, and attachment cost calculation belong to `src/features/ai/model/tokenEstimation.ts`; keep it pure and aligned with generation request modes.
- AI generation request projection belongs to `src/features/ai/model/generationRequest.ts`. `resolveAiGenerationRequestPolicy` owns Bestiary/image type and parsing precedence; `buildAiGenerationRequestOptions` owns structured entity and encounter/custom-monster gates; the context, target, and attachment helpers own their respective payload fragments; `buildAiGenerationRequest` only composes the final transport payload. Preserve Bestiary non-image remapping, image parsing/encounter disabling, null-only parse and instruction override semantics, campaign/Bestiary encounter restrictions, custom-monster dependence on encounter generation, removal of session `data` from copied context, caller-owned path/image-target/attachment identity, empty-model omission, and retry-compatible field names.
- Image prompt target shaping belongs to `src/features/ai/model/imageTargets.ts`; keep its public builders as thin projections over the shared runtime-safe field, identity, collection, encounter-summary, and ability policies. Preserve truthy fallbacks for legacy payload fields, nullish preservation for numeric levels/abilities, string-normalized encounter IDs, encounter-index fallback, ignored-note filtering, the eight-note cap, and stable monster-name summaries. Normalize malformed collection fields to empty arrays instead of allowing untrusted API data to reach array operations.
- Image-prompt campaign/session and custom-bestiary loading belongs to `src/features/ai/model/useAiImagePromptData.ts`; reuse injected context loaders. Custom-monster normalization must keep envelope narrowing, `monster`-before-`monsters` selection, root-array fallback, and record filtering in focused private policies. Preserve `monster` precedence whenever it is an array, including an empty array; fall through to `monsters` only when `monster` is not an array; accept a root array only when neither envelope collection applies; reject null, primitives, functions, and nested arrays as monster items; and preserve the original array reference when every item is already a record, including empty arrays.
- Widget-level image-prompt picker state and orchestration belong to the private typed hooks under `src/widgets/ai-assistant/model`. Pure campaign/session entity projection, scene metadata, custom-monster grouping, target titles, and generation validation belong to `imagePromptPicker.ts` and remain available through the Node-safe widget model facade. Generation planning delegates targetless validation, scene-ID projection, and request-option construction to focused private policies; target-title qualification and fallback are private policies behind the existing public function. Keep React hooks private: do not export them from `widgets/ai-assistant/model.js`. Preserve prepare-before-open, Bestiary registered-action cleanup, active base-prompt resets, trimmed instructions and targetless requests, the targetless-only empty-request error, target-owned clearing of `userInstructionsOverride`, scene-only `id || null` projection, session-qualified scene titles even with an empty scene name, truthy `name -> id -> type` title fallback semantics, scene/session identity, and attachment/model state owned by the parent assistant.
- Widget-level AI context orchestration belongs to the private `useAiAssistantContextController.ts` hook. It wraps the feature-owned `useAiContextData`, owns expanded-session hydration/loading and immutable list/config command wiring, and must remain private rather than entering the widget model facade. Route/store projection belongs to the Node-safe `assistantContext.ts` model. Preserve campaign collection merging, session data selection, encounter projection, Bestiary null/empty context, response-parsing locks, custom-monster option visibility, normalized context lists, default session configuration, lazy hydration, and loading cleanup on success or failure.
- Widget-level AI history selection, loading/stats, clipboard state, command wiring, restore projection, and route filtering belong to the private `useAiAssistantHistoryController.ts` hook over feature-owned history commands. Pure history view/confirmation/error policies and `executeAiAssistantHistoryRestorePlan` live in `assistantHistory.ts` and are exposed through the Node-safe widget model facade; the React hook remains private. Restore execution order is history replace/upsert → stats refresh → selected-entry/prompt sync → direct update with `trackUndo: false` and the restored history entry → reload. Preserve functional upserts against the latest history collection, refresh stats only when history changed, selection only for the plan-selected entry, exact entity-type arrays, and independent plan flags. `AiAssistantPanelView.tsx` is the private typed presentation composition for shell, toolbar, API-key, context, history dialog/list, and prompt sections. Keep `AiAssistantPanel.tsx` as orchestration, preserve lazy history loading with stale-effect cancellation, selected-entry synchronization, formatted/plain clipboard fallback, partial apply/undo confirmations, direct-route restore versus reload behavior, and Bestiary/encounter history filtering.
- `AiAssistantPanel.tsx` receives app-wide inputs and effects only through the widget-owned `AiAssistantRuntime`; export only its provider/types through `widgets/ai-assistant/index.js` and keep the Context hook private. `app/ui/AiAssistantRuntimeHost.tsx` alone maps live campaign/session/encounter, language, prompt, and navigation state plus selection, reload, refresh, sync, confirmation, and message commands. `MainContentLayout` must scope that host around both the routed `<Outlet />` and the ordinary route assistant so the injected Bestiary assistant and the route-level assistant share the same runtime without new `AiAssistantPanelProps` or Bestiary slot props. Keep route projection, local context/loading, generation/retry lifecycle, history restore/reload, localized copy, and response-modal composition widget-owned; do not restore `app/model` or `shared/model` imports inside `widgets/ai-assistant` or merge the separate `AiAttachmentAlertRuntime` contract into this runtime.
- Schema: parsed AI must return `{ "version": 2, "operations": [...] }`.
- Allowed operation style is domain-specific (`create`, `update`, `delete`, `appendNote`, `updateNote`, `deleteNote`, `moveScope`), not raw RFC JSON Patch.
- AI history is campaign-scoped in `data/campaigns/{slug}/_aiResponses.json`.

### Images

- What: upload/list/move/rename/delete campaign/general images; attach images to campaigns/entities/scenes/monsters.
- Main UI: `src/features/images/ui/*`, `src/features/images/imageGalleryConfig.js`.
- Backend: `server/routes/images.js`, `server/storage.js`.
- Data: `data/images/{campaignSlug|general}/{category}/{optionalSubcategory}`.
- Categories from config: `maps`, `scenes`, `tokens`, `characters`, `props`, `notes`, `attachments`.

### Global Search and Mentions

- What: campaign-wide search across notes, scenes, NPC, locations, sessions, custom monsters, mentions.
- Main UI: `src/widgets/campaign-search`.
- Mentions: bracketed text like `[Name]` rendered by `EntityLink.tsx` and context utilities.
- Important parsing: do not turn text inside `pre`/`code` into entity links.

### Import, Export, Backups

- What: full backup/archive, campaign archive, partial campaign archive, custom bestiary import/export.
- Backend: `server/routes/backups.js`, `server/storage.js`.
- UI: `src/features/campaign-create/ui/CreateCampaignModalContent.tsx`, `src/pages/campaign/ui/components/PartialArchiveModal.tsx`, `src/widgets/bestiary-browser`.
- Partial campaign archive currently covers sessions, NPC, locations, images, AI history. Custom monsters are handled from Bestiary.

### Settings and Localization

- What: theme, language, simplified notes, encounter UI, AI base prompts, auto-apply parsed AI changes.
- UI: `src/features/settings/ui/SettingsModalContent.tsx`.
- Backend/data: `server/routes/settings.js`, `data/settings.json`.
- Localization: `src/langs/uk.json`; `src/langs/en.json` currently appears empty/minimal.

## Data Flow

- URL -> `src/shared/lib/navigation.js` (through `shared/lib/index.js`) -> `appStore.navigation`.
- `MainContent.tsx` chooses the active view:
  - `/campaign/:slug`
  - `/campaign/:slug/session/:fileName`
  - `/campaign/:slug/session/:fileName/encounter/:encounterId`
- Views і slices викликають власні domain API clients. Усі clients використовують `src/shared/api/httpClient.ts` для Express routes під `/api`.
- `server/storage.js` reads/writes JSON and files under `data/`.
- Campaign metadata: `data/campaigns/{slug}/_campaign.json`.
- Entities: `data/campaigns/{slug}/characters|npc|locations/{entitySlug}/info.json`.
- Sessions: `data/campaigns/{slug}/sessions/{fileName}.json`.
- AI parsed response flow:
  1. `AiAssistantPanel.tsx` sends payload to `/api/ai/generate`.
  2. `server/aiService.js` builds prompt/context.
  3. `server/modules/ai/domain/aiPayloadSchemas.js` validates operation schema.
  4. `server/aiPatchService.js` applies targeted operations.
  5. `server/storage.js` persists changes and response history.
  6. UI shows draft/enhanced diff in `widgets/ai-response-modal` through its public composition.
- Image move/rename updates references across campaign metadata, entities and sessions via `storage.updateAllImageReferences`.
- Service worker caches `/api/bestiary` and `/api/spells` GET responses for reference data. Be careful when debugging stale bestiary/spell data.

## Important Conventions

- Use UTF-8. After editing Ukrainian text, run `npm run check:uk` or `npm run lint`.
- Do not replace Ukrainian text with `?`; watch for mojibake/replacement symbols.
- Prefer existing local patterns over new abstractions.
- Use `rg`/`rg --files` for search.
- Use `apply_patch` for manual edits.
- Do not revert user changes. Worktrees may be dirty.
- Do not edit `data/` unless the user explicitly asks to change local runtime data.
- Do not edit generated `dist/` or `node_modules/`.
- Do not propose running `npm run build`. The script exists, but project instruction says not to propose it.
- If changing logic, remove code made obsolete by the change.
- Frontend styles are mostly BEM-like classes in `src/assets/components/*.css` plus shared SCSS in `src/assets/scss/`.
- Cards/components often have paired model files under `src/models/`; check models before changing display logic.
- Many views debounce autosave; avoid adding immediate writes inside every keystroke unless existing code already does that.

## Target Frontend Architecture (FSD)

Поточний статус і послідовність міграції зафіксовані в `docs/architecture-migration.md`. Оновлюй статус phase та acceptance criteria разом зі структурними змінами.

Frontend використовує Feature-Sliced Design. `src/components`, `src/hooks`, `src/models`, `src/utils`, `src/services`, `src/store`, `src/actions`, and `src/renderers` retired і не повинні відновлюватися. Новий функціонал створюй у `app`, `pages`, `widgets`, `features`, `entities`, or `shared` відповідно до напрямку залежностей.

Дозволений напрям залежностей:

`app -> pages -> widgets -> features -> entities -> shared`

- `app` - providers, routing, app initialization і глобальна композиція.
- `pages` - тонкі route-компоненти; лише композиція widgets/features, без доменної логіки.
- `widgets` - великі самодостатні частини сторінки, що компонують features та entities.
- `features` - завершені дії користувача/use cases (`edit-monster`, `ai-edit-monster`, upload, apply/undo).
- `entities` - доменні моделі, domain API, selectors, нормалізація та повторно використовуване domain UI.
- `shared` - HTTP transport, загальний UI, config та справді доменно-нейтральні helpers.

Обов'язкові правила:

- Нижчий FSD-шар не імпортує вищий. Правила закріплені в `.fallowrc.jsonc`.
- Зовнішній код імпортує slice лише через явний public entry. Спільні `FSD_PUBLIC_API_PATTERNS` та локальне правило `fsd-boundaries/public-entry-imports` застосовуються до frontend-коду під `src` і забороняють JavaScript/JSX та TypeScript/TSX імпорти приватного шляху в чужий slice: кореневі `./features|entities|pages|widgets/...`, Vite-root `/src/features|entities|pages|widgets/...` і шляхи через один або більше `../` для cross-layer та same-layer імпортів. Дозволені фасади: кореневий `index` slice, `ui/index`, JSON-free/Node-safe `model.js`, а для page graph - `graph.js`; внутрішні імпорти всередині того самого slice залишаються дозволеними. Перевірка охоплює static imports/re-exports, dynamic imports, `require`, Vite `import.meta.glob`/`globEager` (включно з масивами статичних patterns), TypeScript `import()` types та external-module `require`; static string/template literals не повинні обходити public API. Backend/server imports не повинні класифікуватися за frontend FSD-іменами.
- Не створюй broad barrel-файли, що реекспортують цілі дерева. Public API slice має бути мінімальним і явним.
- `scripts/eslint-import-boundaries.mjs` є єдиним власником синхронізованого реєстру `FSD_SLICE_NAMES`, спільних public-entry patterns, локального `FSD_BOUNDARY_PLUGIN` і точного `FSD_SAME_LAYER_FILE_EDGE_BASELINE` для `src/features` та `src/widgets`. Правило public-entry перевіряє всі підтримані статичні module-reference форми, а same-layer правило перевіряє importer-file → target-slice, розпізнає relative та Vite-root `/src/...` specifiers, нормалізує Windows paths, забороняє нові edges та вимагає видалення stale allowances. Source-inventory contract ігнорує module-like текст у коментарях та string/template literals. Додаючи або видаляючи slice, одночасно онови реєстр і відповідний мінімальний public entry; baseline змінюй лише разом із явним видаленням або переглядом edge. Не створюй окремі JS та TS списки, що можуть розійтися.
- Fallow discovers `src/entities/*` as separate zones through `autoDiscover`; entity slice may depend only on `shared`, and audited sibling-entity edges must remain zero. Оркестрація двох або більше entities належить feature, а не entity.
- Phase 135 closes the production same-layer baseline for both `features` and `widgets`: each is exactly 0 importer files / 0 directed pairs / 0 file edges / 0 declarations. Production code must not restore any sibling-slice import or allowance. The exported `createFsdSameLayerFileEdgeRule` factory exists only so tests can exercise a synthetic stale baseline; the production plugin always uses the frozen empty baseline. Phase 136 then moved actual global-store composition to `src/app/model/appStore.ts` without moving feature/domain state into the app root. App entry, routing, realtime, and host consumers import the app public entry; lower slices temporarily retained only the typed shared facade during incremental migration. During that transition, `fsd-boundaries/app-store-runtime-owner` restricted the private runtime port to the app owner and that facade across static imports, re-exports, dynamic imports, `require`, and Vite-root paths. Phase 137 removes Settings' direct shared-store facade dependency through the live sidebar-provided `SettingsModalRuntime`. `fsd-boundaries/settings-store-facade` applies only to `src/features/settings/**/*.{js,jsx,ts,tsx}` and blocks every `app/model` or `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript module-reference forms, including Vite-root and filesystem identities. Phase 138 gives Notes a live app-provided scalar preference through `SimplifiedNotesProvider`; the shared injected-runtime checker also powers `fsd-boundaries/notes-store-facade`, which blocks the same complete app/shared-model reference set only under `src/features/notes/**/*.{js,jsx,ts,tsx}`. Phase 139 gives Player Questions a live `PlayerQuestionsRuntime` from its Sidebar modal adapter; `fsd-boundaries/player-questions-store-facade` blocks the same complete app/shared-model reference set only under `src/features/player-questions/**/*.{js,jsx,ts,tsx}`. Phase 140 gives Campaign Entity a widget-provided `onRefreshEntities` command; `fsd-boundaries/campaign-entity-store-facade` blocks the same complete app/shared-model reference set only under `src/features/campaign-entity/**/*.{js,jsx,ts,tsx}`. Phase 141 gives Encounter Editor an `AddMonsterToEncounterModalRuntime` from its Monster Stat Block owner; `fsd-boundaries/encounter-editor-store-facade` blocks the same complete app/shared-model reference set only under `src/features/encounter-editor/**/*.{js,jsx,ts,tsx}`. Do not weaken any scoped rule or move an adapter/store access back into a feature. Source-inventory contracts and the full 427/427 `npm test` gate enforce these checkpoints while complete lint remains tracked under `MD-R04`.
- Імпортуй domain client з власника: наприклад `entities/bestiary`, `entities/campaign`, `features/ai`. Не створюй новий global API facade.
- Phase 159 completes Phase 136's temporary compatibility transition: `shared/model/appStore.ts`, `appStoreRuntime.ts`, and the `app-store-runtime-owner` checker are retired. `app/model` is now the sole stateful store/selector/modal/navigation owner; lower slices retain only injected runtimes and the scoped facade-boundary rules. Do not restore the shared facade, runtime port, or checker. `MD-R02` is closed; `MD-R04` remains open for complete lint/typecheck.
- Phase 142 gives Rules Reference an app-provided `RulesReferenceRuntime` for typed navigation and error reporting. Phase 153 moves Sidebar's corresponding typed request behind its own injected runtime. `fsd-boundaries/rules-reference-store-facade` blocks every `app/model` and `shared/model` reference only under `src/features/rules-reference/**/*.{js,jsx,ts,tsx}` using the same complete static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage as the other scoped rules. Preserve resolver, preview, tooltip, module-scope factory, error-reporting, and target-navigation behavior; do not restore the retired `openRulesReferenceModal` helper. Source-inventory contracts and the full 428/428 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 143 gives AI attachment controls an app-provided `AiAttachmentAlertRuntime` for validation notices without changing either stable attachment-control factory. `fsd-boundaries/ai-store-facade` blocks every `app/model` and `shared/model` reference only under `src/features/ai/**/*.{js,jsx,ts,tsx}` using the same complete static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Do not move general AI panel state into that runtime or restore the old global `RequestId` type coupling. Source-inventory contracts and the full 429/429 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 144 gives `Input` and `EditableField` an app-provided `EditorMentionPickerRuntime` for mention-picker requests while retaining the feature-owned selection/promise policy. `fsd-boundaries/editor-store-facade` blocks every `app/model` and `shared/model` reference only under `src/features/editor/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the entity-link and mention-picker runtime contracts separate. Source-inventory contracts and the full 430/430 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 145 gives Dice an app-provided `DiceRequestRuntime` for stable roll requests while `app/ui/DiceCalculatorHost.tsx` owns the single live `rollRequest` selector and result-publish callback. `fsd-boundaries/dice-store-facade` blocks every `app/model` and `shared/model` reference only under `src/features/dice/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the provider hook private, do not move the selector into `App` or a shared context, and preserve request dedupe, animation, auto-close, and local-result-before-publish order. Source-inventory contracts and the full 431/431 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 146 gives Images an app-local `ImageGalleryRuntimeHost` and a feature-owned `ImageGalleryRuntime` for live gallery preferences plus alert, confirmation, and prompt commands. `fsd-boundaries/images-store-facade` blocks every `app/model` and `shared/model` reference only under `src/features/images/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the Context hook private, do not turn the runtime into a raw dispatch/state facade, and preserve mounted-while-closed gallery state, 250/0 debounce policy, ignored-source derivation, upload/rename order, and bulk-delete loading/confirmation/finally order. Source-inventory contracts and the full 432/432 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 147 gives Campaign Entity Card a widget-owned `CampaignEntityCreationRuntime` and an app-local host that maps only error notification and entity refresh. `fsd-boundaries/campaign-entity-card-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/campaign-entity-card/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the Context hook private, do not inject raw store state or dispatch, and preserve invalid-draft return, custom-create-without-refresh, default-create refresh, close-after-success, error notification, and finally unlock. Source-inventory contracts and the full 433/433 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 148 gives Campaign Search a widget-owned `CampaignSearchRuntime` and an app-local route host that maps only the active campaign identity and fixed ordinary navigation around `MainContentLayout`'s `<Outlet />`. `fsd-boundaries/campaign-search-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/campaign-search/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the Context hook private, do not inject raw store/dispatch or general replace/new-tab controls, and preserve malformed-campaign narrowing, abortable loading, close-before-navigation, target null coercion, URL/hash order, 80 ms scroll, and 2400 ms conditional cleanup. Source-inventory contracts and the full 434/434 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 149 gives Campaign Entity Modal a narrow App-supplied `CampaignEntityModalRuntime` for confirmation requests and entity refresh. The widget retains localized title/message construction, entity validation, persistence, parent-resolver precedence, campaign-scope guard, cancellation semantics, and close-after-delete. `fsd-boundaries/campaign-entity-modal-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/campaign-entity-modal/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Do not inject raw store/dispatch or move localized confirmation copy into App. Source-inventory contracts and the full 435/435 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 150 gives Rules Reference Modal a widget-owned `RulesReferenceModalRuntime` provider for navigation/history state plus modal, error, open-state, and history commands. App owns the selectors and action mapping, while the widget retains stable content factories, modal opening, request de-duplication, mounted-only loading/error effects, history handling, and tab/selection behavior. `fsd-boundaries/rules-reference-modal-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/rules-reference-modal/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, commands stable across state updates, and do not add runtime props to the stable content factory API. Source-inventory contracts and the full 436/436 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 151 gives Spells Browser a widget-owned `SpellsBrowserRuntime` provider for the debounce policy, active campaign/source settings, global ignored-source list, campaign replacement, ignored-source patch, and error reporting. App owns the live selectors and stable action mapping, while the widget retains API loading, source derivation, persistence branching, localization, local selection, and abortable loader behavior. `fsd-boundaries/spells-browser-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/spells-browser/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, do not inject raw store/dispatch, and do not add runtime props to the stable Rules Reference factory slot. Source-inventory contracts and the full 437/437 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 152 gives Monster Stat Block a widget-owned `MonsterStatBlockRuntime` provider for the live campaign snapshot plus modal, error, reload, and dice commands. App owns the live campaigns selector and stable command mapping, while the widget retains custom add override precedence, click-time campaign filtering, localized modal config, the feature-owned add-to-encounter runtime adapter, token/favorite/spell behavior, and both raw formula roll call sites. `fsd-boundaries/monster-stat-block-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/monster-stat-block/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, do not inject raw store/dispatch, and do not add runtime props to stable Bestiary, AI Response, or Rules Reference factory slots. Source-inventory contracts and the full 438/438 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 153 gives Sidebar a widget-owned `SidebarRuntime` provider for live navigation, modal/error, Rules Reference, Settings, and Player Questions dice contracts. `app/ui/SidebarRuntimeHost.tsx` owns the selector/action mapping and wraps both Sidebar and root modal rendering so delayed modal children stay live. `fsd-boundaries/sidebar-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/sidebar/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, preserve import/export and close-before-navigation ordering, retain raw Settings debounce plus false-only Player Questions normalization, and do not add a runtime prop to `SidebarProps`. Source-inventory contracts and the full 439/439 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 154 gives Bestiary Browser a widget-owned `BestiaryBrowserRuntime` provider for live language/debounce/campaign/source/sync state plus campaign replacement, global-source patch, message, and confirmation commands. `app/ui/BestiaryBrowserRuntimeHost.tsx` owns the selector/action mapping and scopes it around `MainContentLayout`'s routed `<Outlet />`; `BestiaryBrowser` keeps raw sync parsing, API loaders, selection/history/abort state, localized copy, and existing slots. `fsd-boundaries/bestiary-browser-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/bestiary-browser/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, preserve false-only debounce, raw sync-event identity, campaign-versus-global source-save order, confirmation-before-delete, and runtime-free public props/slots. Source-inventory contracts and the full 440/440 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 155 gives AI Assistant a widget-owned `AiAssistantRuntime` provider for live campaign/session/encounter, language, prompt, and navigation state plus selection, campaign reload, entity refresh, AI sync, confirmation, and message commands. `app/ui/AiAssistantRuntimeHost.tsx` alone maps those selectors/actions and wraps both `MainContentLayout`'s routed `<Outlet />` and its route-level assistant, keeping the injected Bestiary assistant live without runtime prop threading. `fsd-boundaries/ai-assistant-store-facade` blocks every `app/model` and `shared/model` reference only under `src/widgets/ai-assistant/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, do not inject raw store/dispatch or add runtime props to `AiAssistantPanelProps` or Bestiary slots, and preserve AI sync-event construction, generation/retry, history confirmation/reload, route state, local loaders, and response composition. Source-inventory contracts and the full 441/441 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 156 gives Session Page a page-owned `SessionPageRuntime` provider for live campaign/session/sync state plus campaign/session/encounter navigation, active-session selection, campaign reload, entity refresh, confirmation, prompt, and message commands. `app/ui/SessionPageRuntimeHost.tsx` alone maps selectors/actions and scopes the provider around `<SessionPage />` inside `SessionRoute` after its existing campaign guard. `fsd-boundaries/session-page-store-facade` blocks every `app/model` and `shared/model` reference only under `src/pages/session/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, do not inject raw store/dispatch or add a runtime prop to `SessionPage`, and preserve active-session publication, sync discard/reload policy, rename replace-then-reload order, scope-move refresh-then-reload order, confirmation/prompt coercion, encounter navigation/new-tab behavior, and all page-owned session workflows. One added static regression plus source-inventory contracts and the full 442/442 `npm test` gate enforce this checkpoint while complete lint remains tracked under `MD-R04`.
- Phase 157 gives Encounter Page a page-owned `EncounterPageRuntime` provider for live campaign/session/encounter/sync, dice, theme/language, and encounter-view settings state plus session navigation, UI-settings patch, entity refresh, campaign reload, dice, prompt, active-selection, and message commands. `app/ui/EncounterPageRuntimeHost.tsx` alone maps selectors/actions and scopes the provider around `<EncounterPage />` inside `EncounterRoute` after its existing campaign guard. `fsd-boundaries/encounter-page-store-facade` blocks every `app/model` and `shared/model` reference only under `src/pages/encounter/**/*.{js,jsx,ts,tsx}` with the same static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, do not inject raw store/dispatch or add a runtime prop to `EncounterPage`, and preserve encounter load/save and active selection, sync/AI updates, HP-roll dedupe, UI-settings update-before-API order, participant refresh, and existing error/finally behavior. Source-inventory and static regression coverage pass with the full 443/443 `npm test` gate while complete lint remains tracked under `MD-R04`.
- Phase 158 gives Campaign Page a page-owned `CampaignPageRuntime` provider for live active-campaign, entity-refresh/sync, theme, and language state plus campaign-list/renamed-campaign/session navigation, graph-note modal, campaign reload, confirmation, prompt, and message commands. `app/ui/CampaignPageRuntimeHost.tsx` alone maps selectors/actions and scopes the provider around `<CampaignPage />` inside `CampaignRoute` after its existing campaign guard. `fsd-boundaries/campaign-page-store-facade` blocks every `app/model` and `shared/model` reference only under `src/pages/campaign/**/*.{js,jsx,ts,tsx}` with the same full static/re-export/dynamic/require/Vite-glob/TypeScript bypass coverage. Keep the hook private, expose only provider/types through the page root, do not inject raw store/dispatch or add a runtime prop to `CampaignPage`, and preserve campaign/session/entity loading and persistence, history, sync/AI flows, graph layout/note editing, archive/import/export, prompt/confirmation ordering, and runtime-free page contracts. Source-inventory and static regression coverage pass with the full 444/444 `npm test` gate; architecture, performance, and Ukrainian encoding checks pass while complete lint remains tracked under `MD-R04`.
- Phase 159 retires `shared/model/appStore.ts` and `shared/model/appStoreRuntime.ts` after the final page migration. `SessionPageRuntimeHost` imports navigation from `app/model`; `shared/model` contains action/type/reducer policies only. Source-inventory and static retirement coverage pass with the full 445/445 `npm test` gate; architecture, performance, and Ukrainian encoding checks also pass. Do not recreate a shared store facade/runtime or give lower layers global store access. `MD-R02` is closed; `MD-R04` remains open for complete lint/typecheck.
- Phase 160 keeps the Editor's injected mention-picker runtime feature-owned while moving the app-wide request subscription, entity loading, option projection, and modal lifecycle from `App` to `app/ui/MentionPickerModalHost.tsx`. Keep the host inside `EditorMentionPickerRuntimeProvider`, do not move the editor's promise/selection policy upward, and do not give the feature direct store access. Static source-inventory coverage passes with the full 446/446 `npm test` gate; architecture, performance, and Ukrainian encoding checks also pass. `MD-R04` remains open for complete lint/typecheck.
- Phase 161 moves the Rules Reference Modal's mounted/controller/requested-tab/loading/error lifecycle from `RulesReferenceModalContent.tsx` to private `ui/useReferenceTabLoading.ts`. Keep seven-tab API and `AbortController` effects widget-owned and private; preserve the Node-safe `model.js`, public widget entry, runtime provider, and stable content factory contracts. Do not export or deep-import the hook. Static source-inventory coverage passes with the full 447/447 `npm test` gate; architecture, performance, and Ukrainian encoding checks also pass. `MD-R04` remains open for complete lint/typecheck.
- Phase 162 moves AI Response Modal creature-field draft editing from `AiResponseModal.tsx` to private `ui/aiResponseCreatureFieldEditing.ts`. Keep the configured widget factory, public entries, raw renderer hook order, injected card/editor slots, and preview rendering intact. Preserve draft/applied/object guards; identity fields `id`, `instanceId`, and `participantType`; encounter participant replacement; draft-resource resolution/update; and update-before-close behavior. Do not publicly export this controller or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 163 moves AI Response Modal JSON-diff markup from `AiResponseModal.tsx` to private `ui/AiResponseJsonDiff.tsx`. Keep the raw renderer's configured factory, state/controller/guard order, injected card/editor slots, preview tree, and `AiResponseModalView` composition intact. Preserve per-resource/field/line keys, localized `Changed fields`, resource-state lookup, `is_${line.type}` class, added/removed/unchanged markers, and falsy number/text fallbacks. Do not publicly export this component or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 164 moves AI Response Modal generic snapshot-diff presentation from `AiResponseModal.tsx` to private `ui/AiResponseGenericDiff.tsx`. Keep the raw renderer's configured factory, state/controller/guard order, injected card/editor slots, preview tree, and `AiResponseModalView` composition intact. Preserve `before === null ? after : before`, the `id`/`slug`/`source`/`createdAt` exclusion, value fallback, ordered field projection, nested notes delegation, per-resource/field keys, localized Before/After labels, formatted values, and CSS classes. Do not publicly export this component or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 165 moves AI Response Modal card-diff presentation from `AiResponseModal.tsx` to private `ui/AiResponseCardDiff.tsx`. Keep `renderEntityCard` and every injected CharacterCard, LocationCard, MonsterStatBlock, and MonsterEditorModal tag in the raw configured renderer. Preserve new/deleted/changed routing, snapshot selection, card-surface classes, localized labels, draft/apply edit eligibility, highlight construction, callback argument order, outer resource wrapper/header/key, and `AiResponseModalView` composition. Do not publicly export this component or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 166 moves AI Response Modal encounter participant-list presentation from `AiResponseModal.tsx` to private `ui/AiResponseEncounterParticipantList.tsx`. Keep encounter-side panels, encounter MonsterStatBlock slots, and all encounter editing/resource composition in the raw configured renderer. Preserve before/after entry derivation and maps, empty-state copy, participant key `${side}-${key}-${index}`, before/after/mutated classes in their existing order, participant-name rendering, and the two metadata evaluations. Do not publicly export this component or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 167 moves Bestiary Browser header-action presentation/action dispatch, menu-local state, and DOM refs to private `ui/BestiaryHeaderActions.tsx`. `BestiaryBrowser` retains custom-monster import parsing/persistence/messages, JSON export, and undo/redo operations and stacks. Preserve the always-mounted menu, hidden `.json` input, functional toggle, classes/titles/icons/copy, close-before-import click/export/undo/redo order, and disabled capability flags. Do not publicly export this component or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 168 centralizes the identical Campaign Page, Session Page, and Bestiary header pointer-dismissal lifecycle in public `shared/ui/usePointerDownOutsideDismissal.ts`. Keep each consumer's state/ref/menu/action ownership local; preserve open-only registration/cleanup, `Node` container containment, functional toggles, and close-before-action order. Do not alter the distinct Encounter or Dice dismissal policies, merge this hook into portal lifecycle behavior, or widen Bestiary public entries. `MD-R04` remains open for complete lint/typecheck.
- Phase 169 moves AI Response Modal note-diff presentation from `AiResponseModal.tsx` to private `ui/AiResponseNoteDiff.tsx`. Keep the raw renderer's configured factory, state/controller/guard order, injected card/editor slots, `AiResponseNoteCard` factory, `renderNoteCard` draft-update callback, twice-resolved resource flow, nested note-array ID/label/list-index synthesis, preview classification, and `AiResponseModalView` composition intact. Preserve single new/deleted and changed Before/After routing, note-surface classes, localized labels, draft/apply edit eligibility, note highlight construction, outer resource key/header, and callback argument order. Do not publicly export this component or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 170 moves Campaign Page header-action presentation, menu-local state/ref, and shared pointer-dismissal composition from `CampaignPage.tsx` to private `ui/components/CampaignHeaderActions.tsx`; `CampaignHeader` retains title/rename/created metadata and passes only narrow workflow callbacks. The public shared `ui/UndoRedoButtons.tsx` owns only the duplicate Campaign/Session undo-redo pair, its optional disabled gate, and existing button copy. Preserve the always-mounted menu, classes/titles/icons, open-only containment lifecycle, functional toggle, direct search/undo/redo behavior, capability/saving-disabled flags, and close-before-export/partial-archive/delete order. Do not publicly export the page component, give it store/runtime access, move page workflow/modal state into it, or add menu ownership to `UndoRedoButtons`. `MD-R04` remains open for complete lint/typecheck.
- Phase 171 moves AI Response Modal encounter monster-change presentation from `AiResponseModal.tsx` to private `ui/AiResponseEncounterMonsterChange.tsx`. Keep the raw renderer's configured factory, state/controller/guard order, `renderEncounterMonsterCard` MonsterStatBlock/field-edit slot, participant entries/maps/change detection, single/paired routing, draft/apply eligibility, highlight construction, localized label evaluation, resource keys, and encounter composition intact. The private renderer receives only precomputed card/label slots and preserves single/paired stack/column/frame/title markup and order. Do not publicly export this component or import it outside its widget. `MD-R04` remains open for complete lint/typecheck.
- Phase 172 moves Monster Field Edit Modal action-section presentation from `MonsterFieldEditModal.tsx` to private `ui/MonsterActionSections.tsx`. Keep the raw feature modal's draft/JSON synchronization, `updateAction` and functional add/remove mutations, rule-shortcut detection/selection state, parsing, save, and error lifecycle intact. Forward original change/keyboard events back to raw callbacks so it continues to read values and build action rule targets. Preserve Fragment-only output, section order, `${section.key}-${index}` keys, classes, localized copy, empty/add/remove/name/text behavior, and no public export. `MD-R04` remains open for complete lint/typecheck.
- Phase 173 moves Monster Field Edit Modal fields-mode layout from `MonsterFieldEditModal.tsx` to private `ui/MonsterFieldSections.tsx`. Keep the raw feature modal's draft/JSON synchronization, field renderers/field reads/select fallback, update and Ctrl+K rule-picker callbacks, action composition, parsing, save, error, and modal lifecycle intact. Precompute basic/ability/text/action slots in raw evaluation order; the private component only retains the Fragment-based four wrapper groups. Preserve field/ability/text order, keys/classes, disabled source, custom select-option fallback, textarea rows/title, and no public export. `MD-R04` remains open for complete lint/typecheck.
- Phase 174 moves Session Page header action-menu markup from `SessionPage.tsx` to private `ui/components/SessionHeaderActions.tsx`. Keep `SessionView` ownership of `isHeaderActionsOpen`, `headerActionsRef`, `usePointerDownOutsideDismissal`, global-search state, the functional toggle, and each close-before-search/undo/redo/delete command; `SessionHeader` retains title/back/rename and encounter quick-access mapping/keys. Preserve the always-mounted ref root, classes, `is_open`, menu/button order, icons/titles/copy, and undo/redo saving-disabled gate. Do not publicly export the component or move runtime/store/workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 175 moves AI Response Modal preview-resource header markup from `AiResponseModal.tsx` to private `ui/AiResponsePreviewResourceHeader.tsx`. Keep raw `renderPreviewResourceHeader` ownership of its default `resource.label` read, fallback `label || resource.label` read, then `getDiffResourceState(resource)` and `renderResourceActions(resource)` evaluation in that exact order; pass only prepared React nodes to the leaf. Preserve the root/first-label-span/nested-actions/state-span hierarchy, classes, order, and CSS selectors. Do not pass a resource, callbacks, localization, or action-policy logic into the private component; do not alter the distinct draft-resource header in `AiResponseModalView` or publicly export this component. `MD-R04` remains open for complete lint/typecheck.
- Phase 176 moves Session Page scene-note presentation from `SessionPage.tsx` to private `ui/components/SceneNotes.tsx`. Keep the module-scoped `SessionNoteCard = createNoteCardComponent(...)` identity in the raw page and pass an injected note-render slot; `SceneCard` retains scene/card composition and every title/text/delete/collapse mutation binding. Preserve one `getSessionSceneNotesPresentation` call, bulk collapse through one `getSceneNotesWithCollapsedState` plan then reorder command, and the root/header/collapse/label/bulk/list hierarchy. Preserve virtual-note rendering, `getAiIgnoredNoteListProps` (including isolated drag controls), list order/last-note calculation, `enableHistory={false}`, classes, labels, and no public page entry. `MD-R04` remains open for complete lint/typecheck.
- Phase 177 moves Encounter Page header-action presentation from `EncounterPage.tsx` to private `ui/components/EncounterHeaderActions.tsx`. Keep `EncounterView` ownership of `headerActionsRef`, `isHeaderActionsOpen`, its distinct dismissal policy, the functional toggle, and normalized settings `patchUiSettings` then `api.updateSettings` commands. Keep `EncounterHeader`/identity/metrics raw; pass the private leaf only a narrow encounter-view projection and controlled display/grid/menu/ref/tooltip/callback inputs. Preserve the always-mounted root, classes/open state, independent encounter monster-count read, button/menu/input order, `.json` input/ref, display/grid gates, undo/redo saving guards, and direct import/export actions. Do not replace the distinct Encounter dismissal policy, add close-before-action behavior, publicly export the component, or move runtime/API/workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 178 moves Campaign Page session-list presentation from `CampaignPage.tsx` to private `ui/components/CampaignSessionsSection.tsx`. Keep `CampaignView` ownership of session-search state, `filterCampaignSessions` memoization, the trim-derived reorder gate, `CampaignViewModel`, runtime navigation, and the configured `ListCard` render/delete slot. Pass the leaf only controlled search/list/branch inputs, explicit create/reorder/drop commands, and the card-render slot. Preserve the sessions-pane DOM/classes/id, localized copy, controlled input, draggable-vs-static branch, filtered list source, `fileName` key extraction, exact reorder/drop delegation, empty state, href/click navigation, and delete `stopPropagation` order. Do not calculate filtering or introduce hooks/runtime/API/store/navigation/delete workflows in the private component or publicly export it. `MD-R04` remains open for complete lint/typecheck.
- Phase 179 moves Session Page checklist-overlay presentation from `SessionPage.tsx` to private `ui/components/SessionChecklistOverlay.tsx`. Keep raw `SessionView` ownership of the `isChecklistOpen` gate, checklist state, and `updateData`; mount the leaf only when open so `progress` and `checklistItems` retain their open-gated reads. Pass explicit close and item-change commands. Preserve the title, shared confirm/cancel close callback, no-footer modal, hierarchy/classes, literal progress-percent width, item order/key, Boolean dynamic `item.id_check` lookup, and direct ID-to-raw `updateData(..., true)` policy. Keep the floating-action trigger raw. Do not add a public export, runtime, API, store, navigation, or workflow ownership to the leaf. `MD-R04` remains open for complete lint/typecheck.
- Phase 180 moves Session Page header presentation from `SessionPage.tsx` to private `ui/components/SessionHeader.tsx`. Keep raw `SessionView` ownership of the null-session guard, quick-access navigation and modifier policy, header action state/ref/pointer dismissal, global-search state, and all close-before-search/undo/redo/delete commands. Pass the leaf only explicit display values/callbacks plus the action ref. Preserve header hierarchy/classes, title/back/rename behavior, encounter-length gate, map order/key, scene-number localized fallback through `renderMentionText`, and the sibling `SessionHeaderActions` root/ref/menu behavior. Do not publicly export the component or move runtime, API, store, navigation, or mutation workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 181 moves Session Page scope-import overlay presentation from `SessionPage.tsx` to private `ui/components/SessionScopeImportOverlay.tsx`. Keep raw `SessionView` ownership of the post-session-guard `scopeImportModal` snapshot, `getSessionScopeImportPresentation` derivation, and scope-move/close lifecycle. Keep the leaf unconditionally composed in its existing JSX slot and retain its own `!modal || !copy` guard. Pass only modal/copy/type plus explicit close and move commands. Preserve shared confirm/cancel close callback, no-footer modal, loading-before-empty order, lazy display-name fallback, `slug || id || displayName` key policy, item order, mention rendering, click-only non-awaited move command, and no local close/removal behavior. Do not add a public export, runtime, API, store, or workflow ownership to the leaf. `MD-R04` remains open for complete lint/typecheck.
- Phase 182 moves Campaign Page description-section chrome from `CampaignPage.tsx` to private `ui/components/CampaignDescriptionSection.tsx`. Keep raw `CampaignView` ownership of `useCampaignView`, section state, the `hasDescriptionData` guard, `next = !isDescriptionCollapsed`, exact setter then `triggerSave({ isDescriptionCollapsed: next })` order, and the description editor callback. Pass only `hasData`, `isCollapsed`, an explicit toggle command, and an expanded-only editor render slot. Preserve the always-mounted section/id/classes, title-group and toggle-button callback behavior, medium control size, localized title, and `!isCollapsed` invocation of the editor slot so `view.description`, placeholder localization, and `handleDescriptionChange` remain closed-gated. Do not publicly export the component or move runtime, API, store, persistence, or editor ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 183 moves Campaign Page entity-section presentation from `CampaignPage.tsx` to private `ui/components/CampaignEntitySection.tsx`. Keep raw `CampaignView` ownership of all three per-type collapse guards, `next` values, exact setter-then-save commands, bulk `items.map` then local reorder then `persistEntitiesReorder` order, drag-drop persistence, global cross-type drop listener, and card/AI-control/drag render slots. Pass the leaf only controlled list values, display configuration, render/drag slots, and explicit toggle/bulk/reorder/drop commands. Preserve always-mounted section placement, outer `data-character-drop-type`, shared title/button toggle callback, medium control, collapsed action/list gates, `BulkCollapseButton` current-items delegation, `DraggableList` prop order and separate reorder/drop callbacks, and pure same-slice key policy. Do not publicly export the component or move runtime, API, store, navigation, persistence, or entity workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 184 moves Session Page scenes-section presentation from `SessionPage.tsx` to private `ui/components/SessionScenesSection.tsx`. Keep raw `SessionView` and its raw `SessionSceneItem` ownership of scene projection, `handleBulkScenesCollapse` map then forced `updateData`, direct add/reorder commands, duplicate-ID `findIndex` numbering, encounter-name localization, wrapper ID, and every `SceneCard` mutation/navigation binding. Pass the leaf only `scenes`, explicit bulk/add/reorder commands, and a scene render slot. Preserve the always-visible actions for empty lists, localized section/button copy, exact button props, nonempty-list gate, `DraggableList` prop order/key extraction, and direct render-slot delegation. Do not publicly export the component or move runtime, API, store, navigation, persistence, `SceneCard`, or session workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 185 moves Session Page floating checklist/search actions from `SessionPage.tsx` to private `ui/components/SessionFloatingActions.tsx`. Keep raw `SessionView` ownership of global-search state and its close setter, checklist-open state, and the explicit `view.progress` read. Pass the leaf only progress, controlled search state, and explicit open/close commands. Preserve unconditional trigger composition, tooltip/button/icon/classes, strict `progress < 100` badge gate, default button behavior, and open-only `GlobalSearchModal` mount with the original cancel command. Do not publicly export the component or move runtime, API, store, navigation, persistence, checklist/search workflow, or state ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 186 moves Session Page notes-section presentation from `SessionPage.tsx` to private `ui/components/SessionNotesSection.tsx`. Keep raw `SessionView` ownership of note data state, bulk-collapse and sanitizing reorder persistence, the AI-ignore mutation command, `SessionNoteCard` factory/render slot, DOM IDs, last-note calculation, and every note-card callback. Pass the leaf controlled raw/renderable notes, `hasData`/collapsed state, explicit toggle/bulk/reorder/AI commands, and a card render slot. Preserve raw `notes` as the bulk source; let the leaf gate its controlled toggle and expanded bulk/list presentation, reuse the Notes feature's virtual-note/key/AI-list policy, and explicitly retain `isolateDragEvents={false}` for the Session list. Do not publicly export the component or move runtime, API, store, navigation, persistence, note-card, or AI workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 187 moves Session Page result-section presentation from `SessionPage.tsx` to private `ui/components/SessionResultSection.tsx`. Keep raw `SessionView` ownership of exact `String(sessionData.result_text || "")` coercion and `view.updateData("result_text", value)` persistence. Pass the leaf only the controlled string and explicit value command. Preserve localized section/placeholder copy, textarea type/class, disabled history, value propagation, and the event-to-value adapter. Do not publicly export the component or move runtime, API, store, navigation, persistence, session data, or editor workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 188 moves the paired Session NPC/location list shells from `SessionPage.tsx` to private `ui/components/SessionEntitySection.tsx`. Keep raw `SessionView` ownership of type-specific AI/bulk persistence, localized titles/actions/empty text, create/import/reorder/export commands, DOM IDs, `CharacterCard`/`LocationCard` render slots, and every card callback. Pass the leaf controlled display values, items/list class, explicit bulk/reorder/AI commands, and a card render slot. Preserve always-visible actions, direct current-items bulk delegation, the nonempty-list/empty-state branch, ID keys, AI-control `Boolean` reads, all-draggable/default non-isolated behavior, and exact NPC/location ID segments. Do not publicly export the component or move runtime, API, store, navigation, persistence, cards, or entity workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 189 moves Session Page `SceneCard` presentation from `SessionPage.tsx` to private `ui/components/SessionSceneCard.tsx`. Keep raw `SessionSceneItem` ownership of its wrapper DOM ID, `SessionViewModel.sceneSchema`, eager `getEncounterName(scene)` evaluation, every view callback, and the module-scoped `SessionNoteCard` factory/injected note-render slot. Pass the leaf only controlled scene/card values and callbacks plus that render slot. Preserve private SceneCard/header/expanded-content/fields/notes/media composition, `enableHistory={false}`, and the localized `New encounter` fallback. Do not publicly export the component or move runtime, API, store, navigation, persistence, note-card, or session workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 190 moves Encounter Page player-picker and character-modal presentation from `EncounterPage.tsx` to private `ui/components/EncounterCharacterOverlays.tsx`. Keep raw `EncounterView` ownership of player/modal state, the available-player list, close/reset/start/create workflows, `handleCharacterChange`, `getParticipantInstanceId`, and the injected `getModalCharacterOnChange` factory. Pass the leaf only controlled values and explicit callbacks. Preserve picker/modal null gates, create/list/empty presentation, button actions, list key/meta policy, `CharacterCard` flags, and modal identity evaluation after its null gate. Do not publicly export the component or move runtime, API, store, navigation, persistence, player, character, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 191 moves Encounter Page detail grid/single/stat-block presentation from `EncounterPage.tsx` to private `ui/components/EncounterDetail.tsx`. Keep raw `EncounterView` ownership of `getParticipantInstanceId`, display/selection/focus state, grid refs, AI/edit/character callbacks, and image-override policy; inject them into the leaf. Preserve the grid's repeated participant-ID evaluation for the key and grid item, selection/ref/classes, grid-empty and single-empty presentation, character-card flags, the null gate before selected-character ID/callback evaluation, and stat-block callback/override delegation. Do not publicly export the component or move runtime, API, store, navigation, persistence, selection, image, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 192 moves Encounter Page participant-row/combat-stats/HP-input/action presentation from `EncounterPage.tsx` to private `ui/components/EncounterMonsterRow.tsx`. Keep raw `EncounterView` ownership of `hpDrafts`, participant-ID policy, selected-instance derivation, the `DraggableList` key/reorder/drop/render adapter, and HP handlers; pass the leaf only narrow row-view commands. Preserve instance-ID → character classification → display-name evaluation order, `undefined` draft fallback semantics, HP number coercion/color, native input-event ordering, click stop-propagation-before-action order, duplicate-by-monster identity, and the character delete-only action path. Do not publicly export the component or move runtime, API, store, navigation, persistence, selection, HP, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 193 moves Encounter Page header/identity/metrics presentation from `EncounterPage.tsx` to private `ui/components/EncounterHeader.tsx`, which forwards the existing private `EncounterHeaderActions`. Keep raw `EncounterView` ownership of header open/ref/dismissal lifecycle, grid/display settings persistence, and the inline actions toggle. Pass the leaf its narrow unified view and display/action inputs. Preserve header/identity/actions order, direct view-method handlers, mention-rendered empty-name fallback, eager metric-tuple construction, participant-count gate, `String(label)` keys, and all metric/header classes. Do not publicly export the component or move runtime, API, store, navigation, persistence, header-menu, settings, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 194 moves Session Page session-scoped NPC/location modal card presentation from `SessionPage.tsx` to private `ui/components/SessionScopedEntityModal.tsx`. Keep raw `SessionView` ownership of the session-only resolver, current-entity lookup, ID guard, and every change/delete/close command. Pass the leaf only the source entity, modal type, campaign slug, and controlled card callbacks. Preserve the `locations`-then-NPC branch, session-entity normalization, card keys, forced expanded state, modal flags, and delete-before-close ordering. Do not publicly export the component or move runtime, API, store, navigation, resolver, or entity workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 195 moves Campaign Page header presentation from `CampaignPage.tsx` to private `ui/components/CampaignHeader.tsx`, forwarding the existing private `CampaignHeaderActions`. Keep raw `CampaignView` ownership of campaign construction, state, modal triggers, and action commands. Pass the leaf a narrow header-view projection, `CampaignViewModel`, and explicit modal-open commands. Preserve the header/title/created metadata hierarchy, rename tooltip/handler, undo/redo capability reads, direct action callbacks, action-menu forwarding, classes, and localized copy. Do not publicly export the component or move runtime, API, store, navigation, persistence, header-menu state, or campaign workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 196 moves Campaign Page partial-archive overlay lifecycle/presentation from `CampaignPage.tsx` to private `ui/components/CampaignPartialArchiveOverlay.tsx`. Keep raw `CampaignView` ownership of global-search/partial-archive open state, the search close callback, and direct `handleExportPartial`/`handleImportPartial` commands. Pass the leaf only open/close/export/import inputs. Preserve always-mounted hook state, closed-overlay null gate, busy-before-await/finally reset, import success before close, `PartialArchiveModal` prop routing, and global-search placement. Do not publicly export the component or move runtime, API, store, navigation, archive command, or campaign workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 197 moves Encounter Page Bestiary overlay shell from `EncounterPage.tsx` to private `ui/components/EncounterBestiaryOverlay.tsx`. Keep raw `EncounterView` ownership of Bestiary composition, all stable widget slots, the `handleAddMonster` command, and the monster cast. Pass the leaf only open/close/action inputs plus a Bestiary render slot. Preserve the closed-overlay null gate before slot evaluation, custom Modal title/props, `onConfirm` no-op, slot's `onAdd` forwarding, imports at the raw composition root, and sibling overlay order. Do not publicly export the component or move runtime, API, store, navigation, Bestiary, AI, monster-add, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 198 moves Encounter Page notification presentation from `EncounterPage.tsx` to private `ui/components/EncounterNotification.tsx`. Keep raw `EncounterView` ownership of notification state and its close/reset command. Pass the leaf only controlled message/close inputs. Preserve the truthy message null gate, shared Notification props, and sibling mount order. Do not publicly export the component or move runtime, API, store, navigation, notification state, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 199 moves Encounter Page request-cleanup lifecycle from `EncounterPage.tsx` to private `model/useEncounterRequestCleanup.ts`. Keep raw `EncounterView` ownership of the focus-timeout and AI-edit-controller refs and pass them to the hook. Preserve mount cleanup ordering: clear a present timeout, then abort a present controller. Do not publicly export the hook or move runtime, API, store, navigation, AI-model loading, header dismissal, notification state, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 200 moves Encounter Page AI-model loading lifecycle from `EncounterPage.tsx` to private `model/useEncounterAiModelLoading.ts`. `useEncounterMonsterAiEditor` owns editor/model state and supplies narrow setters; raw `EncounterView` retains its localized loading-error adapter. Preserve the open-edit/empty-model gate, feature-owned `loadAiModelOptions` call, and original effect dependencies. Do not publicly export either hook or move runtime, API, store, navigation, AI request cancellation, header dismissal, notification state, or encounter workflow ownership into them. `MD-R04` remains open for complete lint/typecheck.
- Phase 201 moves Campaign Page hash-navigation lifecycle from `CampaignPage.tsx` to private `model/useCampaignHashNavigation.ts`. Keep raw `CampaignView` ownership of campaign/section state, section setters, notes-view setter, and all persistence commands. Preserve hash decoding, navigation-plan construction/execution order, `120ms` delayed scroll, cleanup, and original dependencies. Do not publicly export the hook or move runtime, API, store, drag-drop, editor, archive, or campaign workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 202 moves Session Page hash-navigation lifecycle from `SessionPage.tsx` to private `model/useSessionHashNavigation.ts`. Keep raw `SessionView` ownership of session state, rendered collections, and `handleToggleSectionCollapse`. Preserve the Session-specific notes-expand predicate, `140ms` delayed scroll, cleanup, and original dependencies. Do not publicly export the hook or move runtime, API, store, entity-link resolution, pointer dismissal, editor, scope-import, or session workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 203 moves Campaign Page character-type drag-drop browser lifecycle from `CampaignPage.tsx` to private `model/useCampaignCharacterTypeDrop.ts`. Keep raw `CampaignView` ownership of view identity and `handleCharacterTypeDrop`. Preserve `elementFromPoint`, closest drop-zone lookup, plan construction, listener registration/removal, and effect dependency on the raw view identity. Do not publicly export the hook or move runtime, API, store, editor, archive, or campaign workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 204 moves Encounter Page grid-focus state, refs, and timeout policy from `EncounterPage.tsx` to private `model/useEncounterGridFocus.ts`. Keep raw `EncounterView` ownership of grid projection, participant-selection plan, and selection command. Preserve representative lookup, ref map add/delete, auto-center scroll, present-timeout clearing, conditional focus reset, `1800ms` delay, and cleanup integration through `useEncounterRequestCleanup`. Do not publicly export the hook or move runtime, API, store, header dismissal, AI workflow, or encounter selection ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 205 moves Encounter Page player-character creation state and lifecycle from `EncounterPage.tsx` to private `model/useEncounterPlayerCreation.ts`. Keep raw `EncounterView` ownership of runtime/view adapters, localization, route guard, and overlay composition. Preserve initial/reset draft creation, submitting close guard, blank-name message, payload defaults, create → refresh → add → reset order, error logging/message, and `finally` submitting reset. Do not publicly export the hook or move runtime, store, selection, AI, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 206 moves Encounter Page HP draft/edit lifecycle into private `model/useEncounterHpEditing.ts`. Keep raw `EncounterView` ownership of participant identity and `updateMonsterHp` adapter. Preserve per-instance draft replacement, absent-draft no-op, HP normalization, and delete-after-commit policy. Do not publicly export the hook or move runtime, store, selection, AI, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 207 moves Encounter Page character-modal state and synchronization into private `model/useEncounterCharacterModal.ts`. Keep raw `EncounterView` ownership of participant selection and update adapter. Preserve open/close, exact instance match, character projection, and participant-type override. Do not publicly export the hook or move runtime, store, selection, AI, or encounter workflow ownership into it. `MD-R04` remains open for complete lint/typecheck.
- Phase 208 moves Encounter display-settings patch/persistence into private `model/useEncounterDisplaySettings.ts`. Keep raw `EncounterView` ownership of runtime patch adapter and header composition. Preserve mode/column normalization, patch-before-request order, error logging, and no public page/runtime/store expansion. `MD-R04` remains open for complete lint/typecheck.
- Phase 209 moves Encounter local/global monster field-edit state and save orchestration into private `model/useEncounterMonsterFieldEditing.ts`. Keep raw `EncounterView` ownership of API/runtime/view adapters, localization, and modal/detail composition. Preserve the character/ID guard, `none` edit-plan exit, action close before editor state, local-vs-persistent update options, refresh/close/error ordering, and no public page/runtime/store expansion.
- Phase 210 moves the distinct Encounter header pointer-dismissal lifecycle into private `model/useEncounterHeaderDismissal.ts`. Keep raw `EncounterView` ownership of header ref/open state and the close adapter. Preserve open-only registration, the `Node` containment check, outside-only close, and `pointerdown` cleanup; do not substitute the shared dismissal hook or export this page model.
- Phase 211 moves Encounter AI action-modal transition state into private `model/useEncounterMonsterAiAction.ts`. `useEncounterMonsterAiEditor` owns the editor session state and `useEncounterMonsterAiGeneration` owns the request lifecycle; raw `EncounterView` retains history, localization, runtime adapters, and composition. Preserve the name guard, target-ID update before opening, in-flight close guard, image-prompt no-op, close-before-editor transition, and no public page/runtime/store expansion.
- Phase 212 moves Encounter render-context projection into `model/encounterPagePresentation.ts`. Keep raw `EncounterView` ownership of loading presentation and all workflows. Preserve encounter → campaign → session guard order, the second encounter read in the result, and the raw loading guard/deconstruction order.
- Phase 213 moves Encounter AI draft save/restore/close state and lifecycle into private `model/useEncounterMonsterAiDraft.ts`. Keep raw `EncounterView` ownership of modal composition, localized history-error adapter, diff presentation, and injected runtime adapters. Preserve plan guards, API request/result/error/finally ordering, target identity, local/global update routing, restore close guard, and no public page/runtime/store expansion.
- Phase 214 moves Encounter AI editor session state into private `model/useEncounterMonsterAiEditor.ts`. Keep raw `EncounterView` ownership of the AbortController ref, localized model-loading error adapter, runtime adapters, and modal composition. Preserve start/reset, in-flight close guard, success reset, model selection, and no public page/runtime/store expansion.
- Phase 215 moves Encounter AI generation/cancellation/result lifecycle into private `model/useEncounterMonsterAiGeneration.ts`. Keep raw `EncounterView` ownership of its AbortController ref, runtime dependencies/effects, localized adapters, and modal composition. Preserve the name and generation-plan validation exits; start/error reset before controller assignment; exact request payload; draft/local-update result routing; conditional active-controller clearing; error/finally ordering; and no public page/runtime/store expansion.
- Phase 216 moves the four Encounter header tooltip ReactNodes into private `ui/components/EncounterHeader.tsx`. Keep raw `EncounterView` ownership of menu ref/open/dismissal state, settings persistence, and the functional actions toggle. Preserve unconditional tooltip construction, localized copy/order, encounter participant-count gate, initiative values, header/action forwarding, and no public page/runtime/store expansion.
- Phase 217 moves Encounter participant selection and token-image update adapters into private `model/useEncounterMonsterInteractions.ts`. Keep raw `EncounterView` ownership of selected state, character modal, grid-focus action, and encounter update command through injected dependencies. Preserve selection-plan/execution ordering, selected ID/display-mode forwarding, token missing-instance no-op, exact image forwarding, and no public page/runtime/store expansion.
- Phase 218 moves the paired Encounter AI/field action-modal composition into private `ui/components/EncounterMonsterActionModals.tsx`. Keep raw `EncounterView` ownership of action state, transition commands, and editor/persistence workflows. Preserve action-monster identity, custom-source global-edit gate, local-edit availability, localized labels, field-edit title/icon, callback forwarding, and no public page/runtime/store expansion.
- Phase 219 moves Encounter participant-list chrome and `DraggableList` presentation into private `ui/components/EncounterParticipantList.tsx`. Keep raw `EncounterView` ownership of add-state commands, reorder/drop, row identity/HP/selection bindings, and the injected row render slot. Preserve localized buttons, list classes, key extraction, callback forwarding/order, and no public page/runtime/store expansion.
- Phase 220 moves Encounter selected-grid identity and layout projection into `model/encounterPagePresentation.ts`. Keep raw `EncounterView` ownership of state and composition. Preserve falsy selected handling, lazy `instanceId -> id -> ""` identity coercion, representative fallback, single-monster display override, and bounded grid columns.
- `shared/api/httpClient.ts` знає лише HTTP-механіку. Domain endpoints не додаються до transport layer.
- Campaign entity lookup that needs HTTP belongs to `entities/campaign/api/resolveEntityByName.ts`; `entities/campaign/model/entityLookup.ts` stays transport-free. Conditions, diseases, variant rules, skills, and senses belong to `entities/reference/api/referenceApi.ts`, not `entities/spell/api/spellApi.ts`. Cross-entity reference preview/resolution belongs to `features/rules-reference`, while the five reference cache/normalizer modules remain entity-owned and public. External consumers use the owning slice public entry point; all 10 private/deep imports found by the Phase 121 audit were redirected in Phase 122, including campaign state utilities through the minimal `features/campaign/index.js` plus `index.d.ts`. Public-import restrictions apply consistently to TypeScript/TSX and JavaScript/JSX; do not leave a language-specific deep-import gap. These directions are enforced in `eslint.config.js`; do not weaken the restrictions to bypass an import error. Scoped overrides must compose the baseline arrays from `scripts/eslint-import-boundaries.mjs`, because a later flat-config rule replaces the earlier value of `no-restricted-imports`.
- Read-only domain clients accept optional `RequestInit` and must forward its `signal` unchanged. Bestiary, spells, and rules-reference loaders own an `AbortController` per active request, abort superseded/unmounted reads, suppress only `AbortError`, and guard loading/finalization state by active-request identity. Rules-reference modal loads must verify that the controller is still the active controller for the tab before applying success, any non-`AbortError` failure side effect (including requested-set deletion or alert), controller cleanup, or loading-state finalization; a stale request must not mutate the lifecycle of its replacement. Do not replace native cancellation with stale-result flags alone.
- Campaign global search hydrates sessions through `mapWithConcurrency` with concurrency `6`, preserves source order, forwards one cancellation signal to every read, and caps ordinary displayed results at `80`. Keep the early-stop result cap and do not reintroduce unbounded `Promise.all` session hydration.
- Performance-sensitive collection policies must remain linear where possible: Bestiary source/favorite membership uses precomputed normalized `Set`s, and campaign result filtering stops once its finite non-negative limit is reached. Changes to Bestiary filtering, spell filtering, campaign search, or encounter-grid projection must keep `npm run check:performance` green.
- Не перенось state у глобальний store автоматично. Persistent domain state належить filesystem/server; route state - router; workflow/modal/filter/draft state - відповідному feature/widget. Store використовуй лише для справді глобального app state.
- Не перетворюй кожен компонент або click handler на feature. Feature повинна представляти завершену дію/use case.
- Під час роботи з legacy-файлом мігруй лише той вертикальний workflow, якого стосується зміна; не роби масове переміщення без окремого запиту.
- Після структурних змін запускай `npm run check:architecture`; результат має не містити нових cycles або boundary violations.

Bestiary є пілотним FSD slice:

- Entry point з Sidebar відкриває Bestiary у rules-reference modal flow; окремий route не створюється без зміни navigation UX.
- Reusable browser/widget: `src/widgets/bestiary-browser`.
- Monster edit use case: `src/features/edit-monster`.
- AI monster edit/draft policies and action chooser: `src/features/ai-edit-monster`; encounter-specific edit/draft shell: private `src/pages/encounter/ui/components`.
- Bestiary API owner: `src/entities/bestiary`.
- Bestiary browser UI is strict TSX behind its JavaScript barrel and typed facade; pure policies are available to Node tests only through `widgets/bestiary-browser/model.js`.
- Bestiary favorite composite identities must use a collision-free tuple encoding of normalized name and source, such as a JSON-encoded array. Never concatenate the fields with a delimiter, including a NUL delimiter, because delimiter-bearing values can collide.
- Keep the raw sync event delivered by `BestiaryBrowserRuntime` stable and memoize `parseBestiarySyncEvent(rawSyncEvent)` from that identity before using it as an effect dependency. Parsing directly during render creates a fresh object and can loop favorites refreshes or `reloadToken` updates.
- Bestiary reference matching belongs to the private policies behind `monsterMatchesReference` in `widgets/bestiary-browser/model/bestiaryBrowser.ts`. Preserve nullable-reference rejection, canonical name equality without identity-style non-empty-name eligibility, source lookup only after a name match, an empty normalized reference source as a wildcard that does not read the monster source, and canonical string/number-equivalent source comparison otherwise. Do not export the private matcher helpers.
- Bestiary AI monster-edit request execution belongs to `executeAiMonsterEditRequest` behind `widgets/bestiary-browser/model.js`. The UI owns start-plan input, busy/error start setters, `AbortController` creation and active-controller identity, while the executor owns exact payload/signal forwarding, response normalization, apply-result then reset ordering, transport/apply/reset error classification, `DOMException` `AbortError` suppression, and one guaranteed settled callback. Keep active-controller-only clearing and final busy release in the injected settled effect; do not import React or instantiate browser controllers in the Node-safe model.
- Bestiary AI generation result interpretation belongs to `getAiMonsterGenerationResultPlan` behind `widgets/bestiary-browser/model.js`. Preserve `draft === true` plus a truthy response as the first branch, source-monster image enrichment before draft selection, fallthrough to updates when the response is absent even if the draft flag is set, truthy `updated` eligibility, exact updated/generated identity, selected-name projection only for exact `edit` mode, and `trackUndo: false`. React executes only the resulting draft/update effect; do not duplicate these branches in UI.
- Realtime Bestiary sync effects execute through `executeBestiarySyncEventPlan` behind `widgets/bestiary-browser/model.js`. A null plan must not read injected dependencies. For a valid plan, start the favorites request first without awaiting it, then apply pending selection, auto-selection suppression, and reload in that order. Preserve pending-reference and successful favorites-array identity, nullish favorites fallback to a fresh empty array, original refresh-error identity, and independent synchronous effects even when the refresh promise later rejects. Keep transport, logging, refs, and React setters injected from UI; the effect depends only on `syncEvent`.
- Bestiary field-editor saves execute through `executeBestiaryFieldEditSave` behind `widgets/bestiary-browser/model.js`. Missing or falsy editing names must return a silent skipped outcome before reading any other dependency. Preserve exact draft/editing identity, create-based versus update transport selection, empty previous name only for create-based application, raw editing name otherwise, apply → close success order, updated-monster identity, and one failure path for exceptions from transport, apply, or close. The executor forwards the original error; UI owns localized alert copy and uses its injected runtime message command with the injected API/state effects.
- Bestiary selected-source persistence executes through `executeBestiarySelectedSourcesSave` behind `widgets/bestiary-browser/model.js`. Derive the ignore list from exact filter/selection references, then enable auto-selection before entering persistence error handling. A truthy campaign slug must perform campaign update → campaign reload → nullish-to-empty campaign replacement and never write global settings. Global scope accepts only an array-valued saved ignore list and otherwise reuses the requested-list identity. Persistence and success-effect failures execute log → message with original error identity and do not roll back auto-selection; derivation/ref-effect failures remain outside that path. Keep transport, refs, logging, and localization in UI; inject app-global campaign/settings/message effects through `BestiaryBrowserRuntime`, and preserve the callback's dependency list.

Не імпортуй повний Bestiary widget з rules/reference, entity або feature layers. Reference browser повинен використовувати власний lightweight list/detail view, щоб не створювати цикли.

## Backend Architecture Direction

Backend залишається modular monolith. FSD не застосовується до Express буквально. Новий або суттєво перероблений backend functionality організовуй вертикальними domain modules під `server/modules/<domain>`:

- `domain` - invariants, validation, pure transformations.
- `application` - use cases та orchestration.
- `infrastructure` - filesystem/Gemini/archive adapters.
- `http` - router, request mapping, response mapping.

Routes не повинні напряму будувати filesystem paths. Поступово вводь focused repositories (`CampaignRepository`, `SessionRepository`, `BestiaryRepository`, `AiHistoryRepository`, `ImageRepository`) поверх чинного `server/storage.js`. Filesystem залишається source of truth; не додавай DB або microservices без окремої вимоги.

HTTP mutation payloads validate at the route boundary through `server/http/requestValidation.js` and domain-owned schemas under `server/modules/*/http`. Campaign/session commands consume `req.validatedBody`. Backup JSON/gzip uploads must be parsed once, validated before any command, and passed to `backupCommands` as the validated `payload`; an empty import collection, empty archive envelope, malformed campaign/session record, non-array `images` container, or partial archive without selected sections must fail with status `400`, code `INVALID_REQUEST`, and structured `details` before `wipe_and_replace` can clear data. Individual archive image rows remain intentionally tolerant and are interpreted or skipped only by the archive image restoration/storage policy. Keep validation mechanics out of application commands and business schemas out of generic HTTP helpers.

## Business Rules

- This is local-first: filesystem data is the source of truth.
- Campaign/session/entity IDs must remain stable. Do not change entity IDs unless explicitly required.
- `updatedAt` should not be reintroduced.
- Campaign and session scopes matter:
  - Session-scoped NPC/location data should stay session-scoped unless moving scope is requested.
  - AI in session scope should not copy campaign-scoped NPC/location content into session as duplicates.
- AI context can ignore notes/entities via `_aiIgnored`; ignored content must not be sent to AI.
- Parsed AI should return precise operations, not full unchanged datasets.
- Campaign-scoped character/NPC/location AI mutations in `server/aiPatchService.js` must stay behind the prototype-safe campaign-entity command dispatcher. Preserve permission checks before storage reads, one list/target context per operation, campaign-before-session duplicate precedence, session removal only after a successful campaign write, stable existing IDs/slugs/images, client-ID mapping, exact replacement/move warnings, and mention-reference updates only after persistence for non-empty changed names. Missing targets and unsupported operation names remain silent.
- Session-scoped NPC/location AI mutations in `server/aiPatchService.js` must stay behind the prototype-safe session-entity command dispatcher. Preserve missing-session and permission checks before list access, session-before-campaign duplicate precedence, fail-open campaign duplicate lookup, stable existing IDs/slugs/images, client-ID mapping only after successful mutation, and exact replacement/move warnings. Campaign-to-session duplicate moves must remove the appended in-memory session entity when campaign deletion fails; missing targets and unsupported operation names remain silent.
- AI `moveScope` operations must use the focused direction dispatcher and the shared rollback stack in `server/aiPatchService.js`. Preserve scope validation, target/client-ID resolution, duplicate-owned IDs/slugs/images, exact duplicate warnings, and final session persistence. A failed campaign delete must immediately undo the session mutation; any later operation or session-persistence failure must restore session memory and reverse created, overwritten, or deleted campaign entities in reverse move order while preserving the original error.
- Shared AI entity identity resolution in `server/aiPatchService.js` is ID-first, then slug, then normalized name; list order must not let a weaker name match beat a later exact ID or slug. Numeric ID `0` is valid. Entity scope resolution is explicit valid scope, then mapped `targetClientId`/`ownerClientId` scope, then valid default, then campaign. Patch and duplicate-version projection must preserve existing/destination IDs, slugs, and existing-first image behavior, reject arrays as entity records, and avoid mutating stored source objects.
- AI character names, levels, and notes in `server/aiPatchService.js` must use the focused normalization prelude. Preserve truthy canonical-to-alias first/last-name fallback before full `name|fullName|title` tokenization, sanitized bracket/whitespace handling, empty level strings, parseInt-compatible values and 1–20 clamping. String notes trim their text; object notes use `title|name` and nullish `text → description → content`, retain truthy IDs and boolean collapse state, and blank titles in simplified mode. Arrays and scalar values are invalid notes. List normalization filters content-empty notes and creates one empty placeholder only when requested; direct empty-string append remains valid.
- AI-ignored note restoration in `server/aiPatchService.js` must use the focused merge planner. Return the exact incoming visible collection when stored notes contain no `_aiIgnored` entries. Otherwise preserve ignored note object identity and source order, remove visible collisions only for non-empty string-coercible IDs, and place ignored notes by previous visible ID before next visible ID before a bounded original-index fallback. Numeric IDs, duplicate ignored IDs, adjacent ignored notes, malformed collections, and source-array immutability are part of this contract.
- Campaign-entity persistence from `server/aiPatchService.js` must use the focused slug/write policy. Existing truthy slugs precede payload truthy slugs; allocate a unique slug lazily only when both are absent/falsy. Preserve location `name → locations` and character/NPC `firstName → name → type` base-name fallbacks, pass the normalized base through `storage.campaignSlug`, write the resolved slug over the copied payload, return the storage result unchanged, and never mutate the caller payload. Numeric slug values retain JavaScript truthiness behavior, including allocation for `0`.
- Scene encounter-client handling in `server/aiPatchService.js` must use the focused payload/discovery/resolution pipeline. Object payload selection, including arrays, is `data → value → patch` for operation data and `patch → data` for patches. Discover links only from create/update plus scene/scenes aliases, preserve insertion-ordered Set deduplication, and retain raw truthy data-ID precedence over patch before text coercion. Pending links require a mapped `encounter` with a truthy ID, warn exactly for unresolved mappings, stay silent for a missing target scene, compare current IDs strictly, and mutate only the matched scene. Numeric mapped ID `0` remains unresolved.
- Ordinary AI entity routing in `server/aiPatchService.js` must use the focused route request. Narrow the established character/PC, NPC, and location/faction aliases first; unknown entities remain silent. Route case-insensitive `moveScope` before ordinary scope selection. Character operations always delegate to campaign ownership even under explicit session scope. NPC/location delegation uses explicit valid scope, then mapped client scope, then the request default, with campaign fallback. Return the selected delegate result/promise unchanged and preserve delegated errors.
- AI system-instruction composition in `server/modules/ai/application/buildSystemInstruction.js` must remain an ordered private append-policy pipeline. Preserve base-mode fallback, language, structured JSON, simplified-note/mention, detail, scene/encounter, custom-monster, generation-toggle, entity-scope, user-base-prompt, and image-style section order and exact eligibility. Base prompts remain trimmed, missing paired prompts render `(none)`, image mode omits the mandatory language rule, image style is image-only, and final sections join with exactly two newlines. Do not expose private append policies through the module API.
- AI request preparation in `server/modules/ai/application/prepareGenerateAiRequest.js` must remain a focused policy composition. Preserve normalized language before API-key validation and both errors before path/settings reads; exact Bestiary image classification and synthesized path fallback; object-like path identity including arrays; encounter-target parse suppression unless encounter generation is truthy; image parse suppression; custom-monster gating; parse-dependent false-only character/NPC/location toggles; mixed scope only for parsed session targets without an encounter; nullish settings recovery; false-only auto-apply disablement; own-property image override precedence including empty values; campaign image prompt before global fallback; trimmed UTF-8 prompts; and exact result/error shapes. Keep only the established path/classifier/coordinator exports public.
- AI operation validation in `server/modules/ai/domain/aiPayloadSchemas.js` must remain an ordered private validator pipeline. Preserve the non-object early exit, complete error accumulation, and exact error path/message/order; case-sensitive operation/entity aliases; implicit campaign updates; `id|slug|name|targetClientId` identity semantics, including falsy numeric zero and truthy scalar compatibility; `data|value`, `patch|data`, and note payload acceptance; moveScope's dedicated identity/direction errors; and undefined required scope versus supplied invalid scope behavior. Runtime validation remains authoritative and the public CommonJS API stays limited to `validateAiGeneratedContent` and `assertAiGeneratedContentContract`.
- AI response-history normalization in `server/storage.js` must use focused response, request, change-set, resource-state, and kind-specific identity policies. Preserve untrimmed nonblank response text, request-level string instructions before the legacy top-level fallback, object-like nested arrays for options/context/summary/retry metadata, exact failed/completed and applied/undone/draft defaults, falsy metadata normalization, generated IDs/timestamps, own-property `before`/`after` presence, null/null resource rejection, custom-monster `name → after.name → before.name` precedence, and raw snapshot identity for direct calls. Malformed root rows must be filtered independently so one null or primitive row cannot hide valid persisted history; do not expose these private normalizers through the storage API.
- AI history snapshot writes in `server/aiResponseHistoryService.js` must dispatch through private kind commands. `custom-bestiary` and `custom-monster` remain campaign-free; every other kind validates a truthy campaign before kind lookup, preserving no-campaign-before-unknown error precedence. Preserve whole-list Bestiary replacement, custom-monster filtering by trimmed IDs and case-insensitive trimmed names from before/after/snapshot/resource suffix, survivor order plus snapshot append, numeric-zero identity exclusion, campaign deletion rejection, basename-normalized session/entity targets, force session deletion, entity type validation and slug injection, sequential resource writes, exact errors, and no history-state update after a delegated write failure. Do not expose snapshot commands or identity helpers.
- Campaign AI history change-set construction in `server/aiResponseHistoryService.js` must remain behind the existing parsed-response persistence API. Focused private policies own bundle eligibility, campaign/session/entity resource projection, `Map` indexing, ordered union traversal, nullable lookup, snapshot comparison/cloning, label sorting, and summary construction. Preserve an empty change set when either bundle is falsy; exact campaign, session, and entity IDs/labels; nullish campaign metadata fallback; `storage.ENTITY_TYPES` traversal; last-write-wins duplicate filenames/slugs with first-key insertion order; `Map.has` handling before snapshot normalization; unchanged-resource omission; undefined-to-null JSON snapshot cloning; final `label.localeCompare` ordering; cloned resource isolation from source bundles; and the existing exceptions for truthy malformed collections. Do not export change-set internals merely for testing; exercise them through `saveParsedAiResponse` or `saveDraftParsedAiResponse`.
- Failed AI history persistence and retry-payload sanitization belong to `server/ai/AiHistoryWriter.js`. Keep the class methods and exported `asText` compatibility stable; private policies may own attachment projection, parsing eligibility, snapshot inputs, error/status formatting, and response construction. Preserve `shouldSave` as the first lazy guard; object-like path acceptance (arrays included); trimmed campaign requirement before instruction/snapshot/clone work; dynamic dispatch through `getUserInstructions`, `buildRequestSnapshot`, and `cloneRetryPayload`; own-property instruction precedence; image exclusion and encounter-gated parsing, including the raw truthy `generateEncounters` result; strict-false character/NPC/location defaults; Boolean encounter/custom flags; and one propagated `storage.addAiResponse` call. Failure text includes a status line only for truthy status while error metadata retains the raw status; message selection applies raw `message || error` before `asText` and the default. Retry cloning remains JSON-based, preserves its circular/BigInt failures, filters only array attachments, retains order, normalizes scalar image metadata and file names, converts truthy numeric sizes, replaces truthy image data with `omittedData`, removes file data, and never mutates the source payload.
- Generated custom-monster normalization in `server/aiCustomMonsterService.js` must remain a private ordered projection pipeline over closed object, string-list, and action-entry field sets. Preserve truthy `name → title` eligibility, object-like legacy arrays, scalar ID coercion and lazy ID generation, forced CUSTOM source, size/type/alignment/combat defaults, canonical-before-alias nullish precedence (including canonical `0`, `false`, and empty strings), the observable second alignment read for non-empty alignment, record AC/HP/speed/CR shapes, truthy object-field acceptance including arrays, non-empty string-list projection, array-only action collections, string/object action entry rules and extra properties, optional image/source-name inclusion, and final recursive bracket stripping that clones nested structures. Only `normalizeCustomMonster` is public.
- AI custom monster generation must avoid app entity links (`[Name]`) inside monster fields.
- 5eTools inline tags in monster text should use English/canonical lookup values, e.g. `{@condition stunned}`, `{@spell Gust of Wind|XPHB}`.
- Monster attack/damage text should use parseable tags like `{@hit N}`, `{@damage FORMULA}`, `{@dc N}`, `{@recharge N}`.
- `{@recharge N}` is a d6 recharge threshold, not a d20 modifier.
- Custom monster spellcasting should use the official-style top-level `spellcasting` array when applicable.
- Image defaults used in UI:
  - Location image upload default category: `scenes`.
  - Character/NPC image upload default: `characters` / `npc`.
  - Custom monster token upload default: `tokens`, no subfolder.

## Testing and Validation

- `npm test` runs `tests/run-tests.mjs` and covers utilities/storage/AI patching/parsing/model behavior.
- Focused regression runs may set `TEST_FILTER` to a case-insensitive test-name fragment before running `tests/run-tests.mjs`; this is a development aid, not a replacement for the full `npm test` gate required before completing a phase.
- `npm run lint` runs ESLint and `npm run check:uk`.
- `npm run check:uk` validates UTF-8 and suspicious Ukrainian text corruption in `src` and `server`.
- `npm run check:performance` enforces representative Bestiary/spell/search/encounter budgets and the campaign-search/encounter render caps. Keep fixtures deterministic and Node-safe.
- Existing warnings may exist; distinguish pre-existing warnings from new regressions.
- Do not use `npm run build` as a default validation command.
- When changing AI operations, add or update tests around:
  - `server/modules/ai/domain/aiPayloadSchemas.js`
  - `server/aiPatchService.js`
  - `tests/run-tests.mjs`
- When changing parsers/renderers, test `src/entities/reference/model/parserTags.ts`, `src/entities/reference/model/contentTokens.ts`, `src/features/rich-content/model/richContentPresentation.ts`, and `src/features/rich-content/ui/RichContentRenderer.tsx`.

## Common Pitfalls

- PowerShell can corrupt Ukrainian text if writing through the wrong encoding path. Prefer `apply_patch`; verify with `check:uk`.
- `data/` contains real local campaign/user data and may have Ukrainian path names.
- Service worker cache can hide fresh `/api/bestiary` or `/api/spells` changes.
- Custom bestiary should be fetched separately from cached official bestiary data.
- `ReactList` expects stable item rendering; changing list row heights/drag behavior can cause visual jumps.
- Drag/reorder persistence usually belongs on drop/final action, not every hover.
- Entity mentions should not be parsed inside code/pre blocks.
- AI modals can stack; confirmation dialogs must remain inside the active modal flow or be reachable.
- Session encounter data is nested in sessions; do not assume a standalone encounter collection.
- `package.json` has `"main": "server.js"`, but actual backend entry used by scripts is `server/server.js`.

## Do Not Change Without Checking

- `server/storage.js` path normalization, import/export/archive behavior, and image reference updates.
- `server/aiService.js`, `server/modules/ai/http/router.js`, `server/aiPatchService.js`, `server/modules/ai/domain/aiPayloadSchemas.js` prompt/schema/apply flow.
- Preserve the `server/modules/ai/application/buildPromptContext.js` contract when changing campaign/session/encounter context selection; add focused tests for field filtering and simplified notes.
- Preserve exact scope, mention, encounter-linking, and custom-monster rules in `server/modules/ai/application/buildUserPrompt.js`; prompt wording is behavioral code and needs mode-focused regression tests.
- Treat `buildSystemInstruction.js` wording and composition order as behavioral code; keep option-dependent contracts covered without duplicating policy constants in `aiService.js`.
- Keep `server/aiService.js` independent of `@google/generative-ai`; SDK-specific behavior belongs behind the injectable Gemini gateway and should be tested with a fake client.
- Keep filesystem and attachment encoding concerns out of application prompt builders; validate text and binary attachment behavior through `attachmentParts.js`.
- Preserve the complete `parseAiResponse.js` decision matrices when changing it: fence variants, scanner quote/escape state, sparse/inherited/`__proto__` projection, plain-mode laziness, parsed normalization, and exact callback/coercion failure order.
- Add decision-table tests when changing mode/scope/model behavior in `resolveAiRequest.js`; `aiService` must consume the resolved request instead of reimplementing boolean combinations.
- Keep Express objects out of `prepareGenerateAiRequest.js`; it returns a data/error contract that the route maps to HTTP status and JSON.
- Keep Express request/response objects and direct Gemini/storage imports out of generation commands; test success and failure paths using injected fakes.
- Application commands and HTTP history endpoints should depend on the AI history repository port rather than raw `storage.*AiResponse*` methods.
- History routes only extract campaign/id/resource IDs and map command results/errors; draft/apply/undo rules belong to `aiHistoryCommands.js`.
- Keep generation selection and failed-history recovery out of Express handlers; keep `.env` persistence out of application commands and routes.
- Entity scope/move logic belongs to `features/campaign-entity` on the frontend and campaign application commands/repositories on the backend; page hooks only compose those commands.
- Bracketed mention parsing and rendering: `src/features/entity-link/ui/renderMentionText.tsx`, `EntityLink.tsx`, and `entityLinkModalUtils.ts`; tagged reference parsing/rendering remains in `parserTags.ts` and private `RichContentRenderer.tsx`.
- `CampaignNotesGraph.tsx` layout/drag collision code; small constants can drastically change UX.
- `DraggableList.tsx` and `DraggableList.css`; drag hover/layout is sensitive.
- `public/service-worker.js`; cache behavior affects reference data debugging.
- `database/` reference files; they can be large generated/bundled data.
- `src/langs/uk.json`; preserve valid JSON and UTF-8.
- `data/custom-bestiary.json`; user-created monsters live here.

- Monster-stat-block token/name presentation belongs to `model/monsterStatBlockPresentation.ts`. `getMonsterTokenSectionPresentation` owns dropzone/image/skeleton mode plus cancel/replace visibility; keep drag `DataTransfer` effects browser-owned and preserve `text/uri-list`, `text/plain`, `text/html`, then `DownloadURL` writes with `effectAllowed = "copy"`. Token sources retain first-truthy custom URL → string monster image → override → separate local/external model fallback, and `CUSTOM` detection remains uppercase-only without trimming. `getMonsterNameRowPresentation` owns exact name coercion, favorite state/title, and action visibility; private UI controls preserve name-click versus copy, lazy translated titles, button variants/classes, and favorite/add callback ownership. Entity callbacks must go through `executeMonsterAction` with the original monster identity. Do not export private token/name section components or reintroduce the removed `effectiveName` token-section prop.

- Monster token upload lifecycle is model-owned by `executeMonsterTokenUpload`. Empty upload URLs must return before other dependencies. Preserve provisional URL → clear image error ordering; injected callback exclusivity and original monster identity; callback-before-replacement-clear behavior; API mutation-key ownership; server string URL or provisional fallback; success URL → replacement-clear order; and persistence/logger failure behavior without forced cleanup. Metadata presentation, structured-spellcasting line projection, token dropzone eligibility, and token-section action visibility also belong to `monsterStatBlockPresentation.ts`; keep JSX composition private, preserve empty-array truthiness, insertion order, exact cantrip/level/truthy-slot labels, source/original-name visibility, and short-circuit getter order. External consumers continue through `widgets/monster-stat-block/model.js` or the widget public index only.

- Spells-browser one-shot initial scrolling uses `getInitialSpellScrollPlan`; preserve enabled and truthy-reference/selected eligibility, source-qualified matching with source wildcard support, raw `${source ?? ""}:${name}` key, last-key suppression, exact displayed index, key recording before one animation frame, and cleanup cancellation. Row presentation uses `getSpellListItemPresentation`; preserve source resolver execution, pre-pipe name capitalization, strict numeric-zero cantrip handling, translated level variables, case-sensitive school lookup, string-only classes including empty strings, truthy raw source visibility, tooltip disablement from the resolved source name, selected toggle target, and exact spell identity. Optional double-click insertion goes through `executeSpellInsertAction`. Keep row/meta components and `ui/SpellCard.tsx` private; the Phase 126 ownership move must not alter spell-card rendering or reintroduce the retired `widgets/spell-card` slice.

- Rules-reference inline tag, Backspace, and tab decisions belong to `widgets/rules-reference-modal/model`. Keep the closed seven-tab tag syntax, status-versus-condition kind, source-qualified spell/creature names, empty-name rejection, modifier/editable/back-history guards, and ordinary-first-item versus embedded-spell pending navigation behavior in `getReferenceInlineTag`, `getReferenceKeyboardPlan`, and `getReferenceTabSelectionPlan`. React UI retains DOM editable-target detection, listener and `preventDefault` effects, refs, history recording, state writes, list focus/scrolling, and private spell/Bestiary compositions. Node consumers use `widgets/rules-reference-modal/model.js`; do not deep-import the typed implementation.

- Rules-reference selection reconciliation, state application, scroll eligibility/indexing, and history availability also belong to `widgets/rules-reference-modal/model`. Preserve loaded/loading and embedded-spell guards, active/filtered selection retention, empty-result clearing with unchanged state identity when already empty, first-item fallback, source-aware Bestiary matching, blocked scroll requests remaining pending, eligible misses consuming the request with index `-1`, index-zero scrolling, and exact back/forward boundaries. React owns refs, setters, timers, listeners, history writes, meta callbacks, and private rendering; do not move browser effects into the Node-safe model.

- Rules-reference Bestiary response envelopes, qualified identities, tab-navigation outcomes, name matching, creature rank, and selected-item lookup are closed model policies. Preserve direct arrays and `monster → monsters → results` nullish precedence, including present non-array shadowing; truthy/trimmed name and source construction; invalid-tab rejection; ordinary versus embedded-spell fallback; raw ordinary names; qualified-or-raw spell matching; Bestiary ranks `0` mismatch, `1` source fallback, `2` wildcard, `3` exact; first exact or first positive fallback selection; first equal-rank stability; and exact-match termination. Extend these policies through `widgets/rules-reference-modal/model.js`; keep API loading and rendering out of the Node-safe model.

- Rules-reference request deduplication, initial-navigation precedence/execution, tab-load eligibility and loaded-selection application, load-error text, tab-only state application, tab-click effect order, and host-open gating are model policies. Preserve force-reference clearing before named navigation; request IDs marked handled before open-modal suppression; global-search loading of every missing/unrequested tab; requested-tab retry deletion on failure; mounted guards for result, alert, and loading-finalization effects; ordinary first-item selection versus embedded-spell ownership; and scroll → pending → history → active-tab order. Keep loaders, refs, API calls, dispatch, timers, and modal opening in UI. Reference details/sidebar/creature rows remain private compositions: preserve Bestiary stat blocks versus recursive content, localized loading/empty copy, virtual-list keys, token/image-error behavior, source tooltips, size/type/CR fallbacks, and insertion controls.

- Private `widgets/rules-reference-modal/ui/useReferenceTabLoading.ts` owns the mounted state, requested-tab and active-controller refs, tab-load effect, API/`AbortController` lifecycle, result/selection application, localized error reporting, retry release, and loading finalization. `RulesReferenceModalContent.tsx` supplies its local setters plus the injected runtime commands; it retains navigation/history, search, selection, scrolling, and rendering. Preserve active-request identity checks, abort cleanup, abort-error silence, custom-Bestiary abort propagation, result/error/finally mounted guards, and error-before-loading-finalization ordering. This browser-only hook is not part of `model.js` or the widget public API; Node-safe model code remains pure policy only.

## Useful Commands

- Install dependencies: `npm install`.
- Start frontend dev server only: `npm run client` (Vite, port 3000, proxies `/api` to `http://localhost:5000`).
- Start backend dev server only: `npm run server` (Express watch mode, port 5000 by default).
- Start client and server together: `npm start`.
- Windows helper: `run_project.bat`.
- Project helper: `npm run project` performs `git pull`, may install deps, may build, then starts production server; use only when this side effect is intended.
- Tests: `npm test`.
- Focused tests (PowerShell): `$env:TEST_FILTER='request validation'; node --experimental-specifier-resolution=node tests/run-tests.mjs`.
- Lint + Ukrainian encoding check: `npm run lint`.
- Encoding check only: `npm run check:uk`.
- Performance budgets: `npm run check:performance`.
- TypeScript contracts: `npm run typecheck`.
- Bestiary copy materialization: `npm run bestiary:materialize-copies`.
- Database bundle scripts: `npm run database:bundle`, `npm run database:bundle:compact`.
- Update 5eTools data: `npm run database:update`.
- `scripts/update-5etools-data.mjs` is only the 5eTools updater composition root: keep CLI constants, path construction, help/exit behavior, native adapters, updater creation, top-level cleanup, and failure exit there. Deterministic flags, remote-file filtering, newline handling, monster/token keys, condition/source preference, pruning, local-exhaustion merge, and JSON-entry classification belong to `scripts/update-5etools-data-policies.mjs`. Filesystem, GitHub/raw/token transport, subprocess, logging, and full workflow sequencing belong behind `create5eToolsUpdater` in `scripts/update-5etools-data-runtime.mjs`; tests inject those adapters and must not inspect source text. Preserve `--check` as dry-run, first `--ref=` selection, immediate help exit, seven parallel download requests, dry-run cleanup before database effects, current-monster snapshot before remove/copy, reference writes before tokens, tokens before materialization/bundling, and final support-file cleanup. Remote lists retain file-only JSON/exclusion filtering and locale filename sorting; data files download sequentially, require a truthy URL, parse as JSON, and gain a final newline only when absent. Monster identity is trimmed lowercase name plus uppercase source; `all.json` is authoritative when present; fallback JSON traversal and new-monster selection preserve filesystem/input order and first identity. Token processing remains sequential: unsafe names increment `skipped`, existing files do nothing, 404 plus any caught fetch/mkdir/write/log failure increment `missing`, and successful writes increment `downloaded`; the per-token catch retains partial counters. Conditions prefer truthy `basicRules2024`, then `XPHB/XDMG`, then `PHB/DMG`, then other sources, with the first candidate retained on ties and raw-name locale sorting. Preserve local Exhaustion merge, prune/inline/point-buy policies, adventure-before-book source deduplication, nonrecursive lowercase-`.json` file operations, exact logs/errors, and temporary cleanup on success or top-level failure.
- Build script exists in `package.json`, but do not propose it as routine validation.
