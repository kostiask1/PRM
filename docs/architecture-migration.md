# Architecture Migration Plan

This document is the source of truth for the incremental architecture migration. The project remains a working modular monolith throughout the migration; phases are completed through vertical workflows rather than a repository-wide rewrite.

## Status legend

- **Complete** — acceptance criteria are met and validation is green.
- **In progress** — actively being migrated; new code must follow the target rules.
- **Planned** — sequenced work that has not started.
- **Optional** — evaluate only after the structural phases are stable.

## Target architecture

Frontend dependency direction:

`app -> pages -> widgets -> features -> entities -> shared`

Backend dependency direction inside a domain module:

`http -> application -> domain`

Infrastructure implements ports required by application/domain code. Express, filesystem storage, Gemini, archives, and realtime transport stay at boundaries.

## Phase 0 — Baseline and guardrails

Status: **Complete**

Delivered:

- Fallow architecture zones and dependency-direction rules in `.fallowrc.jsonc`.
- `npm run check:architecture` for cycles and boundary violations.
- UTF-8/Ukrainian encoding validation remains part of lint.
- Transitional policy documented in `AGENTS.md`.

Acceptance criteria:

- No circular dependencies.
- No FSD boundary violations.
- Tests, lint, and encoding checks pass.

## Phase 1 — API ownership and transport separation

Status: **Complete**

Delivered:

- HTTP mechanics moved to `src/shared/api/httpClient.ts`.
- Domain clients created for campaigns, sessions, Bestiary, and spells.
- Use-case clients created for AI, backups, images, and settings.
- All production consumers migrated to their owning clients.
- Transitional `src/api.js` compatibility facade removed.
- Existing regression tests now mock the owning client instead of a global facade.

Follow-up rule:

- Add focused client contract tests when introducing non-trivial request construction or response mapping.

Acceptance criteria:

- No production import of `src/api.js`.
- Transport contains no domain endpoints.
- Every endpoint has one clear frontend owner.

## Phase 2 — Bestiary FSD pilot

Status: **Complete**

Delivered:

- Browser composition moved to `src/widgets/bestiary-browser`.
- Monster editing moved to `src/features/edit-monster`.
- AI monster editing/draft policies and their initial UI moved to `src/features/ai-edit-monster`; Phase 132 later lifted the cross-feature edit/draft shell to the encounter page owner.
- Bestiary API ownership moved to `src/entities/bestiary`.
- Rules reference uses its own lightweight creature list/detail view instead of embedding the complete Bestiary widget.
- Three Bestiary/AI/rules-reference import cycles removed.

Follow-up improvements belong to later phases: split the large Bestiary orchestration component by workflow and move stable monster presentation/model code into an entity slice.

## Phase 3 — Reference-data vertical slice

Status: **Complete**

Delivered:

- Spells, conditions, diseases, skills, senses, variant rules, creature previews, and rules-reference browsing now use owned domain clients.
- `MonsterStatBlock`, `Spells`, settings source selection, and reference preview no longer import the compatibility API facade.
- Rules-reference browsing uses `entities/spell` and `entities/bestiary` directly.
- Inline-tag parsing remains independent from React UI and API access.

Deferred intentionally:

- Introduce a dedicated reference-data entity/widget only when it provides clearer ownership than the current focused services. Avoid a speculative wrapper layer.

Acceptance criteria:

- Reference-data code does not import `src/api.js`.
- Rules-reference UI does not import page-level or Bestiary widget code.
- Parser and reference resolver regression tests pass.

## Phase 4 — AI workflow decomposition

Status: **Complete**

Delivered:

- Pure token-estimation and context-compaction logic moved to `src/features/ai/model/tokenEstimation.ts`.
- AI history change detection and retry-payload reconstruction moved to `src/features/ai/model/historyWorkflow.ts`.
- AI request-mode resolution, payload construction, and removal of heavy session data moved to `src/features/ai/model/generationRequest.ts`.
- Generation and retry lifecycle now uses the explicit request-aware state model in `src/features/ai/model/generationLifecycle.ts`; context loading remains separate.
- History upsert, campaign ownership, partial apply/undo mode, and restore synchronization planning moved to `src/features/ai/model/historyState.ts` with concurrency-safe functional updates in React.
- Delete, clear, apply, undo, and draft-save commands moved to `src/features/ai/model/historyCommands.ts`; the hook serializes restore commands with a ref-backed lock and exposes results through UI callbacks.
- Stable visual composition moved to `src/features/ai/ui`: `AiAssistantShell`, `AiPromptComposer`, and `AiHistoryResponseDialog` own modal plumbing and presentation while the panel supplies workflow state and callbacks. UI exports use `src/features/ai/ui/index.js`, keeping the model/API entry Node-compatible.
- Remaining AI-only toolbar, context settings, API-key, and history-list views moved from legacy `components/ai` into `src/features/ai/ui`.
- Context list normalization, nested scene defaults, item toggles, and bulk selection moved to `src/features/ai/model/contextConfig.ts`.
- Campaign entity/session loading, selected-session hydration, and context-list synchronization moved to `src/features/ai/model/useAiContextData.ts`; API operations are injected as ports and reused by image-prompt loading.
- Token-estimate request shaping, route-mode context assembly, list filtering, and attachment costs moved to the pure `src/features/ai/model/tokenEstimation.ts` model.
- NPC, location, scene/encounter, and custom-monster image target construction moved to `src/features/ai/model/imageTargets.ts`.
- Campaign session hydration, custom-bestiary normalization, loading state, and fetch caching for image prompts moved to `src/features/ai/model/useAiImagePromptData.ts`.
- Focused regression tests cover ignored context, mode selection, retry reconstruction, generated entity types, and campaign-change detection.
- `AiAssistantPanel` now consumes these feature model APIs instead of owning their implementations.
- Backend module extraction has started: pure campaign/session/encounter input shaping and ignored-content filtering moved from `server/aiService.js` to `server/modules/ai/application/buildPromptContext.js`.
- Deterministic input serialization and mode-specific task instructions moved to `server/modules/ai/application/buildUserPrompt.js`; `aiService` now supplies orchestration inputs instead of owning task branches.
- Gemini client/key caching, model configuration, multimodal request-part shaping, invocation, and response retrieval moved behind `server/modules/ai/infrastructure/geminiGateway.js` with an injectable factory for tests.
- Plain-text normalization, JSON fence/prose extraction, recursive escaped-newline repair, and invalid-response shaping moved to `server/modules/ai/application/parseAiResponse.js`.
- Base mode policies and conditional system contracts moved as one unit to `server/modules/ai/application/buildSystemInstruction.js`; `aiService` now passes resolved mode/options and receives the final instruction string.
- MIME/size validation, local image file resolution, base64 handling, text attachment rendering, and binary inline parts moved to `server/modules/ai/infrastructure/attachmentParts.js`.
- Effective parsing/mode selection, entity target scope, generation permissions, response language normalization, and requested-model fallback moved to `server/modules/ai/application/resolveAiRequest.js` with decision-table coverage.
- Shared generate-request validation, normalized paths, settings/base prompts, auto-apply policy, and route-facing generation flags moved to injectable `server/modules/ai/application/prepareGenerateAiRequest.js`.
- The first complete HTTP-independent generate use case, Bestiary image prompting and history persistence, moved to injectable `server/modules/ai/application/generateBestiaryImagePrompt.js`.
- Custom-monster generation, missing-ID normalization, selected-target context, optional campaign/session context, failure history, contract validation, and normal/encounter-local draft dispatch moved to `server/modules/ai/application/generateCustomMonster.js`.
- Campaign/session/encounter generation context, custom monster-name lookup, target completion, mention processing, validation, failure history, and flow persistence moved to `server/modules/ai/application/generateCampaignContent.js`.
- Configured campaign/session context loading now uses injected entity/session ports in `server/modules/ai/application/campaignContext.js`; current scene/encounter/custom-monster target completion moved to `fillCurrentTargetIds.js`.
- An explicit AI history repository port now lives at `server/modules/ai/application/ports/aiHistoryRepository.js`, with filesystem delegation in `server/modules/ai/infrastructure/fileAiHistoryRepository.js`; history CRUD and Bestiary-image writes consume the port.
- Draft history editing, nested ID preservation, entry lookup/not-found behavior, and apply/undo snapshot dispatch moved to `server/modules/ai/application/aiHistoryCommands.js`; HTTP routes now only map identifiers and resource selections.
- Top-level request preparation, generation workflow selection, and unexpected-failure history recovery moved to `server/modules/ai/application/generateAiRequest.js`; `/generate` now only maps HTTP input/output/errors.
- Gemini API-key validation and cache invalidation moved to `server/modules/ai/application/saveGeminiApiKey.js`, with `.env` and process-environment persistence isolated in `server/modules/ai/infrastructure/envApiKeyStore.js`.
- The AI HTTP composition root moved to `server/modules/ai/http/router.js`, and the parsed-operation schema contract moved to `server/modules/ai/domain/aiPayloadSchemas.js`; legacy paths remain compatibility re-exports for existing mounts/imports.
- Mention candidate collection and operation text-field canonicalization moved from the HTTP router to `server/modules/ai/application/mentionProcessing.js`; smart apostrophe normalization now uses an encoding-safe Unicode escape.

Frontend scope:

- Split `AiAssistantPanel` into context selection, prompt editing, generation, history, draft review, apply/undo, and image-prompt workflows.
- Replace boolean combinations with explicit workflow statuses.
- Keep campaign/session/bestiary adapters outside reusable AI workflow UI.

Completion evidence:

- `server/modules/ai/{domain,application,infrastructure,http}` contains the domain contract, independently tested use cases, filesystem/Gemini adapters, and HTTP composition root.
- Generation, history draft/apply/undo, request preparation, prompt/context construction, response parsing, and API-key use cases have fake-driven tests that require neither Gemini nor real campaign files.
- AI HTTP handlers map request values and `{ status, body }` command results; workflow selection, persistence, validation, and recovery live outside Express handlers.
- Parsed-operation schema and apply/undo regression coverage passes with the full suite, while Fallow reports zero cycles, boundary violations, unresolved imports, or dead-code findings.

Backend scope:

- Create `server/modules/ai/{domain,application,infrastructure,http}`.
- Split context collection, prompt construction, Gemini invocation, response parsing, validation, history, apply, and undo.
- Introduce explicit ports for Gemini and AI history persistence.

Acceptance criteria:

- AI use cases can be tested without Gemini or real campaign files.
- Route handlers only map HTTP requests/responses.
- Parsed-operation schema and apply/undo behavior remain regression-tested.

## Phase 5 — Campaign, session, and encounter slices

Status: **Complete**

Current checkpoint:

- Campaign character/NPC/location create/update/delete now delegates through `src/features/campaign-entity`, removing direct CRUD calls and the legacy create utility from page-level callers.
- Backend CRUD defaults, type validation, stable ID/slug preservation, mention updates, and deletion now live in `server/modules/campaign/application/campaignEntityCommands.js` over an explicit repository port and filesystem adapter.
- Debounced campaign entity persistence, pending update ownership, flush/discard, and per-entity timer cancellation moved from `useCampaignView` to `src/features/campaign-entity/model/useCampaignEntityPersistence.ts`; pending writes retain their originating campaign slug.
- Optimistic changes, collapse persistence, rename confirmation and mention updates, deletion, and reload-on-error for all three campaign entity collections moved to `useCampaignEntityCollection`; `useCampaignView` now wires campaign-specific callbacks instead of implementing three workflows.
- Character/NPC type movement, pending-write flushing, optimistic collection transfer, entity ordering, one-shot reorder undo tracking, persistence, and reload-on-error moved to `useCampaignEntityOrdering`.
- NPC/location movement between campaign and session scope now uses one backend application command over an explicit cross-scope repository port. It preserves entity IDs, compensates partial filesystem writes, and replaces the previous two-request UI transaction.
- Scope import state, confirmation orchestration, pending-session-save flushing, mutation requests, and refresh notifications moved to `useCampaignEntityScopeMovement`.
- Session and scene mutation rules now live in `src/features/session-editor/model/sessionMutations.ts`; `useSessionEditing` owns scene/note editing and linked-encounter removal orchestration.
- Debounced session persistence, flush/discard, rename notification, failure propagation, and unmount flushing moved to `useSessionPersistence`. Undo/redo snapshots and AI replacement history moved to `useSessionHistory`.
- Encounter creation and scene linking now use an idempotent backend application command with server-generated IDs over the new `SessionRepository` port. The frontend `useEncounterCreation` workflow flushes session edits, creates or resolves the link, updates local state, and navigates.
- Encounter edits now use a focused backend update command instead of client-side session read/modify/write. `useEncounterPersistence` owns debounce state, pending writes, unmount flushing, and refresh notifications.
- Custom Bestiary participant synchronization and campaign character/NPC image sourcing now live in `useEncounterParticipantSynchronization`. Pure merge rules preserve combat instance IDs, display names, bounded current HP, and exclude official monsters and character participants.
- Campaign, session, encounter, and modal callers now use feature clients for entity mutations. Phase 5 is complete; Phase 6 continues repository extraction behind the established application commands.

Migrate one complete workflow at a time:

1. Campaign entity create/edit/delete.
2. NPC/location scope movement. **Completed.**
3. Session and scene editing. **Completed.**
4. Encounter creation, linking, and persistence. **Completed.**
5. Encounter participant synchronization with custom Bestiary monsters. **Completed.**

Acceptance criteria:

- IDs and scope rules stay stable.
- Persistent state is server-owned; workflow state stays feature-local.
- Hooks stop acting as page-wide service locators.
- Each migrated workflow has domain/application tests.

## Phase 6 — Backend repositories and storage decomposition

Status: **Complete**

Current checkpoint:

- `SessionRepository` now owns session existence, listing, reads, atomic writes, deletion, rename, default creation, name sanitization, unique file naming, and ID generation through its filesystem adapter.
- Session list/create/get/update/delete/reorder behavior moved to `server/modules/session/application/sessionCommands.js`, including stable IDs, rename semantics, default dates/order, 404 handling, and reorder validation.
- `server/routes/sessions.js` is now an HTTP adapter only; it no longer constructs session paths, reads/writes JSON, renames files, or deletes files directly.
- Session, entity-scope, scene-encounter, and encounter-update commands share repository-backed persistence through explicit ports.
- `CampaignRepository` now owns campaign metadata existence/read/write, initialization, rename, deletion, image checks, export delegation, unique slugs, image-reference replacement, and ignored-source normalization.
- Campaign lifecycle and ordering moved to `campaignCommands`; bulk entity replacement and character/NPC movement moved to `campaignEntityCommands`. Campaign and entity IDs remain stable across updates and moves.
- `server/routes/campaigns.js` is now an HTTP-only adapter with no direct storage calls.
- `BestiaryRepository` now owns the mutable custom-monster index, custom collection, and favorites persistence. Search, favorite toggling, full replacement, rename/image updates, duplicate validation, favorite rename/removal, and deletion moved to `bestiaryCommands`.
- Custom monster replacement continues to preserve token images when omitted and keeps legendary actions on the monster object.
- Bundled `all.json` reads, source discovery, direct/`bestiary-` filename fallback, source filtering, and legendary-group reads now use `BestiaryRepository`; copy-chain and array-mod resolution live in `bestiaryCommands`.
- `server/routes/bestiary.js` is now an HTTP/cache-header adapter with no filesystem, path, JSON, or direct storage operations.
- `SettingsRepository` and `settingsCommands` now own normalized settings reads/patches; `server/routes/settings.js` is HTTP-only.
- `ImageRepository` now owns upload directories, UTF-8 filename decoding, collision-safe names, gallery reads/search/stats, readonly token reads, subcategories, rename/move/delete, and the underlying image-reference updates. `server/routes/images.js` contains no path or filesystem operations.
- `ReferenceRepository` now owns aggregate/index/source spell files plus conditions, diseases, variant rules, skills, and senses. Search, source filtering, fallback, deduplication, and XPHB/XDMG precedence moved to `referenceCommands`; `server/routes/spells.js` is HTTP-only.
- `BackupRepository` and `backupCommands` now own full/partial exports, gzip serialization, uploaded archive parsing, append/replace-by-ID/wipe strategies, campaign-only imports, and archive persistence delegation. `server/routes/backups.js` only maps HTTP, multipart buffers, and download headers.
- Static image/token directories are resolved by the assets infrastructure adapter rather than the route.
- A global route audit confirms no `server/routes/*.js` file constructs filesystem paths or calls JSON/filesystem storage primitives. Phase 6 is complete.

Introduce focused repositories over the existing filesystem storage:

- `CampaignRepository`
- `SessionRepository`
- `EntityRepository`
- `BestiaryRepository`
- `ImageRepository`
- `AiHistoryRepository`
- `SettingsRepository`

Initially, adapters may delegate to `server/storage.js`. Move implementation only after callers depend on repository contracts, then remove obsolete storage exports.

Acceptance criteria:

- Routes do not construct filesystem paths or manipulate JSON files directly.
- Path normalization and atomic UTF-8 JSON writes remain centralized.
- Archive/import and image-reference behavior remains covered by regression tests.

## Phase 7 — Shared UI and legacy-folder retirement

Status: **Completed**

Initial legacy inventory:

- `src/components`: 70 files
- `src/hooks`: 5 files
- `src/models`: 7 files
- `src/services`: 11 files
- `src/utils`: 31 files

Current checkpoint:

- The complete image-gallery workflow moved to `src/features/images`: `ImageGallery`, `ImageTargetSettings`, `ImageDropzone`, `ImageAssetField`, and `useImageGallery`.
- Legacy image UI/hook files were removed; external consumers import only from `features/images/index.js`, while slice internals use relative imports.
- Generic presentation primitives `Panel`, `Notification`, and `Switch` moved to `shared/ui` and are consumed through its public index. Their legacy files were removed.
- Generic `Button`, `Icon`, and `Tooltip` joined the shared UI foundation; all callers were migrated through `shared/ui/index.js`.
- Generic `Checkbox`, `Select`, and `MultiSelect` moved to `shared/ui`; app-coupled `Input` and `EditableField` remain outside shared until their store/entity dependencies are separated.
- `classNames` moved completely to `shared/lib`; all callers use its public index and the legacy compatibility export was removed.
- Generic note-state helpers moved from `utils/noteUtils` to `shared/lib`; campaign character/location card models and their note-model base moved to `entities/campaign/model` behind the campaign entity public API.
- `CharacterCard` and `LocationCard` moved to the `campaign-entity-card` widget because they compose entity models with image/editor features and app-aware UI. Consumers use only the widget public API; their legacy component paths are retired.
- Character/location creation modals also live in `campaign-entity-card`: their submit rules remain in `features/campaign-entity`, while the widget owns composition with the card UI and modal shell.
- Pure spell presentation logic (`SpellCardModel`) moved to `entities/spell/model`; rich rendered `SpellCard` composition moved to `widgets/spell-card`. Both are consumed through their slice public APIs and the legacy paths are retired.
- Pure monster stat-block derivation moved from the legacy models directory to `entities/bestiary/model` and is exposed only through the bestiary entity public API.
- `CampaignViewModel` and `SessionViewModel` moved to their respective entity model segments. The generic `idsEqual` helper moved to `shared/lib`, allowing the legacy `src/models` directory to be fully retired.
- Generic `useDebounce` moved to `shared/lib` and all feature/widget/component consumers use the shared public API; its legacy hook path is retired.
- Campaign screen composition moved to `pages/campaign`: `CampaignPage` owns the former `CampaignView` UI and its orchestration hook, while `MainContent` imports only the page public API. Legacy component/hook paths are retired.
- Session screen composition moved to `pages/session`: `SessionPage` owns the former `SessionView` UI and orchestration hook, with `MainContent` consuming its public API and legacy paths retired.
- Encounter screen composition moved to `pages/encounter`: `EncounterPage` owns the former `EncounterView` UI and orchestration hook. This retires the legacy `src/hooks` directory completely.
- The embedded spells reference browser moved from `components/Spells` to `widgets/spells-browser`; the rules-reference modal consumes only its public API, while individual spell cards remain in the lower `spell-card` widget.
- Rich `MonsterStatBlock` composition moved to `widgets/monster-stat-block`; the pure derivation model remains in `entities/bestiary`, and all AI, encounter, rules-reference, and bestiary consumers use the widget public API.
- Pure dice parsing, rolling, and probability distribution moved to `shared/lib`; interactive `RollDice`, `DiceCalculator`, and probability modal UI moved to `features/dice`. Consumers use public indexes and all former component/utility paths are retired.
- Rules reference link/preview behavior and the navigation-opening command live in `features/rules-reference`. Modal composition lives in `widgets/rules-reference-modal`, while `widgets/monster-editor-modal` injects reference content into `features/edit-monster`; no feature imports the widget layer.
- The edit-monster feature now receives rules-reference content through an injected component. `widgets/monster-editor-modal` owns the feature/widget composition, and `RulesReferenceModalContent`/host moved to `widgets/rules-reference-modal`; the former feature-to-widget dependency and legacy modal paths are removed.
- Dependency-safe `ListCard`, `CollapseToggleButton`, and `DraggableList` moved to `shared/ui`; all consumers use the shared public API and their former common-component paths are retired.
- Entity-link identity, contexts, inline rendering, modal resolution, and opening behavior moved to `features/entity-link`. Campaign-specific modal rendering moved to `widgets/campaign-entity-modal` because it composes character/location card widgets.
- The app-aware modal shell and global message box moved to `features/modal`; it remains above shared UI because it owns localized prompt/input behavior and store-backed message orchestration.
- AI attachment controls moved into `features/ai/ui`; AI prompt, image-prompt, and monster-AI consumers now use feature-local or public AI imports instead of reaching into legacy components.
- Widget-heavy AI response rendering moved to `widgets/ai-response-modal`. AI history and bestiary draft features receive the response component through injection, eliminating feature-to-widget dependencies; page/widget callers own the composition.
- AI assistant orchestration and image-prompt picker composition moved to `widgets/ai-assistant`; it composes AI feature UI/model APIs with entities, app state, and the AI response widget. Main content and Bestiary use its public API.
- Session-only scene field/header/media and todo item/section components moved under `pages/session/ui/components`; they remain private page implementation details and their former legacy paths are retired.
- Campaign notes graph and partial archive modal moved into private `pages/campaign/ui/components`. Cross-page global campaign search moved to `widgets/campaign-search` and is consumed through its public API by campaign/session pages.
- Route composition and empty-state project guide moved into `app/routing`; navigation/sidebar composition moved to `widgets/sidebar` and is consumed through its public API by the app root.
- Theme switching and settings modal UI moved into `features/settings/ui`; the API barrel remains model-safe, while browser consumers use the UI public entrypoint.
- App-aware input, rich editable field, and mention-picker modal content moved into `features/editor/ui`. The modal prompt now uses a plain styled native input, removing the modal↔editor feature cycle.
- Note card editing, AI-ignore controls/list props, and bulk collapse behavior moved into `features/notes/ui`; pages and composed card/response widgets use the note UI public entrypoint.
- The final legacy components moved to behavior-specific features: clipboard feedback, status badge, encounter monster insertion, campaign creation, and player questions. `src/components` now contains no production files and is fully retired.
- Cross-domain JSON guards, download helpers, byte formatting, and deep object search moved from `src/utils` to `shared/lib`; consumers use the shared public index.
- Navigation URL/event helpers, DOM target navigation, and undo/redo transitions moved from `src/utils` to `shared/lib`; consumers use the shared public index and their former paths are lint-blocked.
- Monster type normalization and bestiary search matching moved to `entities/bestiary/model` and are exposed through the bestiary entity public API; the legacy utility path is retired.
- Encounter participant identity, HP derivation, and instance creation moved to `entities/encounter/model` behind `entities/encounter/index.js`; page and editor features no longer depend on a legacy utility.
- Campaign graph projection and deterministic layout moved to the campaign page model. The Node-safe `pages/campaign/graph.js` entrypoint supports the page UI and regression tests without loading the page's React entrypoint.
- AI attachment validation, diff construction, model-option loading, and response-resource helpers moved to `features/ai/model` behind the Node-safe feature public API. The model loader now depends directly on the feature API adapter instead of importing its own barrel.
- Condition/disease/skill/sense/variant-rule caches, reference previews, and input resolvers moved from legacy services and compatibility utilities into `entities/reference/model`. Browser consumers use `entities/reference/index.js`; Node regression tests use `entities/reference/model.js` so the browser-only JSON source catalog is not evaluated.
- Content-token parsing, 5eTools tag preprocessing, source labels/filtering, and spell metadata also moved into the reference entity. Generic search highlighting moved to `shared/ui`.
- Mention boundary behavior and picker orchestration moved into `features/editor/model`, exposed through the Node-safe `features/editor/model.js` entrypoint.
- Campaign entity display-name lookup and async resolution moved into `entities/campaign/model` behind the campaign entity API. Theme constants/application moved to `features/settings/model`.
- Localization moved to `shared/lib`, while WebSocket reconnect and data-refresh orchestration moved to `app/realtime`. The legacy `src/services` tree is now fully retired.
- Global action contracts, action creators, action/state types, and lower-level reducer policies live in `shared/model`. The actual stateful external store, React selectors, modal request coordination, and navigation commands are owned solely by `app/model`. Phase 136's typed shared compatibility facade was temporary and Phase 159 retired it with its runtime port; the legacy `src/actions` and `src/store` trees remain retired.
- Interactive recursive content rendering moved to `features/rich-content`, where composition of dice, rules-reference, and entity-link controls has an explicit owner. Token extraction and tag preprocessing remain in `entities/reference`; consumers use the feature public API and `src/renderers` is retired.
- Shared HTTP transport is exposed through `shared/api/index.ts`; domain API adapters no longer deep-import its implementation.
- ESLint rejects deep image/shared imports and retired image/primitive paths.
- Legacy counts are now 0 component files, 0 hook files, 0 model files, 0 utility files, 0 service files, 0 action files, 0 store files, and 0 renderer files. The legacy `src/components`, `src/hooks`, `src/models`, `src/utils`, `src/services`, `src/actions`, `src/store`, and `src/renderers` trees are retired.

- [x] Move only truly generic controls into `shared/ui`.
- [x] Move domain-aware cards and models into entity slices.
- [x] Retire legacy frontend paths as their final owners migrate.
- [x] Tighten Fallow zones so legacy-to-FSD exceptions disappear.

Acceptance criteria:

- No generic dumping-ground directories.
- Cross-slice imports use public `index.js` APIs.
- Legacy folders contain no production code.

## Phase 8 — TypeScript at contracts

Status: **In progress**

Start only after module ownership stabilizes. Prioritize API contracts, AI operation schemas, repository ports, identifiers, and workflow events. Do not combine TypeScript conversion with domain moves in the same change unless required by that slice.

Current checkpoint:

- Added a strict, no-emit TypeScript configuration and an explicit `npm run typecheck` gate.
- Added TypeScript-aware ESLint parsing and recommended rules without weakening the existing JavaScript checks.
- Converted the stable AI generation lifecycle contract to TypeScript. State/status types and the discriminated start/retry/finish/reset event union now compile alongside the existing reducer regression tests.
- Added a shared numeric `RequestId` contract and applied it to AI workflow events.
- Converted URL parsing/building and modifier-event handling to TypeScript with explicit campaign slug, session filename, encounter ID, parsed-route, and modifier-event contracts. Runtime navigation behavior remains covered by the existing tests.
- Added discriminated TypeScript contracts for AI operation kinds, entity aliases, scopes, targets, note operations, generated-content envelopes, validator options, and validation errors. A declaration file types the existing CommonJS validator while the JavaScript validator remains the runtime authority.
- Extracted modal open/close constants and creators into a typed workflow module with explicit modal configuration, request payload, and action unions.
- Extracted rules-reference navigation/history actions into TypeScript with typed request IDs, navigation options, navigation requests, history entries, and a discriminated action union. The legacy action barrel remains a runtime-compatible public facade.
- Extracted mention-picker actions with typed callback/request envelopes and request IDs.
- Extracted generic dice request/result actions with typed generic payload envelopes and stable request/result IDs.
- Extracted message-box actions and promise-returning thunks with explicit alert/confirm payloads, resolver callbacks, dispatch contracts, and action unions.
- Converted all remaining navigation, active campaign/session/encounter, campaign reload, language, UI-settings normalization, entity refresh, and realtime-sync actions into a focused typed module. `actions.js` is now only a compatibility facade over typed workflow modules.
- Added a typed global state contract composed from the workflow action payloads, including modal, mention, dice, navigation, active entities, campaigns, localization, UI settings, sync, and rules-reference history.
- Added declarations for the existing JavaScript store public API: typed selectors, overloaded dispatch/thunks, modal promises, rules navigation, router integration, and navigation commands. The reducer implementation remains JavaScript until its domain-shaped state can migrate without broad `any` types.
- Migrated the localization, normalized UI-settings merge, and realtime-sync reducer cases into a strict TypeScript reducer section. The JavaScript store delegates to it before handling the remaining domain-shaped cases.
- Migrated modal, mention-picker, dice, message-box, and rules-reference navigation/history reducer cases into a strict workflow reducer using the discriminated action union.
- Migrated entity refresh, route synchronization, campaign collection/reload, and active campaign/session/encounter reducer cases into a strict navigation-state reducer. Unknown domain objects are inspected through narrow property readers rather than `any`.
- The JavaScript store reducer is now only a typed reducer pipeline with unchanged fallback semantics; state transition logic no longer lives in its untyped switch.
- Converted the remaining store infrastructure to TypeScript: initial state, listener registry, typed action/thunk dispatch overloads, language normalization, modal resolver map, React external-store selectors, rules-reference commands, router adapter, and browser history navigation.
- Removed the temporary `appStore.d.ts`; the strict implementation now owns its public types directly. Added React type declarations required by the typed hook surface.
- Added settings and session repository-port declarations, precise filesystem adapter contracts, and application-command payload/result contracts. Existing runtime port factories still enforce required methods.
- Converted shared HTTP transport to TypeScript with generic JSON results, blob responses, sync-client header state, and structured HTTP errors. Converted the settings API client to a focused typed payload boundary and exposed transport through a typed shared API barrel.
- Completed TypeScript contracts for every backend repository port: campaign lifecycle, campaign entities and scope movement, Bestiary, images, backups, reference data, settings, sessions, and AI history. Filesystem/HTTP/archive values remain runtime-validated at their trust boundaries.
- Converted the Bestiary pilot API client to TypeScript with focused monster, favorite, source, legendary-group, mutation, and search result contracts while retaining its entity public barrel.
- Converted campaign/entity, session/encounter, and spell/reference API clients to TypeScript. Their public entity barrels remain the only supported import surface, while request payloads and response envelopes now capture stable IDs, archive results, scope moves, encounter persistence, and reference searches.
- Converted image-gallery and backup feature clients to TypeScript with filesystem location, asset, storage-statistics, move/delete, archive-mode, and import-strategy contracts.
- Converted the AI client to TypeScript with model discovery, history entries/resources/stats, draft and restore payloads, API-key persistence, generation options, and response envelopes. No frontend API client remains as untyped JavaScript.
- Applied the AI client contracts to model discovery, history resource inspection, retry-payload construction, restore planning, command services, and React history orchestration. Nullable transport responses are handled explicitly, and callback/restore-mode contracts no longer rely on implicit JavaScript shapes.
- Added the frontend discriminated AI operation contract and applied it to campaign-change inspection. Converted generation request construction and AI response/history helpers to TypeScript with typed context sanitization, request modes, change summaries, monster draft updates, route visibility, and a declaration boundary for the still-JavaScript line-diff algorithm.
- Converted the AI line-diff engine to TypeScript and removed its temporary declaration. Snapshot narrowing, granular session/custom-monster expansion, field summaries, and line metadata now compile strictly; a focused regression test covers parent resource IDs and added/removed line output.
- Converted AI attachment utilities, image-prompt target builders, and context-list configuration to TypeScript. MIME allowlists, attachment identity, file reading, entity/scene/encounter target shapes, generic context item keys, immutable nested updates, and scene defaults now have explicit contracts; existing feature-model coverage now also verifies frontend MIME and identity behavior.
- Converted AI token estimation to TypeScript with explicit mode, note/entity/session/scene compaction, context-list filtering, attachment, request-shape, and estimate-result contracts. The existing approximation constants and Cyrillic/Latin/file/image calculations remain behaviorally unchanged and regression-covered.
- Converted image-prompt data loading and campaign context loading to TypeScript. Async loader dependencies, nullable API results, session/entity collections, loaded-session tuples, cancellation, and React state contracts are explicit. Restored and regression-locked the unchanged-list reference identity used to suppress redundant context state updates. No JavaScript file remains in `src/features/ai/model`.
- Converted the encounter participant model, session view projection, and spell-card formatter to TypeScript. Monster HP/AC variants, stable participant identity, character projections, session notes/scenes/encounters, spell time/range/components/duration, translation callbacks, and permanent-duration rules now have entity-owned contracts. A narrow declaration bridge keeps the session model on the shared public utility barrel.
- Converted rules-reference navigation, theme application, and Bestiary search/type normalization to TypeScript. Navigation options remain behind the shared model public barrel, theme values are narrowed to light/dark with DOM-safe application, and monster type choices/tags have entity-owned search contracts. Added a complete declaration facade for the shared model barrel and a theme normalization regression test.
- Converted `MonsterStatBlockModel` to TypeScript, completing the Bestiary entity model layer. Official/custom and legacy monster shapes now cover HP, AC, speed, size, alignment, structured types/tags, abilities, saves, skills, languages, challenge rating, actions, and nested damage defenses without weakening runtime fallbacks.
- Converted the entity-link identity and modal-resolution model pair to TypeScript. Entity identity precedence, scope-aware equality, current-modal suppression, injected session resolution, and campaign fallback behavior now have explicit contracts; the Node-safe JavaScript model barrel remains the compatibility entrypoint.
- Converted the complete campaign entity model layer to TypeScript: entity lookup/resolution, campaign view projection, shared card-note mutations, and character/location card derivation. The public JavaScript barrel remains runtime-compatible, while its declaration facade now re-exports the owning TypeScript contracts instead of duplicating them.
- Converted the editor mention model layer to TypeScript. Lexical selection/node boundaries, zero-width marker insertion, keyboard-event handling, picker dispatch, and the selected/cancelled result union now compile strictly while the Node-safe JavaScript model barrel remains stable.
- Converted the pure session-editor mutation core to TypeScript. Scene/note/resource IDs, immutable session updates, encounter cleanup, note materialization/sanitization, collapse transitions, and updater callbacks now have feature-owned contracts; React persistence/history hooks remain separate migration slices.
- Converted encounter participant synchronization to TypeScript. Custom payload envelopes, entity image identity, participant/local-combat fields, synchronization results, custom-only refresh rules, and entity-owned participant inputs now have explicit contracts. Four internal normalization/merge helpers were removed from the public feature surface.
- Converted the complete reference entity model layer to TypeScript: condition/disease/skill/sense/variant caches, previews and resolvers, content-token extraction, tag preprocessing, source filtering/names, and spell metadata. Typed public facades preserve both the browser entrypoint and the JSON-free Node entrypoint; nullable API collections now normalize safely at this trust boundary.
- Converted the complete campaign-entity feature model to TypeScript. Create/update/delete ports, payload sanitization, debounced pending writes, optimistic collection changes, mention-aware rename recovery, reorder undo tracking, character/NPC movement, and campaign/session scope movement now share feature-owned contracts behind the existing JavaScript public facade.
- Converted the complete session-editor feature model to TypeScript. Editing commands, delayed and immediate save lifecycles, flush/discard semantics, rename notifications, pending-save detection, undo/redo snapshots, history-application suppression, and external session replacement now share feature-owned contracts behind the existing JavaScript facade.
- Converted the complete encounter-editor feature model to TypeScript. Existing-link navigation, server-created encounter validation, pending session flush, encounter persistence/debounce, unmount flushing, campaign participant image loading, custom Bestiary synchronization, realtime version guards, and selected participant preservation now share feature-owned contracts behind the JavaScript facade.
- Converted the complete image-gallery model to TypeScript and typed its category configuration. Gallery source/category/subcategory paths, content scopes, official/read-only assets, storage statistics, selection and move groups, file uploads, drag/drop events, and mutation results now have feature-owned contracts behind the existing JavaScript public facade. Nullable HTTP collections, unknown errors, modal results, and parsed drag payloads are handled explicitly.
- Converted the encounter page model to TypeScript with explicit page/session/sync/dice/import/update contracts and a typed page entry declaration. Encounter-editor updates are normalized before entering page state, nullable route identifiers and session payloads are handled explicitly, and unknown import errors no longer assume an `Error` instance. Initiative and challenge-rating derivation moved to a pure typed module with regression coverage for fractional and structured CR values; persistence and participant synchronization remain in `features/encounter-editor`.
- Converted the session page model to TypeScript with explicit loaded-session, sync-event, checklist, entity-list, scene, note, navigation, and cross-feature setter contracts plus a typed page entry declaration. Session results from scope movement and encounter creation now pass through typed normalization adapters before entering page state. Session NPC/location normalization moved into a pure typed page-model module with regression coverage for UTF-8 names, internal-field stripping, `_aiIgnored`, notes, and location defaults; editing/history/persistence, encounter creation, and scope movement remain in their owning feature slices.
- Converted the campaign page model to TypeScript with explicit campaign/entity/session/sync/history/archive/AI and graph-note contracts plus typed page and graph entry declarations. Campaign graph projection and deterministic force layout are now strict TypeScript behind the Node-safe `graph.js` facade, with official D3 force declarations supplied by `@types/d3-force`. Campaign state sanitization/history helpers are typed behind their existing JavaScript compatibility facade. This completes the remaining JavaScript model implementations under `src/entities`, `src/features`, and `src/pages`; compatibility entrypoints remain where Node or JavaScript consumers require them.
- Started bounded UI-contract adoption with the localized status-badge feature. Its completed state, optional span click handler, class name, and public component contract are strict TypeScript while the feature ownership and JavaScript public runtime entrypoint remain stable.
- Extended UI-contract adoption through clipboard feedback and its shared presentation dependencies. `ClickToCopy`, `Notification`, and `Tooltip` now own strict React/event/timer/DOM contracts; nested tooltip registry/timer coordination and viewport-position derivation were extracted into a Node-safe typed model with focused regression coverage. Anchored portal positioning, drag-mode cancellation, clipboard success feedback, and public JavaScript entrypoints remain behavior-compatible.
- Migrated the shared icon/button/checkbox control boundary to strict TSX. Icon names are derived from the closed SVG catalog, button variants/sizes/native attributes/ref/static `Button.SIZES` API are explicit, and checkbox controlled state/native input props are typed. Button size normalization and class/stroke derivation moved into a Node-safe typed model with regression coverage for defaults, legacy aliases, invalid fallbacks, disabled classes, and create/small strokes. Same-slice imports and lint boundary patterns now follow the TSX owners while the JavaScript public barrel remains stable.
- Migrated shared panel, collapse-toggle, and switch primitives to strict TSX. Section content/classes, collapse rotation/size/click contracts, and controlled switch label/description/native-input behavior are explicit. Optional switch text rendering was split into a focused presentation helper, and public declarations plus import-boundary patterns now match the TSX owners.
- Migrated shared list-card and search-highlighting presentation to strict TSX. Modifier-aware link/button click decisions and regex-safe, case-insensitive highlight splitting moved into Node-safe typed models with regression coverage for plain/modifier links, missing callbacks, non-link cards, nullish/numeric text, casing, and regex metacharacters. Article drag attributes, active/action composition, original text casing, and the JavaScript public barrel remain stable.
- Migrated the shared Select controller to strict TSX. Strict option selection and first-option fallback, compatibility change-event construction, viewport-clamped portal placement, upward-opening rules, and selected-option scroll targeting moved into a Node-safe typed model with focused regression coverage. Shared portal outside-click/resize/scroll listener ownership is now a typed same-slice hook reused by Select and MultiSelect; refs, portal rendering, animation-frame scheduling, disabled behavior, and the JavaScript public barrel remain stable.
- Migrated the shared MultiSelect controller to strict TSX. String-normalized identity, selected-count derivation, option-order toggle/single selection, delegated option-click behavior, close/keep-open plans, active-option scroll targeting, and its 340px height policy moved into a Node-safe typed model with focused regression coverage. Select and MultiSelect now share a single typed viewport-clamping model and portal lifecycle hook while retaining their distinct value/event and maximum-height contracts.
- Migrated the shared DraggableList controller to strict generic TSX. Default render-key/ID/index precedence, the five-pixel pointer threshold, bounded immutable reorder, and key-based final-order comparison moved into a Node-safe typed model with focused regression coverage. Pointer capture, handle and interactive-target filtering, DOM hit-testing, preview animation, body modes, click suppression, custom cross-list payload events, and reorder/drop callback timing remain in the controller with explicit item/pointer/preview state contracts.
- Completed strict TypeScript implementation coverage for `src/shared/ui`: every component, controller hook, and pure model is now `.tsx`/`.ts`. The JavaScript `index.js` remains the runtime compatibility/public import boundary, paired with its typed declaration facade; no legacy shared UI implementation remains behind the barrel.
- Migrated the complete notes UI feature slice to strict TSX. Note-card collapse eligibility, simplified/classic presentation, preview truncation, real/virtual filtering, and bulk-collapse intent moved into a Node-safe feature model with focused regression coverage. AI-ignore drag-control composition and editable note rendering retain their existing behavior behind the JavaScript UI barrel and a typed declaration facade; editor UI, rich-content, and shared note-key public contracts are now explicit for typed consumers.
- Migrated the complete entity-link UI slice to strict TSX, completing TypeScript implementation coverage for the feature behind its JavaScript browser/model compatibility barrels. Scope providers, inline-link modal state/events, resolver-rendered React content, and generic modal props now use entity-owned contracts. Location/NPC/character modal presentation moved into the Node-safe model with focused coverage, and nullable route slugs plus the modal feature public contract are explicit for typed consumers.
- Migrated the complete app-aware modal feature to strict TSX. Promise API construction, lazy localized status formatting, prompt/checkbox confirm-value derivation, standard-modal focus targeting, Enter/Escape planning, and cancel-disabled close routing moved into a Node-safe typed model with focused regression coverage. Portal/listener/focus effects and the global store-backed message-box adapter remain in UI; message-box title/message contracts are now strings, and consumers retain the JavaScript public barrel plus typed facade.
- Migrated the campaign-creation modal to strict TSX. Create, archive-import, and close callbacks now have an explicit feature-owned public contract; file selection and input refs use native DOM types, accepted archive formats and field-reset behavior remain unchanged, and campaign-name normalization stays with the app orchestration that executes the create command.
- Migrated the player-question modal to strict TSX. Standard-die factorization, deterministic roll-formula construction, bounded numeric search, and unknown dice-result narrowing moved into a Node-safe typed feature model with focused regression coverage. Virtual-list refs, two-frame positioning, animated centering, debounce preference, result-ID deduplication, and dice dispatch remain behavior-compatible in UI; external consumers use the feature public barrel.
- Migrated `RollDice` and the probability-modal portion of the dice feature to strict TSX. Multiplication-glyph normalization, legacy truthy-context payload wrapping, probability precision labels, and relative bar-width derivation moved into a Node-safe typed model with focused regression coverage. Click cancellation, tooltip text, probability calculation limits, empty-state behavior, chart rendering, and the public feature barrel remained stable, establishing the contracts used by the calculator migration.
- Completed strict TypeScript implementation coverage for `features/dice` by migrating `DiceCalculator`. Unknown store requests now narrow through a typed request decoder; shortcut, die-button formula, capped history, history replay payload, recharge state, single-die detection, current-formula selection, and breakdown-label policies live in the Node-safe dice model with focused regression coverage. DOM outside-click exceptions, panel unmount timing, two-frame roll animation, player-question auto-close, modal composition, localized presentation, and dispatch behavior remain in the controller. The JavaScript public barrel remains the supported runtime import boundary with a matching typed facade.
- Migrated `ColorThemeSwitcher` to strict TSX and added typed settings model/UI facades. Next-theme and icon selection now live with the existing theme normalization contract and are regression-covered; tooltip copy and the API/model-safe versus browser-UI entrypoint split remain unchanged. Phase 137 supersedes the former store fallback: the switcher is controlled through `theme` and `onToggle`, with persistence and global-store integration owned by its Settings runtime/controller composition.
- Removed the duplicated campaign-scope option rendering reported by Fallow from the settings controller. A private typed `CampaignScopeOptions` composition now serves both source and AI-prompt selectors without entering the public UI API; each selector retains its own global option and state behavior.
- Completed strict TypeScript implementation coverage for `features/settings` by migrating `SettingsModalContent`. Store campaign values, nullable source APIs, source catalogs, prompt maps, campaign ignore-source maps, selected-scope recovery, sanitized save payloads, and PATCH response fields now narrow through a Node-safe typed settings-modal model with focused regression coverage. React retains synchronization effects, API orchestration, save statuses, notifications, and shared-control composition; Phase 137 supplies current global values and optimistic commands through `SettingsModalRuntime` rather than direct selectors or dispatch. Empty PATCH responses still enter the failure path, campaign/global fallbacks remain stable, and JavaScript API/UI barrels retain their model-safe versus browser-only split with typed facades.
- Migrated the encounter add-monster modal to strict TSX and removed its client-side whole-session read/modify/write flow. A focused `POST .../encounters/:encounterId/monsters` client method now delegates to a session application command that validates the participant object, locates the encounter, appends without dropping existing monsters, preserves encounter identity, and persists through `SessionRepository`; command behavior and error statuses are regression-covered. Active-campaign/session/encounter discovery and stable target IDs moved into a typed encounter-editor model with nullable API normalization coverage, while loading/error UI, submit locking, reload dispatch, and modal closure remain in the controller.
- Completed strict TypeScript implementation coverage for `features/ai-edit-monster`. At this checkpoint the action chooser, edit modal, draft-response adapter, and aggregate modal composition shared feature-owned monster, model, attachment, history-resource, diff, ref, and callback contracts. Mode-specific presentation moved into a Node-safe typed model with focused regression coverage, while active-request cancellation, optional attachments, injected response composition, resource-level apply/undo, and the JavaScript public barrel remained behavior-compatible. A typed AI UI facade exposed the attachment state contract and shared immediate-child model-option rendering without moving browser UI into the model-safe AI entrypoint; the latter also removed duplicate option composition from the image-prompt picker. Phase 132 supersedes this UI-ownership checkpoint by retaining policies/action choice in the feature and moving the cross-feature edit/draft shell to the encounter page owner.
- Completed strict TypeScript implementation coverage for `features/edit-monster`. The former 781-line mixed controller was split into a Node-safe typed edit model and a TSX modal composition with a typed public facade. Field extraction/update rules, dice-average calculation, scalar/structured speed conversion, 5eTools type-choice preservation, legacy `desc` versus `entries` action mutation, action collection edits, Ctrl/Cmd+K rule-reference insertion, JSON-object parsing, name validation, deep cloning, and source restoration are regression-covered. The critical field-update complexity hotspot was replaced with focused field updaters, reference content remains injected from the widget layer, and ESLint now enforces the feature public import boundary.
- Completed strict TypeScript implementation coverage for `features/rules-reference`. Link navigation now resolves through typed per-reference resolver tables; preview loading returns spell/creature/reference data contracts through typed loader tables; and tooltip parsing returns text/roll descriptors before TSX rendering. Recursive list/section/table rendering, stale async-load suppression, localized click errors, token-image fallback, and interactive dice remain in UI. Browser-only source/spell formatters are injected into the Node-safe model, preserving the JSON-free reference entrypoint. Focused tests cover spell/creature/status navigation, UTF-8 creature previews, source/type/CR/AC/HP metadata, skill metadata, tagged labels, hit/damage/recharge roll descriptors, and the feature has no remaining JavaScript implementation.
- Completed strict TypeScript implementation coverage for `features/ai/ui`: shell/modal composition, toolbar generation controls, Gemini API-key setup, context settings, image/file attachments, prompt/token summary, virtualized response history, model options, and the history-response adapter are now typed. Toolbar visibility and encounter/custom-monster coupling, API-key decisions, optional token rows, empty-history visibility, attachment capacity/deduplication/removal/validation, and session/scene context defaults live in a Node-safe typed presentation model with focused regression coverage. Attachment processing preserves MIME and 10 MB validation, four-item limits per kind, gallery URLs, duplicate suppression, partial-selection alerts, and base64 previews. Context contracts now describe campaign entities, hydrated sessions, scenes, and per-field configuration instead of relying on arbitrary objects. Fallow identified the initial toolbar controller as a critical complexity hotspot, so it was reduced to thin orchestration over private model/context/entity/parsing/encounter action groups; no critical AI UI toolbar finding remains. A shared injected-response contract replaces duplicate modal shapes across the assistant and AI monster editor. Browser UI remains behind `features/ai/ui/index.js`; the Node-safe model/API `features/ai/index.js` is unchanged.
- Completed strict TypeScript implementation coverage for `features/editor/ui`: `Input`, `EditableField`, and mention-picker composition now expose feature-owned React/event/ref/entity contracts through the browser-only JavaScript barrel and typed facade. Mention filtering/grouping, initial raw/preview cursor mapping, square-bracket paste detection, tab/mention/format/block text edits, value normalization, and input/Lexical shortcut decisions moved into a Node-safe typed presentation module with focused regression coverage, including Ukrainian keyboard-layout aliases. Lexical nodes, selection commands, external update tags, application versus editor history routing, formatted clipboard behavior, mention boundaries/tooltips, and entity-modal resolution remain behavior-compatible in the controllers. Fallow's initial four critical editor complexity findings were split into focused policies and command executors; the scoped editor UI report now contains no critical findings. `Input` and `EditableField` remain app-coupled and are not promoted to `shared`.
- Completed strict TypeScript implementation coverage for `features/images`. `ImageAssetField`, `ImageDropzone`, `ImageTargetSettings`, `ImageGallery`, the gallery hook, API client, category configuration, and all policy modules are typed behind the JavaScript compatibility barrel and typed facade. Encoded URL/target presets, filename/source/campaign/result policies, folder navigation, path/history/deduplication/row projection, runtime-validated drop payloads, grouped move plans, readonly-aware range selection, drag plans, and nullable local/scoped/database loading live in focused models with UTF-8 regression coverage. The former 1200-line gallery controller was split into private folder/image renderers and navigation/search/actions/grid/dialog compositions; its hook now orchestrates the extracted loading and interaction plans. Fallow's UI and six hook critical hotspots are eliminated, and the full `features/images` scoped report has zero critical findings. Readonly official tokens, multi-selection, drag/drop, rename/move/delete, search scopes, virtual rows, storage stats, fullscreen preview, upload cleanup/locking, and nullable HTTP results remain explicit. The invalid `move` icon reference that rendered no icon was replaced with a valid catalog entry. The ambient `react-list` contract moved from the player-question feature into `src/types`, and no temporary UI declaration bridge remains.
- Completed strict TypeScript implementation coverage for `features/rich-content`. The former mixed recursive renderer is now a typed UI composition over `model/richContentPresentation.ts`, where ordered token handlers produce discriminated roll, damage, rules-reference, or text plans. List, section, table, and generic object inputs are narrowed at runtime; Markdown escaping, query highlighting, mentions, creature source fallback, recharge behavior, hit/damage formatting, condition/status links, quick references, and Ukrainian surrounding text remain regression-covered. Browser consumers use the public JavaScript UI barrel and typed facade; Node/model tests use the JSON-free `model.js`/`model.d.ts` facade. ESLint blocks private model/UI imports, unstable random React keys were removed, and the two former critical parser/recursive-renderer complexity hotspots were eliminated. The scoped Fallow report has zero critical findings.
- Completed strict TypeScript implementation coverage for `widgets/bestiary-browser`, extending the Bestiary FSD pilot through its reusable browser composition. Canonical case-insensitive monster/source identity, explicit reference lookup, source/sync/API/AI envelope narrowing, legendary-group enrichment, favorites and search filtering, fractional CR sorting, official-first selection, custom refresh/update selection, runtime-validated JSON imports, case-insensitive merge replacement, edit payloads, AI instruction planning, abort detection, and draft metadata preservation moved into a JSON-free typed model facade with focused Ukrainian-data regression coverage. The former mixed list/detail controller is now strict TSX with private toolbar, virtual-list, detail, and row compositions; `null` replaces the legacy empty-string selection sentinel and stable identity keys are used consistently. Supporting public declarations now expose the existing AI model facade, composed monster editor/response/stat-block/assistant contracts, missing shared download/deep-search declarations, and the complete Bestiary API result types. ESLint enforces the widget public boundary, and Fallow's 12 original critical browser hotspots are eliminated with zero scoped critical findings and no suppressions.
- Completed strict TypeScript implementation coverage for `widgets/monster-stat-block`, continuing the Bestiary pilot through its rich reusable renderer. Legacy URL spell loading/cache and level grouping, structured 5eTools spellcasting narrowing, action/content arrays, sense-reference tokenization, AI changed-field classes, custom/override/official token-source precedence, upload-result validation, mutation identity, dropzone visibility, and drag MIME payloads moved into a JSON-free typed model facade with focused UTF-8 regression coverage. The former 851-line controller is now thin TSX orchestration over private token, name/meta, stats, ability, legacy/structured spellcasting, action, and content sections. The public token callback contract was corrected to the behavior-compatible `(monster, imageUrl)` signature, nullable Bestiary API results and weak store campaign values are narrowed at the owning boundary, and async spell loading has an unmount guard. ESLint now protects both private model and UI paths. Fallow's two original critical findings (`MonsterStatBlock` and `handleCustomTokenUpload`) are eliminated with zero scoped critical findings and no suppressions.
- Completed strict TypeScript implementation coverage for `widgets/campaign-entity-card`. Character/location card-data and collapse plans, canonical display names, AI field/note highlights, immutable note-ignore updates, deterministic character/NPC/location drafts, and required-name validation moved into a JSON-free typed model facade with focused Ukrainian regression coverage. The two former mixed card controllers are split into typed headers, character identity/details, location composition, and a shared private notes renderer; both creation modals are strict TSX and retain feature-owned submit commands. Entity card models now preserve their concrete entity type through generic note mutation, and the shared note-render declaration exposes virtual-note metadata without weakening callers. An obsolete `strokeWidth` create-button prop was removed because the typed shared Button never applied it to the icon. ESLint protects private model/UI paths, all four legacy JSX implementations are gone, and Fallow's two original critical card findings are eliminated with zero scoped critical findings and no suppressions.
- Completed strict TypeScript implementation coverage for the spell presentation vertical: `widgets/spells-browser` and `widgets/spell-card`. Reference-key identity, nullable source/spell/settings envelopes, campaign source settings, class/school option derivation, source/level/class/school/basic/detailed predicates, stable level/name sorting, sort-state transitions, displayed/all-list initial selection, source-filter recovery, and safe error messages moved into a JSON-free typed browser model with focused Ukrainian regression coverage. The former 515-line browser is now typed orchestration over private controls and virtualized list/detail composition; filter state is derived rather than mirrored through an effect, async source/data loads have cancellation guards, and `null` replaces the empty-string deselection sentinel. `SpellRecord` now extends the entity-owned `SpellData` contract, while the strict TSX spell card retains localized model labels and recursive rich content. ESLint protects the browser's private model/UI paths. Fallow's three original critical browser findings are eliminated with zero scoped critical findings and no suppressions.
- Completed strict TypeScript implementation coverage for `widgets/rules-reference-modal`. The seven-tab catalog, nullable official/custom Bestiary aggregation, source-qualified spell and creature identities, exact/fallback creature ranking, simple versus recursive detailed search, selection matching, and insertion-tag construction moved into a JSON-free typed model facade with focused Ukrainian regression coverage. The former 920-line modal controller is now typed orchestration over private search/tab/detail, virtual-list, and Bestiary row compositions; embedded spell selection, `forceTab`, global back/forward history, lazy/global loading, tab match indicators, scroll-to-selection, token fallback, and reference insertion remain behavior-compatible. Browser consumers use the typed public composition facade, Node consumers use `model.js`, and ESLint protects both private model and UI paths. Fallow's four original critical findings are eliminated with zero scoped critical findings and no suppressions.
- Completed strict TypeScript implementation coverage for `widgets/campaign-search`. Recursive searchable-text extraction, hidden/image field exclusion, campaign/session result indexing, nullable collection normalization, injected API hydration, four-filter state, non-empty filter toggling, normalized matching, 80-result limiting, snippet windows, highlight terms, and stable navigation targets moved into a JSON-free typed model facade with focused Ukrainian regression coverage. The former 586-line mixed modal is now typed orchestration over a private mention-aware Markdown/results composition; campaign in-memory entities still take precedence over fetched collections, every listed session is hydrated before indexing, `code`/`pre` content avoids mention rewriting, result rows retain keyboard activation guards, and delayed hash scrolling plus cleanup remain browser-owned. ESLint protects private model/UI paths, and Fallow's three original critical findings are eliminated with zero scoped critical findings and no suppressions.
- Completed strict TypeScript implementation coverage for `widgets/campaign-entity-modal`. Modal-entity slug narrowing, transient-field and note sanitization, normalized mention-name comparison, rename-confirmation planning, character/location card selection, stable card keys, and campaign-scope ownership moved into a JSON-free typed model facade with focused Ukrainian regression coverage. The provider now composes a private typed modal-card renderer over the existing campaign-entity and entity-link public contracts; parent resolver precedence, session-scope refusal, optimistic local editing, refresh dispatch, confirmed mention-reference updates, cancelled rename baselines, expanded headerless cards, and close-after-delete behavior remain intact. ESLint protects private model/UI paths, and Fallow's original critical modal-content finding is eliminated with zero scoped critical findings and no suppressions.
- Completed strict TypeScript implementation coverage for `widgets/sidebar`. Campaign grouping, active/completed group recomposition, slug-to-index order payloads, campaign-root toggle navigation, expanded/mobile class projection, keyboard activation, and safe async error messages moved into a JSON-free typed model facade with focused Ukrainian regression coverage. The former 487-line mixed controller is now typed orchestration over private campaign, navigation-link, resource, and archive compositions; responsive hover/pin/mobile behavior, archive import strategy selection, backup download naming, gallery/settings/player-question modal flows, and forced Bestiary/Spells tabs remain browser-owned. The campaign API reorder contract was corrected from an array declaration to the object map required by the backend command. ESLint protects private model/UI paths, and Fallow's original critical sidebar finding is eliminated with no scoped critical findings or suppressions.
- Migrated the `widgets/ai-assistant` image-prompt picker to strict TypeScript as an independent slice of the larger assistant controller. Detail/selection mode, context-request requirements, generation eligibility, stable item fallback keys, custom-monster type/CR summaries, and session-qualified scene keys/descriptions moved into a JSON-free typed model facade with focused Ukrainian regression coverage. The former 267-line mixed picker is split into a thin modal shell, details form, selection composition, and generic target column; attachments, immediate-child AI model options, targetless continuation, Bestiary image/no-image groups, campaign scene hiding, and injected target builders remain behavior-compatible. The obsolete `required` attribute on the contenteditable-based `EditableField` was removed because eligibility is enforced by the typed picker policy. ESLint protects private model/UI paths, and Fallow's original critical picker finding is eliminated with zero scoped picker critical findings and no suppressions; the larger assistant panel remains a later Phase 8 slice.
- Began the larger `widgets/ai-assistant` panel migration with a strict, injected-localization presentation contract. Markdown-free response previews, numeric/string/Date formatting, nested and escaped JSON prompt cleanup, retry request recovery, mode/generation/context summaries, history detail rows/titles/state labels, character/location names and keys, scene titles/descriptions, and image preview truncation moved out of the 1,300-line JSX controller into the Node-safe typed facade with focused Ukrainian regression coverage. Existing feature-owned failed/history-change predicates remain injected instead of being duplicated. This removes three panel-local critical helpers (`getCharacterDisplayName`, `getHistoryOptionsSummary`, and `getHistoryContextSummary`) plus several high/moderate helpers, reducing the panel's scoped critical findings from nine to six without suppressions. Async generation, apply/restore, API-key refresh, retry, and the main composition remain explicit follow-up slices.
- Extracted Gemini API-key persistence and model-refresh retry orchestration from `AiAssistantPanel` into `features/ai/model/apiKeyWorkflow.ts`, where it belongs beside typed model discovery. The dependency-injected command trims and rejects empty keys, persists once, retries discovery up to five times with four 500 ms waits, preserves null-response behavior, reports only the terminal refresh error, treats refresh exhaustion as non-fatal after a successful save, normalizes model arrays, and selects the server default or first model. Focused tests cover eventual success, missing input, permanent discovery failure, null discovery, and fatal key-save failure. The panel now owns only localized errors and React state transitions; Fallow reduces `handleSaveApiKey` from critical (CC 14/CRAP 210) to moderate (CC 6/CRAP 42), lowering panel critical findings from six to five without suppressions.
- Extracted AI history retry planning and execution from `AiAssistantPanel` into the typed `features/ai/model/historyWorkflow.ts` boundary. The retry plan now owns eligibility, failed-entry deletion identity, payload reconstruction, Bestiary type fallback, and image parsing policy; the dependency-injected executor owns delete-before-generate ordering and explicit success/cancel/failure outcomes. React retains request IDs, `AbortController`, localized notifications, visible history updates, and generated-result application through lifecycle callbacks. Focused tests cover blocked retries, failed-entry deletion order, successful generation, abort cancellation, and failure propagation. Fallow eliminates the former critical `retryResponseHistoryEntry` hotspot (CC 19, cognitive 21, CRAP 380) and leaves four panel critical findings; the new plan/executor boundary has no critical findings (`buildAiHistoryRetryPlan` is moderate and `executeAiHistoryRetry` is high), with no suppressions. The older retry-payload reconstruction helper remains a separate follow-up hotspot.
- Extracted generated-result interpretation from `AiAssistantPanel` into the typed `features/ai/model/generationResultWorkflow.ts` plan/executor boundary. Prompt history fallback identity, prompt/draft/update precedence, malformed-update rejection, campaign-versus-session direct-apply rules, campaign reload decisions, generated/history entity refreshes, prompt clearing, dialog closure, and notification kinds are now regression-covered policies. The callback executor preserves history-before-presentation/application order while React retains localization and state/dispatch effects. `AiGenerationResult` now explicitly exposes the untrusted draft/generated/updated fields, which the workflow narrows at runtime. Fallow eliminates the former critical `handleGeneratedAiData` hotspot (CC 26, cognitive 23, CRAP 702), reduces panel critical findings from four to three, and reports no critical findings in the new workflow; no suppressions were added.
- Extracted application-state projection for updated AI data into `features/ai/model/updatedDataWorkflow.ts`. A typed plan now validates record-shaped updates, resolves explicit or generated/history entity types, derives Bestiary monster sync identity, selects encounters by normalized ID, preserves session filename fallbacks, merges campaign updates over current state, and distinguishes applied versus reload-only results. Its callback executor preserves state-update, reload, sync, and refresh ordering. Focused tests cover Bestiary, session/encounter, campaign, malformed, and foreign-route flows. Directly applied generation results no longer dispatch the redundant second entity refresh. The architectural source assertion now verifies Bestiary sync ownership in the feature model and panel delegation through the public boundary. Fallow eliminates the former critical `applyUpdatedAiData` hotspot (CC 20, cognitive 20, CRAP 420), reduces panel critical findings from three to two, and reports only moderate findings in the new workflow with no suppressions.
- Extracted AI generation transport execution into `features/ai/model/generationExecution.ts`. The typed command now returns explicit success/cancelled/API-key-missing/failed outcomes, forwards the original abort signal, captures a persisted failed-history response before classifying API-key failures, normalizes unknown error message/status fields, and formats status-aware alert details. Required callbacks make every lifecycle effect explicit; React retains request inputs, controller/reference cleanup, request IDs, localized fallback copy, result application, and lifecycle dispatch. Focused tests cover signal forwarding, callback order, success data, aborts, API-key failures with history, ordinary status failures, and alert formatting. Fallow eliminates the former critical `generate` hotspot (CC 10, cognitive 11, CRAP 110); the new executor has no finding and its only scoped helper finding is moderate. The aggregate `AiAssistantPanel` component is now the sole remaining panel critical finding, with no suppressions.
- Began splitting the remaining aggregate `AiAssistantPanel` finding by extracting its complete image-prompt state and orchestration cluster into private strict TypeScript hooks. Picker open/selection/context state, prepare-before-open, target selection/reset, targetless validation, generation request shaping, registered Bestiary monster actions, and builder composition now live outside the JSX controller. Pure campaign/session NPC/location selection, session-qualified scene projection, custom-monster sorting/image grouping, target titles, and generation plans extend the Node-safe `imagePromptPicker.ts` model with focused coverage. The typed AI facade now exposes the existing image-target builder contracts required by the widget hook; the hooks remain private and are not added to the Node-safe model facade. Fallow reduces the aggregate panel from 1,219 lines, CC 47, cognitive 38, CRAP 2256 to 1,104 lines, CC 34, cognitive 28, CRAP 1190. The extracted image-prompt scope has zero critical findings and no suppressions; additional history/context/view splits remain before the aggregate finding is cleared.
- Continued the aggregate `AiAssistantPanel` split by extracting context loading, expanded-session hydration, immutable context-list/config commands, normalized list projection, and route-specific context selection into the private strict `useAiAssistantContextController.ts` hook. The hook composes the existing feature-owned context loader instead of duplicating API ownership; pure campaign/session/encounter/Bestiary projection moved to the Node-safe `assistantContext.ts` facade with focused route and malformed-input coverage. Bestiary still produces null campaign context and empty session data, campaign mode merges loaded entity collections, session defaults and lazy hydration remain intact, and loading is cleared on both success and failure. Fallow reduces the aggregate panel from 1,104 lines, CC 34, cognitive 28, CRAP 1190 to 1,018 lines, CC 19, cognitive 21, CRAP 380. The panel remains a critical aggregate component, but neither the private controller nor the pure projection has a critical finding; no suppressions were added. History/view composition remains the next bounded split.
- Completed the aggregate `AiAssistantPanel` decomposition by extracting history selection, loading/stats, clipboard state, feature-command wiring, apply/undo restoration, and route filtering into the private strict `useAiAssistantHistoryController.ts` hook. Pure history view, localized confirmation, error, route, title, and prompt-placeholder policies live in the Node-safe widget model with focused campaign/session/encounter/Bestiary and Ukrainian-data coverage. A private typed `AiAssistantPanelView.tsx` now composes the shell, toolbar, API-key setup, context modal, history response dialog/list, prompt composer, notifications, and error view from explicit prop groups; retry transport and generation lifecycle remain in orchestration because they share request cancellation and generated-result application. History loading now ignores stale effect completions while preserving stats fallback, clipboard HTML/plain behavior, selected-entry synchronization, partial/full apply/undo copy, direct-route restore versus reload, and entity refreshes. Fallow reduces the panel from 1,018 lines, CC 19, cognitive 21, CRAP 380 to 816 lines, CC 8, cognitive 7, CRAP 72. The panel and all newly extracted history/view/route modules have zero critical findings and no suppressions, clearing the last `widgets/ai-assistant` aggregate critical hotspot.
- Completed strict TypeScript implementation coverage for `widgets/ai-assistant` by converting the final `AiAssistantPanel.jsx` orchestration to `AiAssistantPanel.tsx` and moving its public prop contract to the implementation owner behind the unchanged JavaScript compatibility barrel. The compiler made nullable/numeric navigation IDs, optional session filenames, context-session adapters, attachment payload indexability, model/history/generation results, abort-controller refs, notification state, nullable modal commands, and unknown retry/API-key failures explicit. Campaign notes are narrowed to record-shaped values before token estimation, fetched sessions gain validated filenames or request-key fallback, and partial history apply/undo callbacks adapt without weakening feature command contracts. The AI public declaration facade now matches its existing runtime exports for generation lifecycle/request/token/history/context-image workflows. Retry failure normalization and status formatting moved into the feature-owned history workflow with focused malformed/status/history-entry coverage; retry payload fallback construction was split into a focused non-parsed request builder, eliminating both the new normalization hotspot and the previously recorded critical retry-payload helper. Fallow reports `AiAssistantPanel.tsx` at 829 lines, CC 8, cognitive 7, CRAP 72, with zero critical findings across the complete widget plus the touched history workflow and no suppressions.
- Completed strict TypeScript implementation coverage for `widgets/ai-response-modal`. Snapshot formatting/parsing, preview/card/note classification, encounter participant identity and metadata, changed-field projection, nested session/campaign/entity/custom-Bestiary draft lookup, immutable nested replacement, and history resource identity live in a Node-safe typed model with focused UTF-8 and nested-session regression coverage. A private typed draft controller owns resource synchronization, JSON edits, preview resolution, save-before-apply, resource apply/undo, localized error narrowing, and view-mode reset. The former 1,100-line mixed JSX controller is now typed orchestration over focused encounter, note, card, one-sided snapshot, changed-field, and JSON preview renderers plus a private `AiResponseModalView.tsx` for toolbar, details, diff switching, and draft editing. Response detail rows and React-Markdown components are explicit feature UI contracts; preview monsters, campaign cards, notes, editor state, callbacks, and nullable refs are narrowed at their owning boundaries. Matching retains `listIndex` → `id` → `instanceId` → structural fallback and JSON cloning semantics. The JavaScript public barrel remains stable, no JavaScript widget implementation remains, and Fallow reduces the original 12 critical findings to zero across the complete slice without suppressions.
- Completed strict TypeScript implementation coverage for `widgets/monster-editor-modal`, removing the final JavaScript implementation under `src/widgets`. The widget remains a thin composition boundary that injects `RulesReferenceModalContent` into the feature-owned monster editor, but its public contract now omits that internal dependency. A typed selection adapter bridges the rules-reference modal's concrete selection to the feature's extensible insertion contract. The compiler exposed that successful JSON saves previously validated names through string coercion while retaining a potentially non-string value; `prepareMonsterDraftForSave` now returns feature-owned `NamedMonsterData`, preserves valid string names, normalizes other accepted values to strings, and the generic clone helper retains the narrowed result type. Focused regression coverage locks numeric JSON-name normalization, all consumers compile without casts, and scoped Fallow remains at zero critical findings without suppressions.
- Migrated the campaign route composition from `CampaignPage.jsx` to strict `CampaignPage.tsx` behind the existing page barrel and direct typed facade. Weak active-campaign store values, session filenames, hash targets, character/NPC cross-list drops, card notes, entity keys, and section visibility/collapse state now narrow through the typed `campaignPagePresentation.ts` boundary with focused UTF-8 regression coverage. Header, sessions, description, notes/list/graph switching, repeated character/NPC/location sections, and search/partial-archive dialogs were split into focused private components; shared entity-section composition now owns collapse, bulk collapse, reorder, and persistence wiring without moving domain commands out of `useCampaignView`. Empty sections remain expanded, stored collapse flags apply only when content exists, partial archive operations retain busy locking and cleanup, and malformed sessions without filenames no longer produce `undefined` navigation targets. Fallow reduces the original aggregate `CampaignView` finding from CC 36/cognitive 32/CRAP 1332 to no finding and reports zero critical findings across the new page/presentation slice without suppressions. The private graph and partial-archive JSX implementations were queued here for the independent migrations completed below.
- Migrated the private campaign `PartialArchiveModal` to strict TSX and removed its temporary declaration bridge. The five supported section IDs now form an entity-owned closed contract reused by campaign API export/import methods, `useCampaignView`, route dialog callbacks, and the modal instead of unrelated `string[]` declarations; server runtime normalization remains authoritative for external input. A focused page policy owns immutable toggles and canonical request ordering with regression coverage, while localized labels, file input reset, busy/empty disabling, and asynchronous callback composition stay in the modal. `CampaignImportResult.sections` now reflects the same trusted frontend contract. TypeScript exposed the modal's required hidden-footer confirm callback, which now closes consistently through the existing cancel path. Scoped Fallow reports no findings in the modal, selection policy, or touched campaign API contract, and the campaign private UI now has only the graph JSX implementation remaining.
- Migrated the final private campaign JSX implementation, `CampaignNotesGraph`, to strict TSX and removed its declaration bridge. Filter/search visibility, source-label formatting, edge presentation, minimap geometry, open-target resolution, and note-save request narrowing now live in the typed `campaignGraphPresentation.ts` policy boundary, while React Flow state, topology/layout effects, drag collision, debounced note editing, and focused private toolbar/canvas/details compositions remain in UI. Session contracts now expose scoped NPC/location collections used by graph navigation, graph metadata records scene identity explicitly, and the Node-safe graph declaration matches its runtime facade. Runtime narrowing rejects invalid note and scene identities instead of relying on casts. Focused UTF-8 regression coverage locks nested scene-note labels, neighbor-expanded search, edge handles/presentation, minimap bounds, scoped entity opening, and note-save normalization. Fallow eliminates both remaining critical hotspots—the source formatter and edge mapper—and reports zero scoped critical findings without suppressions; no campaign page JSX implementations remain.
- Completed strict TypeScript implementation coverage for the session page by migrating `SessionPage` plus all five private scene/todo components from JSX to TSX and replacing the loose page declaration with its implementation-owned export. Pure keyboard routing, realtime-sync reload/discard decisions, meaningful-note and collapse state, first-scene encounter quick-access projection, localized scope-import copy, hash-driven note expansion, and scene-note bulk-collapse plans now live in `sessionPagePresentation.ts` with focused UTF-8 regression coverage. Session NPC/location normalization now validates primitive IDs, string image URLs, and record-shaped notes, supplies complete card-note fields, preserves supported unknown fields, and strips internal fields except `_aiIgnored`; entity-owned session scene/note contracts now match their editor consumers. The page is split into focused header, notes, NPC, location, scene, checklist, scope-import, and floating-action compositions, with scene notes separated from the card shell. Hidden-footer modals now provide explicit close-through-confirm behavior. Fallow reduces `SessionView` from CC 25/cognitive 22/CRAP 650 to CC 9/cognitive 6/CRAP 90, eliminates the critical scene-card and model findings, and reports zero critical findings across the complete session page slice without suppressions. No JSX implementations remain under `pages/session`.
- Completed strict TypeScript implementation coverage for the encounter page by migrating `EncounterPage.jsx` to `EncounterPage.tsx` and replacing its loose declaration with the implementation-owned export. Grid grouping/selection, relative HP edits, keyboard/history routing, realtime synchronization, participant selection, AI monster replacement, dice-result HP updates, and fractional/structured CR parsing now live in focused typed page policies. Custom-monster field-save planning, identity preservation, duplicate-name handling, injected persistence, AI mode/prompt construction, request payloads, generated-result narrowing, and restore scope live at the `features/ai-edit-monster` boundary. The page composition is split into focused metrics/header, participant combat stats/actions, monster detail/grid, Bestiary, character-picker, and notification components. Fallow reduces the original `EncounterView` from CC 32/cognitive 33/CRAP 1056 and eliminates all thirteen original critical encounter findings; the complete encounter page/model slice now reports zero critical findings without suppressions. No JSX implementations remain under `pages/encounter`.
- Completed strict TypeScript implementation coverage for the application shell by migrating `App`, `MainContent`, `ProjectGuide`, and `main` to TSX and converting realtime synchronization to TypeScript behind its compatibility barrel. Settings projection, editable-target detection, mention callback validation and UTF-8 entity-option normalization, campaign completion-date planning, sync-event classification, and unknown-error extraction live in the typed `app/model/appShellPresentation.ts` boundary with focused regression coverage. `App.tsx` retains HTTP/store/modal/router/effect orchestration; routing retains campaign/session/encounter page ownership; the entrypoint retains service-worker registration, cache clearing, update activation, and reload fallbacks. TypeScript exposed and removed an obsolete Sidebar `className` prop that was never consumed. Fallow reduces the mention-picker controller from its migrated critical CC 10 finding to CC 9 and reports zero critical findings across the app shell without suppressions. There are now zero `.jsx` implementations anywhere under `src`.
- Refactored the AI image-target model into thin public builders over focused field fallback, identity, collection normalization, encounter lookup/summary, and monster-ability policies. NPC slug fallback, zero-valued levels and abilities, legacy custom-monster `desc`, default `CUSTOM` source, session-qualified scene metadata, string-normalized encounter IDs, encounter-index fallback, ignored-note filtering, the eight-note cap, and monster-name summaries are regression-covered. Malformed scene NPC and encounter-monster collections now normalize to empty arrays before projection. Fallow eliminates all three former critical builders (`buildNpcImageTarget`, `buildSceneImageTarget`, and `buildCustomMonsterImageTarget`); the complete image-target model now has only three moderate findings and no suppressions.
- Refactored AI token estimation into explicit context-projection, request-shape, attachment-cost, and aggregate-composition policies. `buildAiTokenEstimateContext` owns the Bestiary-empty, campaign, session, and encounter shapes; `estimateAiAttachmentTokens` owns fixed image costs and byte-based file costs; the public aggregate now only combines mode, request text, and category totals. Campaign/session descriptions, session entity-list precedence, context item filters, ignored notes/entities, selected-session compaction, Cyrillic-aware text approximation, two image costs, invalid file-size fallback, and byte rounding are regression-covered with Ukrainian fixtures. The adjacent entity compactor now delegates name and field fallbacks instead of retaining a hidden critical hotspot. Fallow eliminates the former `buildAiTokenEstimate` critical finding and reports zero critical findings across `tokenEstimation.ts` without suppressions.
- Replaced the critical UI-settings action cascade in `shared/model/appStateActions.ts` with a closed typed field-policy table and the pure public `normalizeUiSettingsPatch` boundary. The stable `setUiSettingsAction` API now only wraps the normalized patch in the unchanged `ui/setSettings` envelope. Focused coverage locks all eleven fields: own-property filtering, unknown/inherited-key rejection, theme/view defaults, grid parsing and clamping, simplified-note truthiness, prompt coercion, record-only campaign maps, uppercase source normalization/deduplication/sorting, and false-only disabling for both default-enabled flags, including Ukrainian prompt values. Fallow reduces `setUiSettingsAction` from CC 29/cognitive 32/CRAP 870 to no finding and reports zero findings across the complete action module without suppressions.
- Decomposed the critical AI generation-request builder into explicit request policy, structured-option gating, context sanitization, target projection, attachment projection, and instruction-selection boundaries. Bestiary remapping, image parsing/encounter restrictions, explicit false parse overrides, null/empty instruction override semantics, structured entity flags, encounter/custom-monster dependency, copied context without heavy session `data`, and caller-owned path/image-target/attachment identity are regression-covered with Ukrainian filenames and prompts. The stable aggregate still emits the retry-compatible transport field names and now only composes the focused projections. Fallow reduces `buildAiGenerationRequest` from CC 24/cognitive 19/CRAP 600 to no finding and reports zero critical findings across `generationRequest.ts` without suppressions.
- Split the critical shared workflow reducer into private modal, mention-picker, dice, message-box, and rules-reference reducers behind an ordered first-match `workflowReducer.ts` composer. Rules-reference history validation/deduplication, forward-history truncation, append indexing, finite index normalization, clamping, and no-op identity now live outside the action router. Focused regression coverage exercises all workflow domains, unmatched-action delegation, unchanged sibling references, independent dice request/result updates, Ukrainian modal/message/reference values, invalid and duplicate history identity, branch replacement, empty history, and index rewinding. The architectural source assertion now follows the rules-reference owner rather than assuming all transitions remain in one file. Fallow reduces `reduceWorkflowState` from CC 22/cognitive 13/CRAP 506 to no finding and reports zero critical findings across the complete workflow reducer family without suppressions.
- Decomposed the critical campaign-graph session projection into focused session identity/node/detail, scoped NPC/location, scene, and shared note projection helpers. The aggregate graph builder now delegates each session while preserving deterministic filename/index fallbacks, session nodes without loaded detail, UTF-8 labels, scoped navigation metadata, virtual/empty-note filtering, simplified campaign/session/scene note labels, mention propagation, and scene-sequence source metadata. Regression coverage locks these contracts, including missing filenames and nested note metadata. Fallow removes the former anonymous session callback at CC 21/cognitive 13/CRAP 462 and reports zero critical findings in `campaignGraph.ts` without suppressions. The roadmap wording was corrected to reflect the existing graph contract: encounters are not projected as graph nodes.
- Decomposed the critical `SettingsModalContent.tsx` controller into a thin public composition wrapper, a private `useSettingsModalController.ts` for injected-runtime reads, synchronization, source loading, optimistic simple-setting updates, prompt/source saves, notifications, and scope recovery, plus a private `SettingsModalView.tsx` with focused general, source, and AI presentation groups. Selected prompt/source projection and normalized campaign source-map updates moved into the Node-safe settings model with regression coverage for global/campaign values, missing campaign overrides, campaign-to-global source fallback, deduped copied sources, and Ukrainian prompts. The feature public barrels remain unchanged and the private controller/view do not enter the API; Phase 137 prevents that private controller from directly importing the global store. Fallow removes the former 545-line CC 19/cognitive 17/CRAP 380 component finding and reports zero critical findings across the touched settings slice without suppressions.
- Split the critical navigation-state reducer into an ordered `navigationStateReducer.ts` composer over private route, active-resource, and refresh/reload-version reducers. Strict campaign lookup, string-compatible session/encounter route matching, route projection, no-op comparison, and active-slice replacement live in the private Node-safe `navigationStateModel.ts`. Recognized no-op route, campaign collection, and active-resource actions now preserve the exact state reference; real transitions retain untouched sibling references, clear stale session/encounter descendants when ownership disappears, and keep refresh/reload counters monotonic. Focused UTF-8 regression coverage exercises same-route identity, partial route changes, campaign replacement/removal, each active-resource transition, unmatched actions, and version increments. Fallow removes `reduceNavigationState` at CC 18/cognitive 19/CRAP 342 and reports zero critical findings across the complete navigation reducer family without suppressions.
- Refactored the critical encounter-creation callback into a thin `useEncounterCreation.ts` React adapter over the injected `executeEncounterOpen` command and focused policies in `encounterCreation.ts`. Existing-link/create/no-op planning, string-compatible scene lookup, filename precedence after pending-save flush, runtime API-result narrowing, state replacement, navigation, and handled flush/API/result failures now compile independently of React and HTTP. Cancellation still occurs before flushing; existing links navigate without asking for a name or calling the API; creation flushes with `throwOnError`, trusts only string/number server IDs, and preserves request-name error ownership outside the handled persistence boundary. Focused UTF-8 regression coverage exercises zero-valued existing IDs, missing scenes/sessions, filename fallbacks, cancellation, exact injected call ordering, successful state/navigation updates, and malformed responses. Fallow removes the anonymous callback at CC 18/cognitive 12/CRAP 342 and reports zero critical findings across the encounter-creation pair without suppressions.
- Replaced the critical AI character-context fallback chain with explicit feature-owned identity policies in `contextIdentity.ts`. Character/NPC keys now follow trimmed `slug → id → full name → name → title` precedence; location keys follow `slug → id → name`; zero-valued numeric IDs are retained, modern and legacy first/last-name fields share normalized fallback behavior, and unidentified entities remain keyless. `useAiContextData.ts` uses these policies for immutable context-list synchronization, while `widgets/ai-assistant` consumes the same runtime export instead of maintaining a divergent key function that could generate an “Untitled” key. Campaign character/NPC/location maps remain separate, so identical IDs across scopes do not overwrite each other. Focused UTF-8 regression coverage locks precedence, whitespace normalization, numeric IDs, legacy names, empty keys, location exclusions, separate maps, and widget parity. Fallow removes `getCharacterContextKey` at CC 18/cognitive 3/CRAP 342 and reports zero findings in `contextIdentity.ts` plus zero critical findings in the touched loader/presentation scope without suppressions.
- Decomposed the critical `NoteCard.tsx` presentation controller into a thin store/composition wrapper and private strict `NoteCardParts.tsx` header, simplified-preview/action, and editable-body components. Collapse eligibility, collapsed-card expansion intent, preview truncation, classic/simplified visibility, and field-highlight checks now live in the Node-safe notes model with focused UTF-8 regression coverage. The public notes UI barrel remains unchanged; AI-ignore controls and drag-list composition remain outside the card. Last/virtual note behavior, trimmed collapse eligibility, raw preview slicing, delete/collapse event propagation, history delegation, mention rendering, and campaign-aware textarea behavior are preserved. Fallow reduces `NoteCard` from CC 16/cognitive 10/CRAP 272 to CC 4/cognitive 1/CRAP 6 and reports zero critical findings across the touched notes slice without suppressions.
- Decomposed campaign/session entity scope movement into direction-specific typed plans, an injected Node-safe `executeEntityScopeMove` command, immutable import-modal projection, and a thin `useCampaignEntityScopeMovement` React adapter. The plans validate session filenames, select the correct NPC/location collection with string-compatible IDs, retain numeric ID `0`, and encode direction-specific payload requirements as a discriminated union. The executor owns confirmation-before-flush ordering, `throwOnError`, flushed filename precedence, API delegation, result validation, and explicit cancelled/moved/failed outcomes; the hook alone applies session/modal state and user-facing error callbacks. Backend `campaignEntityScopeCommands` remains the sole owner of filesystem writes and compensation. Focused UTF-8 coverage locks invalid-plan no-ops, both direction payloads, cancellation, exact call ordering, flush failure, malformed results, stable identity, and immutable modal removal. Fallow removes both former critical hook callbacks (CC 15 and CC 16) and reports zero critical findings across the scope-movement pair without suppressions.
- Replaced the critical AI history direct-restore cascade with explicit restored-data classification and a closed Bestiary/campaign/session/encounter compatibility policy table in `historyState.ts`. Untrusted arrays and malformed `monsters`/`data` fields are rejected; Bestiary updates require Bestiary-shaped data and matching campaign identity; campaign updates require the active campaign and a campaign-scoped entry; session updates require matching campaign/session identity; encounter routes additionally enforce string-compatible encounter identity only when the entry is encounter-scoped, allowing session-level history to remain applicable inside an encounter. Numeric encounter ID `0`, UTF-8 campaign data, cross-route rejection, and direct-versus-reload restore planning are regression-covered. The AI history path contract now owns the encounter ID explicitly. Fallow removes `canApplyRestoredAiDataDirectly` at CC 16/cognitive 7/CRAP 272 and reports no critical or high finding for the replacement aggregate without suppressions.
- Decomposed AI diff construction into a thin public `aiDiff.ts` facade over private typed contracts, snapshot/identity policies, LCS line projection, and session/custom-Bestiary expansion modules. Stable pairing now retains numeric ID `0` and follows `id → slug → derived name → index`; granular projection preserves before-key then after-only ordering, after-side indexes, deletion `null` indexes, derived labels/IDs, and parent identity. Session expansion remains ordered as name, summary, notes/NPCs/locations/scenes/encounters, uncovered data, then uncovered top-level fields; unchanged resources fall back to the parent. Line projection preserves JSON formatting, context/add/remove numbering, removal-first LCS ties, and the 200,000-cell full-replacement guard. Focused UTF-8 coverage now locks zero-ID rename pairing, additions/deletions, custom Bestiary expansion, extra-field ordering, unchanged fallback, localized state labels, and large-diff fallback. Fallow removes all four former critical findings: `createLineDiff` (CC 15/cognitive 18/CRAP 240), `expandSessionDiffResource` (CC 14/cognitive 14/CRAP 210), `pushGranularArrayDiff` (CC 11/cognitive 8/CRAP 132), and `getDiffItemKey` (CC 10/cognitive 7/CRAP 110). The complete private diff family has zero critical findings without suppressions.
- Decomposed entity-link identity and modal opening into explicit contracts, a pure identity policy, a pure modal target plan, and thin async resolution/dispatch orchestration. Identity normalization now retains numeric ID `0`, resolves explicit scope before `_scope`/entity scope, requires exact type plus compatible scope, and preserves the existing any-shared-field fallback across ID, slug, then normalized display name. The modal plan returns explicit not-found/same-entity/open outcomes, blocks recursion against both the current entity and already-open modal, and now preserves the resolved campaign/session/encounter scope in modal state so nested links keep correct identity. Scoped resolvers may be synchronous or asynchronous and retain precedence over campaign lookup; missing campaign/name context performs no lookup. Focused UTF-8 coverage locks all identity fields, scope/type mismatches, fallback behavior, current/modal suppression, async resolution, scope propagation, and context no-ops. Fallow removes `isSameEntityIdentity` at CC 15/cognitive 10/CRAP 240 and reduces `openEntityLinkModal` from CC 10/cognitive 8/CRAP 110 to moderate CC 5/cognitive 4/CRAP 30, with zero critical findings across the complete entity-link model family and no suppressions.
- Extracted collapsed-selection mention lookup from `mentionEditor.ts` into the private typed `mentionSelectionPolicy.ts` boundary. The policy validates a collapsed Lexical range, resolves mention, text, then element anchors in that exact order because `MentionNode` is also a `TextNode`, accepts a mention anchor only at its end, accepts following text only at offset `0` or within the one-character zero-width boundary, and requires the adjacent sibling/child to be a mention. Boundary-node construction and the existing Space rewrite/caret behavior remain in `mentionEditor.ts`; the stable Node-safe model facade exports the lookup and boundary predicate. Focused regression coverage locks mention/text/element anchors, both accepted boundary offsets, rejected later offsets, expanded and absent selections, missing previous mentions, ordinary following text, and the existing boundary mutation. Fallow removes `getMentionBeforeCollapsedSelection` at CC 13/cognitive 17/CRAP 182; the touched editor model has zero critical findings without suppressions, and the frontend critical count falls from 13 to 12.
- Decomposed shared pointer-drag finalization into a deterministic finish plan and callback sequencer in `draggableListModel.ts`, plus focused state consumption, presentation cleanup, click suppression, and DOM custom-event dispatch helpers in `DraggableList.tsx`. Finish plans retain immutable original-list reorder calculation, key-based return-to-origin suppression, truthy-only payload details, pointer coordinates, and source-list identity. Callback application preserves `onReorder -> custom drop -> onDrop`, emits the custom cross-list drop even without a local reorder, and invokes reorder/drop callbacks only for a real final order change. Focused coverage locks reordered, unchanged, falsey-payload, external-only, UTF-8 payload, detail projection, and callback-order cases. Fallow removes `finishPointerDrag` from the report (formerly CC 15/cognitive 9/CRAP 240), reports zero critical findings across the touched shared drag scope without suppressions, and reduces frontend critical findings from 12 to 11.
- Extracted campaign keyboard action selection and editable-target classification into `campaignPagePresentation.ts`, following the established session/encounter page policy pattern. `useCampaignView.ts` now only converts `KeyboardEvent` state into the policy input, prevents default for handled actions, and invokes the chosen undo/redo callback. Ordinary inputs, textareas, and contenteditable targets retain native history unless they explicitly opt into app history; `Ctrl/Cmd+Z`, shifted `Ctrl/Cmd+Z`, and `Ctrl/Cmd+Y` retain their prior action mapping, while unrelated and non-history keys remain untouched. Focused coverage locks all editable target forms, native-versus-app history ownership, both redo forms, undo, and no-op keys. Fallow removes the hook-local `handleKeyDown` finding (formerly CC 14/cognitive 11/CRAP 210), reports no critical finding for the replacement policy, and reduces frontend critical findings from 11 to 10 without suppressions.
- Split the critical shared `MultiSelect.tsx` controller into a smaller state/portal/action orchestrator, private `MultiSelectDropdown.tsx` rendering boundary, and expanded Node-safe label/option-presentation policies in `multiSelectModel.ts`. The dropdown boundary retains the all option, select-all/clear actions, selected and active classes, checkbox-only toggles, delegated option clicks, active refs, and option-order rendering without entering the shared public API. Label projection preserves truthy override precedence, all/none label fallbacks, zero-option all-state behavior, placeholder fallback, and count formatting; option projection preserves string-normalized numeric/string identity. Focused regression coverage includes Ukrainian labels, all/none/count/override branches, and selected/active option state. Fallow reduces `MultiSelect` from CC 14/cognitive 12/CRAP 210 to moderate CC 5/cognitive 2/CRAP 30, reports zero critical findings across the touched MultiSelect family without suppressions, and reduces frontend critical findings from 10 to 9.
- Extracted one session save attempt from `useSessionPersistence.ts` into the injected Node-safe `sessionPersistence.ts` command and policy boundary. The hook retains timer/ref ownership, pending-payload consumption, debounce scheduling, instant/explicit flush, discard, and unmount flush; the command normalizes `throwOnError`/`updateUi`, calls the owning session API target, emits UI saving state, detects truthy changed filenames, reports failures before optional rethrow, and always finalizes enabled UI state. Background unmount saves still suppress both UI state and rename callbacks, while explicit flush errors can propagate after notification. Focused UTF-8 regression coverage locks default/explicit policies, exact API arguments and callback order, renamed/background saves, handled failure, rethrow identity, and `finally` cleanup. Fallow removes the former hook-local `save` finding at CC 12/cognitive 8/CRAP 156 and reports no finding across the touched persistence pair without suppressions, reducing frontend critical findings from 9 to 8.
- Decomposed player-question dice-result targeting into focused result-record narrowing, exact context recognition, bounded integer normalization, and final result projection helpers. Truthy result IDs retain their original unknown identity; malformed or unrelated context/result payloads retain the result ID but produce no question target; only inclusive `1..questionCount` integer totals from the `playerQuestions` context select a question. Focused coverage adds null and falsey IDs, object result IDs with Ukrainian data, inclusive bounds, fractional/out-of-range totals, unrelated contexts, and malformed results. Fallow removes `getQuestionDiceRoll` at CC 11/cognitive 7/CRAP 132, leaving no critical finding in the player-question model and reducing frontend critical findings from 8 to 7 without suppressions.
- Replaced the critical graph flow-node dimension fallback cascade with `getCampaignGraphFlowNodeSize` over a focused first-positive-dimension policy and exposed it through the existing Node-safe graph facade. The actual contract—not the earlier speculative content-density wording—is direct node type before nested graph type and independent measured -> direct -> style -> type-default width/height resolution. Positive finite numbers and `parseFloat`-compatible strings remain accepted; zero, negative, non-finite, and malformed values fall through without adding new clamping. Focused coverage locks mixed-source dimensions, CSS-like strings, invalid values, nested type fallback, unknown direct-type precedence, deterministic layout, and collision spacing. Fallow reduces the resolver from CC 11/cognitive 2/CRAP 132 to moderate CC 5/cognitive 0/CRAP 30 and reduces frontend critical findings from 7 to 6 without suppressions.
- Decomposed the critical `MonsterStatBlockModel.formatDamageProperty` array callback into focused first-truthy defense-value, descriptor composition, and item formatting helpers. String/scalar/null behavior, `resist -> immune -> vulnerable -> conditionImmune` precedence, array joining, optional pre-note/note text, original internal spacing for empty-array descriptors, empty-entry removal, and comma-separated output remain unchanged across legacy and 5eTools-compatible defense shapes. Focused coverage adds direct/null/non-array inputs, multiple defense descriptors, notes, fallback properties, and the empty-array edge case. Fallow removes the anonymous callback at CC 11/cognitive 11/CRAP 132, leaving no critical finding in `MonsterStatBlockModel.ts` and reducing frontend critical findings from 6 to 5 without suppressions.
- Decomposed campaign graph-note saving into `campaignGraphNoteSave.ts`, where explicit plans validate campaign/session/scene targets, pure immutable policies update the correct note scope, and an injected async command owns optimistic local session replacement followed by the focused `{ data }` session API patch. The hook now retains only campaign history snapshot timing, debounced sanitized campaign-note persistence, hydrated-session ref/state composition, and localized error reporting. Numeric note and scene ID `0` now follow the declared contract instead of being rejected by truthy guards; session/scene matching remains string-compatible and updates existing notes only, while campaign notes retain shared upsert/materialization behavior. Focused UTF-8 regression coverage locks invalid/missing targets, immutable campaign/session/scene updates, unrelated-scene identity, local-before-API ordering, missing hydrated sessions, exact payloads, and failure outcomes. Fallow removes `handleGraphNoteSave` at CC 10/cognitive 9/CRAP 110, reports no critical replacement finding, and reduces frontend critical findings from 5 to 4 without suppressions.
- Extracted AI history change-summary formatting from the broader response-resource helper into `historyChangeSummary.ts`. Focused policies now read server counters, derive the resource-count fallback, project ordered `+added`, `-deleted`, and `~modified` parts, and format the localized label. The implementation preserves the actual existing contract: a truthy numeric-coercible server total takes precedence; counter parts are shown only when total/resource fallback proves there are changes; zero or invalid totals fall back to the raw resource array length; resources are counted without kind or ID classification/deduplication; and only the `Changes` label is translated. Regression coverage locks empty histories, all counters, localized Ukrainian output, total-only summaries, mixed custom-Bestiary/session resources with duplicate IDs, invalid and string totals, absent-resource counters, and negative totals. Fallow removes `getHistoryChangeSummary` at CC 10/cognitive 7/CRAP 110, reports no finding in the extracted policy, and reduces frontend critical findings from 4 to 3 without suppressions. The earlier roadmap wording about Bestiary/session identity classification was corrected because this formatter never owned that behavior.
- Decomposed Bestiary search matching into focused object narrowing, structured choice formatting, query normalization, type-descriptor/tag projection, truthy search-part collection, and combined-text matching policies in `entities/bestiary/model/bestiarySearch.ts`. The public entity API remains unchanged. Legacy string types, structured `{ type, tags }` values, slash-joined `choose` arrays, native tag-array coercion, field order `name -> type -> tags`, cross-field substring queries, case-insensitive matching, and falsy/trimmed-empty match-all behavior remain intact; malformed or null nested types now safely project to an empty type instead of risking property access on `null`. Expanded regression coverage locks empty/falsy queries, legacy and structured types, empty/invalid choices, Ukrainian case normalization, cross-field matching, array-only tags, native falsey tag coercion, missing monsters, and malformed nested types. Fallow removes `matchesMonsterSearch` at CC 10/cognitive 5/CRAP 110, reduces `getMonsterTypeString` from high CC 7/cognitive 8/CRAP 56 to moderate CC 5/cognitive 4/CRAP 30, and lowers frontend critical findings from 3 to 2 without suppressions.
- Split the strict modal implementation into a thin `Modal.tsx` portal/API shell, private `useModalController.ts` lifecycle/state orchestration, and private `ModalView.tsx` overlay/card/header/body/footer composition. Node-safe modal policies now also own footer/cancel/variant/label projection, blur-before-callback close sequencing, and keyboard-plan side-effect execution. Input and checkbox refs/state, initial focus and input selection, global listener registration/cleanup, active-element blur, overlay-only routing, close-button refusal for alerts, and mouse-event suppression remain browser-owned. The keyboard listener now follows complete callback dependencies, so Enter confirmation uses the latest checkbox value and close policy rather than the initial effect closure. Focused coverage locks alert/confirm/custom footer plans, blocked/cancel/confirm close outcomes with exact callback ordering, prevent/close/confirm keyboard execution, and the existing API/focus/value contracts. Fallow removes the former `Modal` finding at CC 10/cognitive 8/CRAP 110 and `handleClose` high finding at CC 7/cognitive 5/CRAP 56; the full modal slice now has no critical or high findings, reducing frontend critical findings from 2 to 1 without suppressions.
- Replaced the duplicated roll-stat and probability-distribution recursive-descent implementations in `shared/lib/dice.js` with the private injected-operation driver `diceExpressionParser.js`. The driver owns token cursor/consumption, unary recursion, atomic number/dice dispatch, balanced parentheses, multiplication precedence, signed addition, failure propagation, and complete-input validation; `dice.js` still owns rolling/stat composition, probability state limits, kept-dice enumeration, and the intentional evaluator-specific negation behavior. Focused coverage now locks arithmetic and parenthesized precedence, repeated/mixed unary signs, legacy lone-`-` negative-zero/modifier behavior, malformed and partially consumed formulas, roll versus probability failure differences, negated distributions, state/combination limits, shorthand dice counts, and keep-zero outcomes. Fallow removes the final frontend critical finding, distribution `parsePrimary` at CC 10/cognitive 12/CRAP 110, plus the former high distribution `parseExpression` (CC 9/cognitive 18/CRAP 90), roll `parsePrimary` (CC 9/cognitive 9/CRAP 90), and roll `parseExpression` (CC 7/cognitive 12/CRAP 56). The extracted driver has only moderate parser findings, and the frontend now has zero critical complexity findings without suppressions.
- Decomposed high-severity `rollDiceFormula` orchestration into focused invalid-result construction, normalized-stat parsing, critical-state resolution, legacy/advanced presentation, and final result projection helpers. Empty inputs still return `null`; tokenization or parse failures still return the exact zero result without an `expressionBreakdown` field; valid results retain IDs, raw breakdowns, floored averages, min/max ranges, and an always-present advanced-expression field. Exact-one plain d20 natural 1/20 semantics remain intentionally compatibility-first: the d20 value overrides the computed total even when other dice or multiplication are present, while multiple d20s and kept d20 groups are non-critical. Advanced formulas preserve original input case and formatting; legacy formulas remain aggregated. Focused deterministic coverage locks all of these contracts, including Ukrainian invalid input and negative average flooring. Fallow removes `rollDiceFormula` at CC 9/cognitive 8/CRAP 90 with no replacement finding; `dice.js` now has three remaining high hotspots and zero critical findings.
- Decomposed high-severity `rollDiceTerm` into focused random-roll collection, plain-d20 tracking, stable keep-index selection, dropped-roll expression formatting, and plain/kept stat projection helpers. The thin orchestrator still records the exact dice-map group and selects the keep policy. Original breakdown order, stable index-order tie resolution, `h`/`l` behavior, zero and over-count keep bounds, square-bracket dropped markers, min/max ranges, intentionally approximate kept-die averages, and plain-only d20 critical tracking remain unchanged. Deterministic coverage locks tied highest/lowest choices, every dropped flag, `h0`, bounded `h5` over two dice, and plain projection totals/ranges. Fallow removes `rollDiceTerm` at CC 8/cognitive 9/CRAP 72 with no high replacement finding; `dice.js` now has two remaining high hotspots and zero critical findings without suppressions.
- Decomposed high-severity `getDiceProbabilityDistribution` into focused normalized-request validation, limit-aware evaluation delegation, sorted outcome projection, peak/weighted-average/boundary policies, and result construction. The exported function is now thin orchestration. Invalid or resource-limited formulas still return `null`; zero-valued limit options still select legacy defaults; aggregated states remain numerically sorted; original input case is retained in formatted formulas; and a valid empty distribution such as `1d0` still returns empty outcomes with a zero peak, average, and range. Expanded regression coverage locks complete summary projection for non-empty, constant, and empty distributions, formatting, falsy input, and zero-limit fallback alongside existing precedence, negation, keep, and limit tests. Fallow removes `getDiceProbabilityDistribution` at CC 8/cognitive 5/CRAP 72 without a high replacement finding; `dice.js` now has one remaining high hotspot and zero critical findings without suppressions.
- Decomposed high-severity `tokenize` into focused operator, dice, number, and precedence-ordered next-token readers. Each successful reader returns a private `{ token, length }` contract, leaving the tokenizer responsible only for cursor iteration, accumulation, and whole-input failure. Operator-before-dice-before-number precedence, exact match-length advancement, case-insensitive/shorthand dice, the legacy `0dN -> 1dN` fallback, leading-zero parsing, global whitespace removal, and rejection of unknown or partially consumed suffixes remain unchanged. Focused public-API coverage locks uppercase spaced shorthand, zero counts, leading-zero dice/keep/numbers, whitespace-joined numerals, and invalid bare/partial/extended tokens for both roll and probability evaluators. Fallow removes `tokenize` at CC 7/cognitive 9/CRAP 56 with no replacement finding; `dice.js` now has no high or critical findings, only the pre-existing moderate `addStats`, without suppressions.
- Extracted campaign notes presentation and collapse/mode decisions into typed page-model policies, and moved the former inline notes section/list into private `ui/components/CampaignNotesSection.tsx` toolbar and content compositions. `CampaignPage.tsx` now keeps screen orchestration and executes the view-mode plan; the private component executes collapse patches and composes list/graph dependencies. Empty uncollapsed campaigns still render the list/virtual creation note; no-data collapse remains blocked; list/graph visibility is mutually exclusive; bulk collapse remains list-only; active button variants remain mode-driven while collapsed; switching mode still expands and persists exactly `{ isNotesCollapsed: false }`; and graph session navigation retains the campaign slug. Focused model coverage locks both modes, collapsed visibility, empty-content compatibility, toggle refusal/inversion, and mode-change patch presence. Fallow removes `CampaignNotesSection` at CC 9/cognitive 8/CRAP 90; neither the typed policies nor any new private UI composition has a replacement finding, and frontend critical findings remain zero without suppressions.
- Decomposed the two remaining high campaign-page presentation policies. Keyboard routing now delegates editable/app-history permission to a focused guard and exact `KeyZ`/`KeyY` projection to a key mapper. Character drag/drop now composes kind-aware source normalization, exact source/target type normalization, typeof-compatible ID narrowing, and complete-candidate validation before returning a request. Browser adapters remain unchanged and execute only validated actions/requests. Expanded coverage locks guard precedence, editable `KeyY`, Shift-insensitive `KeyY`, exact key casing, missing shortcut classification, null/wrong-kind/wrong-type/wrong-ID drops, same-type moves, and the current permissive string/number ID contract including empty string, `0`, `NaN`, and infinity. Fallow removes both `getCampaignKeyboardAction` and `getCampaignCharacterDropRequest` at CC 7/CRAP 56 with no replacement finding; `campaignPagePresentation.ts` now has no high or critical findings, only pre-existing moderate policies, without suppressions.
- Split high-severity `ImageAssetField` into a thin public shell, strict `useImageAssetFieldController.ts`, pure feature-model presentation/context-menu/selection policies, and private `ImageAssetFieldView.tsx` valid/missing/empty/preview/gallery compositions. The public props and compatibility barrel are unchanged. Image errors still reset only when the URL changes; preview-open state remains retained but visible only for a valid image; disabled context replacement performs no prevent/default propagation effects; image, clear, and missing choose clicks preserve propagation behavior; missing images always expose clear/choose actions; upload and gallery keep the same resolved location; and gallery selection always closes while emitting only a truthy selected URL. Focused Node-safe coverage locks all three content states, valid-only preview gating, empty/error combinations, enabled/disabled context-menu plans, and selection URL narrowing. Fallow removes `ImageAssetField` at CC 9/cognitive 8/CRAP 90; the controller, pure policies, and every private composition have no replacement finding, while frontend critical findings remain zero without suppressions.
- Decomposed high-severity `parseGalleryLocationFromImageUrl` into focused pathname resolution, non-empty path splitting, exact API-prefix validation, safe identity decoding, subcategory derivation, and final location projection. The exported parser remains a thin nullable coordinator. Falsy URLs and non-gallery or undersized paths still return `null`; absolute URLs are parsed only when a base origin is supplied; query/hash removal follows `URL.pathname`; repeated slashes remain ignored; `api/images` matching stays case-sensitive; the minimum source/category/filename path remains valid with an empty subcategory; splitting still happens before decoding; encoded slashes may project into nested subcategories; malformed percent escapes remain verbatim; and the final filename is excluded. Focused UTF-8 coverage locks Ukrainian segments, minimal and repeated-slash paths, `%2F`, malformed escapes, absolute URLs with and without a base, wrong-case prefixes, and incomplete paths. Fallow removes the parser at CC 8/cognitive 5/CRAP 72 with no replacement high finding.
- Extracted high-severity `openGlobalResultPath` from `ImageGallery.tsx` into a Node-safe global-result navigation plan with focused eligibility, category lookup, normalized path, and plan-projection policies. The React callback now only executes a validated plan. Selection mode, local results, missing inputs, and unknown categories remain no-ops; valid global results retain the original category object, `general` source and empty-subcategory fallbacks, clear search, restore local scope, clear current selection, allow the existing location effect to record navigation history, and defer exact-name selection through the normalized source/category/subcategory path key. Focused UTF-8 coverage locks a campaign/nested-folder result, default path fallbacks, and every rejection branch. Fallow removes `openGlobalResultPath` at CC 9/cognitive 7/CRAP 90; neither the thin callback nor any extracted global-result policy has a replacement finding.
- Decomposed high-severity `getGalleryImagePath` into focused ordinary-segment and subcategory fallback policies, leaving path projection branch-free. Source and category still use falsy fallback, including the final `general`/empty defaults; subcategory still uses nullish fallback so an explicit empty string remains the gallery root while omitted or null values inherit the current folder. Deduplication keys remain ordered as resolved source, category, subcategory, image path, URL, and lowercased name with null separators. Expanded UTF-8 coverage locks exact key construction, empty source/category inheritance, default source/category projection, explicit-root versus inherited-subcategory distinction, null inheritance, and deduplication of equivalent inherited paths. Fallow removes `getGalleryImagePath` at CC 7/cognitive 3/CRAP 56 without a replacement finding.
- Decomposed high-severity `loadScopedGalleryImages` into a pure switch-based scope-to-path projection, complete API-query construction, a single transport call, and nullable response normalization. `all` still clears source/category/subcategory; `source` still keeps the selected source while clearing category/subcategory; local, `databaseTokens` if called directly, and unrecognized runtime scopes still fall back to the full selected path; search text, category allowlists, and ignored-source lists pass through unchanged. The public dispatcher still routes `databaseTokens` to its dedicated recursive bestiary-token loader. Null responses and non-array `images` now remain explicitly normalized to `[]`, while valid image arrays retain identity. Focused UTF-8 coverage locks every scope query, fallback behavior, transport delegation, valid results, null results, and malformed result shapes. Fallow removes `loadScopedGalleryImages` at CC 8/cognitive 6/CRAP 72 without replacement findings in the extracted policies.
- Decomposed high-severity `buildGalleryRangeSelection` into focused folder-before-image projection, anchor/current range derivation, additive-or-fresh selection seeding, readonly-aware item accumulation, and thin result assembly. Forward and reverse Shift ranges retain `Array.slice` bounds, skip protected folders and readonly images without ending the range, add names to the correct folder/image Set, preserve the original `lastIndex` anchor, clone prior selections in additive mode, discard them in fresh mode, and never mutate caller-owned Sets. Expanded coverage locks the existing forward mixed range plus reverse additive and fresh ranges, readonly gaps, ordered cross-type selection, anchor preservation, seed retention/replacement, output Set identity, and input immutability. Fallow removes `buildGalleryRangeSelection` at CC 8/cognitive 10/CRAP 72 without replacement findings in the extracted helpers.
- Decomposed high-severity `ImageGalleryFolderItem` into Node-safe folder presentation/nested-path policies, drag-over/drop-target/rename-result interaction plans, small DOM effect executors, and private folder visual/name compositions. Bestiary icon precedence, special `npc`/`players` icons, selected/drag-over/protected/bestiary/file states, checkbox icon selection, nested slash paths, and UTF-8 names remain explicit. Readonly folders still block single-click selection, drag-source behavior, checkbox rendering, and rename prompting while allowing double-click navigation; mutable drag-over still prevents default and updates only a changed target; drop readonly status still derives from the nested path at event time; drag leave still clears; rename still accepts every non-empty string without trimming or same-name rejection. Focused coverage locks presentation variants, root/nested paths, readonly/mutable/repeated drag-over plans, exact UTF-8 drop targets, rejected non-string/empty rename results, and whitespace acceptance. Fallow removes `ImageGalleryFolderItem` at CC 8/cognitive 5/CRAP 72; the thin item, its private parts, executors, and extracted policies have no replacement finding.
- Extracted high-severity gallery `handleKeyDown` into a Node-safe discriminated keyboard plan with focused modal/editable guards, parent-path projection, key routing, and a small hook-side effect executor. A closed gallery and exact uppercase `INPUT`/`TEXTAREA` targets still ignore every key; `SELECT`, contenteditable/non-HTMLElement targets, and lowercase synthetic tag names retain the former unguarded behavior. Delete still delegates bulk deletion without preventing default. Backspace still prevents default at every gallery path, emits no setter action for the exact empty root, and for any truthy path projects its parent through split/filter/pop, including repeated/trailing slash normalization. Other keys remain no-ops. Focused coverage locks all actions, prevent-default flags, nested/repeated/root/slash-only paths, modal and editable guards, exact tag casing, and non-input targets. Fallow removes `handleKeyDown` at CC 8/cognitive 8/CRAP 72 without replacement findings in the plan, target adapter, or executor.
- Extracted high-severity `handleRenameSub` validation and path decisions into a Node-safe subcategory-rename command plan. Whitespace-only new names and exact same names still short-circuit before transport; accepted values retain their original whitespace. Root names remain unprefixed, while a truthy selected path is concatenated verbatim without slash normalization. The unusual selected-path update remains exact: only `selectedSub === oldName` emits the raw new name; nested suffix matches do not. The hook now executes validated old/new paths, reloads subcategories before images after success, applies the optional selection replacement, and retains the same localized error path. Focused UTF-8 coverage locks root/nested paths, repeated slash preservation, accepted surrounding whitespace, invalid values, exact replacement, and nested non-replacement. Fallow removes `handleRenameSub` at CC 7/cognitive 6/CRAP 56 without a replacement finding.
- Decomposed high-severity `handleBulkDelete` into Node-safe safe-selection summary, confirmation presentation/value/result policies, grouped delete-payload planning, and injected non-empty-folder inspection. The hook now owns only loading/error lifecycle, localized modal composition, parallel delete execution, success-only selection clearing, and image/subcategory/storage reloads. Counts still use only safe filenames and folders; zero safe selections remain no-ops before loading; selected folders and both queries per folder start in parallel; non-array image/subcategory responses count as empty; truthy internal confirmation values retain compatibility normalization; image groups remain ordered before the current-location folder group; and extraction requires both detected folder content and explicit opt-in. Focused UTF-8 coverage locks empty/mixed selections, confirmation cancellation/coercion, exact payload ordering and extraction gating, root/repeated-slash folder paths, null/malformed transport results, no-call empty inspection, and eager parallel API invocation. Fallow reduces `handleBulkDelete` from CC 9/cognitive 7/CRAP 90 to moderate CC 6/cognitive 5/CRAP 42; no extracted helper introduces a high finding.
- Decomposed high-severity `buildGallerySingleSelection` into focused readonly eligibility, type-specific selected-item lookup, sole-selection action classification, fresh Set projection, clear state, and anchored single-item construction. Readonly folder/image clicks still return `null` without cloning or mutating caller Sets; the image guard still receives the first matching image or `undefined`; clicking the sole selected item clears both Sets and resets the anchor; clicking a selected item inside a mixed or multi-selection narrows to that item instead of toggling it away; new folder/image selections remain isolated even when their names match; index `0` and other caller anchors pass through unchanged; and every clear/select result owns new Sets. Focused UTF-8 coverage locks readonly image/folder/missing-image guards, sole image/folder clearing, multi-selection narrowing in both directions, same-name cross-type isolation, exact anchors, output Set identity, and input immutability. Fallow removes `buildGallerySingleSelection` at CC 7/cognitive 6/CRAP 56 with no replacement finding, including across the extracted helpers.
- Extracted high-severity gallery `handleHistoryKeyDown` into the Node-safe `getGalleryHistoryKeyboardPlan`, a focused direction-availability policy, and a thin DOM executor. The policy closes a missing lifecycle gap: a mounted but closed gallery no longer handles global history shortcuts. Editable descendants matching `input`, `textarea`, `select`, or `[contenteditable='true']` remain guarded by the browser adapter. Shortcut identity stays exact and modifier-sensitive: plain `Backspace` and `Alt+ArrowLeft` navigate back, `Alt+ArrowRight` navigates forward, casing matters, all Ctrl/Meta/Shift or extra-Alt variants remain no-ops, and direction availability is evaluated independently. Rejected plans never prevent default; accepted plans prevent once and delegate the typed `-1 | 1` direction. Focused coverage locks every accepted shortcut, closed/editable guards, back/forward availability, mixed availability, key casing, unrelated keys, and modifier rejection. Fallow removes `handleHistoryKeyDown` at CC 7/cognitive 6/CRAP 56; the extracted plan is moderate CC 5/cognitive 4/CRAP 30, while its focused helper, executor, and React callback have no findings.
- Decomposed high-severity `ImageGallerySearch` into the Node-safe `getGallerySearchPresentation` boundary plus private search-input and scope-button compositions. Placeholder/Clear localization keys, raw-query Clear visibility, the exact empty clear value, ordered scope controls, icons, active state, and next-scope actions now come from the model. Whitespace-only queries still show Clear; clearing changes only the query; an active scope returns to `local`; an inactive control selects its own scope; the source title uses the exact `selectedSource === "general"` check; database tokens appear only when allowed; and controls remain ordered `source → databaseTokens (optional) → all`. The former functional state-updater cascades are removed, while controller callbacks and CSS classes remain unchanged. Focused UTF-8 coverage locks empty/whitespace/text queries, general/campaign/empty source labels, optional database visibility, every active/inactive transition, exact order and metadata, and tolerant unrecognized runtime scope projection. The roadmap was corrected to remove category-filter behavior that is not part of this component's API. Fallow removes `ImageGallerySearch` at CC 7/cognitive 6/CRAP 56; the model boundary and both private controls have no findings.
- Decomposed high-severity `ImageGalleryStatsAndActions` into the Node-safe `getGalleryStatsAndActionsPresentation` boundary and private storage-stats, selection-actions, and upload controls. Storage row identity/order, raw bytes, selection count, action visibility, and upload eligibility now come from the model, while byte formatting, localization, callbacks, and nullable DOM `FileList` narrowing remain in focused UI owners. Null/undefined stats still fall back through exact `?? 0` semantics without clamping, so negative and `NaN` values intentionally pass through; count remains the direct sum of selected filename/folder Set sizes; `hasSelection` independently controls both Move/Delete; readonly hides only upload; both actions display the same count; and every non-null FileList, including an empty one, delegates without input reset. Focused coverage locks nullable and full stats, exact total/category order and labels, mixed counts, intentionally inconsistent visibility/count states, readonly upload, nullish fields, and non-normalized numeric values. Fallow removes `ImageGalleryStatsAndActions` at CC 7/cognitive 4/CRAP 56; its private controls have no findings, while the policy is moderate CC 5/cognitive 2/CRAP 30 solely from the preserved nullish fallbacks.
- Extracted the remaining high-severity `ImageGalleryGrid` drag-over callback and its paired drop branch into strict Node-safe target, drag-over, and drop plans plus thin DOM executors. Drag-over still always prevents default; search-result mode explicitly clears overlay state; mutable different or absent drag sources show it; exact same-location and readonly targets emit `nextDraggingOver: null`, preserving the subtle no-setter behavior rather than clearing a previously true state. Location identity remains strict across slug/category/subcategory, including the legacy `undefined !== ""` root distinction. Search drops prevent default and clear overlay without stopPropagation or controller delegation; every local drop, including readonly, passes the exact target and original event to `handleDrop`, which remains the owner of prevent/stop and drag-state cleanup. Focused UTF-8 coverage locks target projection, missing/exact/mismatched sources across every path field, search precedence, readonly behavior, strict root identity, reject effects, delegated readonly payloads, and target reference preservation. Fallow removes the anonymous grid callback at CC 7/cognitive 4/CRAP 56; plans, executors, and callbacks have no findings, leaving `ImageGallerySections.tsx` with zero high findings.
- Split the high-severity aggregate `ImageGallery.tsx` into a thin public open-state wrapper, private `useImageGalleryUiController.ts`, and private `ImageGalleryView.tsx` modal/sidebar/tab/toolbar/virtual-row/layout compositions. The existing domain `useImageGallery` controller remains the sole data/API owner; the UI controller now owns only modal-local history, preview/Escape routing, pending selection/highlight and scrolling, responsive columns, move-dialog state, row projection, and global-result transitions. Closed renders still run and retain controller state before returning `null`; preview retains capture-phase Escape priority over selection; selection resolution keeps double animation frames and exact-name/path behavior; highlights clear after 2800ms; pending selection suppresses top scrolling; history application suppresses one record; virtualization retains threshold 400, uniform rows, stable joined keys, and 170px estimator fallback; sidebar/tab/drop/dialog callbacks and public props are unchanged. Escape priority moved to `getGalleryEscapePlan`; responsive sizing moved to `getGalleryColumnCount` with exact `parseFloat(gap) || 16`, 120px width, floor, and minimum-one semantics, including legacy non-finite passthrough. Focused coverage locks Escape no-op/selection/preview precedence and prevent/stop flags plus valid, zero, invalid, negative, infinite, and `NaN` column inputs. Fallow removes `ImageGallery` at CC 9/cognitive 6/CRAP 90 with no replacement high: the public wrapper and private view have no findings, while the UI controller is moderate CC 5/cognitive 3/CRAP 30. Private controller/view files remain outside the public API.
- Decomposed the final image-slice high hotspot `useImageGallery` into a 121-line stable facade over four private strict hooks: `useImageGalleryLocation` owns selected location, search debounce/scope, campaign ignored-source projection, and search flags; `useImageGalleryLoading` owns campaign/image/folder/official-token/stat state, initial-prop application, and read reload callbacks; `useImageGallerySelection` owns readonly rules, selected Sets/anchor, range/single selection delegation, subfolder projection, and grouped movable items; `useImageGalleryMutations` owns upload/create/rename/move/delete execution, localized confirmation/errors, keyboard routing, drag state, and mutation reload sequencing. The facade retains the exact controller keys consumed by gallery UI. Initialization still registers before the location reload effect; every location/category/folder/open/scope transition still clears selection; move-selection clears its Shift anchor while drop and delete preserve their historical Set-only clearing; successful mutations retain fire-and-forget reload order and every loading lifecycle retains `finally` cleanup. Fallow removes the original aggregate at CC 9/cognitive 7/CRAP 90 with no replacement high or critical finding across all five controller files; remaining focused callbacks are moderate only. The four private hooks stay outside the feature public API.
- Split AI assistant route projection into private route, prompt, and target policies behind the unchanged `getAiAssistantRouteState` boundary. Campaign/session/encounter values retain their original falsy behavior and raw nullable/numeric passthrough; campaign-specific empty prompts still fall back through `||`; Bestiary still forces `bestiary` history and `general` image assets while retaining navigation session/encounter fields; empty sessions still select campaign mode; encounter ID `0` remains non-encounter; and generation defaults remain disabled for campaign and Bestiary modes. The adjacent context-data high hotspot now delegates campaign collection merging and session `data` narrowing to focused helpers while preserving Bestiary → encounter → campaign → session precedence, including conflicting runtime flags. Focused UTF-8 regression coverage now locks root/session/encounter/Bestiary prompt and target matrices, numeric/falsy IDs, conflicting mode precedence, and the source ownership assertion. Fallow removes `getAiAssistantRouteState` at CC 9/cognitive 8/CRAP 90 entirely and reduces `getSessionData` from high CC 8/cognitive 6/CRAP 72 to moderate CC 5/cognitive 4/CRAP 30, leaving no high or critical finding in `assistantContext.ts` and adding no public exports.
- Decomposed campaign token-estimate context into private header, conditional-note, and filtered entity-list projections behind the unchanged `buildAiTokenEstimateContext` API. Context-off campaign output remains header-only; campaign-note disablement still omits the property instead of emitting an empty list; session-owned collections, including empty arrays, retain precedence over fallback lists; disabled lists and explicit item exclusions remain exact; NPCs still use the character key resolver; and property insertion order stays header → notes when enabled → characters/NPCs/locations. Entity display-name precedence now uses one private truthy-value resolver while preserving `firstName/first_name`, `lastName/last_name`, joined full name, `name`, then `title` semantics. Focused UTF-8 coverage locks aliases/title fallback, context-off header fallback, note omission, disabled and item-filtered lists, empty session collection precedence, current-session compaction, and selected-session inclusion/exclusion. Fixed image/file estimates, four-byte formula, system costs, and request shaping are untouched. Fallow removes `buildCampaignModeContext` at CC 9/cognitive 4/CRAP 90 and `getEstimateEntityName` at CC 8/cognitive 3/CRAP 72 entirely; `tokenEstimation.ts` now has no high or critical findings and gains no public exports.
- Decomposed granular AI diff expansion without changing its public `aiDiff.ts` surface. Keyed array expansion now separates after-first label/suffix derivation and per-item metadata construction from before-key/after-only iteration. Session expansion now composes private snapshot normalization, known name/summary diffs, ordered known-array diffs, uncovered `data.*` diffs, uncovered top-level diffs, and original-resource fallback. Custom Bestiary uses the same empty-expansion fallback. Ordering remains `name → summary → notes → NPCs → locations → scenes → encounters → data extras → top-level extras`; derived resources retain `parentResourceId`; after items retain exact `listIndex`; deletions use `null`; undefined sides normalize to `null`; labels trim and fall back through localized type names or stable keys. Focused UTF-8 coverage locks every ordered group, localized empty-name fallbacks, trimmed identities, additions/deletions, scalar/data normalization, parent identity, and Bestiary deletion metadata in addition to existing zero-ID, unchanged-resource, LCS, and large-diff tests. Fallow removes the anonymous granular callback at CC 9/cognitive 5/CRAP 90 and `expandSessionDiffResource` at CC 7/cognitive 6/CRAP 56; `aiDiffExpansion.ts` now has no high or critical findings, with the item constructor only moderate CC 5/cognitive 1/CRAP 30 and no new exports.
- Decomposed AI response resource selection, monster-name fallback, token URL resolution, draft image injection, and encounter-route visibility into focused private policies behind the unchanged feature barrel. Null or malformed resource collections still normalize to `[]`; a null ID filter still permits every custom-monster resource while any array becomes a string-normalized truthy-ID set, including empty-set match-none behavior. Monster names retain first-truthy `after -> before -> resource` precedence and validate the selected value only after fallback. Explicit non-empty string image URLs still beat derived tokens; derived source and `originalBestiaryName -> name` identity remain trimmed, string-coerced, and segment-encoded. Only added custom monsters with record-shaped `after` data and no truthy image are copied; existing summaries and original names win, no-op calls preserve entry identity, and generated summaries retain the existing counter rules. Bestiary responses still depend only on `isBestiary`; truthy encounter paths require exact campaign/session/encounter equality; absent and falsy encounter IDs remain globally visible. Focused UTF-8 coverage locks null entries, empty ID filters, truthy non-string name behavior, direct and encoded token URLs, missing token identity, existing token/modified-resource no-ops, summary identity, and encounter ID `0`. Fallow removes all four former high findings: `getFirstChangedMonsterName` CC 9/cognitive 2/CRAP 90, `getMonsterTokenImageUrl` CC 9/cognitive 7/CRAP 90, `addSourceMonsterImageToDraft` CC 8/cognitive 6/CRAP 72, and `isAiResponseVisibleForRoute` CC 7/cognitive 4/CRAP 56. The module now has no high or critical findings and no new public exports.
- Decomposed custom Bestiary image-prompt normalization into private envelope narrowing, nested-collection selection, root-array fallback, and record-retention policies behind the unchanged `normalizeCustomMonsterCollection` export. The `monster` array still wins over `monsters`, including when empty; a non-array `monster` still falls through to an array-valued `monsters`; a root array remains the final supported transport shape; and null/scalar/malformed envelopes normalize to an empty list. Runtime narrowing now makes the declared record-array contract authoritative by removing null, primitive, function, and nested-array entries. Arrays containing only record-shaped values, including empty arrays, retain their original reference; mixed arrays allocate only for the filtered result. Focused UTF-8 coverage locks both envelopes, their precedence and fallback, root-array identity, empty-array identity, malformed input, mixed-item filtering, and preserved Ukrainian monster records. Fallow removes `normalizeCustomMonsterCollection` at CC 9/cognitive 5/CRAP 90; the public function has no finding, the only scoped result is the private envelope selector at moderate CC 5/cognitive 2/CRAP 30, and no public exports were added.
- Decomposed AI history retry execution into private failed-entry deletion, transient generation, successful-outcome, and cancellation/failure policies behind the unchanged executor API. Delete-before-generate ordering and immediate deletion notification remain exact; nullable/non-array deletion results normalize to `[]`; the original retry payload and `AbortSignal` are passed through; null generation results remain successful; and callbacks execute inside the guarded transaction, so a throwing success callback still reaches `onFailed` with the same error. Only object-shaped errors named `AbortError` produce cancellation and `onCancelled`; every other thrown value is returned unchanged in the failure outcome. The adjacent generated-entity type selector now uses an ordered `characters -> npc -> locations` table: array presence counts even when empty, and deduplicated history-resource types remain the fallback only when no generated collection is present. Focused UTF-8 coverage locks deletion/generation/success order, nullable deletion and generation, signal identity, callback-error delegation, cancellation before generation, original failure propagation, collection order, empty arrays, malformed collections, and history fallback. Fallow removes `executeAiHistoryRetry` at CC 9/cognitive 6/CRAP 90 and `getGeneratedEntityTypes` at CC 8/cognitive 4/CRAP 72; `historyWorkflow.ts` now has no high or critical findings, with only three moderate results and no new public exports.
- Decomposed updated AI result planning into private generated-content narrowing, history/generated/session-shape analysis, direct-application, reload, prompt-clear, refresh, close, and notification policies. Prompt → draft → updated precedence and the public plan union remain unchanged. Generated arrays and other non-record values normalize to null; session shape still requires record-shaped `updated.data`; Bestiary always applies directly; campaign screens accept only campaign-shaped data; non-campaign screens accept only session-shaped data; mismatches reload; directly applied session data reloads only for campaign changes; explicit false prompt clearing remains false while undefined defaults true; indirect plans refresh only with generated/history entity types; and only exact `custom-monster` requests use the custom-creature notification. Updated execution now composes persistence/reload effects and presentation effects while retaining exact history → apply → reload → clear → notification → refresh → close order and reading plan flags at effect time. Focused UTF-8 coverage locks prompt precedence over simultaneous draft/update data, invalid generated fallback to history entity types, the full mismatch reload/clear/refresh/close path, direct-apply behavior, and prompt/draft execution order. Fallow removes `buildUpdatedPlan` at CC 8/cognitive 7/CRAP 72 and `executeUpdatedPlan` at CC 7/cognitive 6/CRAP 56; `generationResultWorkflow.ts` now has no high or critical findings, with three moderate CC 5 results and no new public exports.
- Replaced the AI generation lifecycle switch with typed start/finish status maps plus focused start projection, active-request completion, and thin union routing. Every start still replaces the current lifecycle regardless of prior state; generation and retry retain distinct pending statuses; matching succeed/fail/cancel transitions map to their exact terminal status and clear `requestId`; stale terminal events return the original state object; reset returns the frozen initial singleton; and nullish/succeeded/failed/cancelled states remain non-pending. Focused regression coverage now locks request IDs on start and completion, stale cancel identity, retry failure, active-request replacement, matched cancellation, null pending detection, and reset singleton identity. Fallow removes `aiGenerationLifecycleReducer` at CC 8/cognitive 3/CRAP 72 without any replacement finding in `generationLifecycle.ts`; no public exports were added.
- Decomposed AI context-list synchronization into complete-container validation and a focused missing-key inserter. Valid `{ included: boolean, items: record }` configs still preserve exact identity when no key is added; existing own keys, including false, are never overwritten; falsy keys are skipped; duplicate new keys are inserted once as true; scalar/null configs retain their `included !== false` default; and partial or invalid containers now normalize to the declared complete contract instead of leaking array-valued `items` or missing `included`. Nested context updates now compose JSON-compatible cloning, parent-aware node creation, path traversal, and leaf assignment. Empty paths still return the original input; non-record intermediates are replaced; only a node immediately below `scenes` receives a fresh complete default scene context; existing scene records retain only their own fields plus the changed leaf; the input remains untouched; and unrelated JSON-compatible branches remain deeply cloned as before. Focused UTF-8 coverage locks valid identity, invalid/partial normalization, false preservation, falsy/duplicate keys, empty-path identity, new-scene defaults, existing-scene non-backfill, Ukrainian custom data, source immutability, and deep-clone identity. Fallow removes `ensureContextListItems` at CC 8/cognitive 6/CRAP 72 and `updateContextConfigValue` at CC 7/cognitive 9/CRAP 56; `contextConfig.ts` now has no high or critical findings, with only existing `getContextListConfig` at moderate CC 5/cognitive 4/CRAP 30 and no new public exports.
- Decomposed AI attachment identity into private string URL/name narrowing, numeric size projection, and a shared `name:size` metadata-key policy. Image keys still prefer every non-empty string URL, including whitespace, and otherwise use the metadata key; file keys continue to ignore URL entirely. Nullish descriptors produce `:`; missing names and sizes leave their segment empty; numeric zero and `NaN` remain empty through truthiness semantics; negative and infinite numeric sizes remain stringified; valid gallery/file descriptors keep their exact previous keys; and malformed non-string URLs now safely fall back instead of violating the declared string return. Focused UTF-8 coverage locks URL precedence, empty-URL fallback, Ukrainian names, null identity, zero and negative sizes, malformed URL narrowing, and file URL independence. Fallow removes `getAttachedImageKey` at CC 7/cognitive 2/CRAP 56 and the former moderate `getAttachedFileKey` result; `aiAttachments.ts` now has no high or critical findings, with only its two MIME helpers at moderate CC 5/cognitive 4/CRAP 30 and no new public exports.
- Decomposed LCS line transition classification into private cursor-availability, exact-current-line equality, and removal look-ahead policies. Context still requires both sides and exact string equality; exhaustion of the old side produces additions; exhaustion of the new side produces removals; unequal lines compare the same lower/right LCS cells; and `>=` ties remain removal-first. The aggregate still advances old counters for context/removal, new counters for context/addition, emits full replacement above 200,000 cells, and retains pretty-JSON Unicode text verbatim. Public-boundary regression coverage through `buildDiffResources` locks Ukrainian replacement text, removal-before-addition ties, added-before-later-old-match transitions, removed-before-later-new-match transitions, context braces, and exact nullable old/new line numbers in addition to the existing unchanged and large-diff guards. Fallow removes `getNextDiffLineType` at CC 7/cognitive 5/CRAP 56; `aiLineDiff.ts` now has no high or critical findings, with only aggregate `createLineDiff` at moderate CC 6/cognitive 5/CRAP 42 and no new exports.
- Decomposed AI context-data orchestration into private visibility policies, hydration-campaign projection, pending-session selection, per-session loading, parallel tuple loading, cancellation-aware application, tuple merging, and generic entity-list synchronization. Campaign entities retain their broader modal/picker visibility even with context disabled; session-list loading still requires context plus any open context surface; session hydration remains limited to a non-Bestiary open panel with context enabled and a truthy campaign. Only included sessions without truthy data load; loads remain parallel; each failure reports the same message and contributes no tuple; nullable session data falls back through `|| {}`; cancellation suppresses all state application. Merge keeps truthy existing data, current-config-over-fallback precedence, first successful duplicate data, original root identity for no-op batches, and root/sessions cloning only when a tuple applies. Character/NPC/location effects now share one owner-keyed synchronizer and still preserve root identity whenever `ensureContextListItems` preserves the owned config. Focused UTF-8 coverage locks current-config precedence, already-loaded identity, nullable sessions maps, duplicate first-win behavior, and Ukrainian loaded data alongside existing initial/no-op merge tests. Fallow removes the visibility callback at CC 8/cognitive 4/CRAP 72, `mergeLoadedAiSessionData` at CC 7/cognitive 6/CRAP 56, and the hydration callback at CC 7/cognitive 4/CRAP 56; `useAiContextData.ts` now has no high or critical findings, with four moderate CC 5 results and no new public exports.
- Decomposed AI history restore orchestration into focused transport selection, nullable-result normalization, confirmation/mode resolution, lock acquisition/release, and guarded post-restore execution policies. Apply and undo still receive the exact `{ resourceIds }` payload, including an explicit undefined property when no IDs are supplied; empty transport responses now fail at the command-service boundary with the existing restore error; confirmation remains outside restore error handling; and successful, failed, empty-response, or throwing effect paths all release the ref-backed lock. Lock acquisition is rechecked after asynchronous confirmation, closing the race where two confirmations could previously start parallel restores while preserving immediate suppression once a restore owns the lock. Focused command-service coverage locks apply/undo routing, payload shape, nullable rejection, and all existing delete/clear/save delegations. Fallow removes `restoreEntry` at CC 7/cognitive 5/CRAP 56; `historyCommands.ts` now has no high or critical findings, only the existing moderate delete/save callbacks, and no public exports were added.
- Split `AiAttachmentControls` into a thin state/effect owner plus private action, image-list, and file-list compositions. `presentationModel.ts` now derives image-action visibility, file-button disablement, and the campaign/general gallery source from explicit counts and limits. Hidden image actions remain independent from the file cap; disabled state still blocks every add/remove control; file and image inputs retain immediate value reset; gallery selection still closes after valid, invalid, or cancelled selection; image/file deduplication, four-item caps, MIME/size checks, preview creation, and partial-selection alerts are unchanged. Focused UTF-8 coverage locks the at-cap, disabled, campaign-source, and general-fallback projections. Fallow removes the component-level `AiAttachmentControls` finding at CC 7/cognitive 6/CRAP 56; the slice now has only moderate attachment handlers/render callbacks and adds no browser-barrel exports.
- Replaced the complex virtualized history callback with a private `AiResponseHistoryRow` composition and a pure row-view projection in `presentationModel.ts`. The projection owns summary/date/retry/state/title evaluation and preserves the original callback order, exact `createdAt`/language forwarding, truthy fallback-title semantics, optional retry default, and raw label strings. The React list callback now only resolves an indexed entry and returns null for a missing slot. Selection remains on shared `ListCard`'s native button, retaining click plus Enter/Space activation without local keyboard handlers; retry and delete remain separate shared Button actions, and empty summary/state labels remain omitted. Focused UTF-8 regression coverage locks fallback titles, localized labels, retry visibility, callback arguments/order, and the absent retry callback. Fallow removes `renderHistoryEntry` at CC 7/cognitive 5/CRAP 56 and reports no finding in `AiResponseHistory.tsx`; no browser-barrel exports were added.
- Decomposed AI toolbar entity and encounter action groups around explicit presentation views plus a private shared generation-toggle button. Entity actions still require parsed output and disappear entirely on encounter screens; character generation remains the campaign-only subset while NPC/location actions remain available on other parsed non-encounter screens. Encounter action projection independently controls the encounter toggle, custom-monster toggle, and direct custom-creature action, preserving campaign suppression, caller-owned custom-monster visibility, encounter-only direct creation, and current-versus-scenes title copy. The shared toggle retains functional state inversion, active/ghost variants, loading disablement, icons, titles, and labels. Encounter toggling still applies its existing plan in order: encounter state first, custom-monster false only when disabling encounters, and no custom-monster write when enabling. Focused matrix coverage locks parsed, encounter, campaign, character, custom-monster, title-kind, and direct-creation gates. Fallow removes `AiEntityGenerationActions` at CC 7/cognitive 6/CRAP 56 and reduces `AiEncounterGenerationActions` from CC 8/cognitive 7/CRAP 72 to moderate CC 6/cognitive 5/CRAP 42; `AiToolbarControls.tsx` now has no high or critical findings and adds no browser-barrel exports.
- Replaced generated-monster JSON boundary scanning with explicit outside/inside/escaped string transitions, string-aware brace-depth deltas, and a thin completion loop. Prefix detection, localized/English label lookup, labeled-object removal, first-truthy compatibility, generation-option rows, and context rows now live in focused private policies. Nested objects, escaped quotes/backslashes, closing braces inside Unicode strings, translated text, and complete-object suffix extraction remain intact; an absent label/opening brace or incomplete object/string still returns the original trimmed prompt. Request precedence remains retry text after trimming, then first-truthy request-level instructions before legacy instructions; whitespace truthy aliases retain their historical pre-trim precedence. Character first/last aliases and name/title fallback retain the same rule, and `Untitled` translation remains lazy. Options retain known/unknown mode labels, parsing-gated generation rows, and context state; context retains fallback, disabled, empty, ordered truthy counters. Expanded UTF-8 regression coverage locks all of these scanner and fallback edges. Fallow removes `findJsonObjectEnd` at CC 9/cognitive 15/CRAP 90 plus five adjacent high findings (`stripGeneratedMonsterEditPrompt`, `getHistoryRequestText`, `getCharacterDisplayName`, `getHistoryOptionsSummary`, and `getHistoryContextSummary`); `assistantPresentation.ts` now has no high or critical findings, with only moderate policies, and no public exports were added.
- Extracted restored-history effect execution from the private React controller into the Node-safe public `executeAiAssistantHistoryRestorePlan` policy in `assistantHistory.ts`. Feature-owned `buildAiHistoryRestorePlan` still classifies replace/upsert, selected entry, direct-route compatibility, entity types, and reload need; the widget executor now owns exact effects in history replace/upsert → stats refresh → selection/prompt sync → direct apply → reload order. Replace keeps the server collection identity; upsert remains a functional React update against the latest collection; stats refresh occurs exactly once only for a non-null history update; selection receives the plan entry; direct apply receives the original updated payload plus exact entity types, restored history entry, and `trackUndo: false`; reload receives the unchanged entity-type array. Focused executor coverage locks both upsert/direct-apply and replace/reload paths with exact ordering and payloads. Fallow removes `refreshAfterRestore` at CC 8/cognitive 5/CRAP 72; the controller now has no high or critical findings, and the executor is intentionally available through the widget's Node-safe model facade.
- Decomposed AI image-prompt generation planning into private targetless-validation, scene-ID projection, and request-option policies, and split target-title qualification from its truthy fallback chain. The public model surface remains unchanged. Instructions and targetless requests are still trimmed; only a missing target plus an empty request emits the existing error; any selected target clears `userInstructionsOverride`; only scene targets project `id || null`; a truthy session name still qualifies a scene title even when its name is empty; and fallback remains exact `name -> id -> type` truthiness, including whitespace names and numeric zero falling through. Focused UTF-8 coverage locks targetless Ukrainian requests, blank errors, scene/non-scene targets, falsy scene IDs, empty names, session qualification, whitespace names, and nullish targets. Fallow removes both former highs: `buildAiImagePromptGenerationPlan` at CC 7/cognitive 5/CRAP 56 and `getImagePromptTargetTitle` at CC 7/cognitive 4/CRAP 56. `imagePromptPicker.ts` now has no high or critical findings, only two moderate collection/state policies, and adds no public exports.
- Extracted Bestiary AI draft-restore eligibility/snapshot creation and nullable restore-result interpretation into the Node-safe `getAiDraftRestoreStartPlan` and `getAiDraftRestoreResultPlan` policies. The React controller now owns only apply/undo transport, busy/error/finally lifecycle, and entry → optional undo snapshot → custom update effect execution. Missing or falsy entry IDs and already-active restores remain no-ops; resource-ID arrays retain their exact identity; apply alone deep-clones the current custom list; null results retain the original entry; a truthy response still wins through `response || entry`; and absent `updated` data changes only the selected draft entry. Apply selection is resolved from the effective restored entry and exact resource filter, its snapshot is pushed only when the resulting custom list differs, and both modes delegate updates with `trackUndo: false`; undo never selects a monster or creates an undo snapshot. Focused UTF-8 coverage locks invalid/locked starts, clone immutability, resource-list identity, null/response-only results, changed/equal apply data, filtered Ukrainian monster selection, empty filters, and undo behavior. Fallow reduces `restoreAiDraftResponse` from CC 9/cognitive 6/CRAP 90 to moderate CC 6/cognitive 4/CRAP 42; both new plans are moderate CC 5/CRAP 30, with no replacement high finding. The plans are intentionally exported only through the widget's Node-safe model facade.
- Extracted the embedded Bestiary initial-selection scroll decision into the Node-safe `getBestiaryInitialSelectionScrollPlan` policy and a focused eligibility helper. The React effect now only applies a plan, records its once-key, schedules `scrollTo` for one animation frame, and cancels that frame during cleanup. Scrolling still requires the opt-in flag, truthy reference and selected names, and normalized name/source matching; absent displayed targets remain no-ops. The once-key remains the exact raw `${source || ""}:${name}` string and is recorded before the frame executes, so cancellation does not permit an automatic retry; it is still reset only when initial name/source props change. Displayed lookup continues to use canonical case/whitespace-normalized identity, selects the first duplicate, and accepts index `0`. Focused UTF-8 coverage locks disabled/missing/mismatched states, source mismatch, missing displayed identity, raw Ukrainian key preservation, exact once suppression, sourceless fallback, and first-index behavior. Fallow removes the anonymous effect finding at CC 9/cognitive 5/CRAP 90; the UI effect has no finding, while the two model policies are moderate CC 5/CRAP 30 with no replacement high. The plan is intentionally exported only through the widget's Node-safe model facade.
- Split the Bestiary monster list row into the Node-safe `getBestiaryMonsterRowPresentation` policy plus private action/content compositions and small UI executors. The policy now owns selected/favorite/custom state, click-toggle output, add-versus-insert primary action and title, token source, favorite title, and CR display. `onSelectMonster` still wins over `onAddMonster` when both exist; a selected row clears selection while an unselected row returns the original monster object; explicit string image URLs, including `""`, still beat the model token; non-string values use the fallback; and falsy CR values still display `--`. Double-click and Plus delegate the same action, favorite/edit/primary/custom buttons stop propagation before callbacks, Edit remains universal, and AI/Delete remain custom-only. Source tooltip/highlighting, size/type/tag rendering, lazy image hiding, and localized labels remain private UI behavior. Focused UTF-8 coverage locks official/custom, selected/favorite normalized identity, callback precedence, null action, original selection identity, empty-string image precedence, non-string fallback, fractional CR, and zero-CR fallback. Fallow removes `MonsterListItem` at CC 9/cognitive 8/CRAP 90; the row shell, action controls, and content composition have no findings, while the model policy is moderate CC 6/cognitive 6/CRAP 42 and the UI primary executor is moderate CC 5/cognitive 2/CRAP 30. The plan is intentionally exported only through the widget's Node-safe model facade.
- Extracted realtime Bestiary synchronization into the Node-safe `getBestiarySyncEventPlan` boundary with focused supported-event, reload, and pending-selection policies. The React effect now only executes declarative favorite-refresh, selection, auto-selection, and reload flags. A parsed event still requires a truthy version, so numeric `0`, `NaN`, and `""` remain ignored while string `"0"` is accepted; resources remain the exact case-sensitive `bestiary`, `custom-bestiary`, and `ai` allowlist. Every accepted event refreshes favorites independently; `bestiary` never reloads monsters or honors a monster target; custom/AI always reload; and only a truthy monster name creates a pending selection and suppresses auto-selection. Names and non-empty sources retain raw whitespace, while an empty/missing source falls through `|| "CUSTOM"`. Favorites remain nullable-to-empty and refresh failures only log, without blocking synchronous selection/reload effects. Focused UTF-8 coverage locks null/falsy versions, case rejection, string-zero acceptance, official event target ignoring, custom reload without selection, Ukrainian fallback selection, and whitespace identity preservation; the source-ownership assertion now verifies policy delegation instead of requiring domain branches in React. Fallow reduces the anonymous effect from CC 8/cognitive 7/CRAP 72 to moderate CC 6/cognitive 5/CRAP 42; the extracted plan/helpers have no findings and add only the intended Node-safe model export.
- Decomposed the authoritative `parseBestiarySyncEvent` runtime boundary into required-field, version, and optional-string narrowing policies. Non-record roots, arrays, scalars, functions, missing/non-string resources, and non-primitive-string/number versions still return `null`. The parser deliberately does not perform planner semantics: empty and whitespace strings, numeric `0`, `NaN`, and infinities retain their exact values; an empty resource remains a structurally valid parsed event; optional empty strings remain empty; and invalid optional values become explicit `undefined` fields. This keeps external-envelope validation separate from later truthy-version and resource-allowlist decisions. Focused regression coverage locks every malformed root/required-field family, bigint/boolean/object rejection, Ukrainian optional data, raw empty/whitespace values, numeric edge cases, and exact optional-key output. Fallow removes `parseBestiarySyncEvent` at CC 7/cognitive 6/CRAP 56; the parser and all extracted helpers now have no findings, with no new public export or weakened runtime validation.
- Extracted Bestiary AI-edit eligibility, instruction validation, and exact custom-monster request construction into the Node-safe `getAiMonsterEditStartPlan` policy. Missing or empty-name targets remain silent no-ops; edit/local-edit still require trimmed instructions; create-based mode still prefixes the localized copy instruction and accepts an otherwise empty request. The ready plan preserves target, attachment-array, and file-array identity while forwarding the selected model through the original truthy fallback, language, mode, Bestiary path, parsed-response flag, disabled entity-generation flags, custom-Bestiary scope, and null context exactly. Abort-aware error projection now suppresses only `DOMException` `AbortError`, retains non-empty `Error.message`, and otherwise uses the localized fallback; controller finalization clears only the same active request. React remains the owner of controller creation, transport, busy state, normalized-result application, and the exact apply-result → reset-editor success order. Focused UTF-8 coverage locks skip/invalid/ready plans, payload shape and identity, Ukrainian attachment names and errors, abort suppression, fallback errors, and controller identity. Fallow reduces `saveAiEditedCustomMonster` from CC 8/cognitive 7/CRAP 72 to moderate CC 6/cognitive 6/CRAP 42, with the extracted start plan also moderate and no replacement high finding. The policies are intentionally exported only through the widget's Node-safe model facade.
- Extracted custom-versus-official field-editor initialization into the Node-safe `getBestiaryFieldEditStartPlan` policy with private eligibility, image fallback, and plan-construction helpers. Missing targets and falsy names remain no-ops. Custom sources still normalize through the existing canonical source policy and preserve one exact object reference as both original and draft in `edit` mode; their token resolver is never invoked. Official monsters retain their original reference, enter `create-based` mode, create a new draft with `CUSTOM` source, preserve every truthy string image URL verbatim including surrounding whitespace, and lazily delegate empty/non-string image fallback to entity-owned `MonsterStatBlockModel.localTokenSrc`. React now only executes mode → original → draft setters in their original order. Focused UTF-8 coverage locks null/empty targets, normalized custom identity and lazy resolver behavior, official cloning, exact explicit-image preservation, and local-token fallback. Fallow reduces `openEditMonster` from CC 7/cognitive 5/CRAP 56 to CC 2/cognitive 1 and splits the model into policies no higher than CC 3/cognitive 2, without suppressions or duplicate token logic. The start plan is intentionally exported only through the widget's Node-safe model facade.
- Extracted custom-monster deletion eligibility, list replacement, and favorite removal into Node-safe Bestiary policies. Eligibility still requires a truthy name plus the normalized `CUSTOM` source and forwards the exact untrimmed name into confirmation and transport. Confirmation remains before undo-snapshot cloning; cancellation therefore creates no snapshot. On successful transport, UI effects remain undo snapshot → disable auto-selection → clear selected ref → clear selected state → replace list → remove favorite. List replacement retains every current non-custom monster in order, then appends the returned array unchanged; null or non-array results append nothing. Favorite cleanup preserves exact case-sensitive name comparison while normalizing only the source, so official same-name and custom case-variant favorites remain. Failures still skip every success effect and dispatch the localized deletion error. Focused UTF-8 coverage locks official/nameless skips, whitespace name/source handling, official-first replacement, nullable response fallback, retained object identity, and exact favorite semantics. Fallow reduces `handleDeleteCustomMonster` from CC 7/cognitive 4/CRAP 56 to CC 4/cognitive 3; all extracted policies/helpers are no higher than CC 3/cognitive 2, without suppressions.
- Extracted Bestiary detail visibility, favorite state, insert/add/delete callback normalization, add-picker state, and localized add-title resolution into the Node-safe `getBestiaryDetailPresentation` policy. A null selection still renders nothing and does not resolve the add title. Selected monster and callback identities are preserved exactly; null optional insert/add actions normalize to `undefined`; the add callback continues to drive both name-click and encounter-picker actions; and title translation remains lazy, occurring exactly once only when an add action exists. Favorite state still uses canonical monster identity. Official monsters suppress deletion even when a delete callback exists, while custom sources normalize through the shared source policy and receive the original callback. The private JSX keeps the same detail container/ref, conditional Insert control, stat-block composition, favorite updater, AI/edit callbacks, and search highlight forwarding. The architectural source assertion now verifies model delegation instead of the retired inline `Boolean(onAddMonster)` policy. Focused UTF-8 coverage locks null, official, favorite, full custom-action, callback invocation/identity, custom-null-delete, and lazy-title cases. Fallow reduces `BestiaryDetail` from CC 7/cognitive 6/CRAP 56 to CC 3/cognitive 2; every extracted helper/aggregate is CC 2/cognitive 1, without suppressions.
- Decomposed Bestiary challenge-rating parsing into private raw-value extraction, text normalization, scalar parsing, fraction parsing, text-kind dispatch, and numeric passthrough policies. Structured `{ cr }` and scalar shapes retain precedence; numeric values, including `NaN` and infinities, pass through unchanged. Nullish values stringify as `"0"`; scalar strings retain `parseFloat(...) || 0`, including prefix parsing; fraction strings still split once into defaulted first numerator/denominator values, coerce the denominator with `Number`, divide only for a truthy denominator, ignore later slash parts, and preserve `NaN` from an invalid numerator with a valid denominator. Sorting now delegates direction and comparison to private policies: `none` clones without reordering, descending alone negates CR differences, ties and `NaN` differences use `localeCompare`, and every ordered path clones before sorting so input order and item identities remain untouched. Focused coverage locks numeric/structured/fraction/null/invalid/infinite parsing, zero denominator, extra fraction parts, ascending/descending CR-first order, ascending name ties in both directions, `none` cloning, input immutability, and item identity. Fallow reduces `parseMonsterCr` from CC 7/cognitive 6/CRAP 56 to a thin wrapper; all parsing and sorting policies are at most CC 3/cognitive 2, while `sortBestiaryMonsters` is CC 2/cognitive 1. The prior roadmap attribution of the nearby anonymous CC 7 finding to the sort comparator was corrected: current source evidence identifies it as `filterBestiaryMonsters`.
- Decomposed Bestiary filtering into private selected-source membership, exact source-filter, favorite-gate, matcher-selection, and per-monster policies behind the unchanged `filterBestiaryMonsters` facade. Selected sources and monster sources still compare after trim/case normalization; an empty selected-source list still matches nothing; only the exact lowercase `"all"` source filter bypasses exact-source filtering; and other values compare after normalization. Favorite lookup remains lazy behind `onlyFavorites`; detailed/simple matcher selection still happens only after preceding gates; and the original search string, monster order, monster object identity, and input array remain untouched. Focused UTF-8 coverage locks normalized membership, canonical favorites, exact lowercase bypass semantics, empty-selection short-circuiting, matcher choice/call order, exact Ukrainian search forwarding, order, and identity. Fallow removes the anonymous filter callback at CC 7/cognitive 6/CRAP 56; the public facade has no finding and the replacement policies are no higher than moderate CC 4, with no new export.
- Decomposed custom Bestiary update planning into private updated-envelope/list normalization, generated-versus-requested selection, and undo-eligibility policies behind the unchanged `getCustomBestiaryUpdatePlan` facade. Only record-shaped results whose `monsters` lookup resolves to an array still count as an update, including empty arrays; other envelopes do not fall through to `results`. Malformed returned entries are filtered while valid object identity is retained. The first valid generated monster still wins over `selectedName`; a normalized-name match prefers the returned custom monster identity, while a missing returned match preserves the generated draft. Requested selection remains truthy-name gated and canonical; undo defaults on only for a recognized updated array and exact `trackUndo: false` suppresses it. Focused UTF-8 coverage locks null/wrong envelopes, empty-array presence, malformed entries, selected-name resolution, explicit undo suppression, generated fallback/precedence, and returned-versus-generated identity. Fallow removes `getCustomBestiaryUpdatePlan` at CC 7/cognitive 4/CRAP 56; the facade and extracted policies have no reported finding and no public export was added.
- Decomposed Bestiary source-list normalization into private record-candidate precedence and string narrowing behind the unchanged `getBestiarySourceCodes` runtime boundary. Non-array roots still normalize to `[]`; direct strings pass through unchanged; records retain nullish `value -> source -> id -> name` precedence; and only the selected candidate is string-checked. Consequently, `0`, `false`, objects, and empty strings remain blocking non-nullish candidates rather than falling through, while empty output strings are removed afterward. Whitespace, duplicates, casing, order, and source text remain untouched. Focused UTF-8 coverage locks malformed roots/items, all aliases, nullish fallback, non-string blocking, empty removal, whitespace, duplicates, and Ukrainian source names. Fallow removes the anonymous callback at CC 7/cognitive 4/CRAP 56; `getBestiarySourceCodes` and both private policies have no finding, with no new export.
- Decomposed legendary-group reference derivation into private record narrowing, name fallback, and source coercion behind `enrichMonstersWithLegendaryGroups`. Only record-shaped `legendaryGroup` values can override identity; string overrides remain authoritative even when empty. Non-string/missing override names retain the monster name, while non-string/missing override sources retain exact `String(monster.source ?? "")` behavior, including numeric sources and nullish empty fallback. Group and monster references still meet through canonical trim/case-normalized keys. Unmatched monsters preserve object identity; matched monsters are copied and replace both `lairActions` and `regionalEffects` exactly, including missing group fields becoming own `undefined` values; the input collection remains untouched. Focused UTF-8 coverage locks canonical Ukrainian matching, empty overrides, invalid-field fallback, numeric coercion, array rejection, matched replacement, unmatched identity, and immutability. Fallow removes `getMonsterLegendaryReference` at CC 7/cognitive 4/CRAP 56; its aggregate and all extracted policies have no finding, with no new production export.
- Decomposed canonical Bestiary identity into private normalized-name eligibility/equality and normalized-source equality behind the unchanged `isSameMonsterIdentity` facade. Both names are still read and normalized before eligibility is decided; an empty normalized left name rejects the pair; source values are read only after non-empty names match. Trim/case normalization and nullish/string coercion remain exact, including two missing sources comparing equal and numeric/string-equivalent sources matching. Valid-name comparison remains symmetric, while nullish, empty, name-mismatch, and source-mismatch cases remain false. Focused UTF-8 coverage also locks getter read/short-circuit order plus the existing selection/favorite/refresh consumers. Fallow removes the final high finding in `bestiaryBrowser.ts` (`isSameMonsterIdentity`, CC 7/cognitive 2/CRAP 56); the file now has zero findings in the top-300 health report and no new export.
- Further decomposed Bestiary list-row presentation into private CR fallback, favorite-title, next-selection, and primary-action-title projections behind the unchanged `getBestiaryMonsterRowPresentation` plan. Canonical selected/favorite/custom identity remains shared; select still wins over add; selected rows clear selection while unselected rows return the original monster object; and title output remains exact `Insert`, `Add to encounter`, or `null`. Explicit string `imageUrl`, including `""`, still wins over the fallback token; non-strings fall back; and all falsy CR display values remain `--`. Existing matrix coverage locks no-action/select-over-add/add-only/favorite/selected behavior, with added whitespace-custom, numeric-image, and empty-CR edges. Fallow removes the former top moderate model finding at CC 6/cognitive 6/CRAP 42; the aggregate and extracted policies have no finding and add no export.
- Decomposed custom Bestiary refresh selection into private pending-name recovery and current-custom identity recovery behind the unchanged `getCustomRefreshSelection` facade. Pending recovery still has absolute precedence, canonicalizes only the name, intentionally ignores `pendingSelection.source`, skips official entries, selects the first matching custom entry, and preserves that exact collection object. Empty/missing/unmatched pending names fall through. Current recovery still requires a normalized `CUSTOM` source and then uses canonical name/source identity to recover the matching collection object; official, missing, and unmatched current selections return `null`. Focused UTF-8 coverage locks wrong-source pending references, official-first lists, duplicate custom names, pending-over-current precedence, normalized current fallback, official rejection, missing matches, and null inputs. Fallow removes `getCustomRefreshSelection` at CC 6/cognitive 4/CRAP 42; its facade and private policies have no finding and add no export.
- Decomposed Bestiary reference matching into private canonical-name and optional-source policies behind the unchanged `monsterMatchesReference` facade. A null reference still returns `false` without reading the monster. Name comparison retains canonical trim/case/string coercion but deliberately has no identity-style non-empty eligibility guard, so a nullable monster can still match an empty reference name. A name mismatch returns before either source is read. The reference source is normalized next; an empty normalized value remains a wildcard and returns without reading the monster source, while a non-empty value uses canonical comparison, including numeric/string-equivalent sources. Focused getter-based and UTF-8 coverage locks null rejection, empty-name equality, both short-circuit stages, whitespace wildcard behavior, Ukrainian names, and numeric source equivalence. Fallow removes the final reported model finding at CC 6/cognitive 3/CRAP 42; `bestiaryBrowser.ts` now has zero findings even in the top-500 health report, with no export added.
- Extracted ready Bestiary AI monster-edit execution into the Node-safe, dependency-injected `executeAiMonsterEditRequest` boundary. React still owns skip/invalid handling, localized validation feedback, busy/error start ordering, `AbortController` creation, active-controller identity, and the settled effect that conditionally clears that controller before releasing busy state. The executor forwards the exact ready-plan payload and caller signal, normalizes nullable/untrusted transport output, applies the normalized result to the original target before resetting editor state, classifies failures from transport or either success effect through the existing error policy, suppresses only `DOMException` `AbortError`, returns a discriminated success/cancelled/failed outcome, and invokes settlement exactly once through `finally`. Focused async UTF-8 coverage locks payload/signal/target identity, normalization, generate → apply → reset → settle order, cancellation without an error effect, ordinary failure projection, apply-effect failure without reset, original error identity, and guaranteed settlement. Fallow removes `saveAiEditedCustomMonster` at CC 6/cognitive 6/CRAP 42 without adding a model finding; the remaining Bestiary UI findings are moderate and the model remains finding-free in the top-500 report. The executor is intentionally exported only through the widget's Node-safe model facade.
- Extracted Bestiary AI generation-result interpretation into the Node-safe `getAiMonsterGenerationResultPlan` boundary with private draft and update projections. Exact `draft === true` plus a truthy response retains absolute precedence, so neither `updated` nor `generated` is read on that path; the feature-owned source-image helper enriches the response before the draft effect is returned without mutating the source entry. A draft flag without a response still falls through to update handling. Update eligibility retains the original truthy `updated` check, preserves updated and generated identity, projects `selectedName` only for exact `edit` mode, and always disables update undo tracking; other results become an explicit no-op. React now only executes the draft or update effect. Focused UTF-8/getter coverage locks draft precedence and lazy reads, image enrichment and immutability, response-missing fallthrough, exact object identity, edit/local-edit selection behavior, update paths that never read token fields, truthy non-object updates, and falsy-update skips. Fallow removes `applyAiGenerationResult` at CC 6/cognitive 5/CRAP 42 without adding a model finding; `bestiaryBrowser.ts` remains finding-free in the top-500 report. The plan is intentionally exported only through the widget's Node-safe model facade.
- Extracted realtime Bestiary synchronization execution into the Node-safe, dependency-injected `executeBestiarySyncEventPlan` boundary with focused favorites, pending-selection, suppression, and reload helpers. A null plan returns before reading any injected dependency. For a valid plan, the favorites transport is invoked synchronously first and its promise is exposed only as a completion handle; pending-selection assignment, auto-selection suppression, and reload execute immediately afterward without awaiting refresh. Successful arrays retain exact identity, nullish results become a fresh empty array, and rejection forwards the original error to logging without rolling back or blocking the already-applied synchronous effects. Pending references also retain exact identity, false plan flags invoke no corresponding callback, and the UI reload effect keeps its functional monotonic increment. The React effect now only builds the plan and injects transport, logging, refs, and setters, while its dependency remains exactly `syncEvent`. Focused async/Proxy/UTF-8 coverage locks null-plan dependency silence, refresh-start-first ordering, pending → suppression → reload order, non-blocking settlement, nullable fallback, array/reference identity, false-flag silence, monotonic reload simulation, and failure-after-sync ordering. Fallow removes the anonymous effect at CC 6/cognitive 5/CRAP 42 without adding a model finding; only three moderate CC 6/cognitive 4 Bestiary UI findings remain in the top-500 report. The executor is intentionally exported only through the widget's Node-safe model facade.
- Extracted Bestiary field-editor save orchestration into the Node-safe, dependency-injected `executeBestiaryFieldEditSave` boundary with focused transport and previous-name policies. The editing monster is captured once; null or falsy-name targets return a discriminated skipped outcome before any draft, mode, transport, or effect dependency is read. Create-based mode receives the exact draft and uses an empty previous name; edit mode receives exact draft/editing references and forwards the raw untrimmed editing name. Both modes retain updated-monster identity and execute apply → close before returning success. Exceptions from transport, apply, or close share one failure path, forward the original error unchanged, suppress later success effects, and return a discriminated failed outcome; React owns the localized alert plus injected API/state callbacks. Focused async/Proxy/UTF-8 coverage locks dependency-silent null and empty-name skips, create/update exclusivity, exact identities, raw previous-name behavior, success order, and transport/apply/close failure ordering and identity. Fallow removes `saveEditedCustomMonster` at CC 6/cognitive 4/CRAP 42 without adding a model finding; two moderate Bestiary UI findings remain in the top-500 report. The executor is intentionally exported only through the widget's Node-safe model facade.
- Extracted Bestiary selected-source persistence into the Node-safe, dependency-injected `executeBestiarySelectedSourcesSave` boundary with focused campaign, global, saved-value, and scope-selection policies. Ignore-list derivation receives exact filter/selection references and remains before the auto-selection ref effect; both execute outside the persistence `try`, preserving their historical error boundary. A truthy campaign slug is forwarded verbatim and exclusively executes campaign update → campaign reload → campaign dispatch; the update result is ignored, a successful campaign array retains identity, and only nullish reload results fall back to a fresh empty array. Global scope never touches campaign dependencies, forwards the requested-list identity in its payload, accepts any array-valued saved list by identity, and otherwise reuses the requested list. Persistence or success-effect failures retain auto-selection, run log → alert with the original error, and return a discriminated failure; campaign/global success returns its exact scope and requested list. The UI callback now only injects derivation, refs, transports, actions, logging, and localized alert effects, retaining `[activeCampaignSlug, dispatch, filterSourceOptions]`. Focused async/UTF-8 coverage locks derivation inputs and external failure boundary, whitespace slug truthiness, campaign/global exclusivity and order, payload/list identities, nullable campaign fallback, array-only global acceptance, requested-list fallback, no rollback, log-before-alert, and store-effect failure handling. Fallow removes `saveSelectedSources` at CC 6/cognitive 4/CRAP 42 without adding a model finding; `restoreAiDraftResponse` is now the sole Bestiary UI finding in the top-500 report. The executor is intentionally exported only through the widget's Node-safe model facade.
- Extracted the final Bestiary AI draft-restore lifecycle into the Node-safe, dependency-injected `executeAiDraftRestore` boundary over the existing start/result plans. A null start returns before reading any other dependency. Valid execution sets busy state before guarded work, routes exclusively to apply or undo with the exact entry and `{ resourceIds }` payload, interprets nullable results, and applies effects in next-entry → optional undo snapshot → custom update order. Transport, planning, and success-effect failures share one error callback with original error identity; later success effects stop after a failure; settlement always clears busy state in `finally`, including when the error callback itself throws. The UI now only builds the start plan and injects API, state, undo, update, and localized alert adapters. Focused async coverage locks dependency-silent skips, apply/undo exclusivity, payload/list identity, both success paths, transport and entry-effect failures, effect short-circuiting, original failure outcomes, and `finally` cleanup after a throwing alert. Typecheck, 202 regression tests, lint plus UTF-8 validation, and the architecture gate pass; Fallow reports zero findings for the complete `widgets/bestiary-browser` slice in the top-500 health report without suppressions.
- Completed Phase 9 for the three high-severity `widgets/monster-stat-block` targets. `MonsterTokenSection` now delegates mode, cancel visibility, and replace visibility to `getMonsterTokenSectionPresentation`, then composes private dropzone/image/skeleton sections; browser-only drag execution retains `copy` plus `text/uri-list → text/plain → text/html → DownloadURL` write order and remains a no-op without a payload. `MonsterNameRow` delegates name coercion, favorite title/state, and action visibility to `getMonsterNameRowPresentation`, then composes private name/favorite/entity/add controls. Name click versus copy behavior, truthy tooltip disabling, lazy translations, exact callback monster identity, favorite/add callback ownership, and button variants/classes remain unchanged. `executeMonsterAction` provides the Node-safe identity-preserving callback boundary. Token-source resolution now uses a focused first-truthy policy while preserving custom URL → monster image → override → model fallback precedence, distinct local/external fallbacks, string-only monster image normalization, nullish override behavior, whitespace truthiness, and the intentionally untrimmed case-only `CUSTOM` check. The obsolete unused `effectiveName` token-section prop was removed. Focused UTF-8 coverage locks source precedence, lower/whitespace custom sources, malformed image values, every section mode, both replace eligibility branches, favorite/action matrices, falsy-name coercion, and callback identity. Fallow removes `MonsterTokenSection` and `MonsterNameRow` (both formerly CC 9/CRAP 90) plus `getMonsterTokenSources` (formerly CC 7/CRAP 56); the complete slice now has zero high findings without suppressions.
- Completed Phase 10 and reduced the complete `widgets/monster-stat-block` slice to zero top-500 Fallow findings. Token upload orchestration now belongs to the dependency-injected Node-safe `executeMonsterTokenUpload` boundary. Invalid or empty URLs return before reading other dependencies; valid uploads apply provisional URL → clear image error first. Injected callbacks receive the original monster and URL, skip persistence, then clear replacement state; callback exceptions still propagate before cleanup. API-owned uploads persist by the existing mutation key, apply a truthy string server URL or the provisional fallback, then clear replacement state. Transport or API-success-effect failures retain replacement state, forward original error identity to the persistence logger, and return a discriminated failure; logger exceptions still propagate. Metadata planning now owns exact name/original-name string narrowing and truthy source visibility, while private UI components preserve changed-field classes, source label, highlight calls, and the always-rendered size/type/alignment row. Structured spellcasting now projects optional header/footer identity, at-will content, insertion-ordered daily lines, and level lines with exact cantrip/level and truthy-slot labels before private UI composition. Empty arrays remain renderable because they are truthy in the original contract; missing collections become absent lines. Token dropzone eligibility retains guard and getter short-circuit order through focused manage/need policies, while section cancel/replace conditions are separately derived. Focused async/Proxy/UTF-8 coverage locks injected/API exclusivity, state/effect ordering, canonical and fallback URLs, transport and injected-callback failures, mutation identity, metadata variants, empty content, negative/zero slot behavior, and token guard reads. Fallow removes all five former moderate CC 6/CRAP 42 findings (`handleTokenUpload`, `MonsterMetadata`, structured-spellcasting renderer, `shouldShowMonsterTokenDropzone`, and `getMonsterTokenSectionPresentation`) with no replacement finding or suppression.
- Completed Phase 11 and reduced `widgets/spells-browser` to zero top-500 Fallow findings while leaving the already-clean lower `widgets/spell-card` unchanged. The embedded one-shot scroll effect now consumes `getInitialSpellScrollPlan`: focused eligibility, source-qualified reference matching, raw key projection, unseen-key gating, and displayed-index projection preserve disabled/missing/mismatch no-ops, source wildcard matching, first duplicate selection, exact selected identity, missing displayed targets, and once-only behavior. React records the key before requesting one animation frame and cancels that frame on cleanup exactly as before. `SpellListItem` now consumes `getSpellListItemPresentation` and private school/classes/source controls. The plan preserves source resolver invocation, `name.split("|")[0]` capitalization input, strict `level === 0`, translated non-cantrip level variables including `undefined`, case-sensitive school codes, string-only class filtering with empty strings retained, truthy raw source visibility, tooltip disablement from the resolved full name, active state, and selected-row toggle to `null` versus original spell identity. `executeSpellInsertAction` preserves exact optional double-click no-op and callback identity. Focused UTF-8 coverage locks displayed/all/first-item selection precedence, retained current selection on missing targets, every scroll rejection and success route, Ukrainian keys and metadata, resolver/translator inputs and order, malformed class values, whitespace sources, invalid schools, and insert identity. Fallow removes the effect (formerly CC 8/CRAP 72) and `SpellListItem` (formerly CC 7/CRAP 56); a temporary moderate scroll-plan aggregate was further split into private key/unseen/index policies, leaving the complete slice finding-free without suppressions.
- Completed Phase 12, the first `widgets/rules-reference-modal` complexity pass. Inline insertion syntax now uses a closed per-tab policy table while retaining condition/status kind selection, simple disease/sense/skill/variant tags, source-qualified spell/creature identity, trimmed names, and empty-name rejection. Backspace history routing uses `getReferenceKeyboardPlan`; React still owns editable-target detection, `preventDefault`, the window listener, and history effects. Tab routing uses `getReferenceTabSelectionPlan`; the validated seven-tab allowlist, existing-selection precedence, first-item fallback for ordinary tabs, embedded-spell pending navigation, history recording, refs, and active-tab state remain exact. Focused UTF-8 tests cover tag kinds, qualified Ukrainian names, every modifier/editable/back-history rejection, ordinary versus embedded tabs, and invalid IDs. Fallow reduced the slice from six high findings to three: `getReferenceInlineTag`, `handleKeyDown`, and `selectTab` are no longer high, and the extracted plans add no replacement high finding or suppression.
- Completed Phase 13 and removed all remaining high Fallow findings from `widgets/rules-reference-modal`. `getReferenceSelectionReconciliationPlan` now owns loaded/loading and embedded-spell guards, active-selection retention, filtered-selection retention, empty-result clearing, and first-filtered-item fallback; `applyReferenceSelectionReconciliationPlan` preserves the existing state object for an already-empty clear and otherwise applies only the active tab. `getReferenceScrollPlan` keeps the one-shot request unconsumed while blocked, uses source-aware item resolution, returns exact zero-based indexes, and returns `-1` after an eligible miss so React still consumes the request without scheduling a scroll. React remains responsible for refs, state setters, deferred list scrolling, and effect dependencies. History availability and private meta/optional-handler projection were also extracted from the component body, removing its aggregate CC 7/CRAP 56 finding. Focused UTF-8 coverage locks every guard, no-op identity, ordinary/embedded ownership, source-qualified duplicate creature ranking, index zero, eligible misses, and all back/forward boundaries. The two effects and component aggregate are no longer high, with no replacement high or suppression; the slice now has zero high and fifteen moderate findings.
- Completed Phase 14 and reduced `widgets/rules-reference-modal/model` to zero Fallow findings. Bestiary response reading now uses an ordered `monster`, `monsters`, `results` candidate policy with exact nullish precedence: direct arrays remain accepted, null falls through, and a present non-array payload intentionally shadows later candidates and normalizes to empty. Qualified identities use one truthy-value string policy plus trimmed non-empty segments, preserving empty/falsy names, optional sources, and exact source casing. Tab selection validates the closed tab catalog, delegates ordinary versus embedded name choice, and constructs immediate-history versus pending-navigation outcomes explicitly. Reference-name matching now dispatches through per-tab policies: ordinary tabs retain raw-name equality, spells retain qualified-or-raw matching, and Bestiary retains positive-rank fallback. Creature ranking keeps `0` mismatch, `1` source fallback, `2` source wildcard, and `3` exact source; lookup selects the first exact result or the first positive fallback, preserving first equal-rank stability and stopping after exact resolution. Focused malformed-envelope, whitespace/falsy, UTF-8, tab-precedence, rank, casing, duplicate, raw-spell, and post-exact getter coverage locks these contracts. All six former moderate model findings were removed without replacements or suppressions; the full slice now has zero high and nine moderate UI findings.
- Completed Phase 15 and reduced the complete `widgets/rules-reference-modal` slice to zero Fallow findings. Navigation requests now normalize truthy request IDs, deduplicate handled IDs, and distinguish force-tab-without-name from named reference navigation. Initial navigation uses a priority plan for force reference, force tab, explicit initial reference, saved history, then validated default tab; execution preserves force-reference tab clearing before named navigation. Tab loading selects missing/unrequested active or global-search tabs, keeps requested-tab retry deletion on failure, gates every post-await state/alert/finalization effect by mounted state, preserves exact `Error.message` versus localized fallback text, and delegates ordinary first-item versus embedded-spell selection identity. Tab clicks apply scroll, pending history, navigation recording, and active-tab effects in the original order. The host records each request before suppressing modal opening when one is already active. Details, embedded spells, ordinary sidebar states, and creature rows were split into private compositions: Bestiary still renders `MonsterStatBlock`, other references still recurse through rich content, list keys retain tab/query/search mode, loading and empty states retain localized copy, and creature rows retain custom/local token fallback, image-error hiding, size/type/source/tooltip/CR rules. Source-ownership assertions now follow the model/View split, and focused orchestration tests lock request deduplication, precedence, execution order, state identity, loading eligibility, errors, pending tabs, and host suppression. All nine former UI moderate findings and two temporary model replacements were removed without suppressions.
- Completed Phase 16, the model pass for `widgets/campaign-search`. Deep searchable-text projection now dispatches nullish, scalar, array, record, and ignored values through focused private policies while retaining recursive order, newline joins, numeric coercion, and nested `_...`/`imageUrl` exclusion. Snippet construction separates normalized match lookup and ellipsis projection while preserving collapsed whitespace, original casing, the 70-character lead, 180-character window, absent-query fallback, and exact prefix/suffix boundaries. Entity titles retain first/last-name precedence followed by `name`, `title`, fallback, and localized untitled text. Campaign index loading now separates four-way parallel source acquisition, ordered per-session hydration, campaign/current-data merging, and nullish collection fallback; injected API call order, `Promise.all` result ordering, complete hydration, and original source/hydration error identity remain intact. Focused UTF-8 regression coverage locks malformed recursive values, nested exclusions, exact snippet edges, name precedence, empty-array overrides, nullish fallback, request order, invalid session normalization, and fail-fast errors. Fallow removed all four former high findings (`campaignSearchValueToText`, `buildCampaignSearchSnippet`, `getEntityName`, and `loadCampaignSearchIndex`) without replacements or suppressions; only three pre-existing moderate findings remain in the slice.
- Completed Phase 17 and reduced the complete `widgets/campaign-search` slice to zero Fallow findings. `executeCampaignSearchIndexLoad` now owns dependency-injected index execution, exact `Error.message` versus localized fallback projection, success/error/loading effect order, and cancellation checks around every post-await effect; the private controller retains campaign selection, initial loading/error resets, filter/query state, memoized filtering, and effect cleanup. The modal shell moved to a private View with focused filter and loading/error/results compositions, while target navigation, delayed hash scrolling, and conditional URL hash cleanup remain browser-owned. Recursive Markdown mention rendering now delegates element/children and `code`/`pre` exclusion checks to narrow private policies. Scene result construction separates truthy ID fallback, lazy translated title fallback, target projection, and note expansion while preserving zero-based fallback IDs, titles, subtitles, text, hashes, and note identity. Focused async and UTF-8 tests lock success/failure/finalization order, fallback errors, complete cancelled-effect suppression, scene title precedence, lazy fallback translation, and stable scene IDs. The three former moderate findings were removed without replacements or suppressions.
- Completed Phase 18, the first campaign-graph complexity pass. React Flow node mapping now delegates current-position → layout-position → fresh-zero fallback, type sizing/color/label fallback, selection, focus muting, openability, visibility, and type-class decisions to `getCampaignGraphFlowNodePresentation`. New-node collision placement is a pure sequential projection: fresh layouts retain the projected array, existing-node IDs are skipped, each new node is resolved against the latest projected positions, inputs remain immutable, and unaffected node identity is retained. React still owns the Flow node shape, callbacks, translation, refs, and topology decisions. Session-scene graph construction now separates ID/name/label, four-field summary ordering, all-text detail ordering, aliases, navigation metadata, and `texts`-preferred mention source from command orchestration; contains/mention/note/sequence command order and zero-based fallback identities remain unchanged. Focused UTF-8 tests lock position precedence, visibility/selection/muting/openability, color/type fallbacks, fresh/preserved collision behavior, scene summary/detail order, aliases/meta, note identity, and `texts`-only mention scope. Fallow removed both CC 9/CRAP 90 targets (`nextNodes` and `projectSessionScene`) without replacement findings or suppressions.
- Completed Phase 19, the graph normalization pass. Minimap sizing now delegates to the shared `getCampaignGraphFlowNodeSize` policy, so measured → direct node dimension → style → direct-type/nested-type default precedence, independent width/height recovery, positive finite values, and `parseFloat`-compatible CSS-like strings stay identical across collision and minimap geometry. Recursive graph text extraction now dispatches nullish, string, array, record, and ignored values through focused private collectors while preserving mutation of the shared output accumulator, depth-first array/object order, root `text` fallback, indexed/dotted field paths, whitespace-only rejection, and nested private/known-field exclusion. Session-detail lookup now selects focused Map, array, or record readers after the unchanged missing-store/empty-filename guard; Map keys remain authoritative, arrays retain first exact `fileName` match, records retain direct-key lookup, and missing/falsy entries normalize to `null`. Focused malformed/UTF-8 coverage locks nested path metadata, ignored `_...`/`imageUrl` fields, primitive/null handling, Map/array/record selection identity, first-array-match precedence, dimension source precedence, direct type priority, and per-axis fallback. Fallow removed all three Phase 19 findings (`getCampaignGraphMiniMapNodeSize`, `collectStrings`, and `getSessionDetail`) without replacements or suppressions.
- Completed Phase 20 and removed every high finding from `pages/campaign/model/campaignGraph.ts`. Character/NPC labels now compose truthy first/last values before the unchanged name → title → fallback chain and final trim. Session projection separates deterministic filename fallback, Map/array/record detail lookup, truthy session-data recovery, list-name → detail-name → numbered-label precedence, result/detail/source-ID projection, and node construction. Node and edge registry operations moved out of the aggregate: first node identity still wins, labels/meta/default aliases normalize once, aliases retain insertion order and duplicates, invalid/self edges are rejected, related endpoints canonicalize by `localeCompare`, and duplicate edges mutate count/source history in place. Campaign, person, location, and session collections now project through focused commands; pending mentions still flush only after every alias exists, propagated mentions suppress related edges, unresolved aliases are reused, and finalization clones node records, filters orphan edges, counts each retained edge once per endpoint degree, and derives exact stats. Focused UTF-8 coverage locks first-node-wins identity/content, fallback labels, unresolved reuse, canonical related-edge identity, duplicate count/source order, degree semantics, and stats. The six former highs (`getCharacterLabel`, `createCampaignSessionProjection`, `createCampaignSessionNode`, private `addNode`/`addEdge`, and `buildCampaignGraph`) were removed without replacement highs or suppressions; the file now has only six moderate findings.
- Completed Phase 21 and reduced `pages/campaign/model/campaignGraph.ts` to zero Fallow findings. Bracket parsing now separates string/marker eligibility from per-match whitespace normalization and non-empty accumulation while retaining regex order, duplicates, malformed/newline rejection, and non-string fallback. Recursive text kind selection uses the same nullish/array/`typeof` dispatch pattern as its collectors. Note projection now has focused regular-title and eligibility policies: virtual/null/empty notes still skip before commands, ordinary titles retain precedence, simplified notes ignore titles, Markdown-stripped text remains the next label/summary source, empty stripped text falls back to the numbered label, aliases stay empty only in simplified mode, and add-node → contains-edge → mention-queue order is unchanged. Session detail projection delegates its result, NPC, location, note, and scene collections without changing order or truthy malformed-input behavior. Edge insertion now narrows valid non-empty/non-self endpoints before canonicalization and still mutates duplicate edge counts/sources in place. Focused malformed and UTF-8 coverage locks non-string/no-marker/empty/newline/duplicate mention cases, regular/simplified/Markdown-only/virtual/empty note behavior, aliases/meta, and exact session participant/note/scene insertion order. All six former moderate findings were removed without replacements, new exports, or suppressions.
- Completed Phase 22 and reduced `pages/campaign/model/campaignGraphPresentation.ts` to zero Fallow findings. Edge styling now uses relation presentation records plus focused animation/count policies; opacity uses explicit idle/focused/muted readers while preserving contains/sequence/default values and muted precedence. Editable-note lookup dispatches campaign, session, and scene scopes through identity-preserving finders. Open-target resolution separates session, entity, and note plans; character lookup remains campaign-owned, while NPC/location lookup retains campaign-versus-session scope and slug fallback. Note-save narrowing uses closed note-type and string/number resource-ID guards, preserves numeric zero, forwards update object identity, and normalizes invalid scene IDs to `undefined`. Focused UTF-8 matrix coverage locks relation styling, focus/muting/animation/count behavior, every opacity mode, campaign/session/scene note identity, session-scoped entity recovery, session/note/no-op targets, runtime ID rejection, and update identity. All five former high findings and the moderate open-target finding were removed without replacements, new public exports, or suppressions; the regression suite now has 220 passing tests.
- Completed Phase 23 and removed all four high findings from private `pages/campaign/ui/components/CampaignNotesGraph.tsx`. Parsed detail text now consumes a pure text/accessibility plan and a pointer/keyboard activation policy while React retains Markdown mention recursion and DOM interactive-target detection. Node cards consume a class/summary/degree/open plan and render handles through a focused private component. Selected details split header, stats, and connections into private compositions without widening the page API. Initial fit eligibility moved to a pure six-condition topology policy; the effect still owns animation-frame scheduling, cancellation, fitted-topology tracking, and the nullable React Flow instance. Focused UTF-8 matrix coverage locks falsy text normalization, click/keyboard activation, interactive descendant suppression, node-card class/visibility decisions, and every fit rejection condition. The four former CC 7/CRAP 56 findings were removed without replacements or suppressions; only seven moderate findings remain in the private UI and the suite now has 221 passing tests.
- Completed Phase 24 and reduced both private `pages/campaign/ui/components/CampaignNotesGraph.tsx` and its `campaignGraphPresentation.ts` policy model to zero top-500 Fallow findings. Markdown child traversal now delegates each child to focused string/recursive-element policies while retaining `code`/`pre` identity and recursive mention rendering. Canvas error, loading, and query-sensitive empty states are independent private compositions, preserving simultaneous error/loading output and empty-state precedence. Topology keys now use a Node-safe nested graph-type projection; flow reconciliation uses an explicit stable/fresh plan based on campaign continuity, empty state, topology changes, and manual positions. Open-target effects route through injected session/entity/note handlers, while modal construction and note-save narrowing remain UI-owned. Connection presentation derives the opposite node, count/source metadata, and session-versus-selection action without cloning; UI retains translation, mention rendering, and callback execution. Focused UTF-8 coverage locks topology ordering/type precedence, all fresh-layout branches, manual-position preservation, connection identity/metadata/actions, missing-node no-ops, and exact open-target handler routing. All seven former moderate findings were removed without replacements or suppressions; the suite now has 222 passing tests.
- Completed Phase 25 and reduced `campaignGraphLayout.ts` plus `campaignGraphNoteSave.ts` to zero top-500 Fallow findings. Flow dimensions now separate string `parseFloat`/raw candidate selection from positive finite validation while retaining measured → direct → style → type fallback precedence. Position recovery delegates each axis to finite-number normalization. Collision resolution now composes collection normalization, dragged-node lookup/eligibility, finite non-negative margin recovery, visible peer projection, and deterministic nearest free-candidate selection; missing/hidden nodes retain their normalized origin and inputs remain immutable. Layout orchestration separates non-null ID string normalization/sorting, object-or-scalar endpoint normalization, self/orphan edge rejection, stable edge sorting, seeded simulation-node creation, force configuration, fixed tick execution, and final collision cleanup. The campaign node remains fixed at zero and numeric ID `0` remains valid. Note-save planning now dispatches validated note/update bases through focused campaign/session/scene readers, preserving update identity, truthy filename requirements, numeric zero note/scene IDs, explicit no-op plans, and the legacy unknown-type scene fallback. Focused malformed and identity coverage locks null collections, invalid nodes/edges, object endpoints, deterministic invalid-edge exclusion, hidden/non-finite positions, negative/NaN margins, invalid updates/IDs, missing scene IDs, and fallback routing. All four former high findings and the moderate layout aggregate were removed without replacements or suppressions; the suite remains at 222 passing tests.
- Completed Phase 26 and reduced `campaignPagePresentation.ts` plus `CampaignPage.tsx` to zero top-500 Fallow findings. Editable-target detection now uses a closed exact-case `INPUT`/`TEXTAREA` membership check plus truthy `isContentEditable`, preserving lowercase rejection and legacy truthy non-boolean behavior. Hash resolution uses an ordered marker table, retaining note → character → NPC → location precedence and `String(hash || "")` coercion. A pure hash-navigation plan owns list-mode selection and collapsed-section expansion; a dependency-injected executor preserves list-before-expand effect order, while URI decoding, timers, scrolling, and cleanup remain in the page. Entity AI-ignore planning selects the NPC/location collection by closed type, rejects only `undefined` IDs before strict identity lookup, returns an immutable entity copy with the requested flag, and exposes an explicit no-op; the page routes the plan to the existing feature callbacks. Focused malformed and UTF-8 coverage locks competing hash markers, custom string coercion, falsy hashes, collapsed/expanded/missing targets, effect silence/order, numeric zero IDs, strict numeric-versus-string mismatch, collection ownership, source immutability, and editable-target casing/truthiness. All four former moderate findings were removed without replacements or suppressions; the suite remains at 222 passing tests.
- Completed Phase 27 and removed all five high findings from `pages/campaign/model/useCampaignView.ts`. The Node-safe `campaignViewOrchestration.ts` boundary now owns campaign-state projection, nullable entity normalization, sync-event scope/resource planning and ordered execution, fail-open image checks, localized delete-confirmation projection, delete execution outcomes, AI update planning, and injected entity reload routing. The hook retains React state/refs, history transitions, feature hooks, store dispatch, navigation, and user-facing notifications. State projection preserves truthy fallbacks and note/entity identity; sync ignores absent versions and foreign campaigns, reloads entities before sessions for shared resources, and keeps images entity-only; deletion checks images before confirmation, defaults to image-safe handling on check failure, honors cancellation, and navigates only after API success; AI refresh preserves explicit empty entity-type requests, normalizes nullable responses, and still requests the global campaign reload. Focused UTF-8 synchronous/asynchronous coverage locks defaults, identity, the complete sync matrix and effect order, image-check failure, both confirmation forms, delete cancellation/success, AI defaults/empty selection, nullable collections, and setter routing. Fallow reports only the two pre-existing moderate `handleCreateSession` and `handleRename` findings in the hook and zero findings in the new orchestration module; the suite now has 223 passing tests.
- Completed Phase 28 and reduced both `pages/campaign/model/useCampaignView.ts` and `campaignViewOrchestration.ts` to zero top-500 Fallow findings. Session creation now uses an explicit cancelled/create plan plus an injected executor with created/cancelled/failed outcomes; only prompt `null` cancels, string values retain exact identity for backend sanitization, and other prompt values preserve the legacy empty-name date fallback. Successful creation still appends the returned session, navigates to its filename, then requests campaign reload; nullable API success is treated as the same explicit failure, and status-bearing errors retain the localized `[Status: value] message` format. Campaign rename uses a cancelled/rename plan that rejects non-string, empty, and unchanged values without introducing frontend trimming; the executor owns the exact `{ name }` patch, nullable-result failure, and callback outcome. The hook retains prompts and applies reload-before-replace-navigation only after success. Focused UTF-8 coverage locks raw whitespace identity, non-string/date fallback, cancellation silence, exact API arguments, callback order, nullable results, thrown-error identity, status formatting, rename eligibility, and localized rename errors. No suppressions or replacement findings were introduced; the suite now has 224 passing tests.
- Completed Phase 29 and removed all six targeted high findings from the session page boundary. Session entity display names now dispatch location/NPC precedence through focused truthy-value and full-name policies while preserving whitespace trimming only after candidate selection, current/legacy fields, non-string truthy coercion, and localized fallback. Exact uppercase input/textarea plus truthy contenteditable recognition moved into the Node-safe keyboard policy. Realtime sync uses a closed resource-policy table and a separate pending-save policy: AI still discards pending state and reloads, session/import/entity/image events reload only without pending writes, unknown/foreign/missing-version events remain ignored, and string-compatible session IDs including numeric zero are preserved. Note-content checks delegate title/text normalization instead of retaining the former anonymous high callback. Session page data and scope-import presentation are pure model projections with identity-preserving data fallback. The private UI now splits scope-import loading/empty/item composition, session-scoped entity resolver/modal composition, hash navigation, and outside-click ownership; `SessionView` only composes these boundaries and the public page API is unchanged. Focused malformed and UTF-8 coverage locks name precedence/whitespace, editable-target casing, the complete sync-resource/pending matrix, note emptiness, localized scope copy/type, and session-data identity. The roadmap wording was corrected: the anonymous model high was note-content matching, not sync-resource selection. Fallow reports only `SceneNotes` plus three pre-existing moderate findings across the session slice, with no replacement highs or suppressions; the suite remains at 224 passing tests.
- Completed Phase 30 and reduced the complete `pages/session` slice to zero top-500 Fallow findings. Loaded-session normalization moved from the hook callback into the Node-safe `normalizeSessionPageSession` policy: non-object input becomes an empty session, objects without `data` preserve exact identity, and data-bearing sessions immutably retain unknown fields while normalizing notes, scenes, scoped NPCs, and locations. Scene normalization preserves scene fields, defaults notes, and boolean-normalizes note collapse. Entity ID recovery now dispatches through closed string/number readers, retaining truthy strings and finite numbers including zero while applying the exact location/NPC timestamp prefix to empty or invalid values. Session rename now uses an explicit cancelled/rename plan and synchronous executor; non-string, empty, and unchanged prompts remain silent, raw string identity is preserved, and the hook still delegates `{ name }` to the existing delayed-save owner with immediate mode. Scene-note rendering consumes a pure presentation containing normalized notes, virtual render rows, content/collapse state, bulk-action visibility/direction/labels, and list visibility; UI retains drag callbacks, localized rendering, and card composition. Focused malformed and UTF-8 coverage locks object identity, unknown fields, null notes/scenes, zero and invalid IDs, raw rename values, cancellation silence, virtual trailing notes, collapse refusal without content, and both bulk-action directions. The prior roadmap's nullable API/error wording was corrected because session rename schedules through `useSessionEditing` rather than calling transport directly. No replacement findings or suppressions were introduced; the suite remains at 224 passing tests.
- Completed Phase 31 and removed all seven high findings from the encounter model boundary. Editable-target recognition now follows exact uppercase input/textarea plus truthy contenteditable policy; history routing uses a closed KeyZ/KeyY plain/shift table after native-editor ownership checks. Encounter loading separates string-compatible ID lookup, retry/not-found/loaded planning, and injected effect execution while the hook retains transport, active-session dispatch, the existing 300ms retry, localized failure/back navigation, selected-first-participant state, and optional history reset. Import parsing validates raw text and the monster array, preserves truthy imported-name fallback, injects clock/random sources for deterministic instance IDs, and retains entity-owned monster ID normalization. Dice processing now owns truthy/duplicate result-ID gating, marks a new result processed before context validation exactly as before, and applies a focused HP update only for matching campaign/session/encounter/instance context. Monster-drop planning preserves nullable/current-list projection, JSON order comparison, history-update suppression, reorder-ref clearing only for persistable state, undo → redo-clear → persistence ordering, and caller-owned participant identity. Focused malformed, numeric-zero, UTF-8, cancellation, effect-order, invalid-context, and import-ID coverage was added. The roadmap wording was corrected to identify the actual file-reader and dice-effect callbacks rather than participant lookups. All original highs and the temporary dice-target replacement high were removed without suppressions; only six moderate model findings remained and the suite reached 225 passing tests.
- Completed Phase 32 and reduced the complete `pages/encounter/model` boundary to zero top-500 Fallow findings. Encounter updates now use an explicit none/update plan and injected executor: state normalization occurs before effects; undo eligibility respects push flags, current state, and history application; defaults retain immediate save, persistence, undo, and null preferred selection; execution preserves undo/redo-clear → local state → selected participant sync → optional persistence order. Monster row stats use one nullish-precedence policy, retaining zero values and AC/max-HP fallback order. Navigation separates Bestiary Escape priority from editable-target blocking and a closed back-key set. Sync separates version/pending eligibility, campaign/session scope, and the exact sessions/AI/import resource allowlist. Dice HP narrowing splits required result/encounter/context/instance validation from route-context matching without changing the minimum-one roll or immutable participant replacement. Challenge rating parsing separates number passthrough, structured CR unwrapping, fractional parsing, and decimal fallback. Focused matrices lock update defaults/overrides/effect order, null update silence, state normalization, zero stat values, navigation priority, sync resources/pending writes/numeric session IDs, missing dice targets, and existing structured/fractional CR behavior. No replacement findings or suppressions remain; the suite stays at 225 passing tests.
- Completed Phase 33 and removed all seven high findings from private `pages/encounter/ui/EncounterPage.tsx`. Generated-monster and apply/undo restore result policies now live in the Node-safe encounter page model with injected draft, session-update, and participant-update effects; global restores still skip encounter replacement during undo, local restores still update the returned session, resource IDs remain exact, and draft source-image enrichment precedes presentation. The AI monster feature now owns an abort-aware request lifecycle executor that reports non-abort failures and always settles busy state. Private header identity/actions/view-mode/grid/history compositions and character picker/create/list/detail overlays replace the former aggregate branches without widening the page public API. Encounter request cleanup, outside-click dismissal, model loading, generation, and restore orchestration are focused lifecycles; render context narrows encounter/campaign/session once before the full page composition. Focused async cancellation/failure/finally, callback-order, local restore, generated identity, and Ukrainian-text coverage was added. The first test run also caught and removed a Node-safe model dependency on the UI-bearing AI-edit barrel. No high page-UI findings or suppressions remain; ten moderate encounter page/model findings are intentionally queued for the next pass, and the suite reaches 226 passing tests.
- Completed Phase 34 and reduced the complete `pages/encounter` slice plus the touched `features/ai-edit-monster` model to zero top-500 Fallow findings. The AI monster feature now exposes a Node-safe `model.js` facade for generated-result parsing without loading TSX and owns field-edit plans, local/persistent save execution, draft-save scope/null-result policy, restore request plans, request cancellation/finalization, and split edit/create persistence. Encounter model policies now own grouped-grid projection, available-character filtering, character double-select versus participant-select/focus plans, add-character and rename plans, navigation/history effect execution, player-create lifecycle order, and generated/restore result application. Private UI splits combat HP/max-HP controls and grid/single/character/monster detail composition; the aggregate consumes stable normalized collections. The page reuses `buildCreateEntityPayload`, while the existing client remains authoritative for removing `id`, `slug`, and `createdAt`. After the original ten findings were removed, newly visible top-500 entries in the hook/model were also split: shortcut effects, session envelope reading, load/import plans, drop history, selected-participant fallback, update defaults, AI HP merge, and dice HP projection. Focused tests preserve monster deduplication/representatives, character availability, selection and shortcut effect order, create/add/reset/finally order including nullable creation failure, raw rename behavior, numeric-zero AI history rejection, local save closure, Ukrainian identities, draft scopes, and edit identity. No suppressions were added; the suite reaches 228 passing tests.
- Completed Phase 35 and reduced `server/modules/ai/application/mentionProcessing.js` to zero top-500 Fallow findings. Candidate discovery now composes focused campaign, current-session, configured-session, generated-entity, and generated-operation collectors. Shared array/record readers tolerate malformed optional collections; configured sessions still require `conf.included`, non-empty scene configuration still becomes an allowlist, and empty/missing scene configuration still includes every scene. Operation payload selection preserves object `data` before object `patch`; character/NPC and location/faction alias sets route through their established display-name helpers, while scene operations collect nested NPC names. Final candidate normalization still text-coerces, removes names shorter than two characters, deduplicates exact values, and sorts longest-first. Recursive operation mention processing was also split by string/array/object shape, and generated-content mutation now has focused eligibility/operations helpers; identifier and unknown fields stay untouched while allowlisted text fields recurse. Focused coverage locks every source, generated duplicates, data precedence, excluded sessions/scenes/entities, empty scene filters, malformed collections, short-name rejection, existing-link canonicalization, mutation identity, recursive text fields, and Ukrainian names. The former critical collector, high recursive processor, and two moderate bracket/application findings were removed without suppressions; the suite reaches 229 passing tests.
- Completed Phase 36 and replaced the critical `applyAiOperations` branch tree in `server/aiPatchService.js` with explicit loading, state construction, operation selection, dispatch outcome, outcome aggregation, encounter-link finalization, persistence, and result-projection helpers. Custom-monster operations remain a separate batch; all other operations still run sequentially over shared client-ID, campaign-entity cache, warning, pending-link, and created-encounter state. Invalid/malformed operations remain silent, default scope and failed session-read fallback are unchanged, and note outcomes still classify campaign versus session writes from the effective entity scope. Pending scene links resolve before unlinked created encounters are removed. Campaign metadata persists before session data, and updated-result precedence remains session, then campaign, then standalone Bestiary; changed-monster output and warnings are unchanged. A focused mixed campaign/session regression locks exact persistence order, invalid-operation silence, Ukrainian UTF-8 data, and returned-session precedence. Fallow removes `applyAiOperations` at CC 47/cognitive 70 and all replacement findings in the new coordinator helpers without suppressions; the suite reaches 230 passing tests.
- Completed Phase 37 and decomposed the critical note-operation pipeline in `server/aiPatchService.js`. Target preparation now owns normalized entity/type/scope plus client-ID-aware identity and loads each campaign entity type into the shared cache once. Prototype-safe direct-target readers resolve campaign, session, and scene owners; focused scoped readers resolve session or campaign entities. A closed mutation table owns append/update/delete payload precedence, while a separate persistence command immediately writes campaign entities and replaces their cached object; session, scene, and session-entity changes still flow to the coordinator's final session write. Missing owners remain silent, simplified-note title stripping and placeholder notes are unchanged, and valid operations retain input order. The malformed-input audit also replaced the Phase 36 entity dispatcher object with a `Map`, preventing inherited `constructor`/`toString` names from violating invalid-operation silence. Focused UTF-8 coverage locks campaign and campaign-entity notes, one-time cache loading, append/update/delete persistence, missing targets, session entity and scene client IDs, top-level session notes, placeholder retention, prototype-like invalid names, and simplified notes. Fallow removes `getNotesTarget` at CC 13/cognitive 13 and `applyNoteOperation` at CC 18/cognitive 17 with no findings in the replacement pipeline and no suppressions; the suite reaches 232 passing tests.
- Completed Phase 38 and decomposed the critical custom-Bestiary batch in `server/aiPatchService.js`. Focused policies now detect and materialize missing stored IDs before the `before` snapshot, normalize operation identity, resolve exact IDs before case-insensitive name fallback, and route create/update/delete through a prototype-safe handler map. Create still removes caller-supplied IDs, normalizes the payload, removes every same-name monster, appends the replacement at the tail, and reports it as changed. Update retains the stored ID, preserves the existing name when the patch omits one, and reports the normalized replacement; delete mutates only the working set and stays out of `changedMonsters`. Invalid operations and missing targets remain silent, operation order is unchanged, and the final normalized Bestiary write remains unconditional even for a no-op batch. A focused regression exposed that the old per-item predicate could let an earlier name match beat a later exact ID; the new explicit policy corrects this to the intended ID-first contract. Mocked-storage regressions avoid touching user Bestiary data while locking mixed aliases and mutations, duplicate replacement order, caller-ID rejection, UTF-8 names, changed output, standalone updated-result precedence, missing-ID materialization, two-write materialization/no-op behavior, and `before`/`after` snapshots. Fallow removes `applyMonsterOperations` at CC 15/cognitive 25 with no findings in the replacement batch and no suppressions; the suite reaches 234 passing tests.
- Completed Phase 39 and decomposed the critical scene mutation handler in `server/aiPatchService.js`. A focused operation context now carries session state, the normalized scene list, operation, and note options into prototype-safe create/update/delete commands. Delete still requires an existing target and immutably replaces the session scene list. Create has an explicit encounter-permission sanitizer, normalized scene construction, partial-content eligibility, empty warning, list insertion, pending encounter-link queueing, and client-ID registration. Update separately builds the existing-plus-patch scene with merged text fields, stable ID, and existing-first image URL; its permission sanitizer intentionally retains the existing encounter ID without discarding an incoming encounter client ID, preserving the established unresolved-link warning path. Focused helpers own patch-text narrowing, scene replacement, and client-map effects. UTF-8 regressions lock complete update/create/delete ordering, stable IDs and collapse state, partial text preservation, simplified notes, NPC normalization, note targeting through a newly registered scene client ID, old image precedence, direct encounter IDs, silent missing/unsupported targets, disabled create clearing, disabled update retention, empty creates, unresolved links, disabled encounter warnings, and missing-session fallback. Fallow removes `applySceneOperation` at CC 17/cognitive 22 with no findings in the replacement scene pipeline and no suppressions; the suite reaches 236 passing tests.
- Completed Phase 40 and decomposed the critical encounter mutation handler in `server/aiPatchService.js`. Session and permission eligibility now precede all context work and preserve their exact warnings. Eligible operations prepare a shared context containing the normalized encounter list, operation-ID → target-ID → focused-encounter target, existing object, and Bestiary index; the index intentionally still loads before dispatch for delete, unsupported operations, and missing update/delete targets. Prototype-safe create/update/delete commands then own their effects. Delete immutably replaces the session encounter list. Create validates a non-empty client ID and a predeclared scene encounter-client link, normalizes participants, generates the server encounter ID, appends it, and registers both created-ID and client-map state. Update preserves the encounter object, falls back to its current name and monster list when omitted, and replaces only normalized name/participants. Mocked-storage UTF-8 regressions lock focused-ID fallback, plural aliases, update identity, omitted-monster fallback, official and unknown participant projection, HP/AC/source/name fields, invalid participant removal, generated IDs, link resolution, lifecycle ordering, missing targets, fallback/create warning names, missing client/link, permission and session eligibility, exact Bestiary-index read sequencing, and no session write for no-op batches. Fallow removes `applyEncounterOperation` at CC 18/cognitive 22 with no findings in the replacement encounter pipeline and no suppressions; the suite reaches 238 passing tests.
- Completed Phase 41 and decomposed the critical encounter participant projector in `server/aiPatchService.js`. Required lookup-name narrowing, insertion-ordered Bestiary prefix lookup, instance-ID construction, identity/presentation projection, official HP derivation, official AC derivation, and unresolved combat defaults now have focused policies. Projection still spreads the resolved object before instance overrides; preserves caller ID → Bestiary ID → generated ID, `monsterName` → `name`, first matching `name|source` entry, caller display-name override, original Bestiary identity, resolved source precedence, modern object-HP average, legacy `hit_points`, first truthy AC entry, legacy `armor_class`, and unresolved zeroes. Nullable `hp` now safely falls through to the legacy field instead of throwing. A mocked-storage UTF-8 regression fixes clock/random output and locks first-source selection, resolved-field spread, caller/base/generated identity paths, modern scalar and existing object AC variants, legacy combat fields, malformed nullable HP and zero AC entries, unknown-source projection, invalid participant removal, and exact instance-ID formatting. Fallow removes `buildMonsterInstance` at CC 29/cognitive 27 with zero findings across the replacement participant pipeline and no suppressions; the suite reaches 239 passing tests.
- Completed Phase 42 and decomposed the critical character/NPC normalizer in `server/aiPatchService.js`. Focused policies now own own-property name presence, valid existing-note narrowing, note-source selection, simplified normalization and ignored-note merging, parsed-name fallback, canonical-before-alias field lookup, level selection, UI boolean state, image retention, existing-record narrowing, and ID generation. Valid create/update behavior remains stable across all name variants, `race|species`, `class|role`, `motivation|goal`, `description|bio|backstory`, and `trait|personality|quirk`; explicit name-part clearing, omitted-field retention, existing IDs, first-owned-field precedence, 1–20/empty level normalization, existing collapse/image precedence, matched note ID/collapse, simplified titles, and `_aiIgnored` order are preserved. Malformed non-array stored notes now recover to the normal empty placeholder instead of throwing in `.map()`. A filesystem-backed UTF-8 regression locks canonical and alias inputs, snake-case names, numeric zero and nullable scalar coercion, create defaults, empty levels, generated IDs, inherited-field rejection, malformed notes, explicit clearing, UI/image retention, and ignored-note placement. Fallow removes `normalizeCharacter` at CC 42/cognitive 25 with zero findings across the replacement character pipeline and no suppressions; the suite reaches 240 passing tests.
- Completed Phase 43 and decomposed the critical location/faction normalizer in `server/aiPatchService.js`. Character and location normalization now share private valid-note narrowing, note-source selection, simplified normalization plus ignored-note merging, existing-record narrowing, ID generation, collapse-state, and image-retention policies because those contracts are identical. Location-owned policies separately retain sanitized `name|title` and text-coerced `description|summary|text` first-own precedence. Existing IDs, explicit empty clearing, omitted-value fallback, create placeholders, matched note IDs/collapse, simplified titles, `_aiIgnored` order, and existing UI/image precedence remain stable. Malformed non-array location notes now recover instead of throwing, matching the character boundary. A filesystem-backed UTF-8 regression locks campaign updates under an explicit scope when a session target exists, session faction creation, canonical and alias inputs, numeric and nullable coercion, nested-bracket name sanitization, inherited-field rejection, generated IDs, malformed notes, empty clearing, ignored-note placement, campaign-to-session duplicate movement, retained session identity, campaign data projection, campaign-copy deletion, and the exact duplicate warning. The initial regression intentionally exposed and then documented the existing default session routing for unscoped locations rather than changing it in the normalizer. Fallow removes `normalizeLocation` at CC 25/cognitive 14 with zero findings across both entity normalizers and their shared replacement policies, without suppressions; the suite reaches 241 passing tests.
- Completed Phase 44 and decomposed the critical scene-text projector plus high scene-create eligibility policy in `server/aiPatchService.js`. A closed text-field list now drives focused nested-versus-flat source selection and own-field value projection; `normalizeSceneTexts` remains an explicit summary/goal/stakes/location result. Eligibility now composes independent text, normalized-list, note-alias, NPC-alias, and encounter/image resource predicates. Truthy object `texts` still takes precedence over flat fields, including arrays; non-object/null/string `texts` still falls back to the flat scene. Per-field update fallback, scalar coercion, partial creates, note/NPC normalization, numeric zero, boolean false stringification, encounter ID and image eligibility, and exact empty warnings remain stable. Malformed note/NPC collections are now explicitly narrowed to empty lists inside the eligibility boundary instead of relying on caller shape. A filesystem-backed UTF-8 regression locks partial nested updates, existing location retention, nested precedence over flat data, null/scalar flat fallback, number/boolean/null projection, and every eligibility branch: text, note, NPC, encounter, image, and zero-valued text. It also locks inherited nested-field rejection, array-source precedence, malformed collections, empty alias values, three skipped creates, scene ordering, and saved resource values. Fallow removes `normalizeSceneTexts` at CC 15/cognitive 10 and `hasSceneContent` at CC 8/cognitive 4 with zero findings across the replacement scene-text/content pipeline and no suppressions; the suite reaches 242 passing tests.
- Completed Phase 45 and decomposed the critical full-scene normalizer, high NPC mapper callback, and high encounter-ID resolver in `server/aiPatchService.js`. Focused policies now own string/object NPC projection, normalized NPC list filtering, direct and mapped encounter lookup, note normalization plus ignored-note merging, omitted-versus-explicit participant selection, existing-only scene UI state, and final aggregate projection. Stable existing IDs, generated create IDs, update replacement objects, nested text behavior, explicit note/NPC clearing, valid omission retention, simplified titles, matched note collapse, `_aiIgnored` reinsertion, string NPCs, `name|firstName` and `description|trait` aliases, invalid participant removal, existing collapse flags, and existing-first image URLs remain stable. Encounter resolution still applies direct ID before mapped client ID before existing fallback; pending-link finalization remains unchanged. Malformed stored note/NPC objects now safely normalize to empty arrays instead of persisting invalid collection shapes or reaching array operations. A mocked-storage UTF-8 regression locks stable IDs, update object replacement, explicit clears, malformed recovery, valid-list retention, private-field removal, all NPC aliases, numeric/invalid values, ignored notes, simplified titles, UI/image precedence, generated create identity, ignored create collapse flags, direct numeric ID coercion, created-encounter client mapping, unresolved-client fallback and warning, final linking, and non-orphan encounter retention. Fallow removes `normalizeScene` at CC 25/cognitive 12, its NPC callback at CC 9/cognitive 8, and `resolveEncounterId` at CC 8/cognitive 5 with zero findings across the replacement scene aggregate pipeline and no suppressions; the suite reaches 243 passing tests.
- Completed Phase 46 and decomposed the critical campaign-entity application handler in `server/aiPatchService.js`. Permission eligibility, one-time list/target context, delete, create validation, campaign-duplicate replacement, session-duplicate promotion, plain creation, update payload construction, post-write mention updates, client-ID mapping, and warning projection now live in focused commands behind a prototype-safe dispatcher. Disabled operations still warn before storage access; campaign duplicates retain precedence over same-name session entities; location and character/NPC validity rules remain distinct; existing IDs, slugs, and images remain stable; session entities are removed only after the campaign write succeeds; and missing targets plus unsupported operations remain silent. A mocked-storage UTF-8 regression locks every command branch, campaign-first duplicate resolution, promotion identity, generated creation, client-ID update targeting, invalid creates, stable rename/delete behavior, write-before-mention ordering, exact warnings, permission short-circuiting, and the failed-promotion boundary without a session write or entity removal. Fallow removes `applyCampaignEntityOperation` at CC 30/cognitive 40 with zero findings across the replacement campaign-entity pipeline and no suppressions; the suite reaches 244 passing tests.
- Completed Phase 47 and decomposed the critical session-entity application handler in `server/aiPatchService.js`. Missing-session and permission eligibility, list/target context, generated create input, normalized version validation, session-duplicate replacement, fail-open campaign lookup, campaign-duplicate movement, plain creation, delete, stable update projection, client-ID mapping, and warning construction now live in focused commands behind a prototype-safe dispatcher. Session duplicates retain precedence without a campaign read; campaign lookup failures still fall back to plain session creation; existing IDs, slugs, and images remain stable; mapping and warnings occur only after successful mutations; and missing targets plus unsupported operations remain silent. Campaign-to-session duplicate movement now compensates its in-memory append when campaign deletion fails, while preserving exception propagation and preventing the final session write. The initial refactor exposed a hoisted helper-name collision with the Phase 46 pipeline; the helper was renamed rather than hidden with a suppression. A mocked-storage UTF-8 regression locks command coverage, duplicate precedence, generated creation, client-ID targeting, invalid input, update/delete stability, exact warnings, permission and missing-session short-circuits, fail-open lookup, persistence counts, and compensated deletion failure. Fallow removes `applySessionEntityOperation` at CC 32/cognitive 42 with zero findings across the replacement session-entity pipeline and no suppressions; the suite reaches 245 passing tests.
- Completed Phase 48 and decomposed the remaining critical `applyMoveScopeOperation` handler in `server/aiPatchService.js`. Missing-session eligibility, source parsing, valid/different-scope checks, target and duplicate lookup, version projection, direction dispatch, campaign-write rollback, session insertion/replacement rollback, exact duplicate warnings, and result construction now have focused policies. A per-apply rollback stack joins cross-scope filesystem effects to the coordinator's final session persistence: successful persistence clears the stack, while any later operation or persistence failure executes compensations in reverse order and preserves the original error. Session-to-campaign failures now restore the removed session entity and either delete a newly created campaign entity or restore the overwritten duplicate. Campaign-to-session delete failures immediately undo the session mutation; later failures restore both the original session list and deleted campaign source. A mocked-storage UTF-8 matrix locks both successful duplicate directions, stable destination IDs/slugs/images, campaign-created `targetClientId` routing, exact warnings, invalid/equal/missing/no-session behavior, write/delete ordering, overwritten-duplicate restoration, deleted-source restoration, immediate delete rollback, and pre-mutation write failure. Fallow removes `applyMoveScopeOperation` at CC 15/cognitive 17 with zero findings across the replacement movement and persistence pipeline and no suppressions; the suite reaches 246 passing tests.
- Completed Phase 49 and decomposed the four shared critical entity policies in `server/aiPatchService.js`. Record narrowing, first usable identity selection, normalized identity projection, focused value lookup, mapped operation lookup, scope normalization, existing-record narrowing, normalization-source construction, and final version projection now replace the combined fallback trees. Entity lookup now applies explicit ID → slug → normalized-name precedence across the whole list, correcting the former item-order bug where an earlier name match could beat a later exact ID or slug. Numeric ID `0` is retained through operation targeting and patch normalization. Scope ownership remains explicit valid scope → mapped `targetClientId|ownerClientId` scope → valid default → campaign; invalid explicit scope does not override a valid mapping. Patch and duplicate-version projection preserve destination IDs/slugs and existing-first image behavior, reject arrays as entity records, recover safely from malformed storage lists, and do not mutate source objects. A mocked-storage UTF-8 regression locks all three identity levels and fallback, zero IDs, explicit/mapped/default scope precedence, duplicate identity/image retention, malformed patch/data/list handling, nullable patches, client mapping, and source immutability. Fallow removes `findByIdentity` at CC 11, `operationScope` at CC 11, `mergeEntityPatch` at CC 14, and `mergeNewEntityVersion` at CC 14 with zero findings across their replacement policies and no suppressions; the suite reaches 247 passing tests.
- Completed Phase 50 and decomposed the complete normalization prelude in `server/aiPatchService.js`. Record narrowing, explicit first/last-name projection, full-name tokenization, level empty detection/parsing/clamping, string-note construction, note-record validation, simplified-title projection, text-alias projection, and object-note construction now replace the critical/high combined branches. Canonical truthy name fields still precede snake-case aliases and any full `name|fullName|title`; sanitized nested brackets and Ukrainian whitespace remain stable. Empty level strings remain `""`; other values retain parseInt behavior and clamp to 1–20 with invalid/null values becoming 1. Direct string notes retain trimmed text including an empty result, while list normalization still filters content-empty notes and creates a placeholder when required. Object notes preserve truthy IDs, `title|name`, nullish `text → description → content`, boolean collapse state, and simplified title removal. Arrays are now explicitly rejected as invalid note records instead of being materialized as empty object notes. A filesystem-backed UTF-8 matrix locks canonical/alias conflicts, single/multi-part names, bracket cleanup, every relevant level boundary, note alias and scalar coercion, direct empty-string append, invalid array/scalar rejection, list filtering and placeholder insertion, simplified mode, ID/collapse preservation, ignored-note reinsertion, and malformed character creates. Fallow removes `normalizeNote` at CC 10, `parseNameParts` at CC 9, and `normalizeLevel` at CC 7 with zero findings across the replacement prelude and no suppressions; the suite reaches 248 passing tests.
- Completed Phase 51 and decomposed the ignored-note merge in `server/aiPatchService.js`. Focused policies now narrow note collections, retain ignored entries with their original indices, normalize string-compatible IDs, remove visible ID collisions, build the stable visible-ID index, discover previous and next visible anchors, choose bounded insertion positions, and materialize ordered insertion buckets without mutating either source array. The no-ignored fast path still returns the exact incoming visible value. Ignored note objects and their source order are retained, including adjacent and duplicate-ID notes; this corrects the former forward-`splice` reversal of adjacent ignored notes sharing one previous anchor. Non-empty ignored IDs still remove numeric/string-equivalent visible collisions, while empty IDs do not. Placement remains previous visible anchor before next visible anchor before original-index fallback, and malformed visible collections recover to an ignored-only array. A focused UTF-8 matrix locks array and object identity, next/previous anchor priority, beginning/middle/end placement, stable adjacent order, numeric and duplicate collisions, missing-anchor fallback, malformed inputs, and input immutability. Fallow removes `mergeAiIgnoredNotes` at CC 9/cognitive 8 with zero findings across the replacement planner and no suppressions; the suite reaches 249 passing tests.
- Completed Phase 52 and decomposed campaign-entity persistence in `server/aiPatchService.js`. Type-specific base-name readers, existing-before-payload slug selection, lazy normalized-base allocation, resolved-slug projection, and direct storage delegation now replace the high combined branch. Truthy existing slugs still precede truthy payload slugs and bypass both slugification and uniqueness checks; missing/falsy slugs still allocate from location `name → locations` or character/NPC `firstName → name → type`. Numeric values retain JavaScript truthiness, so `42` is supplied unchanged while `0` allocates. The persisted object is a fresh shallow projection whose resolved slug overrides the payload, the caller payload remains untouched, storage failures propagate, and successful storage results return without post-processing. A mocked-storage UTF-8 lifecycle matrix drives the public operation pipeline through existing update, generated location and character creation, supplied/numeric/zero/empty session-to-campaign moves, blank-location note persistence, session persistence, and a failed write. It locks lazy call counts and order, exact base names/types, resolved payload slugs, source immutability, fallback names, and failure propagation without exposing private helpers. Fallow removes `writeCampaignEntity` at CC 8 with zero findings across the replacement persistence policy and no suppressions; the suite reaches 250 passing tests.
- Completed Phase 53 and decomposed the complete scene encounter-client link pipeline in `server/aiPatchService.js`. Focused policies now select object payloads, preserve the distinct data and patch precedence lists, classify eligible operation/entity aliases, extract and deduplicate encounter client IDs, detect pending work, validate mapped encounters, project exact unresolved warnings, find target scenes, apply strict-ID links, and aggregate changes without skipping later links. Arrays remain eligible object payloads and therefore can suppress lower-priority payloads; null/scalars fall through. Discovery remains case-insensitive create/update plus scene/scenes only, with insertion-ordered Set semantics. A truthy raw data encounter ID still precedes patch before text coercion, including the legacy whitespace case that suppresses a valid patch fallback. Resolution still requires mapped entity `encounter` and a truthy mapped ID, rejects numeric zero, stays silent when the queued target scene is missing, compares current and mapped IDs strictly, mutates only the matched scene, and emits the exact warning for missing/wrong mappings. A three-scenario mocked UTF-8 matrix drives the public pipeline through data/value/patch precedence, uppercase/plural aliases, repeated links, invalid operations/entities, array and whitespace suppression, empty-scene rejection, missing and wrong mappings, missing-scene silence, already-linked IDs, numeric-versus-string replacement, and mapped zero. Fallow removes `operationData` at CC 7, `collectSceneEncounterClientIds` at CC 8/cognitive 11, and `resolvePendingSceneEncounterLinks` at CC 9/cognitive 10 with zero findings across the replacement pipeline and no suppressions; the suite reaches 251 passing tests.
- Completed Phase 54 and decomposed the final entity dispatcher in `server/aiPatchService.js`. Focused policies now recognize move-scope operations, select character-versus-session defaults, choose the campaign/session delegate, and materialize one route request before direct invocation. Established character/characters/PC/player-character, NPC/NPCs, and location/locations/faction/factions aliases remain unchanged; unknown entities stay silent. Case-insensitive moveScope routing still precedes and bypasses ordinary scope routing. Characters remain campaign-owned even with explicit session scope, while NPC/location operations retain explicit valid → mapped client → request default → campaign scope semantics. The dispatcher now returns the selected delegate promise directly, preserving successful results and rejected errors without a redundant async wrapper. A mocked UTF-8 routing matrix drives the public pipeline through uppercase aliases, explicit-session characters, default-session NPC/location creation, mapped invalid-scope updates, explicit-campaign factions, conflicting-scope moveScope, no-session campaign fallback, unknown-entity silence, immutable operations, final session persistence, and delegated write failure. Fallow removes `applyEntityOperation` at CC 6/cognitive 5 and confirms zero top-500 findings across the complete `server/aiPatchService.js` file without suppressions; the suite reaches 252 passing tests.
- Completed Phase 55 and replaced the critical `buildSystemInstruction` branch tree in `server/modules/ai/application/buildSystemInstruction.js` with a closed ordered append-policy pipeline. Private policies now own base-mode language rules, prompt-language exception, structured response contracts, simplified notes, mention selection, generated NPC/location detail, scene combat/data/additive contracts, scene and encounter custom-monster branches, entity toggles, scope wording, session-scope contracts, trimmed user base prompts, and image-style guidance. Long scene/monster/scope texts are named immutable contracts, while mixed/session scope wording is selected from a closed map with campaign fallback. Policy order exactly matches the former function, final joining remains `\n\n`, and the public API remains only `buildSystemInstruction` plus the established `systemInstructions`. Before refactoring, ten representative scene/campaign/encounter/custom-monster/NPC/prompt/image/fallback outputs were captured by length and SHA-256; every post-refactor value matched byte-for-byte. A permanent mode/order/absence matrix locks the full scene section sequence, parse-disabled exclusions, all three scope modes, custom-monster mention isolation, encounter enabled/disabled exclusivity, prompt exception, trimmed `(none)` base prompts, image-only language/style behavior, and unknown-mode fallback. Fallow removes `buildSystemInstruction` at CC 47/cognitive 56 with zero findings across the replacement file and no suppressions; the suite reaches 253 passing tests.
- Completed Phase 56 and replaced the critical `validateOperation` branch tree in `server/modules/ai/domain/aiPayloadSchemas.js` with a closed ordered validator pipeline. Focused private policies now own operation-object narrowing, allowed operation/entity membership, existing-target identity, implicit campaign updates, create/update/append-note payload shapes, note mutation identity, moveScope target and direction checks, required mixed entity scope, and supplied scope validation. The top-level validator separately owns payload narrowing, version checking, operations-array requirements, and result projection while the public CommonJS API and TypeScript declaration remain unchanged. Non-object operations still stop after one error; all record-shaped operations still accumulate every applicable error in the original order with exact paths/messages. Case-sensitive aliases, NPC/location/faction scope normalization, truthy scalar identity compatibility, falsy numeric zero, `data|value`, `patch|data`, empty-string append notes, implicit campaign updates, moveScope's dedicated message, and undefined-versus-invalid scope semantics remain stable. A permanent malformed matrix locks top-level and operation-level multi-error order, payload aliases, target variants, note contracts, all entity-scope aliases, null scopes, and UTF-8 values. Fallow removes `validateOperation` at CC 35/cognitive 27 and reports zero findings across the replacement schema file without suppressions; the suite reaches 254 passing tests.
- Completed Phase 57 and decomposed the critical AI-history normalization cluster in `server/storage.js`: `normalizeAiResponse` and `normalizeAiChangeResource`. Focused private policies now own object-like narrowing, nonblank text eligibility, request-source selection, request-versus-legacy instruction precedence, nested request projection, response path/error/retry metadata, status/apply-state sets, generated IDs/timestamps, change-kind validation, own-property snapshot extraction, base resource projection, and kind-specific session/entity/custom-monster identity. Existing nonblank text remains untrimmed; nested arrays remain accepted as object-like options, context, summary, error, retry, and snapshot values; falsy metadata, label/ID fallbacks, failed-versus-completed defaults, resource/response state sets, and custom-monster `name → after.name → before.name` precedence remain stable. Direct normalization still retains the original nested options/context/summary/retry and before/after object identities. Root null and primitive rows now filter independently instead of throwing into `readAiResponses`' outer recovery and hiding every valid history row in the same file. A filesystem-backed UTF-8 regression drives both direct add and persisted legacy-envelope reads, locking identity retention, arrays, all five change kinds, invalid kinds, null/null filtering, path/error coercion, request fallback, state defaults, malformed-row isolation, and exact normalized shapes. Fallow removes `normalizeAiResponse` at CC 32/cognitive 32 and `normalizeAiChangeResource` at CC 26/cognitive 20 from the findings without suppressions; the suite reaches 255 passing tests.
- Completed Phase 58 and replaced critical `importCampaignPartialArchiveBundle` in `server/storage.js` with an explicit partial-import context and a fixed ordered dispatcher over private session, NPC, location, AI-history, and image commands. Target normalization/existence validation, archive/source projection, zeroed counters, collection narrowing, individual session/entity restoration, imported history projection, image selection, and exact result construction now have focused policies. Effects still execute `sessions → npc → locations → aiHistory → images` independently of caller section order, while normalized/deduplicated sections retain their original input order in the result. Session writes remain sequential and replace an ID-matched filename; entity writes retain imported or generated slugs and overwrite the same path; all content/history image references and campaign fields still rewrite from source to target; imported history receives fresh IDs and appends to existing sorted history; raw history/image list lengths remain the counters; and path traversal remains rejected by the existing image restorer. A filesystem-backed UTF-8 matrix locks comma-string section normalization, duplicates/unknowns, exact counters/results, sanitized session paths, generated Ukrainian slugs, source URL rewrites, all entity/history campaign fields, existing history retention, invalid history counting/filtering, image byte restoration, traversal rejection, malformed empty sections, target preflight failure, and fixed-order partial progress when a later BigInt entity write fails. Fallow removes `importCampaignPartialArchiveBundle` at CC 27/cognitive 28 and reports zero findings across the replacement partial-import pipeline without suppressions; the suite reaches 256 passing tests.
- Completed Phase 59 and replaced critical `writeAiResourceSnapshot` in `server/aiResponseHistoryService.js` with two closed command maps: campaign-free custom Bestiary/custom-monster restoration and campaign-targeted campaign/session/entity restoration. Focused private policies now normalize identity parts and name keys, extract a custom-monster resource-ID suffix, collect target IDs/names, test collisions, project survivor-plus-snapshot lists, require campaign ownership, normalize session filenames, validate entity targets, and execute each persistence command. Custom kinds still bypass campaign validation; all remaining kinds still reject a falsy campaign before unknown-kind detection. Whole-list Bestiary replacement, non-array fallback to empty, current monster order, ID/name collision removal across before/after/snapshot/resource metadata, case-insensitive names, numeric-zero exclusion, snapshot append identity, campaign deletion prohibition, basename session/entity targets, force deletion, entity slug overwrite, exact errors, and sequential resource/history effects remain stable. A mocked UTF-8 matrix drives the public restore use case through all seven write/delete branches, complete collision sources and survivors, malformed Bestiary fallback, custom-monster deletion, campaign/session/entity targets, exact write order, history update ordering, no-campaign-versus-unknown precedence, invalid targets, and original delegated-error propagation without a history update. Fallow removes `writeAiResourceSnapshot` at CC 25/cognitive 28 and reports zero findings across its replacement snapshot-write pipeline without suppressions; the suite reaches 257 passing tests.
- Completed Phase 60 and decomposed the full custom-monster normalization boundary in `server/aiCustomMonsterService.js`. Closed field lists and ordered private projectors now own base combat construction, optional image/original-source identity, object-shaped metadata, non-empty string lists, and action sections. Focused helpers separately narrow object-like/record values, select canonical-versus-alias inputs, preserve double-read alignment behavior, normalize AC entries/arrays, HP records, string-list sources, and string/object action entries. `normalizeCustomMonster` now only validates the object-like input/name, builds the base, runs projectors, and applies final recursive mention-bracket stripping; its public API remains unchanged. Truthy `name → title`, legacy array-shaped roots, scalar/generated IDs, forced CUSTOM source, all size/type/alignment/combat defaults, canonical `0`/`false`/empty-string precedence, object AC/HP/speed/CR variants, optional field truthiness, object/array metadata, scalar and mixed string lists, array-only action collections, entry alias precedence, extra properties, and recursive cloned bracket removal remain stable. A direct UTF-8 matrix locks malformed roots and title suppression, every combat alias/default, numeric and boolean edge cases, generated-ID counts, forced source, all optional field families, cloned nested structures, all five action sections and malformed entries, object content stringification, observable two-read alignment, and legacy array-root acceptance. The first Fallow pass confirmed the coordinator hotspot was gone but exposed the existing action-entry helper and five CRAP-threshold combat helpers; those were decomposed in the same phase, leaving zero findings across the entire service file without suppressions. The suite reaches 258 passing tests.
- Completed Phase 61 and decomposed critical `prepareGenerateAiRequest` in `server/modules/ai/application/prepareGenerateAiRequest.js` into focused safe-property, path/classification, preflight, parse eligibility, generation-toggle, entity-scope, settings, prompt-precedence, and final-result policies. Language normalization and its 400 error still precede API-key validation, and both preflight exits occur before path or settings getters. Bestiary image requests retain exact campaign/custom-monster classification and synthesize the Bestiary path only when campaign ownership is falsy; object-like paths, including arrays, retain identity while malformed scalar/function paths fall back to a fresh object. Image requests never parse; encounter targets parse only when encounter generation is truthy; custom monsters remain encounter-gated; character/NPC/location toggles remain false-only while parsing and default to enabled otherwise; and mixed scope remains limited to parsed session targets without an encounter. Settings now recover from nullish results as an empty record while retaining object/array prompt maps, false-only auto-apply disablement, own-property empty image override precedence, campaign-before-global image fallback, and trimmed UTF-8 prompts. A permanent matrix locks getter-sensitive no-read preflight, error order, malformed and legacy paths, Bestiary classification, all eight entity-toggle combinations, encounter/image gates, truthy generation flags, scope selection, inherited versus own overrides, nullable settings, array prompt maps, and exact defaults. Fallow removes the original CC 23/cognitive 15 coordinator plus the CC 7 path and CC 6 classifier findings and reports zero findings across the complete replacement module without suppressions; the suite reaches 259 passing tests.
- Completed Phase 62 and replaced critical `importCampaignBundle` plus its adjacent AI-history normalization cluster in `server/storage.js` with a private full-import context and focused validation, collection narrowing, slug resolution, replacement preflight, meta projection, session restoration, entity-type restoration, recursive history rewriting, resource projection, and history persistence policies. Invalid or missing meta still fails before effects; nullish options now normalize to an empty record; non-array sessions/history and non-object entity maps recover as empty collections. Source slug still prefers imported meta, forced slugs remain basename-normalized, unforced imports alone allocate a unique campaign slug, and deletion remains limited to forced replace-existing imports with an existing campaign directory. Effects remain strictly replacement, meta, sessions, characters, NPCs, locations, then AI history; original `createdAt`, image URL rewriting, legacy session filename normalization, sequential collision suffixes, falsy session content as `{}`, entity fallback/collision rules, fixed `ENTITY_TYPES`, recursive exact slug/campaign changes, history path/resource campaign projection, label replacement, exact returned meta, and partial-progress failures remain stable. A filesystem-backed UTF-8 matrix locks append collision allocation, duplicate session/entity names, the legacy supplied-extension result, malformed collections, meta/session/entity/history image rewriting, recursive history references, preserved/generated timestamps, forced replacement of campaign and images, null options, exact invalid-bundle errors, and BigInt failure after meta/session/character writes but before location/history writes. Fallow removes the original CC 23/cognitive 19 coordinator, CC 8 AI-history normalizer, CC 7 recursive slug rewriter, and every replacement helper from the top-500 findings without suppressions; the suite reaches 260 passing tests.
- Completed Phase 63 and replaced the critical Bestiary `_copy._mod` cluster in `scripts/materialize-bestiary-copies.mjs` with a private closed `Map` of the twelve supported modifier modes. `applyMod` now owns only literal removal, invalid-object classification, command lookup, exact warning selection, and dispatch; all six array modes continue delegating unchanged to `applyArrayMod`. Focused commands own selected-property, wildcard, and ordinary text replacement; target clearing; cloned property/skills writes; and add/replace/remove spell delegation. Spell mutation is split across primary-block discovery, cloned list append, daily bucket, level bucket, metadata copy, list replacement, level lookup, reverse removal, per-block mutation, and all-block traversal policies. `addSpells` still initializes an empty primary block, respects truthy will/daily/spells gates, preserves legacy array-shaped object buckets, writes metadata other than `spells`, and mutates only the first block; replace/remove still traverse every block, compare through `String`, and remove duplicates from the end. `_mod` property and repeated-mod order, exact warnings, `"remove"`, selected-props-before-wildcard precedence, destructive whole-object replacement, input cloning, and chained inheritance remain stable. The temporary-database CLI fixture now drives all twelve modes, repeated text replacement, property-order dependency, all array behaviors, primary/secondary spell blocks, scalar/object spell entries, metadata overwrite, duplicate removal, two exact warnings, UTF-8 values, a chained copy, and byte-identical `--dry-run` behavior before the write run. Fallow removes `applyMod` at CC 21, `addSpells` at CC 20/cognitive 39, `replaceSpells` and `removeSpells` at CC 11, plus every temporary replacement-helper finding, leaving zero findings across the modifier span without suppressions; the suite remains at 260 passing tests.
- Completed Phase 64 and decomposed the rest of `scripts/materialize-bestiary-copies.mjs` into focused source/property readers, envelope selectors, text-replacement policies, base lookup, cycle validation, plain cloning, required-base resolution, child overlay/source/name/mod cleanup, skipped-file classification, file loading, indexing, materialization context, per-monster/per-file processing, persistence, warning, and summary policies. Direct arrays and truthy `monster → monsters → results` precedence remain unchanged, including scalar shadowing; `_meta.sources[0].json` still precedes filename-derived source; text replacement retains nullish eligibility, literal escaping, raw flags, JSON serialization, and nullish replacement defaults. File loading still preserves filesystem order while excluding aggregate/index files, non-JSON files, and directories. Indexing still normalizes keys, skips falsy names, lets the last exact duplicate win, and preserves first-key insertion order for name fallback. Resolution still checks cycles before plain cloning, prefers exact source, falls back by first name prefix, recursively resolves bases, overlays all child fields except copy controls, fills source only when falsy, applies modifiers after overlay, forces a truthy child name, deletes inherited controls, and updates the live index before later chained copies. CLI orchestration now separates per-monster and per-file mutation, dry-run persistence, verbose reporting, warning emission, and final counts without changing output order. The temporary database adds truthy malformed-envelope shadowing, direct-array data, `_meta` source inheritance, exact-source lookup, insertion-ordered name fallback, last-duplicate lookup, invalid skipped aggregate files, a `.json` directory, verbose-order assertions, exact missing-base failure, and exact circular-chain failure, while retaining the full modifier/dry-run matrix. Fallow removes `main` at CC 12, `resolveMonster` at CC 11, `getMonsterList` at CC 8, text/file loaders at CC 7, base/index helpers, and every replacement helper, leaving zero named-function findings across the complete script without suppressions; the suite remains at 260 passing tests.
- Completed Phase 65 and decomposed `server/aiHistoryService.js` into scalar/array narrowing, ordered option-summary appenders, disabled and enabled context projection, campaign/session collection counts, explicit NPC fallback eligibility, ordered context-summary policies, snapshot-option and image-target projection, attachment item/list ownership, and optional envelope attachment. Scalar text remains limited to trimmed strings, numbers, bigints, and booleans; request mode still prefers an explicit truthy type, then encounter, session, and campaign. Actual parsing remains distinct from requested parsing, generation details remain parse-gated, and model/scene/image-target/global/image/campaign prompt labels retain their exact order and truthy behavior. Disabled context returns a fresh exact zero summary without reading context data; enabled context preserves `included !== false`, undefined-only NPC fallback, array-only counts, unconditional nonzero session/scene parts, and `context: empty`. Object-like targets retain scalar-coerced fields, attachment order and name/URL filtering remain stable, and empty groups/envelopes are omitted. A UTF-8 exact-shape matrix locks scalar edges, malformed collections, null versus undefined NPC configuration, primitive/list configs, mode precedence, all optional summaries, disabled-data short-circuiting, attachment filtering/order, and empty omission. Fallow reports zero findings across the complete file without suppressions; the suite reaches 261 passing tests.
- Completed Phase 66 and decomposed `server/modules/ai/application/buildPromptContext.js` into private scalar/property readers, string/object note projection, entity-name and content policies, optional campaign collections, selected-scene configuration/defaults, encounter lookup and monster projection, selected-session section appenders, current-session data and collection appenders, campaign/custom-Bestiary/session/encounter context owners, and final assembly. Valid prompt behavior remains stable: string-note whitespace is preserved after nonblank eligibility; structured titles trim and disappear in simplified mode; `_aiIgnored` entries are removed; character-name aliases precede name/title; campaign/current NPCs retain trait-only entries while selected-session NPCs do not; and list order remains unchanged. Selected sessions still require `conf.included` per section, retain assigned empty arrays, treat a nonempty object scene map as an explicit allowlist, apply the same default scene fields otherwise, include non-nullish false/zero/empty text values, resolve truthy encounter IDs by string equality, and retain only sessions with a truthy projected section. Truthy object current-session data still overrides request-session data, while the focused current encounter still comes from request-session data; exact campaign-scope suppression, custom-Bestiary identity, and serialized field order remain stable. Present malformed collections now normalize safely to empty arrays while nullish optional campaign collections remain omitted. An exact serialized UTF-8 fixture covers full campaign/current/selected/encounter output, simplified notes, explicit and legacy scene configs, aliases, ignored values, falsy field values, override precedence, malformed collections, target-scope suppression, and custom-Bestiary identity. Fallow removes ten original critical/high/moderate findings and reports zero findings across the complete file without suppressions; the suite reaches 262 passing tests.
- Completed Phase 67 and replaced the critical `fillCurrentTargetIds` cascade in `server/modules/ai/application/fillCurrentTargetIds.js` with a private closed supported-operation set, ordered existing-target fields, focused encounter/scene/custom-monster mutation commands, a closed entity-alias command map, target-command selection, and thin per-operation iteration. The generated envelope, operations array, and operation objects retain identity and eligible entries are still mutated in place. Only array-valued operations and non-array object entries participate; operation names remain trimmed but case-sensitive; entity aliases remain trimmed and lowercase; and scalar-coerced `id`, `slug`, `name`, or `targetClientId` values block inference in the same short-circuit order. Encounter/scene routes retain truthy target requirements and lazy route reads. Custom targets still require truthiness, prefer one scalar-coerced trimmed ID over one name, and preserve numeric/boolean scalar text behavior. The expanded decision table covers all four operations, all seven entity aliases, uppercase/whitespace normalization, malformed entries, array exclusion, broad object acceptance, every existing-key blocker including zero/false/bigint scalars, object-valued non-blockers, custom UTF-8 name fallback, numeric-zero custom IDs, falsy route targets, unsupported operations/entities, identity, and lazy no-op paths. Fallow removes the original CC 19/cognitive 20/CRAP 380 finding and reports zero findings across the complete file without suppressions; the suite remains at 262 passing tests.
- Completed Phase 68 and decomposed `searchImageGalleryAssets` plus its nested local walker in `server/storage.js` into private category-filter/context normalization, category/source eligibility, local search matching and metadata projection, entry/directory/category/source traversal, local result collection, official-token eligibility/subcategory/projection/collection, sort-name derivation, and final ordering policies. Query text remains trimmed and lowercase while source/category filters remain trimmed and case-sensitive; selected subcategories still pass through safe segment normalization; and an explicit category must also belong to any nonempty category set. Local discovery remains directory-only, sequential, and depth-first; image-extension filtering, full filename/source/category/subcategory matching, encoded URL and platform-path construction, file-size reads, labels, mutability flags, and metadata are unchanged. Official assets still require empty/general source plus an allowed tokens category, receive selected subcategory only for an explicit tokens category, preserve their original source as `assetSource`, project into general/tokens, and join local assets before display-name/name locale sorting. The filesystem-backed UTF-8 matrix now locks full local metadata, normalized mixed separators, nested Ukrainian paths and encoded URLs, extension rejection, source/category/subcategory query matching, malformed category-list recovery, category intersection, case-sensitive/missing sources, stable ordering, official metadata/subcategory forwarding, ignore-source behavior, and source/category official gating. Fallow removes `searchImageGalleryAssets` at CC 20/cognitive 37/CRAP 420 and its nested walker finding, with zero findings across the complete replacement search span and no suppressions; the suite remains at 262 passing tests.
- Completed Phase 69 and decomposed the adjacent image-gallery read cluster in `server/storage.js`. `listBestiaryTokenAssets` now delegates to private context/root-source/ignored-path policies, empty-envelope construction, root eligibility, display-name and image projection, recursive entry/directory traversal, direct subcategory/image collection, mode selection, and ordering. Safe subcategory segments, uppercase ignored-source matching, ignored/missing-root empties, query-or-recursive mode, sequential depth-first recursive traversal, direct `Promise.all` file projection, supported extensions, full-relative-path query matching, query-only folder-qualified display labels, URL/path/byte/readonly/source metadata, and the two distinct sorts remain exact. Storage statistics now use private query normalization, directory-name discovery, sequential named-directory measurement, explicit-versus-discovered category selection, selected category/subcategory sizing, and final projection; recursive directory sizing also delegates each entry classification. Basename source normalization, raw category/subcategory strings, array-only category coercion without trim/deduplication, duplicate measurement behavior, directory-only maps, every-file byte totals, missing-path zeros, and response shape remain unchanged. Readonly database coverage locks sorted root/direct/recursive results, normalized/ignored/malformed/missing sources, exact Animated Coffin metadata and filesystem size, and search labels. A new isolated UTF-8 fixture locks basename source selection, nested all-file totals, exact category/subcategory/source sizes, explicit duplicate category precedence, discovered categories, malformed inputs, whitespace keys, missing-source zeros, and total/source maps without touching user content. Fallow removes `getImageGalleryStorageStats` at CC 13/cognitive 14/CRAP 182, the recursive token walker at CC 8, `listBestiaryTokenAssets` at CC 7, and `getDirectorySize` at CC 5, with zero findings across the complete read cluster and no suppressions; the suite reaches 263 passing tests.
- Completed Phase 70 and decomposed the image-gallery write/reference cluster in `server/storage.js` behind unchanged storage and image-repository contracts. Reference propagation now delegates exact URL replacement, serialized session replacement, metadata/entity/session owners, and per-campaign orchestration. Moves now use private decoded request context, recursive regular-file collection, subcategory and URL projection, per-item execution, and batch accumulation. Renames use shared URL projection plus focused validation; deletes use private request context, per-item deletion, and folder-extraction execution. Existing order remains exact: same-directory requests return before destination creation; non-identical destinations are created even when no source item exists; missing items are skipped; entries and recursive files remain sequential; retry-owned renames finish before result projection; and all references update only after the complete move batch. Standalone filenames remain encoded while directory-relative URL segments retain their legacy raw representation. Metadata and all entity types still use exact ordered URL equality; sessions still use serialized global replacement. Rename error text/result shapes, recursive force deletion, extraction through `moveImages`, and partial-progress failure behavior remain unchanged. Focused filesystem coverage now locks exact rename URLs/errors, encoded slugs and filenames, UTF-8 nested path projection, non-image file tracking, empty-directory movement, missing and same-location no-ops, extracted empty folders, extracted file deletion, and NPC reference propagation in addition to existing metadata/character/location/session behavior. Fallow reports zero findings across the complete write/reference cluster without suppressions; the suite reaches 264 passing tests.
- Completed Phase 71 and decomposed backend realtime change classification in `server/realtime.js` behind its unchanged two-export transport API. Request URL selection, null-filled event creation, settings/Bestiary/images/AI descriptors, campaign session/entity projection, nested campaign precedence, API dispatch, and post-response notification eligibility now have focused private owners. The closed descriptor map replaces the critical route cascade while retaining unknown fallbacks. Event versions still use the same monotonic post-read increment; methods and paths remain unnormalized; pathname segments remain encoded; AI query values still use `URLSearchParams` decoding; custom Bestiary requires the exact third `custom` segment; session/entity routing precedes nested resource detection; and nested images still precede import. Middleware continues to require `req.originalUrl` itself to start with `/api/`, ignores only `GET`, `HEAD`, and `OPTIONS`, rejects only status codes `>= 400` (so an absent/non-numeric status retains legacy notification behavior), and waits for `finish`. End-to-end tests register clients through the real upgrade handler, decode short and extended server frames, and lock exact route projections, Ukrainian query/path handling, version sequencing, source-client exclusion, middleware fallback distinctions, read-method suppression, error suppression, and successful/unspecified-status delivery. WebSocket handshake, client storage, frame construction, close handling, and public exports remain unchanged. Fallow removes the former critical `describeChange` finding and reports zero findings across the complete module without suppressions; the suite reaches 266 passing tests.
- Completed Phase 72 and decomposed `server/modules/reference/application/referenceCommands.js` into focused private policies for source-family priority, reference input/name/truthy-field narrowing, extra-field projection, candidate insertion and sorting, three independent spell predicates, criteria construction, indexed entry/file loading, aggregate selection, source projection, named-file reads, condition/status tagging, disease fallback, named extra fields, aggregate/index source lookup, and exact error construction. `createReferenceCommands` is now wiring-only, while the existing compatibility exports remain unchanged. XPHB/XDMG still outrank PHB/DMG and all other sources; strict `>` selection keeps the first record on equal or lower priority and preserves its map position. Names retain string coercion, trimming, lowercase keys, and locale sorting. Core truthy fields are projected before extra fields, allowing name/kind/source/page overrides, while original truthy `entries` is deliberately restored afterward. Spell queries remain lowercased but untrimmed, level remains string-equal, indexed spell files remain parallel and flatten in index order, and source labels overwrite file data. Aggregate `exists` is still authoritative, `all` returns the aggregate array itself, aggregate source filtering is case-insensitive, indexed lookup is case-sensitive, and its missing-source error remains `Source not found.` with status 404. Condition/status tagging, disease-to-conditions fallback, variant-rule/skill/sense fields, nullable named files, and repository call order remain exact. Expanded tests lock the complete priority matrix, identity on candidate selection, malformed/blank/numeric and UTF-8 names, case-insensitive duplicates, equal/higher/lower precedence, extra-field collision order, falsy defaults, aggregate authority/identity, untrimmed queries, indexed source order/overwrites/case errors, condition/status projection, disease fallback reads, every named reference kind, and exact ordering. Fallow removes the original critical normalization result plus every adjacent moderate result and reports zero findings across the complete module without suppressions; the suite reaches 268 passing tests.
- Completed Phase 73 and decomposed `server/modules/ai/application/resolveAiRequest.js` into focused private policies for strict-false generation defaults, encounter-gated custom-monster generation, session-scope eligibility, target-scope classification, encounter-aware parsing, disabled encounter-type normalization, route fallback, parsed use-key selection, image/prompt precedence, and structured-contract eligibility. The public `resolveAiRequest`, language/model normalizers, and model selector remain unchanged. Response-language aliases now use a closed prototype-safe map: the five Ukrainian/English aliases retain exact labels, every other scalar—including prototype-like names such as `toString`—uses its normalized lowercase code, and all falsy/blank inputs keep the exact required-language error. Model normalization still removes only one exact lowercase prefix before trimming, so leading whitespace and uppercase prefixes retain their historical behavior; model selection remains exact and falls back to the server default. Character/NPC/location flags still default on unless strictly false; encounter/custom flags preserve Boolean gating. Entity scope retains truthy session/falsy encounter rules, including numeric encounter ID `0`; custom-monster still forces parsing; truthy encounters still block ordinary parsing unless encounter generation is enabled; disabled explicit encounter requests still route through current context; and image selection still beats prompt parsing. The result keys and ordering remain exact. Expanded tests lock scalar language aliases/errors and prototype safety, prefix-order/model-selection behavior, the complete default/result shape, every generation flag, all entity scopes and falsy sessions, every supported request type, image/custom-monster exceptions, all disabled/enabled encounter combinations, and unsupported encounter/session/campaign/prompt fallbacks. Fallow removes `resolveAiRequest` at CC 18/cognitive 27/CRAP 342 and reports zero findings across the complete module without suppressions; the suite reaches 270 passing tests.
- Completed Phase 74 and decomposed `server/modules/ai/application/generateCampaignContent.js` into focused private policies for campaign-path validation, optional session reads, context creation, mixed-session eligibility/projection, custom-Bestiary eligibility/monster/name projection, generation-envelope construction, parsed target completion, object-only mention eligibility/application, failed-generation history, parsed-contract validation, persistence-envelope construction, and thin execution/factory delegation. The single public factory remains unchanged. Effect order remains campaign validation → campaign read → tolerant session read → campaign context append → optional current-session attach → optional custom-Bestiary read → generation → target completion → mention application → failed-history handling → validation → persistence. A falsy campaign path is still a fully lazy 400 exit; campaign/session/path/context objects retain identity; rejected session reads alone become null; mixed scope requires a truthy session before filtering; and custom Bestiary reads remain gated by truthy encounter or encounter generation. Monster input remains array-only, `asText` retains trimmed UTF-8/string/numeric/boolean names, and empty lists omit custom context. Generation still receives prepared parsing while persistence retains both original and prepared parse flags. Parsed target completion still runs for every generated value; mentions still require a truthy object and therefore include arrays and error objects; errors still reach history after both post-processors and before validation; mixed scope alone requires explicit entity scopes. Generated errors keep exact status/body spread behavior, and generation/history/validation throws retain their original boundaries without persistence. Expanded injected-dependency tests lock early-read laziness, rejected-session recovery, skipped filtering/Bestiary branches, exact unparsed generation data, persistence identity, UTF-8 and scalar monster names, malformed/empty custom lists, parsed error ordering and response overwrite, parsed-array mutation/validation, complete effect order, and generation/history/validation exception propagation. Fallow removes `generateCampaignContent` at CC 17/cognitive 14/CRAP 306 and reports zero findings across the complete module without suppressions; the suite reaches 274 passing tests.
- Completed Phase 75 and decomposed `server/modules/ai/application/generateCustomMonster.js` into focused private policies for missing-ID detection, one-time Bestiary normalization, authoritative monster snapshot selection, compact context projection, target eligibility/lookup/attachment, optional campaign/session recovery, generation-envelope construction, failure-history response creation, target completion and validation, exact draft-envelope construction, routing, execution, and factory wiring. The public factory remains unchanged. The command still begins with the Bestiary read; only array lists with an ID empty through `asText` trigger one normalization write, and its returned array owns all later context and persistence input. Projection retains exactly `id`, `name`, `source`, `type`, and `cr` without sanitizing scalar or UTF-8 values. Truthy object targets—including arrays—remain eligible; candidate matching retains the per-item ID-or-case-insensitive-name expression and therefore its empty-name behavior, with original-target fallback and exact `create-based` versus default `edit` mode. Campaign/session reads remain sequential and independently fail-open only for a truthy non-Bestiary campaign, after which append runs even with null data. Fixed custom-monster generation flags, scope, parsing, language/prompts, route encounter, and object identities remain exact. Generated errors still save failure history and return the exact 500 envelope before target completion, validation, eligibility, or drafting. Successful content still fills against the constant Bestiary path, validates required operations, then chooses encounter-local or ordinary Bestiary persistence with their complete original inputs. Expanded injected-dependency tests lock non-array and normalized Bestiary data, original/normalized array identity, scalar zero IDs, empty-name matching, target fallback and mode, Bestiary-context laziness, independent campaign failure with surviving session context, exact sequential effect order, full generation/local-draft envelopes, Ukrainian text, failed-history short-circuiting, and thrown validation boundaries. Fallow removes `generateCustomMonster` at CC 15/cognitive 12/CRAP 240 and reports zero findings across the complete module without suppressions; the suite reaches 277 passing tests.
- Completed Phase 76 and decomposed `buildAiChangeSet` in `server/aiResponseHistoryService.js` into focused private policies for empty results, campaign resource projection, generic resource indexing, session/entity indexes, ordered union keys, present-versus-missing lookup, indexed change iteration, session/entity resource construction, entity-type traversal, label sorting, final summary projection, and thin orchestration. The service API remains unchanged and the policies stay private. Either falsy bundle still produces the exact empty resource/summary envelope. Campaign metadata retains nullish fallback; sessions and each `storage.ENTITY_TYPES` collection retain ordinary truthy `.map` boundaries, duplicate-key last-write-wins values and first insertion positions, before-then-after union order, and `Map.has` lookup before cloning. IDs, kinds, campaign/type/slug/file fields, and path labels remain exact. `pushAiChange` still owns JSON-string comparison, unchanged omission, deep JSON cloning, and undefined-to-null normalization. Final resources still use `label.localeCompare` before the existing summary builder, and source bundles remain unmodified. New persistence-level tests cover changed campaign metadata, duplicate sessions/entities, created/deleted/modified sessions, all three entity types, unchanged omission, present undefined snapshots, exact summary classification, UTF-8 labels, deterministic ordering, clone isolation, both missing-bundle paths, and the retained malformed-collection TypeErrors. The first test run corrected its expectation for an undefined snapshot becoming an added null-to-object resource; production behavior did not change. Fallow removes `buildAiChangeSet` at CC 18/cognitive 22/CRAP 342 and reports zero findings across the complete replacement span without suppressions; the suite reaches 279 passing tests.
- Completed Phase 77 and decomposed failed AI-history persistence plus retry attachment projection in `server/ai/AiHistoryWriter.js`. Focused private policies now own raw error selection, normalized/default messages, status-line formatting, failed text construction, retry image field collection/eligibility/materialization, shared attachment narrowing, file projection, collection filtering, JSON retry cloning, object-like history-path selection, failed parsing eligibility, exact request-snapshot input construction, and failed response construction. The class API and exported `asText` alias remain unchanged. `saveFailed` still dynamically dispatches through `shouldSave`, `getUserInstructions`, `buildRequestSnapshot`, and `cloneRetryPayload`; suppression is the first fully lazy exit, and a missing trimmed campaign stops before all remaining work. Image requests remain non-parsed; encounter requests still return the raw truthy `generateEncounters` value from the original `&&` chain; generation defaults and response truthy/null fallbacks remain exact. Failure text still omits falsy statuses while error metadata retains them, and raw `message || error` selection still precedes trimming/defaulting. Response construction invokes retry cloning last and performs exactly one storage write whose failure propagates. Retry projection still begins with JSON clone semantics, retains non-array attachment fields unchanged, filters malformed/empty array items in order, trims scalar metadata, converts sizes through `Number(...) || 0`, reads truthy image data for omission markers, strips all file data, and leaves input identity/content unchanged; circular and BigInt payload failures still propagate. Direct tests cover lazy suppression/missing paths, override call order, exact snapshot and response envelopes, instruction ownership, image/encounter parsing branches including falsy encounter ID and raw truthy flags, Unicode error/status formatting, storage failure propagation, comprehensive attachment filtering, scalar/negative size behavior, non-array preservation, source immutability, and JSON failure boundaries. Fallow removes `saveFailed` at CC 16/CRAP 272, the attachment callback at CC 14/CRAP 210, and all replacement findings across the complete writer module without suppressions; the suite reaches 282 passing tests.
- Completed Phase 78 and decomposed the custom-Bestiary normalization cluster in `server/storage.js`. Focused private policies now own string-entry projection, object-entry eligibility, truthy content selection, entry-array selection, record projection, entry dispatch/filtering, base monster projection, HP eligibility and stored-average normalization, HP mutation, spellcasting wrapping, action-family normalization/deletion, and thin monster orchestration. The exported normalizer and writer contracts remain unchanged. Entry families still accept arrays only; nonempty strings trim into anonymous entry records; truthy object values—including arrays—remain record-compatible; existing array `entries` retain identity until the preceding recursive bracket cleanup; and fallback content preserves `text → description → content`, its historical second evaluation, and `String` conversion. Original entry fields remain spread, only projected `name` trims, original `title` whitespace remains, and empty entries are removed. Monster spread still precedes the second reads of ID/name and forced source; falsy IDs allocate through `createId`, aliases and all nested text still pass through recursive mention cleanup. HP remains cloned only for truthy object non-arrays, formula averages take precedence, own stored averages parse/clamp/default exactly, and invalid formulas without an own average remain untouched. Spellcasting and all five action families retain their original shape gates and order, including preservation of an own `undefined` family versus deletion of every other empty/malformed family. A direct UTF-8 decision table locks input immutability, getter read counts, generated-ID counts, every HP/spellcasting branch, aliases, bracket cleanup, scalar/object entry content, preserved title whitespace, and malformed direct-call boundaries. A filesystem-backed writer test byte-restores the user's original Bestiary file and locks malformed/nameless filtering, duplicate-ID regeneration, localized sorting, source metadata, returned/stored identity, and source immutability. The first test run corrected only the title-whitespace expectation. Fallow removes `normalizeCustomBestiaryMonster` at CC 15/cognitive 19/CRAP 240 and the entry callback at CC 13/cognitive 11/CRAP 182, reporting zero findings across the complete replacement span without suppressions; the suite reaches 284 passing tests.
- Completed Phase 79 and decomposed the complete encounter-local custom-monster AI flow in `server/ai/EncounterLocalMonsterAiFlow.js`. Focused private policies now own operation-value normalization, eager entity/operation classification, first eligible operation discovery, object-property selection, update/create raw projection, ID/name fallback, provenance/image inheritance, normalized monster construction, HP candidate parsing, session eligibility and lookup, participant targeting/replacement, resource projection, flow routing, failure projection, request snapshot construction, history persistence, and success projection. The public class API and exported session-change helper remain unchanged. Operation aliases remain trimmed and case-insensitive, object-like arrays remain valid selector values, and selection retains `data -> monster -> payload -> operation` plus `patch -> changes -> data` precedence. Each eligible getter still receives the historical truthiness, type, and selected-value reads; a falsy third value remains selected instead of falling through. Entity and operation are both evaluated for every candidate, the first eligible operation wins, and update/create paths deliberately reevaluate their selectors while retaining exact spread and truthy ID/name precedence. Own `originalBestiaryName` and `imageUrl` values, including `undefined`, still block inheritance before normalization. Session changes retain raw truthiness gates, so numeric-zero route/instance targets fail while string zero remains valid; JSON cloning still precedes encounter lookup and propagates circular/BigInt failures; the first string-compatible encounter alone is considered; truthy non-array encounter collections still throw; monsters remain array-only; and every string-compatible participant match is replaced without mutating the input. Participant source and provenance remain encounter-owned, while max HP retains average -> direct hit points -> previous hit points precedence and the established zero/negative current-HP behavior. Flow eligibility still returns the final truthy encounter text rather than a forced Boolean. `createDraft` retains async parameter-destructuring rejection semantics and path/target/operation/session evaluation order. Successful history construction dynamically dispatches through the current flow and writer properties in format -> snapshot -> retry -> summary -> add order, fully materializes the payload before `addResponse` lookup, and preserves the exact request, retry, resource, draft-state, and response envelopes; all collaborator and persistence failures propagate without being rewritten as generation failures. Four focused regressions lock getter counts and third-read selection, all aliases and first-match behavior, array payloads, exact late-bound history calls, own-field blocking, duplicate participants, immutable before/after snapshots, malformed sessions, every HP source, exact 400 failures, persistence errors, explicit-null generated content, and rejected destructuring. Fallow reports zero findings across the complete module without suppressions; the suite reaches 288 passing tests.
- Completed Phase 80 and decomposed `restoreAiResponseSnapshot` plus updated-object readback in `server/aiResponseHistoryService.js`. Focused private policies now own target-path selection, Bestiary/session/campaign readback, exact restore errors, raw resource validation, optional ID-set construction, selected-resource projection, sequential snapshot writes, resource-state projection, aggregate-state classification, partial/full patch construction, persistence, refreshed-history loading, and final result projection. The service still exposes exactly its established four exports and continues using storage dynamically at execution time. Raw `entry?.changes?.resources || []` behavior remains unchanged, including failures from truthy malformed values; explicit `null` options still reject at the `resourceIds` read; and only array-valued IDs enable partial mode. ID normalization still uses `String(id || "")`, filters empty values, deduplicates through `Set`, and compares against raw resource IDs without coercion. Missing resources and empty selections retain their exact status-400 messages. Selected snapshots still write sequentially through `resource[snapshotKey] ?? null` with no compensation, so earlier writes remain when a later write fails and no timestamp/history patch is created. The timestamp is created once after all writes. Exact `after` still means applied while every other key means undone. Partial restores clone only selected resources, retain unselected object identity, summarize the projected list, and compute root applied/undone/draft plus nullable timestamp from every resource. Full apply clones all resources but summarizes the original list; full undo preserves `entry.changes` identity and resource metadata while updating only root state/time. Persistence and readback retain exact `update -> history list -> updated target` order. Falsy update results remain the returned response but fall back to the original entry only for readback. Exact `bestiary` still precedes session handling; successful session reads force the requested filename; missing sessions fall back to campaign metadata; absent campaigns/targets return null; and all failures propagate after any earlier effects. Three mocked-storage matrices lock fixed timestamps and getter reads, full and partial identities/states, duplicate selections, strict numeric/string IDs, null options, malformed resources, nullish/unknown snapshots, sequential partial progress, every readback branch, UTF-8 paths, and update/history/readback failure boundaries. Fallow removes the restore function at CC 15/cognitive 14/CRAP 240, its nested patch at CC 6/CRAP 42, and readback at CC 8/CRAP 72, reporting zero findings across the complete Phase 80 span without suppressions; the suite reaches 291 passing tests.
- Completed Phase 81 and decomposed the remaining custom-monster change-resource builder in `server/aiResponseHistoryService.js`. Focused private policies now own array-only list normalization, truthy object-like filtering, ID/name indexing, ordered key unions, identity-based matched tracking, ID-first and unmatched-name passes, change ID/name/label projection, JSON comparison/cloning, resource accumulation, and final ordering. The service still exposes exactly its established four exports, and both `CustomMonsterAiFlow` and `CampaignAiFlow` retain the same dependency. Explicit null and non-array sources become empty lists while default parameters still apply only to `undefined`; arrays remain valid monster records, but their named properties disappear from JSON snapshots. Indexing deliberately repeats getter reads across filter and map, trims truthy IDs, lowercases trimmed names, retains first key position with the last mapped value, and marks only mapped object identities. Consequently, earlier duplicate-ID objects may still enter the name pass. Before keys precede after-only keys in both passes. Resource identity remains `after ID -> before ID -> normalized name from after || before -> pass key`; display fields use the same selected object and fallbacks. JSON-string equality retains nullish-to-null normalization, property-order sensitivity, `toJSON` effects, unchanged omission, deep-clone isolation, and propagated circular/BigInt failures. Stable locale sorting still uses `label || id`. Three direct matrices lock malformed inputs, array records, zero/string-zero identity, ID renames, case-insensitive names, duplicate IDs/names, exact getter counts, fallback labels, clone isolation, property-order differences, `toJSON`, and serialization failures. Fallow removes the builder at CC 11/cognitive 12/CRAP 132 and its nested callback at CC 7/CRAP 56, reporting zero findings across the complete service without suppressions; the suite reaches 294 passing tests.
- Completed Phase 82 and decomposed the complete `server/modules/campaign/application/campaignEntityCommands.js` boundary. Focused private policies now own location/type classification, lazy entity-field reads, location and character/NPC display-name projection, payload-field selection, create-name validation, person level fallback, location/person creation defaults, replacement-list narrowing, lazy slug candidates, target-slug indexing, sequential stale deletion, sequential ordered writes, and character/NPC move validation. The module retains exactly its established three exports, while routes and the filesystem repository adapter remain unchanged. Type validation still precedes payload access and keeps both exact status-400 messages. Location display retains lazy truthy `name -> title`; person display evaluates first then last name, preserves template-literal coercion, skips fallback getters when the composed name is truthy, and trims only the final result. Creation still reads the route-specific name candidate before sanitization, then executes `toSlug -> ensureUniqueSlug -> payload construction -> write`. Location and person defaults precede payload spread; payload getters therefore run again during spread; generated IDs execute after spread and override supplied IDs; location name fallback executes after ID creation; and person level retains its one-read empty-string branch versus two-read ordinary branch before any own payload value wins through spread. Updates still strip only the two mention-control fields, retain stored ID/slug, execute `read -> write -> optional mention update -> read returned slug`, and preserve repository result identity. Replacement still treats non-arrays as empty, invokes `toSlug` for every requested item in both indexing and writing passes through lazy `slug -> name -> firstName`, filters falsy results, deletes stale current rows sequentially before writes, writes duplicates in request order with zero-based order overriding the source field, and performs the final list read only after success. Failures retain uncompensated earlier deletions/writes. Three direct matrices lock frozen export types, UTF-8 and Symbol display behavior, lazy getter reads, spread/create-ID timing, exact defaults and payload precedence, missing/null payload boundaries, update and mention ordering, source immutability, every move validation path, two-pass slug calls, duplicate/empty candidates, non-array deletion behavior, and deletion/write partial failures. Fallow removes `getEntityDisplayName` at CC 15/CRAP 240, `createEntityData` at CC 12/CRAP 156, `replaceAll` at CC 11/CRAP 132, and its nested target-slug callback at CC 6/CRAP 42, reporting zero findings across the complete module without suppressions; the suite reaches 297 passing tests.
- Completed Phase 83 and decomposed the complete `server/modules/ai/application/campaignContext.js` boundary. Focused private policies now own lazy context identity fallback, list-inclusion classification, ignored-value filtering, configured item-entry/key selection, visible-entity selection, array-only session collections, scene projection, campaign-note mutation, per-section eligibility/loading/assignment, the legacy NPC fallback, session-entry enumeration, included-session projection, input eligibility, and thin sequential orchestration. The module retains exactly its established two exports and the HTTP composition still injects entity/session readers. Character/location keys preserve lazy truthy `slug -> id -> display name` precedence before `asText`; explicit null private inputs still fail while default parameters apply only to `undefined`. Entity rows are filtered for truthy `_aiIgnored` before any config rejection. Falsy config remains select-none, exact `true` returns all visible rows, object/array configs use `included !== false`, truthy scalars remain included, `items || {}` retains raw object/scalar compatibility, only strict-false item entries are excluded, empty item maps return all visible rows without reading keys, and nonempty maps preserve key insertion and entity order. Session data still spreads first and then replaces notes/NPCs/locations/scenes with array-only projections. Array-valued NPC/location/scene properties retain their historical second getter read after the spread read; ignored scenes remain, only scene notes are filtered, visible row identity is retained, and null/malformed failure boundaries propagate. The append command remains a lazy no-op for falsy target/campaign/config, mutates the supplied target, and executes notes, characters, NPCs, locations, then sessions sequentially. Included character/location configs are reread at assignment. The NPC branch preserves direct-config short-circuiting and the exact undefined-only character fallback, including three NPC getter reads, repeated character reads, and effective `true` filtering. Session config is read for truthiness and again for `Object.entries`; included rows read and push sequentially with exact slug/fileName, raw name, config identity, and filtered data, while skipped rows remain unread. Reader and malformed-input failures retain all earlier target mutations without compensation. Three direct matrices lock the two-export API, UTF-8 keys, Symbol/malformed data, spread/property/scene getter counts, ignored truthiness, scene retention, scalar/array configs, empty and selected item maps, lazy ignored/key reads, changing config values, direct and fallback NPC getter counts, complete reader order, falsy campaign slugs, target getter order, session identity, no-op laziness, and partial session/config failures. Fallow removes `appendConfiguredCampaignContext` at CC 14/cognitive 16/CRAP 210 and `filterEntitiesByContext` at CC 6/CRAP 42, reporting zero findings across the complete module without suppressions; the suite reaches 300 passing tests.
- Completed Phase 84 and decomposed the campaign image lifecycle span in `server/storage.js`. Focused private policies now own safe image slugs, individual directory-entry classification, recursive directory traversal, replacement eligibility, encoded campaign image segments, serialized replacement, conditional existing-JSON updates, per-entity/type/session updates, per-campaign resource sequencing, update eligibility, rename-path projection, image-directory collision/rename handling, and thin campaign rename orchestration. The storage export surface remains unchanged. Image existence still uses `path.basename(String(slug || ""))`, returns before filesystem access for an empty safe slug, checks the root once, traverses `readdir` entries in returned order, constructs each full path before classification, returns at the first file or nonempty recursive directory, ignores other entry types, and propagates access/read/classifier failures. Image reference replacement retains the raw falsy/equal early return, exact `encodeURIComponent` segment construction, one `JSON.stringify`, original identity when the serialized segment is absent, whole-JSON split/join including keys and repeated occurrences, and `JSON.parse` deep-clone semantics when changed. `toJSON`, circular, BigInt, function, and Symbol boundaries remain observable. Campaign reference propagation still lists campaigns once and processes each sequentially. Within a campaign it executes existing metadata, `characters -> npc -> locations` and each listed entity, sorted sessions, then existing AI history; reads are sequential, identity suppresses writes, entity writes use the normalized slug, and failures stop later work without rollback. Campaign rename retains the raw guard, renames the campaign directory first, derives image paths with raw `path.basename`, checks a destination collision only when the old image directory exists, renames images before references, and still updates references when no old image directory exists. Three direct/filesystem matrices lock falsy/scalar/UTF-8 slugs, nested values and JSON keys, repeated segments, unchanged/changed identity, `toJSON` count, serialization failures, mocked depth-first entry order and early exit, missing roots, recursive failures, image collision after campaign rename, destination-image preservation without a source image, byte-stable unchanged entity/session/history files, metadata replacement, and session-failure partial progress after entity updates but before AI history. Fallow removes `updateCampaignImageSlugReferences` at CC 14/cognitive 27/CRAP 210, `replaceImageSlugReferences` and `renameCampaignData` at CC 6/CRAP 42 each, and the recursive `walk` cognitive/CRAP finding, reporting zero findings across the complete span without suppressions; the suite reaches 303 passing tests.
- Completed Phase 85 and decomposed the complete `shared/bestiaryUtils.cjs` cross-runtime policy boundary behind its unchanged six-export CommonJS API. Focused private policies now own object-entry name selection, JSON uniqueness keys, each array-mod command, ordered mode lookup, insertion bounds, replacement/removal projection, dice normalization/tokenization/classification/dimension and keep validation, per-part aggregation/finalization, monster/HP eligibility and average application, plus mention-value kind dispatch and recursive array/object projection. JSON clone failures and `toJSON` behavior, nullish array normalization and array identity, lazy truthy `name -> id -> title` matching, target getter/setter order, item cloning before ordered repeated mode reads, unknown-mode side effects, source-order/property-order uniqueness, insertion coercion, shared cloned replacement identity, per-row replacement getters, and nullish removal inputs remain stable. Dice formulas retain their exact whitespace/sign grammar, default-one zero count, finite die validation, high/low keep equivalence, first-invalid rejection, flooring/minimum one, and constant overflow. HP normalization retains unchanged identity on malformed/invalid inputs, exact repeated getter counts, source-before-HP spreading, and final average override. Mention cleanup retains one-pass nested-bracket behavior, sparse holes, enumerable string-key projection, getter execution, prototype/symbol loss, separate recursive clones, and circular failure. Three direct matrices lock all six exports, every modifier mode and dynamic getter boundary, JSON failures, dice grammar/overflow, HP identity/getters, sparse arrays, nested brackets, object projection, and recursive failures. Fallow removes all five original hotspots and reports zero findings across the complete module without suppressions; the suite reaches 306 passing tests.
- Completed Phase 86 and decomposed the complete `server/modules/ai/infrastructure/attachmentParts.js` boundary behind its unchanged four-export CommonJS API. Focused private policies now own capped attachment lists, URL collection, falsy-default string coercion, decoded path-part safety, URL pathname fallback, route/location projection, storage-root containment, extension/MIME selection, tolerant stat lookup, local-file eligibility and conversion, shared inline-part construction, declared MIME and data normalization, data-URL stripping, base64 decoding/size validation, inline-versus-URL image selection, URL deduplication, sequential image assembly, prompt filename cleanup, text/binary file projection, and file assembly. Four-item and 10 MiB limits, exact MIME tables, declared-before-extension image precedence, extension-before-declared file precedence, lowercase-only `data:` stripping, Buffer normalization, empty/oversized rejection, UTF-8 text output, binary two-part output, CR/LF-only filename cleanup, and all default/coercion behavior remain stable. URL resolution still ignores origins and uses only the parsed pathname, applies raw fallback only when URL construction/coercion throws, splits before one decode, accepts encoded route prefixes, rejects decoded traversal/separators, ignores query/hash, lowercases extensions, permits double-encoded dot text as a literal segment, and propagates malformed decode failures. Valid inline images remain URL-lazy; invalid inline images fall back to a trimmed URL; duplicate URLs are marked before I/O; stat failures, missing files, non-files, and oversize files skip silently; reads remain sequential and failures propagate. Three direct/mocked-filesystem matrices lock the exact export keys, collection mutation and threshold timing, repeated getters, UTF-8/encoded/external/dynamic URLs, every image extension, fallback coercion, traversal and decode failures, inline MIME/data precedence, Buffer normalization, caps, URL order/deduplication, stat/read asymmetry, all file extensions, declared MIME fallback, text/binary shapes, dynamic filename reads, names, invalid data, and both size limits. Fallow removes all eight original findings and reports zero findings across the complete module without suppressions; the suite reaches 309 passing tests.
- Completed Phase 87 and separated the complete 5eTools updater into three explicit boundaries. `scripts/update-5etools-data.mjs` is now a thin CLI composition root for constants, paths, help/exit, native adapters, updater creation, and top-level cleanup/failure exit. The new Node-safe `update-5etools-data-policies.mjs` owns flags/ref selection, help text, remote filename filtering/projection, terminal-newline policy, monster/token keys, Bestiary envelope narrowing, new-monster selection, condition/source keys and priority, condition deduplication/pruning, recursive inline removal, point-buy injection, local Exhaustion merge, source normalization, and JSON directory-entry classification. The injected `create5eToolsUpdater` runtime owns existence/JSON reads, GitHub/raw/token requests, sequential file downloads, token paths and per-token transactions, Bestiary discovery, condition/disease/variant/skill/sense/source writes, nonrecursive JSON removal/copy, subprocess execution, support cleanup, and the dry-run/write workflow. The command name, default repositories/refs, flags, immediate help exit, seven-way input download, summary text, dry-run boundary, current-monster-before-replacement snapshot, remove/copy phases, reference and token ordering, post-processing, source cleanup, and error cleanup remain stable. Data lists still filter/sort before sequential downloads, require URLs, validate JSON, and preserve/add terminal newlines. Monster identity, aggregate authority, fallback file order, first-new identity, UTF-8 token URL encoding, unsafe/existing/404/failure/success counters, and exact per-token catch behavior remain stable, including caught mkdir/write/log failures after earlier counter mutations. Condition 2024/source precedence, tie identity, raw-name sorting, local Exhaustion, prune/inline/point-buy rules, source traversal/deduplication, and nonrecursive case-insensitive JSON operations remain unchanged. The former source-regex test is replaced by three behavioral matrices covering flags/help, remote entries/newlines, getter-sensitive monster keys, malformed envelopes, new-monster identity, token filenames, the complete condition/source policy, recursive rule cleanup, injected downloads and errors, aggregate fallback, every token outcome, reference writes, JSON removal/copy, subprocess success/failure, real temporary files, and both full dry-run and write workflows. Fallow removes all eleven original updater findings and reports zero findings across all three updater modules without suppressions; the suite reaches 311 passing tests.
- Completed Phase 88 and decomposed the complete Bestiary read/index span in `server/storage.js` behind its unchanged `getBestiaryIndex`, `readCustomBestiary`, and `readCustomBestiaryMonsters` exports. Focused private policies now own qualifying-row projection, index creation/append, direct-array versus envelope selection, aggregate loading, exact directory-entry classification, filename fallback and metadata source selection, sequential split-file loading, custom-envelope retention, CUSTOM source forcing, and custom projection. Custom monsters still load before any official-directory check. An absent official directory returns the custom-only map; present `all.json` remains authoritative and suppresses directory enumeration; fallback files retain returned order, exact lowercase `.json`, exact control-file exclusions, and sequential reads. Metadata source still wins through truthiness before one leading case-insensitive `bestiary-` removal, followed by direct uppercase conversion. Index rows retain their original order, skip only falsy names, normalize source through `String(source || fallback).toUpperCase()`, spread before the forced source, keep the first map position during later-value overwrite, and append CUSTOM last. Custom reads retain fresh missing-file output, direct-array/envelope precedence, truthy non-array envelope spreading, array-only mapping, primitive/null row spread behavior, forced CUSTOM source, and null-envelope failure behavior without invoking persistence normalization. Two mocked-filesystem matrices lock UTF-8 custom projection, metadata retention, malformed envelope precedence, fresh missing values, custom-first ordering, absent-directory behavior, aggregate authority, duplicate overwrite/map order, official/custom collisions, exact entry filtering and casing, metadata/filename source selection, sequential fallback reads and partial failures, and non-iterable rejection without touching user data. The suite reaches 313 passing tests; lint, UTF-8 validation, typecheck, architecture boundaries/cycles, syntax checks, diff checks, and the replacement-character scan pass. Fallow reports zero findings across the complete replacement span without suppressions.
- Completed Phase 89 and decomposed the settings normalization/persistence span in `server/storage.js` behind its unchanged `readSettings` and `updateSettings` exports. Focused private policies now own encounter-column parsing/clamping, prompt-map eligibility and entry projection, empty-key filtering, language/theme/view projection, image-prompt fallback, normalized settings assembly, default cloning/writing, ordered rewrite comparison, and existing-file recovery. `SettingsRepository`, `settingsCommands`, and HTTP routing remain unchanged. Normalization still evaluates encounter columns before both prompt maps and the remaining fields. Prompt-map eligibility preserves its truthy object non-array gate and historical repeated property reads; `Object.entries` order, trimmed/coerced keys, falsy-empty prompt coercion, empty-key removal, and later-value overwrite remain stable. Exact `uk`, `dark`, and `grid` narrowing, base-10 finite fallback two, one-through-four clamp, Boolean simplified notes, falsy-empty AI prompts, undefined-only default image prompt, normalized ignored sources, and exact-false opt-outs remain stable. A missing file still writes the frozen defaults outside recovery and returns a shallow clone. Existing files still read, normalize, compare saved before normalized through ordered `JSON.stringify`, rewrite only when different, and recover any guarded read/normalization/comparison/rewrite failure with a defaults write; the recovery write remains outside a second catch. Updates retain read/recovery, current-then-patch spread, normalization, unconditional write, and exact normalized return order. Two mocked atomic-filesystem matrices avoid user data while locking fresh/default/stable/changed reads, UTF-8 prompt-key collisions, malformed prompt collections, every scalar fallback, column boundaries, source normalization, corrupt/null recovery, normalized-rewrite failure followed by default recovery, missing/default/final-update write failures, patch getter evaluation, write payload order, and update-after-recovery behavior. The suite reaches 315 passing tests; syntax, lint, UTF-8 validation, typecheck, architecture boundaries/cycles, diff checks, and the replacement-character scan pass. Fallow removes `normalizeSettings` at CC 14/cognitive 11/CRAP 210 and reports zero findings across the complete replacement span without suppressions.
- Completed Phase 90 and decomposed the complete entity-listing span in `server/storage.js` behind its unchanged `listEntities` export and `fileCampaignEntityRepository` adapter. Focused private policies now own numeric order projection, person-name composition, final display-name fallback, comparator ordering, and sequential directory-entry loading. Storage remains the owner of path construction and filesystem traversal. It still ensures the campaign/type directory before one `readdir(..., { withFileTypes: true })`, consumes entries in returned order, skips non-directories before any name read, checks each directory's `info.json`, skips missing files, reads accepted entries sequentially, spreads each saved value, and forces the entry's second name read as `slug`. All collected rows are sorted in place only after successful loading; classifier/read failures still stop later work without returning partial data. Order comparison retains `Number.isFinite(Number(order)) ? Number(order) : 0`, including one getter/coercion for non-finite values and a second for finite values, and evaluates a fully before b. Equal-order comparison retains lazy truthy `name -> trimmed firstName/lastName composition -> slug -> ""`, final `String` coercion, a-before-b evaluation, `localeCompare`, and stable equal-key order. Two mocked-filesystem matrices avoid user data while locking ensure/readdir order, non-directory and missing-info skips, dynamic entry-name reads, object/null/string projection, stored-slug replacement, `updatedAt` stripping, sequential accepted reads, negative/decimal/numeric-string/non-finite order handling, all name fallbacks, UTF-8 collation, stable equal names, and fail-fast mkdir/readdir/classifier/read boundaries. The first pre-refactor run corrected only the asserted locale position of the slug fallback; the behavioral implementation remained unchanged. The suite reaches 317 passing tests; syntax, lint, UTF-8 validation, typecheck, architecture boundaries/cycles, diff checks, and the replacement-character scan pass. Fallow removes the nested comparator at CC 14/cognitive 7/CRAP 210 and reports zero findings across the complete replacement span without suppressions.
- Completed Phase 91 and relocated generic name-query ranking from `server/routes/searchUtils.js` into the new infrastructure-free `server/modules/search/application/searchResults.js` boundary. Bestiary and reference application commands now import inward from the search application module; the obsolete route utility is deleted, the new module exposes exactly `sortByNameQuery`, and a repository-wide server-module check confirms no application/module code imports from `server/routes`. Focused private policies now own nullable lowered-name projection, exclusive boolean priority, exact priority, prefix priority, length priority, tier orchestration, and comparator creation. A falsy query remains a fully lazy undefined-returning no-op that never inspects results. A truthy query still invokes the collection's own `sort`, mutates native arrays in place, ignores the sort return, and itself returns undefined. Comparator evaluation retains a before b name getter/lowercase calls, `item.name?.toLowerCase() || ""`, exact strict equality before any prefix work, both `startsWith(nameQuery)` calls before exclusive prefix selection, repeated length reads when unequal, and final `localeCompare`. Raw uppercase/whitespace/Symbol queries, custom name/lowered objects, nullish names, stable ties, and malformed collection/item/name failures remain unchanged. A direct matrix locks the single-export surface, all falsy queries, native mutation, exact/prefix/length/locale order, stable exact and empty-name ties, UTF-8 names, raw uppercase behavior, custom sort delegation, exact getter/method order through every late tier, and propagation of missing-sort, null-item, numeric-name, Symbol-query, and lowercase-method errors. The suite reaches 318 passing tests; syntax, lint, UTF-8 validation, typecheck, architecture boundaries/cycles, import-direction checks, diff checks, and the replacement-character scan pass. Fallow removes the former route comparator at CC 12/cognitive 11/CRAP 156 and reports zero findings in the replacement module without suppressions.
- Completed Phase 92 and extracted campaign mention normalization, bracket replacement, recursive value projection, and persistence orchestration from `server/storage.js` into `server/modules/campaign/infrastructure/campaignMentionReferences.js`. Storage now only injects `ENTITY_TYPES`, path builders, JSON operations, entity operations, and session listing into `createCampaignMentionReferenceUpdater`; its `updateCampaignMentionReferences` export and the `fileCampaignEntityRepository` adapter remain unchanged. The new boundary exposes exactly the updater factory plus the three directly testable pure policies. The initial guard retains normalized-old -> raw-trimmed-new -> normalized-old again -> normalized-new evaluation, so invalid or normalized-equal names still return undefined without touching paths or persistence. Mention normalization keeps falsy-empty `String`, trimming, whitespace collapse, and lowercase; replacement still handles only nonempty nonnested bracket matches, preserves unmatched text, and inserts the trimmed/collapsed display name. Recursive projection preserves string dispatch, sparse holes, enumerable own string keys, fresh array/object identity, getter execution, prototype/symbol loss, primitive/function identity, and circular failure. Persistence remains strictly sequential and uncompensated: optional metadata; `ENTITY_TYPES` with listed entity order; then one session list with per-row path reconstruction and reread. Every resource projects before comparing `JSON.stringify(next)` and `JSON.stringify(current)` in that order, suppresses unchanged writes, preserves entity write arguments, and fails fast after retaining earlier writes. Two direct mocked matrices lock the four-export surface, falsy and UTF-8 normalization, adjacent/nested/empty brackets, sparse arrays, prototypes, symbols, getters, dates, functions, circular and getter failures, every guard branch and coercion count, metadata/entity/session order, unchanged-write suppression, next-before-current serialization, UTF-8 replacement payloads, and partial failure without compensation. The original filesystem integration remains green; the suite reaches 320 passing tests. Syntax, lint, UTF-8 validation, typecheck, architecture boundaries/cycles, diff checks, and replacement-character scans pass. Fallow removes the former `updateCampaignMentionReferences` CC 12/cognitive 14/CRAP 156 finding and reports zero findings across the replacement boundary without suppressions.
- Completed Phase 93 and extracted campaign archive-image restoration from `server/storage.js` into `server/modules/backups/infrastructure/archiveImageRestoration.js` behind exactly the `createCampaignArchiveImageRestorer` export. Storage now only injects `IMAGES_DIR`, directory creation, and the file-write adapter; the full/partial import functions, append and replace-by-ID paths, repository port, backup commands, HTTP routes, and partial-import result accounting remain unchanged. Focused private policies own collection eligibility, campaign-root construction, relative-path normalization, payload eligibility, root containment, and one-row restoration. Non-array/default/empty collections still return undefined before slug coercion or path access. Nonempty arrays still build one root using `path.join(imagesDir, path.basename(String(slug || "")))`, resolve it once, and process rows sequentially. Each row reads/coerces its relative path through falsy-empty `String`, converts backslashes, strips leading slashes, then skips empty paths or a falsy first base64 read. Parent and prefix-sibling traversal remain silent skips; encoded traversal text is literal; targets resolving exactly to the root remain accepted. Accepted rows keep directory creation before the second base64 read, permissive `Buffer.from(value, "base64")`, and the write. There is no extension, filename, byte, duplicate, or directory-target validation. Later duplicate paths still overwrite in input order, and getter/coercion/directory/conversion/write failures stop later rows without compensating earlier writes. Two direct dependency-injected matrices lock the one-export surface, lazy malformed/empty collections, basename slug projection, UTF-8 paths/data, slash normalization, leading roots, empty/falsy fields, parent and prefix-sibling rejection, encoded paths, root targets, permissive decoding, duplicate ordering, exact two-read base64 timing, sequential writes, and failures before/after directory creation. The existing real-filesystem partial-import test continues to prove nested UTF-8 restoration, traversal rejection, and original-array-length accounting including skipped rows. The suite reaches 322 passing tests; syntax, lint, UTF-8 validation, typecheck, architecture boundaries/cycles, and replacement-character checks pass. Fallow removes the former CC 12/cognitive 11/CRAP 156 storage finding and reports zero findings in the replacement boundary without suppressions.
- Completed Phase 94 and decomposed `server/modules/ai/application/buildUserPrompt.js` behind its unchanged `buildTaskInstructions` and `buildUserPrompt` exports. Exact prompt text now lives in named private constants; focused builders own image, NPC, location, encounter, and scene decisions; small policies own scope targets, session-rule inclusion, optional rule appends, input/image/user sections; and a closed private `Map` dispatches the eight supported modes with an empty fallback. Character, custom-monster, and campaign text remains byte-identical. Image mode still prefers a truthy target type, reads that type twice before interpolation, then falls back to a truthy scene ID and the generic task. NPC/location mixed, exact-session, exact-campaign, undefined, zero, object, and Symbol behavior remains unchanged, including raw interpolation and session-rule eligibility. Encounter task construction still interpolates the raw ID before a truthy custom-monster append; scene assembly still applies scope guidance for every value except exact campaign before the truthy encounter-link rule. Unknown scalars, symbols, and boxed strings still produce no task. Final prompt assembly retains parameter-destructuring getter order; context pretty-JSON and its failure boundary; exact image-mode/truthy-object gating with arrays included and functions/null excluded; image-target pretty-JSON before task target reads; task text; and final falsy-empty user instructions. `toJSON` returning undefined, circular/BigInt failures, raw Symbol interpolation failures, and exact blank lines/labels remain stable. Two permanent exact-string matrices cover all modes, scopes, truthy/falsy flags, UTF-8 context/targets/instructions, repeated getters, arrays/functions/null, unknown modes, serialization order, undefined output, and thrown boundaries. The first pre-refactor run corrected the expected image-type getter count from one to the observed two without changing implementation. The suite reaches 324 passing tests; syntax, lint, UTF-8 validation, typecheck, architecture boundaries/cycles, and replacement-character checks pass. Fallow removes the former dispatcher CC 12/cognitive 14/CRAP 156, scoped-builder CC 8/cognitive 10/CRAP 72, and prompt-assembly CC 5/CRAP 30 findings, reporting zero findings across the complete module without suppressions.
- Completed Phase 95 and decomposed the complete `server/modules/ai/application/parseAiResponse.js` boundary behind its unchanged four exports and unchanged `aiService.__test` function identities. Private state-transition helpers now own quote/escape/depth scanning; focused helpers own balanced-object termination, recursive object projection, non-string dispatch, raw-text normalization, structured parsing, and invalid-result construction. Outer cleanup still starts with `String(text || "").trim()` and the exact anchored case-insensitive optional-`json` regex. Its legacy permissiveness is now explicit: a `javascript` fence strips the backticks but retains `javascript` as content, `jsonish` consumes the `json` prefix, and four-backtick wrappers retain one backtick; partial or trailing fences remain unchanged after trim. Object extraction still starts only at the first `{`, scans code units sequentially, ignores braces inside quoted strings, honors odd/even backslash escape state, returns the first depth-zero object without trailing prose, and falls back to the complete trimmed source for absent or unterminated objects/strings. Escaped-newline normalization retains sparse array mapping and recursive `for...in` projection into fresh plain objects, including inherited enumerable string keys, getter execution, symbol/prototype loss, `__proto__` assignment semantics, date-to-empty projection, primitive/function identity, and circular failure. Plain mode remains parse/error-callback-lazy. Parsed mode keeps extraction, `JSON.parse`, and recursive normalization under one catch; failures call the supplied callback once with the exact label, original text, and thrown value before the fixed error/raw-response projection, so callback and second raw-text coercion failures still propagate. Three permanent direct matrices cover the four-export API, compatibility identities, every fence family, prose/multiple/nested/unbalanced objects, quote/escape parity, UTF-8 and surrogate code units, dynamic coercion, sparse/prototype/symbol/getter/`__proto__` values, circular structures, parsed primitives/arrays/objects, destructuring order, plain-mode laziness, and all callback/coercion failures. The first pre-refactor run corrected only the fence expectations to the observed permissive regex behavior. The suite reaches 327 passing tests; syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the former extractor CC 11/cognitive 16/CRAP 132, normalizer CC 6/CRAP 42, and parser CC 5/CRAP 30 findings, reporting zero findings across the complete module without suppressions.
- Completed Phase 96 and decomposed the complete `server/modules/ai/application/aiHistoryCommands.js` boundary behind its unchanged `createAiHistoryCommands`, `patchDraftAiChanges`, and `preserveExistingIds` exports. Private predicates now preserve the original array-first and truthy-object operand evaluation; focused array/object projectors own recursive stable-ID retention; separate validators, submitted-row projection, map construction, stored-resource projection, and final assembly helpers own draft editing. Arrays still enter recursive mode only when both operands are arrays, align by index through native `map`, preserve sparse holes, drop named properties, and retain after-only row identity. Mismatched shapes return the exact `after` value. Truthy non-array object pairs still spread `after` first into a plain object, preserve any own `before.id` including non-enumerable values, then recursively visit only `Object.keys(next)` in insertion order. Inherited `before[key]` values remain eligible, an accessor `before.id` is still read twice after all spread getters, symbols are copied but not recursively projected, original prototypes and non-enumerable after-properties are lost, own `__proto__` data remains safe, aligned cycles fail recursively, and an after-only cycle remains attached by identity. Draft validation retains state-before-array order and both exact status-400 messages. Submitted rows remain truthy objects including arrays; IDs use `String(id || "")`, nullish after-values become null, duplicate map keys keep their last values, and existing IDs match strictly against the resulting string keys. Matched stored resources are freshly spread and stable-ID projected; unmatched rows retain identity. The first and second `entry.changes` reads, existing metadata spread, resource override, and summary-last callback order remain unchanged. Factory commands still expose `patchDraft`, `apply`, and `undo` in order, load before every action, throw the exact 404, update by `entry.id`, pass an own `resourceIds` option even when undefined, and return/propagate collaborator results and failures unchanged. Four permanent matrices lock the three-export API, scalar/mismatched identity, sparse/nested arrays, dates, prototypes, symbols, getters, non-enumerable IDs, inherited values, `__proto__`, aligned/unaligned cycles, validation order, raw ID coercion and duplicates, array rows, strict existing-key matching, null fallback, metadata/summary sequencing, repository arguments, restore sides/options, missing entries, and destructuring failures. Both pre- and post-refactor runs pass unchanged. The suite reaches 331 passing tests; syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the former `preserveExistingIds` CC 11/cognitive 8/CRAP 132 finding and reports zero findings across the complete module without suppressions.
- Completed Phase 97 and decomposed the complete `server/modules/bestiary/application/bestiaryCommands.js` boundary behind its unchanged six exports and unchanged router compatibility identity. A frozen private array-mode catalog plus focused classifier, delete, array-delegation, `setProp`, per-property, and group orchestration policies now own legendary modifiers. Exact `Object.entries` and `toArray` order, string-only remove, malformed skips, all six shared array modes, mode getter counts, property-before-value reads, JSON-cloned set values, and sequential mutation remain stable. Copy resolution now delegates cycle checks, direct cloning, copy-source normalization, base lookup, missing-base error creation, metadata classification, child overlay, modifier application, and final cleanup to private policies. It still checks normalized cycles before direct return, retains exact cycle chains and missing-base copy names, recursively resolves the base before overlay, clones every accepted child value, applies `_copy._mod` afterward, and deletes `_copy`/`_mod`; direct groups retain truthy raw source text. Replacement construction now separates name/image fallback policies without changing JSON clone, forced source, omitted-image inheritance, or route `__test` identity. Custom replacement mode still resolves the target and old name/ID before its three `payload.monster` reads, accepts truthy objects including arrays with `toJSON`, trims/requires names, rejects case-insensitive duplicates outside the current index, normalizes HP before the first write, and processes renamed favorites only afterward. Renames still always read/write the favorite list, update only normalized old-name CUSTOM entries, preserve unmatched identities, and select persisted output by normalized new name; missing persisted output still returns the pre-HP-normalized candidate. Image mode keeps one falsy monster read, one exact-null or two non-null image reads, nullable coercion, previous spread, one write, truthy previous-ID lookup before name, and local fallback. Repository effects remain sequential and uncompensated. Focused source policies retain normalized-before-all detection, direct-source before aggregate fallback, exact 404 behavior, spread/source getter order, and all/custom routing. Focused search policies deliberately calculate both name and type matches for every row before conjunction, then use the shared mutating name sorter. Three permanent direct matrices add the exact six-export API, modifier output/order/getter counts/cloning, cycles and missing bases, replacement branch reads, renamed/unchanged favorites, persisted versus pre-normalized fallback, array `toJSON`, validation errors, duplicate rejection, image null/coercion, ID/name laziness, and repository sequencing to the existing command integration coverage. Both pre- and post-refactor suites pass unchanged at 334 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original `updateCustom` CC 11/cognitive 13/CRAP 132 and `applyLegendaryGroupMods` CC 10/cognitive 16/CRAP 110 findings plus the remaining `resolveLegendaryGroup`, `search`, `buildReplacementCustomMonster`, and `getSource` findings, reporting zero findings across the complete module without suppressions.
- Completed Phase 98 and decomposed `server/modules/session/application/createSceneEncounter.js` behind its unchanged two-export API. Focused private policies now own array-only session collection reads, required-scene validation, nullable-link lookup, existing-result projection, name normalization, encounter construction, duplicate-scene linking, and next-session assembly. `findById` still uses `(items || []).find`, compares `String(item.id)` before `String(id)` for every visited row, returns the first exact item identity, recovers falsy collections, and preserves native truthy malformed-collection, null/sparse-row, Symbol, BigInt, and custom-coercion behavior. Request destructuring still reads campaign, filename, scene ID, and name before `repository.read`; scenes and encounters retain their repeated optional-chain/property reads. A missing scene throws the exact status-404 error before encounter access, ID creation, or persistence. Link eligibility still distinguishes only exact null and undefined, with the original two guard reads and third lookup read. Existing links return `{ created: false }`, the exact encounter identity, and a fresh session spread carrying the requested filename; they do not create IDs, coerce the already-read name, or write. Creation still calls the repository's bound `createId` method before `String(name || "").trim() || "Encounter"`, accepts generated IDs including zero, creates a fresh empty monsters array, appends encounters without source mutation, and updates every string-compatible duplicate scene ID while preserving unmatched identity and sparse scene holes. Outer session and nested data spreads remain fresh, encounter spread holes remain explicit undefined values, the repository write receives exactly campaign, requested filename, and next session once, and the result retains the saved-session identity. Three direct matrices lock the two-export surface, lookup/coercion timing, malformed and dynamic collections, request and nested getter order, missing/existing/null/undefined/stale links, duplicate and sparse scenes, default/whitespace/Symbol/zero names, generated zero IDs, immutability, bound repository calls, failure timing, and saved-result identity. Both pre- and post-refactor suites pass unchanged at 337 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original CC 11/cognitive 8/CRAP 132 finding and reports zero findings across the complete module without suppressions.
- Completed Phase 99 and decomposed AI-history filesystem reads in `server/storage.js` behind the unchanged `readAiResponses` export and `fileAiHistoryRepository` adapter. Focused private policies now own legacy-path projection, primary-path eligibility, selected-path resolution, saved-list extraction, timestamp comparison, normalized-list construction, and guarded reading. Falsy campaign values still coerce through an empty string, trim, and return before path or filesystem work; other values retain `path.basename` normalization. Regular campaigns continue using their campaign-local history file. Bestiary still prefers the dedicated root file and falls back to its legacy campaign-local file only when the dedicated file is absent and the legacy file exists. The exact `exists` sequence remains stable: regular and selected paths are checked before reading, a present dedicated Bestiary file avoids legacy access, a legacy selection checks dedicated then legacy then legacy again, and two missing Bestiary files check dedicated then legacy then dedicated again. The adjacent storage-stats operation now reuses the same private selector, removing duplicated fallback policy while retaining its original access order and tolerant size lookup. Reads still use `readJson`, including recursive `updatedAt` stripping; direct arrays remain valid, non-arrays use truthy `saved?.responses || []`, and malformed truthy envelopes still fail at native `.map`. Every row is normalized before Boolean filtering, survivors sort stably in place by descending `String(createdAt).localeCompare`, and any guarded file read, JSON parse, envelope map, row normalization, timestamp coercion, or comparator failure returns `[]` without writes, deletion, or migration. Writes continue targeting only the current `aiResponsesPath`. Two dependency-controlled filesystem matrices lock empty and basename-normalized slugs, regular/dedicated/legacy/missing path order, direct and enveloped arrays, malformed envelopes, invalid and blank rows, UTF-8 content, descending and stable equal timestamps, read/JSON failures, and comparison-coercion recovery. Both pre- and post-refactor suites pass unchanged at 339 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original `readAiResponses` CC 11/cognitive 8/CRAP 132 finding plus the adjacent duplicated stats finding and reports zero findings across the complete replacement span without suppressions.
- Completed Phase 100 and decomposed partial-import entity slug selection in `server/storage.js` without changing the storage export surface or archive command flow. Focused private policies now own nullish-safe dynamic property reads, raw slug coercion, path-basename acceptance, lazy fallback selection, and final slug orchestration. The raw path still reads `slug` once, applies `String(value || "").trim()`, and avoids `path.basename` for an empty result. A truthy raw slug still uses the platform basename and accepts every nonempty result except exact `.` and `..`. Fallback remains lazy `firstName → name → type`, with the selected value delegated unchanged to `campaignSlug`; nullish entities recover while primitives, arrays, objects, Proxies, Symbols, BigInts, custom coercion, whitespace, Windows/POSIX separators, and UTF-8 retain native behavior. Slug selection still happens before image-reference projection and `writeEntity`, so coercion failures create no entity directory. The audit corrected the earlier roadmap assumption about duplicate allocation: partial import never calls `ensureUniqueEntitySlug`; rows persist sequentially to the chosen slug, later duplicates overwrite earlier payloads, every completed row increments the raw import counter, and no `-2` directory is created. Two real-filesystem matrices lock nested basenames, both separator styles, dot fallbacks, first-name/name/type laziness, scalar/null/array records, Symbol and BigInt slugs, exact getter/coercion order, duplicate overwrite behavior, UTF-8 slugs, and failure before persistence. Both pre- and post-refactor suites pass unchanged at 341 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original `importedEntitySlug` CC 11/cognitive 6/CRAP 132 finding and reports zero findings across the complete replacement span without suppressions.
- Completed Phase 101 and decomposed `server/modules/ai/application/generateAiRequest.js` behind its unchanged single export. Focused private policies now own preparation-envelope creation, prepared-error projection, workflow-input assembly, workflow selection/execution, failure status/message projection, saved-failure response construction, and guarded history recovery. Factory destructuring and the undefined-only `console.error` callback default remain unchanged. `historyWriter.getUserInstructions(payload)` still runs before the outer `try`; its failures remain completely outside API-key, preparation, and failed-history recovery. Inside the guard, `isApiKeyConfigured()` is evaluated while constructing the exact payload/API-key/read-settings envelope before the request is awaited. Prepared errors retain one truthiness read followed by separate status and message reads, exact response shape, and workflow/history laziness. Custom-monster type still outranks the Bestiary-image flag, which outranks campaign generation; all branches receive one fresh object carrying exact source identities. The workflow executor intentionally remains synchronous and the coordinator returns it without `await`, preserving the subtle boundary where a synchronous workflow throw is recovered but a rejected workflow Promise bypasses the catch. Recovery still reads `error.status || 500` before the bound `historyWriter.saveFailed` call, awaits persistence, and only a truthy entry triggers a second status read plus `error.message || "AI request failed."`. Falsy results rethrow the exact original value. Save failures and status/message/projection failures remain caught by the inner recovery guard, report the exact fixed label and history error, then rethrow the original unless the callback itself throws. Primitive strings, null failures, dynamic getters, and default console logging remain stable. Four permanent direct matrices add the exact one-export API, missing-factory failure, preparation/getter order, repeated prepared-error reads, all three workflow branches, shared input identity, synchronous versus rejected-Promise failures, dynamic repeated status, message fallback, truthy/falsy history results, save failure, callback replacement, throwing status/message getters, primitive/null errors, and default logger behavior to the existing integration test. Both pre- and post-refactor suites pass unchanged at 345 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original CC 10/cognitive 11/CRAP 110 finding and reports zero findings across the complete module without suppressions.
- Completed Phase 102 and extracted spell-class lookup from the executable database bundler into the side-effect-free `scripts/build-database-bundles-policies.mjs` boundary behind exactly the `getSpellClassInfo` export. The CLI now imports that policy while retaining paths, data reads, bundle projection, deletion flags, output, and process lifecycle. Focused private policies own pre-pipe spell-name projection, repeated source projection, insertion-ordered source-key selection, truthy selected-map access, exact spell-info lookup, class/variant combination, conditional class insertion, collection, and raw comparator execution. Direct non-optional `spell.name` access, falsy-empty `String`, first-pipe splitting, and name-before-source-root failure order remain stable. `Object.keys` still determines candidate order; every candidate uppercases its key before rereading and coercing `spell.source`, and the first case-insensitive match wins. A matched empty-string key still becomes an absent source map because the post-search guard is truthy. Source-map values remain lazy until selection, spell-name lookup stays case-sensitive, and falsy info returns a fresh empty list before class getters. `info.class || []` is fully spread before `info.classVariant || []` is read/spread, so non-iterables and iterator failures retain their exact timing. Combined entries remain ordered; nullable/scalar rows are tolerated through optional name access, truthy names are read twice, and `Set` preserves exact SameValueZero identity before a fresh array uses raw `a.localeCompare(b)`. Case variants remain distinct; a single Symbol/object name survives without comparison, while multiple non-string names preserve native comparator failures. Two direct matrices lock the one-export API, null root/spell failures, pipe names, source key order/casing/getter counts/laziness, empty and duplicate keys, exact-name lookup, Symbol coercion, class-before-variant iteration, null/falsy rows, repeated name reads, exact deduplication, locale sorting, and getter/iterator/comparator failures. The copied pre-integration policy and the wired/decomposed implementation both pass at 347 tests. A real `--dry-run` successfully reads the current database and reports 4,528 monsters plus 936 spells without writes. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original CC 10/cognitive 8/CRAP 110 finding and reports zero findings in the replacement policy module; unrelated bundler functions remain separately tracked.
- Completed Phase 103 and decomposed the private markdown block-line transformer in `src/features/editor/ui/editorPresentation.ts` behind the unchanged exported `applyInputBlockEdit` aggregate and existing `Input.tsx` composition. Focused private helpers now own marker addition, exact marker removal/toggling, list-mode selection, heading projection, and the small list-first/quote-second/heading-fallback dispatcher. The dispatcher still reads `edit.kind` once for list and again only after that check fails for quote; `edit.add` and `edit.level` remain lazy branch-local reads. List add and remove preserve exact `"- "` behavior, quote preserves exact `"> "` toggling, and marker prefixing retains template-string line coercion. Heading markers still use raw `"#".repeat(level) + " "` coercion and native failures before matching only `/^#{1,6} /`; missing headings prepend, identical headings remove, and different recognized headings replace with unchanged shift arithmetic. The exported aggregate remains responsible for `lastIndexOf`/`indexOf` line bounds, native `substring` clamping and reversed-range swapping, newline suffix retention, sequential line transforms, first-line start shift, total end shift, and unguarded malformed-value failures. Two pre-refactor aggregate matrices lock list/quote/heading add-remove-replace cases, exact whitespace, seven hashes, zero/fractional/string/NaN/negative/infinite/Symbol levels, dynamic getter order and laziness, empty and middle multiline selections, newline endpoints, mixed shifts, reversed and out-of-range selections, synthetic line method order, and failure identity. Both the copied policy and decomposed implementation pass unchanged at 349 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original `transformInputLine` CC 9/cognitive 10/CRAP 90 finding and reports no findings for any replacement helper without exports or suppressions.
- Completed Phase 104 and decomposed private preview-to-raw cursor resolution in `src/features/editor/ui/editorPresentation.ts` behind the unchanged exported `resolveInitialCursorPosition` API and existing `Input.tsx` focus/selection effect. Focused private helpers now own the number-plus-real-array gate, first-entry projection, mapped interior projection, and non-empty offset routing; `resolvePreviewCursorPosition` retains only ordered destructuring, validation, the empty-map branch, and delegation. Top-level precedence remains nullish selection, numeric selection, numeric `index`, then preview mapping. Numeric index classification and projection still perform two getter reads. The undefined-only raw-value default remains unchanged, and the preview branch still evaluates `rawValue.length` before invoking the resolver, so a raw-length failure prevents every preview getter. Inside the resolver, `previewOffset` is read before `previewToRaw`; both are read before validation. Invalid shapes return the captured raw length, an empty array returns zero, nonpositive offsets use `Math.max(0, map[0] ?? 0)` without upper clamping, offsets at or beyond the current array length return raw length, and interior entries use the existing clamp with nullish-only fallback. Proxy arrays retain the first empty-check length read and second upper-bound length read; NaN and fractional offsets retain named-property lookup; strings, booleans, sparse/nullish entries, infinities, and native Symbol/coercion failures remain stable. Two pre-refactor aggregate matrices lock top-level numeric/nullish/index precedence, NaN and infinities, undefined-only defaults, repeated getter and raw-length order, all mapping branches, empty/sparse maps, first versus interior coercion, custom fractional/NaN properties, destructuring failures, Symbol failures, and changing Proxy length. A typecheck pass caught that the two-argument runtime gate narrows only the array in TypeScript; the final implementation uses a local post-guard offset assertion without runtime work. Both pre- and post-refactor suites pass unchanged at 351 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original `resolvePreviewCursorPosition` CC 8/cognitive 7/CRAP 72 finding and reports no findings for any replacement helper without exports or suppressions.
- Completed Phase 105 and decomposed exported mention-bracket toggling in `src/features/editor/ui/editorPresentation.ts` behind the unchanged three-argument `toggleInputMention` API and existing `Input.tsx` shortcut composition. Focused private helpers now own selected leading/trailing marker detection, selected-wrapper eligibility, selected-marker removal, surrounding-bound eligibility, exact surrounding-marker detection, surrounding-wrapper eligibility, surrounding-marker removal, and fallback marker insertion. The export still extracts selection first through `value.substring(selectionStart, selectionEnd)`, tests selected wrapping before surrounding wrapping, and otherwise adds brackets. Selected detection retains short-circuit order `startsWith("[")`, `endsWith("]")`, then `length >= 2`; selected removal retains prefix substring, a second selection-length read for its inner substring, suffix substring, unchanged start, and raw end minus two. Surrounding detection retains `start > 0`, then `end < value.length`, then left marker, then right marker; removal retains prefix substring, direct selection concatenation, suffix substring, and both raw positions minus one. Fallback retains prefix, exact `"["`, selection, exact `"]"`, suffix, and both raw positions plus one. Native substring clamping and reversed-range swapping remain independent from unnormalized cursor arithmetic. Two pre-refactor aggregate matrices lock add/remove paths, selected-over-surrounding precedence, nested and empty wrappers, single brackets, empty selection, whitespace, multiline text, reversed and out-of-range indices, exact method/getter/primitive-conversion order for all three paths, short-circuit laziness, initial substring failure identity, null values, and Symbol indices. Both pre- and post-refactor suites pass unchanged at 353 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original `toggleInputMention` CC 8/cognitive 4/CRAP 72 finding and reports no findings for any replacement helper without exports or suppressions.
- Completed Phase 106 and decomposed exported input shortcut dispatch in `src/features/editor/ui/editorPresentation.ts` behind the unchanged object-argument `getInputShortcutAction` API and existing `Input.tsx` keyboard composition. Focused private helpers now own Ctrl/Meta eligibility, exact heading projection, mapped-action-before-heading resolution, and normalized-key routing. Public parameter destructuring remains `ctrlKey`, `key`, `metaKey`, then `type`, so all four getters still run before body logic. Exact non-`textarea` type still returns null without invoking the already-read key. Textareas still call the receiver-bound `key.toLowerCase()` exactly once. Exact normalized `"tab"` returns a fresh tab action before modifier eligibility; otherwise the current truthy Ctrl-or-Meta rule gates every mapped/heading lookup. The frozen lookup table is unchanged and continues returning stable mutable action identities for Latin/Ukrainian mention, bold, italic, list add/remove, and quote aliases. Ordinary property access intentionally retains inherited `constructor` and `__proto__` values; the pre-refactor matrix corrected its initial assumption for a literal `"toString"`, which normalizes to `"tostring"`, while a custom lowercase result of exact `"toString"` still reaches the inherited function. Only a falsy table result reaches exact `/^[1-6]$/`, followed by `Number(normalizedKey)` and a fresh heading action. Two pre-refactor matrices lock every alias and uppercase variant, tab without modifiers, Ctrl/Meta truthiness, heading limits, unknown keys, mapped identity and mutability, fresh tab/heading identities, inherited lookups, full getter/method order and receiver, non-textarea laziness, three-pass custom normalized-key coercion across property lookup/regex/number conversion, disabled-modifier coercion suppression, exact thrown failures, null options/keys, and Symbol regex failure. Both pre- and post-refactor suites pass unchanged at 355 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, and replacement-character checks pass. Fallow removes the original `getInputShortcutAction` CC 7/cognitive 6/CRAP 56 finding and reports no findings for any replacement helper without exports or suppressions; no high findings remain in the editor presentation policy.
- Completed Phase 107 and decomposed the private input-format wrapper detectors in `src/features/editor/ui/editorPresentation.ts` behind the unchanged exported `toggleInputFormat` API and existing `Input.tsx` keyboard/selection composition. Focused private helpers now own inside italic-conflict detection, selected-marker eligibility, outside italic-conflict detection, the surrounding-start bound, and strict surrounding-marker checks; the two wrapper orchestrators retain only their original sequencing. `toggleInputFormat` still extracts selection first, tests a selected wrapper before a surrounding wrapper, and otherwise inserts markers. Inside conflict is still fully computed first as exact marker equality, `startsWith("**")`, then conditional `!startsWith("***")`; the selected-marker predicate then evaluates `startsWith(marker)`, `endsWith(marker)`, and `selection.length >= marker.length * 2`, while the stored conflict is consulted last. Outside conflict likewise precedes all bounds through the exact `substring(start - 2, start)` and conditional `substring(start - 3, start)` calls; the final predicate then evaluates the start bound, strict left marker, strict right marker, and conflict last. Bold markers still skip conflict-only reads. Selected-marker built-ins may string-coerce an object-like runtime marker, whereas outside strict equality intentionally rejects the same marker even when its string form matches; this asymmetry and all repeated `marker.length` reads remain intact. Removal and insertion still use the original substring assembly, repeated lengths, and raw cursor arithmetic independently of native substring index clamping/swapping. Two pre-refactor aggregate matrices lock italic/bold/triple-marker behavior, inside/outside/add paths, inside-conflict-to-outside fallback, single/partial/empty markers, empty/whitespace/multiline selections, reversed and out-of-range indices, exact conflict/wrapper/effect order, strict-equality and string-coercion behavior, repeated and changing marker-length reads, exact thrown failures, null values, and Symbol failures. An initial expectation for object-like surrounding markers was corrected before refactoring after the matrix exposed the intentional strict-equality boundary. Both pre- and post-refactor suites pass unchanged at 357 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, diff, and replacement-character checks pass. Fallow removes both original CC 6/cognitive 2/CRAP 42 findings and reports no findings for `toggleInputFormat` or any replacement helper without exports or suppressions.
- Completed Phase 108 and decomposed editable shortcut classification in `src/features/editor/ui/editorPresentation.ts` behind the unchanged exported `getEditableShortcutAction` object API, unchanged `shouldDelegateEditableHistory` helper, and existing `EditableField.tsx` event composition. Focused private helpers now own history/disabled delegation, type routing, direct textarea keys, mapped/heading routing, and heading projection; the existing single-line and textarea dispatchers remain private. Public destructuring still reads `code`, `ctrlKey`, `enableHistory`, `isDisabled`, `key`, `metaKey`, then `type` before body logic, and `modified` remains the raw `ctrlKey || metaKey` result. History delegation is still evaluated before disabled delegation, with both preceding type routing. Single-line and every non-exact-`textarea` value still blur on exact Enter before using modifier truthiness for delegation. Textareas still invoke receiver-bound `key.toLowerCase()` first, compare the returned value strictly with `"tab"`, test exact raw-key space second, and only then require a modifier. The unchanged frozen ordinary code table still maps `KeyB`, `KeyI`, `KeyK`, `BracketRight`, `BracketLeft`, and `KeyQ`; object/Symbol property keys, inherited truthy values such as `constructor`, and custom inherited falsy values retain ordinary lookup semantics. Only a falsy mapped value reaches raw-key `/^[1-6]$/`, followed by a second template-string coercion that may observe a different value. The standalone history helper remains unchanged, including exact code comparison, early `!enableHistory`, and non-boolean falsy returns despite its TypeScript annotation. Two pre-refactor aggregate matrices lock every mapping under Ctrl/Meta, tab/space/heading precedence, invalid headings, text/textarea/unknown types, Enter precedence, history and disabled ordering, raw modifier truthiness, all seven option getters, receiver-bound lowercase access/call order, normalized-result non-coercion, two-pass heading coercion, code-property coercion, Symbol keys, inherited truthy/falsy lookup, and exact getter/lowercase/null/Symbol failures. Both pre- and post-refactor suites pass unchanged at 359 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, diff, and replacement-character checks pass. Fallow removes the original `getTextareaEditableAction` CC 6/cognitive 5/CRAP 42 and `getEditableShortcutAction` CC 5/cognitive 4/CRAP 30 findings and reports no findings for either export/private dispatcher or any replacement helper without suppressions; `editorPresentation.ts` now has zero Fallow findings.
- Completed Phase 109 and extracted Node-safe input shortcut execution planning into `src/features/editor/ui/editorPresentation.ts` behind the new focused `getInputShortcutExecutionPlan` API while preserving the existing `getInputShortcutAction` classifier and `Input` props contract. The planner returns either a fresh `{ kind: "edit", edit }` result or a fresh `{ kind: "mention-picker" }` request. Private edit-plan, mention, non-mention, and non-tab helpers preserve the original repeated `action.kind` reads and exact `tab -> mention -> format -> block` precedence rather than caching the discriminator. Tab still delegates to `insertInputTab`; mentions use strict `selectionEnd === selectionStart`, avoid every value operation when collapsed, and otherwise delegate to `toggleInputMention`; format reads its marker only inside that branch and delegates to `toggleInputFormat`; fallback passes the original action object to `applyInputBlockEdit`, retaining its later repeated kind/add/level reads and inherited-property behavior. The planner was staged and passed at 361 tests while the original handler remained intact before `Input.tsx` switched to it. `handleKeyDown` now classifies, delegates a null action unchanged, prevents a handled action once, projects target state through the private `getInputSelectionState`, obtains one execution plan, launches the existing picker with `void`, or applies exactly one edit with the original event. Target projection remains `value`, `selectionStart ?? 0`, then `selectionEnd ?? selectionStart`; moving those nullish branches into the private helper removed a residual moderate finding without changing getter order. Obsolete direct imports of the four edit policies were removed from `Input.tsx`. Two planner matrices cover every action, add/remove variants, multiline blocks, fresh plan identity, strict number/string collapsed-selection behavior, inherited and malformed actions, repeated/dynamic kind reads, branch-local marker/add reads, collapsed no-value access, and exact failures. A source-level controller matrix locks classifier/delegation/prevention/target/planner/picker/edit order, exactly one prevention, selection-helper field order, absence of direct action branching, and removal of obsolete policy imports. The post-refactor suite passes at 362 tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, diff, and replacement-character checks pass. Fallow removes the original `handleKeyDown` CC 9/cognitive 9/CRAP 90 plus intermediate CC 6/CRAP 42 findings and reports no findings for the handler, selection helper, planner, or any replacement helper without suppressions.
- Completed Phase 110 and decomposed raw-input mention insertion in `src/features/editor/ui/Input.tsx` behind Node-safe `getInputMentionInsertion` and `getInputMentionCursorPosition` policies in `editorPresentation.ts`, while preserving the existing async picker and controller effects. The insertion policy reads `result.status` first and returns null only for exact `"cancelled"` without touching name or value. Every other status retains the original truthy first `result.name` read, second name read for interpolation, exact `"[]"`/`"[name]"` output, then prefix substring, mention concatenation, and suffix substring. It returns the exact next value plus mention text but deliberately does not calculate the cursor. The cursor policy remains a separate later call implementing raw `cursorStart + (result.status === "selected" ? mention.length : 1)`, so selected status reads mention length before start coercion, other statuses skip mention length, and string/Symbol/coercion behavior remains unchanged. The policies were staged and passed at 364 tests while the original async handler remained intact. The handler now snapshots `currentTarget` through `getInputSelectionState` before `requestMentionSelection(dispatch)` and its await, delegates result projection, returns on a null insertion, emits the original change event, and only then calls the cursor policy. It still schedules with delay zero, reads the ref inside the callback, skips a missing node, focuses, then applies the same cursor to both selection positions. Two policy matrices lock cancellation, empty/whitespace/numeric names, unexpected statuses, negative/oversized/reversed/string cursors, native substring clamping, status/name/value getter order, repeated truthy name reads, template coercion, selected versus fallback cursor order, and exact null/Symbol/failure behavior. The extended controller-source contract locks snapshot-before-await, insertion projection, cancellation, callback-before-cursor, timer/ref/focus/selection order, and absence of direct result branching. The post-refactor suite remains at 364 passing tests. Syntax, lint, UTF-8 validation across 214 files, typecheck, architecture boundaries/cycles/dead code, diff, and replacement-character checks pass. Fallow removes the original `insertMentionWithoutSelection` CC 7/cognitive 5/CRAP 56 finding with no findings for the handler or either policy and no suppressions.
- Completed Phase 111 and decomposed bracket-aware paste handling in `src/features/editor/ui/Input.tsx` behind the Node-safe `getInputBracketPasteEdit` policy in `editorPresentation.ts`, while preserving the unchanged `Input` props contract and controller-owned browser effects. The policy performs the original receiver-bound `.replace(/\r\n/g, "\n")` before value reads, preserves lone carriage returns, assembles native `substring` prefix plus normalized clipboard text plus suffix, and independently computes both cursor positions from raw `selectionStart + normalizedText.length`. It was staged and passed at 366 tests while the original handler remained intact. `handlePaste` now delegates non-textareas before any target or clipboard access, reuses the private `value -> selectionStart ?? 0 -> selectionEnd ?? selectionStart` projector for textareas, evaluates `isRangeInsideSquareBrackets` exactly once, and delegates unchanged when false through a focused helper whose property call retains the props receiver. A handled paste still prevents default before reading `"text/plain"`, passes the clipboard value to the pure policy, and applies one edit with the original event; change dispatch, zero-delay focus, and selection effects remain in the controller. Two policy matrices cover selected replacement, every CRLF pair, lone carriage returns, empty text, clamped/oversized/reversed/string cursors, raw addition, getter/method receiver and coercion order, and exact replace/value/Symbol failures. The expanded controller-source contract locks delegation, target projection, range check, prevention, clipboard, policy, and edit order; exactly two receiver-preserving delegation sites; one range check; and the absence of inline replace/substring transforms. The post-refactor suite passes at 366 tests. Lint, UTF-8 validation across 214 files, typecheck, and architecture boundaries/cycles/dead code pass. Fallow removes the original `handlePaste` CC 7/cognitive 4/CRAP 56 finding and reports no findings for the handler, delegate, target projector, or paste policy without suppressions.
- Completed Phase 112 and decomposed the remaining initial-selection layout-effect decisions in `src/features/editor/ui/Input.tsx` behind Node-safe `getInitialInputSelectionPosition` in `editorPresentation.ts`. Its private eligibility helper preserves the exact nullish-selection check before the existing `supportsSelectionRange(type)` policy, while the export first short-circuits any truthy already-applied value and delegates every eligible numeric/index/preview case unchanged to `resolveInitialCursorPosition`. Applied, nullish, and unsupported paths do not read selection internals or raw-value length. The policy was staged and passed at 368 tests while the original layout effect remained intact. The effect now reads `internalRef.current` and returns on a missing node before reading the applied ref, obtains one nullable position, and returns only for exact `null`; an eligible position still focuses with `{ preventScroll: true }`, passes the same position twice to `setSelectionRange`, and marks the ref applied only after both DOM calls succeed. Its dependency order remains `initialSelection`, `rawValue`, then `type`, and direct cursor/type policy imports were removed from the controller. The eligibility matrix covers all seven supported types, unsupported/nullish/Symbol types, nullish selection, truthy object applied state, numeric clamping, index mapping, and preview mapping. A second matrix locks lazy selection/raw reads, repeated index getter order, and exact index/raw/null failures. The expanded controller-source contract locks node-before-policy, applied/selection/type/raw argument order, null return, focus/range/applied sequencing, dependencies, and the absence of duplicated cursor/type decisions. The post-refactor suite passes at 368 tests. Lint, UTF-8 validation across 214 files, typecheck, and architecture boundaries/cycles/dead code pass. Fallow removes the anonymous CC 5/cognitive 4/CRAP 30 finding and reports no findings for the effect, eligibility helper, or position policy without suppressions. Only the aggregate `Input` CC 8/CRAP 72 finding remains in the file.
- Completed Phase 113 and split `src/features/editor/ui/Input.tsx` into a thin behavior controller over private `InputView.tsx` with shared private contracts in `inputTypes.ts`. The public component still re-exports the exact `InputProps` and `InputValueChangeEvent` types consumed by the unchanged typed facade, while `InputElement`, the bivariant callbacks, and the rest-native-prop shape now have one owner. Neither private module enters the runtime or typed barrel, and both were added to the existing external deep-import restrictions. Node-safe `getInputRawValue` now owns the original repeated `source.value` reads, array test plus receiver-bound `join(",")`, and fallback `String(source.value ?? "")`; `getInputClassPresentation` owns strict textarea base-class selection plus lazy string-only case-sensitive `"["` detection with its repeated value access. Both policies were staged and passed at 370 tests before JSX moved. `Input.tsx` retains dispatch, refs, resize/initial-selection effects, edit/picker timers, keyboard and paste handlers, original props identity, and one `getInputRawValue(props)` call; it now renders only `InputView` with ordered controller inputs. The private view computes classes in base/caller/mention order, then delegates native rendering and optional Tooltip wrapping to focused private helpers. Textarea `rows={1}` still precedes spread, with explicit ref/class/keyboard/paste overrides afterward; input spread still precedes explicit ref/class/type/keyboard/paste; textarea casting, Tooltip content/class, and falsy-title passthrough remain unchanged. Policy matrices lock scalar/array/nullish/NaN values, exact classes, fresh presentation identity, dynamic two-read `join`/`String`/`includes` receiver behavior, one-read non-string laziness, and native failures. The expanded source contract locks public re-exports, private-barrel exclusion, controller/view delegation order, both native spread/override orders, and Tooltip gating. The post-refactor suite passes at 370 tests. Strict TypeScript, lint, UTF-8 validation across 214 files, and architecture boundaries/cycles/dead code pass. Fallow removes the remaining aggregate `Input` CC 8/cognitive 6/CRAP 72 finding and reports zero findings across the controller, private view, native/tooltip helpers, and both presentation policies without suppressions.
- Completed Phase 114 and moved editable change-event construction from `src/features/editor/ui/EditableField.tsx` into Node-safe `createEditableFieldChangeEvent` in `editorPresentation.ts`. The unchanged `EditableFieldChangeEvent` contract now has the policy module as its typed owner; `EditableField.tsx` imports it for internal props and re-exports it so `features/editor/ui/index.d.ts` and existing consumers remain unchanged. Focused source and property selectors replace the three compound ternaries while retaining the original semantics: falsy and truthy non-object source events become fresh empty sources, arrays and objects remain spreadable, target/currentTarget each perform a lazy truthiness read followed by a second `typeof === "object"` read and a third successful-value read, invalid target uses a fresh object, and invalid currentTarget reuses the selected target fallback. The final projection still spreads the outer source first, overrides currentTarget before target, spreads each selected nested value before the supplied value, and never mutates caller data. The policy was staged and passed at 372 tests while the local controller implementation remained intact. A behavior matrix covers every falsy/truthy primitive, functions, arrays, independent/fallback targets, symbol keys, fresh output identities, and immutability. A second matrix locks four outer target/currentTarget reads across selection plus source spread, current-before-target nested spread order, dynamic third-read strings, receiver-independent property access, first/third getter failures, and currentTarget spread failure before target access. `EditableField.tsx` now keeps event construction inside `onChange?.(...)`, preserving callback-nullish laziness, and the obsolete local interface/function were removed; a source contract locks the public re-export and sole call site. The post-refactor suite passes at 373 tests. Strict TypeScript, lint, UTF-8 validation across 214 files, and architecture boundaries/cycles/dead code pass. Fallow removes the original `createChangeEvent` CC 7/cognitive 6/CRAP 56 finding and reports no findings for the export or either replacement helper without suppressions.
- Completed Phase 115 and decomposed `LexicalEditableField` click routing in `src/features/editor/ui/EditableField.tsx` behind the Node-safe `getEditableClickPlan` policy in `editorPresentation.ts`. Focused `shouldActivateEditableMention`, `hasEditableLinkTarget`, and `getEditableMentionName` helpers preserve the original raw-truthiness and lookup semantics: target lookup resolves the mention first; a falsy mention reads no modifiers; truthy Ctrl skips Meta; an activated mention returns before link lookup; and the content path alone performs the exact receiver-bound `closest("a")` call. Mention-name projection remains a separate later call so dataset getter failures still occur only after the controller has prevented default and stopped propagation, with the exact `dataset.mention || ""` fallback. The policy was staged at 375 tests before the controller switch. `handleClick` now narrows the DOM target, obtains one typed plan, and only executes browser/modal/callback effects: mention prevention, propagation stop, dataset projection, modal opening, and early return; otherwise conditional link prevention, unconditional propagation stop, then optional `onClick` with the original event. Policy matrices cover modifier laziness, Ctrl/Meta ordering, receiver-bound closest calls, non-coercing truthiness, null/noncallable/native failures, ordered mention-before-link lookup, plan identity, dataset fallback, and getter failures. A source contract locks the effect order, exactly two prevention and propagation sites, one planner call, dependencies, and the absence of duplicated modifier/closest decisions. The post-refactor suite passes at 378 tests. Strict TypeScript, lint and UTF-8 validation across 214 files, architecture boundaries/cycles/dead code, syntax, diff hygiene, and replacement-character checks pass. Fallow reduces the intermediate handler from CC 7/cognitive 5/CRAP 56 through CC 5/cognitive 3/CRAP 30, then removes the finding completely after the name projection; it reports zero findings for the handler and all four policies without suppressions.
- Completed Phase 116 and decomposed `useCommandHandlers`' mention shortcut in `src/features/editor/ui/EditableField.tsx`. Private `getEditableSelectedMentionText` now owns the Lexical-specific selection gate and text projection: range recognition short-circuits before collapsed-state access, and only a non-collapsed range reads text, delegates to `normalizeEditableText`, and trims. Node-safe `shouldInsertEditableMentionResult` in `editorPresentation.ts` owns picker-result eligibility with exact status-before-name ordering, strict `"selected"` comparison, lazy name access, and non-coercing truthiness. The predicate was staged at 379 tests before the handler switch. `handleMentionShortcut` still prevents default before stopping propagation, reads `$getSelection()` once, inserts truthy selected text and returns before requesting the picker, otherwise awaits the original dispatch request, rejects an ineligible result, focuses the editor, and then updates it. The selected result object remains uncached so `result.name` is intentionally read again only inside the update callback. The predicate matrix covers cancelled/unexpected statuses, every falsy name, object-name truthiness without coercion, boxed status mismatch, getter order, exact getter failures, and null failure. A source contract locks helper short-circuit order, one selection/text read, controller effect and await order, two insertion sites, hook dependencies, and the absence of duplicated range/result decisions. The post-refactor suite passes at 380 tests. Strict TypeScript, lint and UTF-8 validation across 214 files, architecture boundaries/cycles/dead code, syntax, diff hygiene, and replacement-character checks pass. Fallow removes the original CC 6/cognitive 6/CRAP 42 finding and reports zero findings for the handler, private selection helper, and result predicate without suppressions.
- Completed Phase 117 and moved `useCommandHandlers` keydown classification from `src/features/editor/ui/EditableField.tsx` into Node-safe `getEditableKeyDownPlan` in `editorPresentation.ts`. Private modifier and non-history helpers keep the planner below health thresholds while preserving the original dynamic evaluation sequence: initial `ctrlKey || metaKey` with Ctrl short-circuiting Meta; eager classifier values from event `code`, Ctrl, key, and Meta; classification through the unchanged `getEditableShortcutAction`; then a second event-code read and `shouldDelegateEditableHistory`. The three fresh plan variants distinguish history delegation from ordinary delegation and execution. History still overrides any classified action; falsy or exact `"delegate"` actions produce ordinary delegation; every other runtime action, including inherited lookup values, retains its identity. The planner was staged at 382 tests before the controller switch. `handleKeyDown` now obtains one plan and owns only effects: history forwards the original event without stopping propagation, while both non-history paths stop propagation first; ordinary delegation then forwards and returns, and execution passes the original action/event/editor/mention handler to `executeEditableShortcut`. Getter matrices cover both modifier short-circuit paths, exact duplicate property reads, dynamic first/second code values, history override, classifier/action identity, fresh plan objects, and precise first-Ctrl/second-code/null failures. A source contract locks planner argument order, effect precedence, exactly two delegation sites, one propagation stop, execution-option order, hook dependencies, and absence of duplicated event/classifier/history decisions. One obsolete aggregate source assertion was updated from the retired direct history-helper import to the new planner owner. The post-refactor suite passes at 383 tests. Strict TypeScript, lint and UTF-8 validation across 214 files, architecture boundaries/cycles/dead code, syntax, diff hygiene, and replacement-character checks pass. Fallow removes the original CC 5/cognitive 4/CRAP 30 handler finding and reports zero findings for the handler, planner, and both private helpers without suppressions.
- Completed Phase 118 and decomposed formatted-copy orchestration in `src/features/editor/ui/EditableField.tsx`. Generic Node-safe `getEditableCopyRequest` in `editorPresentation.ts` now owns the exact raw-truthiness gate and returns either null or a fresh request retaining the original editor and normalized-markdown identities. The policy was staged at 384 tests before the controller switch; its matrix covers every falsy editor/value, truthy non-coercible objects and Symbols, identity retention, exact keys, and fresh request allocation. Private `replaceEditableCopyResetTimer` now owns the previous timer replacement sequence without caching the ref: truthy check, second current-value read for `clearTimeout`, assignment of one 2000ms timeout, then callback state reset before ref nulling. `handleCopy` still prevents default before stopping propagation, reads `editorRef.current` as the first request argument, and returns before Lexical/clipboard work on a null request. The success path preserves empty HTML initialization, editor-state read and `$generateHtmlFromNodes` assignment, one awaited clipboard write containing one `ClipboardItem`, HTML Blob before plain-text Blob with exact MIME types, success-only `setCopied(true)`, then timer replacement. The catch retains the exact log message and original error. A source contract locks request/effect/Blob/success/catch/dependency order, one write/item/timer delegation, two Blobs, four timer-ref reads, and absence of duplicated eligibility/timer operations. The post-refactor suite passes at 385 tests. Strict TypeScript, lint and UTF-8 validation across 214 files, architecture boundaries/cycles/dead code, syntax, diff hygiene, and replacement-character checks pass. Fallow removes the original CC 5/cognitive 4/CRAP 30 handler finding and reports zero findings for the handler, request policy, and timer helper without suppressions.

Next:

- Completed Phase 119. Node-safe `getLexicalEditableFieldViewPresentation` now projects strict textarea aria/rich-plugin visibility, raw history visibility with `undefined`/`"true"` data markers, and disabled tab index into a fresh result. Its matrix covers text/textarea/unknown modes, enabled/disabled states, raw truthy/falsy history values, shared textarea projection, exact keys, and fresh identity. Private same-file `LexicalEditableFieldView` owns only ContentEditable and plugin composition. The behavior controller retains editor access, command registration, focus/blur/click/input/key/paste/mouse callbacks, DOM/mention lookup, modal/hover effects, refs, and dependencies; mouse leave still allocates a fresh null hover state per call. The view preserves ContentEditable class, attribute, and handler order plus plugin order `EditorContentPlugin → conditional HistoryPlugin → MarkdownValuePlugin → MarkdownChangePlugin → EditableStatePlugin → conditional ListPlugin then MarkdownShortcutPlugin`. The full `npm test` gate exposed two aggregate source assertions that still described the pre-Phase-119 inline conditions; those contracts now follow the presentation owner without relaxing runtime behavior. The complete suite passes at 398 tests, closing the Phase 119 regression gate.
- Completed Phase 120 and split the outer `EditableField` behavior controller from private same-file `EditableFieldView`. Seven focused Node-safe policies now own raw markdown projection, disabled/read-only state, primitive-string title eligibility, mention/field tooltip resolution, lazy campaign-route fallback, copy-button presentation, and mention-hover reconciliation. Private `clearEditableCopyResetTimer` owns unmount cleanup without moving timer effects into the policy module. The controller retains store/context/hooks, refs, URL memoization, clipboard writes, modal opening, editor configuration, and callbacks; the view owns only root container, Button, Tooltip, Lexical composition, and trailing EntityModal presentation. The split preserves outer spread/class/stop/style order, exact raw copy visibility, translated-title laziness, copied icon selection, Tooltip order, EditorRefPlugin-before-editor order, every Lexical prop identity, `value || value === 0`/String timing, normalization timing, `disabled || readOnly`, original-title return, mention precedence, null anchor fallback, lazy `parseUrl().campaign`, cleanup reads, hover identity reuse, and hook dependencies. Five permanent matrices/source contracts raise the full suite to 403 passing tests. Fallow reports no finding for `EditableField.tsx`, and the architecture scan remains at zero reported issues, cycles, boundary violations, or unresolved imports. The performance gate, esbuild TS/TSX parse, diff hygiene, and UTF-8 checks pass; complete lint/typecheck remain tracked under `MD-R04` because the local install still lacks `@typescript-eslint/parser` and `tsc`.
- Completed Phase 121 and corrected the recovered contract drift before widening FSD enforcement. Backup/archive HTTP schemas now validate `images` only as an optional array container, leave individual image rows to the intentionally tolerant archive restoration/storage policy, and expose only the three route-facing validators. Campaign entity-move transport validates the source entity type before reading or validating the body, while the campaign command retains target-type and valid-pair checks with their exact errors. Rules-reference modal reads now require active-controller identity for success, non-abort failure side effects, controller cleanup, and loading finalization, so a stale request cannot overwrite or dismantle its replacement. Bestiary favorite membership now uses collision-free JSON tuple encoding instead of delimiter concatenation. The JavaScript/JSX reference restriction now covers both `entities/reference/api/*` and `entities/reference/model/*`, matching the current TypeScript/TSX ownership rule. Focused regressions cover tolerant archive rows versus a strict array container, private schema exports, source-before-body move validation and exact command errors, stale request ownership, NUL-bearing favorite identities, and the reference API boundary. The complete suite passes 404/404 tests.
- Completed Phase 122 and installed one language-symmetric FSD public-entry baseline for frontend code under `src`. Shared `FSD_PUBLIC_API_PATTERNS` and the synchronized `FSD_SLICE_NAMES` registry now drive both JavaScript/JSX and TypeScript/TSX restrictions. They reject root `./layer/slice/private`, Vite-root `/src/layer/slice/private`, and sibling or cross-layer private paths reached through one or more `../` levels, while allowing only the slice root `index`, `ui/index`, Node-safe `model.js`, and page `graph.js` facades. A non-`src` override prevents backend module names from being mistaken for frontend slices. All 10 audited private/deep imports now use explicit public APIs; campaign state utilities gained the minimal `features/campaign/index.js` plus `index.d.ts` facade instead of a broad barrel.
- Phase 122 also corrected entity ownership. Fallow now uses `autoDiscover` to model every `src/entities/*` slice as its own zone, the entity rule allows only `shared`, and audited sibling-entity edges are zero. `referencePreview.ts` and `referenceResolvers.ts` moved from `entities/reference/model` to `features/rules-reference/model`, with a minimal JSON-free `features/rules-reference/model.js` facade and matching declaration. The five reference cache/normalizer modules for conditions, diseases, senses, skills, and variant rules remain entity-owned and public through `entities/reference/index.js`; the Node-facing entity model facade does not mirror unused browser exports. The complete suite passes 405/405 tests.
- Completed Phase 123 and froze the remaining `src/features` and `src/widgets` same-layer dependencies as an exact importer-file → target-slice baseline. The local ESLint plugin in `scripts/eslint-import-boundaries.mjs` rejects both new edges and stale allowances, normalizes repository-relative and Windows filenames, resolves relative and Vite-root `/src/...` specifiers, and observes static imports, named/all re-exports, dynamic imports, `require` calls, Vite `import.meta.glob`/`globEager` static patterns (including arrays), TypeScript `import()` types, and external-module `require` references with static string/template literals. Its all-`src` public-entry rule independently rejects private paths even when the importer-file → target-slice edge itself is approved. Permanent `npm test` source-inventory contracts ignore module-like text in comments and literals, prove that the baseline matches the current tree, and exercise the rules while complete lint remains blocked under `MD-R04`.
- Phase 123 also removed five plain `Input` dependencies by adding the strict native `shared/ui/TextInput` primitive and migrating `AiApiKeyPanel`, `CreateCampaignModalContent`, `DiceCalculator`, `MonsterFieldEditModal`, and `PlayerQuestionsModalContent`. Feature debt fell from 22 directed slice pairs / 30 file edges / 34 declarations to 18 / 25 / 29; widget debt remains 13 / 13 / 13. The complete suite passes 409/409 tests. Phase 124 is next and must review and lower the next bounded dependency cluster without preselecting its extraction strategy.
- Completed Phase 124 and corrected modal ownership instead of preserving an app-aware feature abstraction. Generic `Modal` plus its Node-safe private `modalModel.ts` policies and private `useModalController.ts`/`ModalView.tsx` implementation now belong to `shared/ui` and are consumed through `shared/ui/index.js`. The synchronized `SHARED_MODAL_PUBLIC_API_PATTERN` protects those implementation paths for both JavaScript and TypeScript consumers; the stale `features/modal` restriction was removed. Store-backed global message rendering moved upward to `app/ui/MessageBoxHost.tsx`, composed only by `App.tsx`; lower layers do not import the app host. The obsolete imperative `Modal.createApi`/`createModalApi` surface and related unused status/API types were removed with the entire `features/modal` slice. Historical Phase 123 ownership statements above remain as the record of that checkpoint; this Phase 124 entry is the correction.
- Phase 124 also lowered the synchronized boundaries exactly: `modal` was removed from `FSD_SLICE_NAMES.features`, and all 11 importer-file → `modal` allowances were removed from `FSD_SAME_LAYER_FILE_EDGE_BASELINE`. The feature baseline fell from 20 importer files / 18 directed pairs / 25 file edges to 10 / 12 / 14, while feature import declarations fell from 29 to 18. Widgets remain at 13 directed pairs / 13 file edges / 13 declarations. Permanent source-inventory coverage locks the lowered catalog and baseline, and the complete suite passes 410/410 tests. Phase 125 is next and must review the first bounded widget-composition cluster; `MD-R05` remains open.
- Completed Phase 125, the first widget-composition reduction. `BestiaryBrowser` now receives the required `ResponseModal` and `MonsterEditorModal` component contracts from its sole page-level composition owner, `EncounterPage`, through the widgets' public entries instead of importing the two sibling widget slices itself. This changes only composition ownership: the browser retains its existing modal state and handlers, the injected components keep the same render placement/order and forwarded props, and nullable response/editing behavior remains unchanged.
- Phase 125 removed exactly the two corresponding importer-file → target-slice allowances. Widget debt fell from 8 importer files / 13 directed pairs / 13 file edges / 13 declarations to 7 / 11 / 11 / 11. Features remain at 10 importer files / 12 directed pairs / 14 file edges / 18 declarations. Permanent source-inventory coverage locks the reduced baseline, and the complete suite passes 411/411 tests. `MD-R05` remains open; Phase 126 must review the next bounded cluster without preselecting its strategy.
- Completed Phase 126 after auditing the next bounded widget cluster. The redundant sole-consumer `widgets/spell-card` slice was absorbed unchanged into private `widgets/spells-browser/ui/SpellCard.tsx`; its public runtime/type barrels, synchronized widget-catalog entry, and now-stale same-layer lint allowance were deleted. `SpellsBrowserContent` remains the sole consumer, and every rendering contract is preserved: localized model labels, rich-content options, inline dice/spell parsing, source formatting, query highlighting, classes, higher-level entries, props, and output composition. Historical entries above describe the ownership at their original checkpoints; this Phase 126 entry records the corrected current boundary.
- Phase 126 reduced the widget catalog from 12 to 11 slices and lowered widget debt from 7 importer files / 11 directed pairs / 11 file edges / 11 declarations to 6 / 10 / 10 / 10. Features remain unchanged at 10 importer files / 12 directed pairs / 14 file edges / 18 declarations. Permanent source-inventory coverage locks the deleted slice, catalog entry, and stale allowance, and the complete suite passes 412/412 tests. `MD-R05` remains open; Phase 127 must audit the next bounded cluster without preselecting its strategy.
- Completed Phase 127 by moving the remaining Bestiary content composition to its sole page owner. `EncounterPage` now supplies the stable public `AiAssistantPanel` and `MonsterStatBlock` component symbols through `BestiaryBrowser` to private `BestiaryContent`, so the browser content no longer imports either sibling widget. This changes only dependency direction: the closed overlay still returns `null` before mounting, the assistant remains unconditionally mounted in its existing position while the Bestiary is open, the stat block remains behind the existing detail-presentation guard, and component identity, prop forwarding, state, and callback ownership remain unchanged.
- Phase 127 removed exactly the two corresponding importer-file → target-slice allowances. Widget debt fell from 6 importer files / 10 directed pairs / 10 file edges / 10 declarations to 5 / 8 / 8 / 8. Features remain unchanged at 10 importer files / 12 directed pairs / 14 file edges / 18 declarations, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks that checkpoint, and the complete suite passes 413/413 tests.
- Completed Phase 128 by moving campaign-entity modal card composition to the app root. `App` now supplies the stable public `CharacterCard` and `LocationCard` component symbols to `CampaignEntityModalProvider`, which forwards the required slots through its private content composition to `CampaignEntityModalCard`; the modal widget no longer imports its sibling `campaign-entity-card` slice. This changes only dependency direction. Parent resolver delegation still runs first and short-circuits on handled content before the campaign/scope guard. Entity initialization, prop-driven reset, update, confirmed rename, delete, refresh, and close state remain with the modal content. The card still selects the location-versus-character branch through `getCampaignEntityModalCardPlan`, forwards the exact planned React key, retains the callback adapters, and preserves `collapsed: false`, `onToggleCollapse={null}`, `viewMode="modal"`, `showDeleteButton={false}`, and `showHeader={false}`.
- Phase 128 removed exactly the corresponding importer-file → target-slice allowance. Widget debt fell from 5 importer files / 8 directed pairs / 8 file edges / 8 declarations to 4 / 7 / 7 / 7. Features remain unchanged at 10 importer files / 12 directed pairs / 14 file edges / 18 declarations, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks the required app-owned slots and reduced baseline, and the expanded complete suite passes 414/414 tests.
- Completed Phase 129 by moving AI response-modal composition to the assistant's owners. `AiAssistantPanel` now requires an injected `ResponseModal` instead of importing its sibling `ai-response-modal` widget. `MainContent` supplies the stable public `AiResponseModal` symbol to route assistants, while the Bestiary path forwards its existing response component through `BestiaryBrowser` and private `BestiaryContent` into the injected assistant. This changes only dependency direction: `MainContent` retains the exact pathname/session/encounter route key and `showAiAssistant` mount guard; `AiAssistantPanel` retains its state, hooks, controllers, and history-dialog composition while forwarding the supplied component into the same `historyDialog.ResponseModal` slot; the Bestiary assistant remains unconditionally mounted in its existing position with `isBestiary` and its registration callback; and the existing closed-overlay and nullable response behaviors remain unchanged.
- Phase 129 removed exactly the `AiAssistantPanel.tsx` → `ai-response-modal` importer-file edge and its stale allowance. Widget debt fell from 4 importer files / 7 directed pairs / 7 file edges / 7 declarations to 3 / 6 / 6 / 6. Features remain unchanged at 10 importer files / 12 directed pairs / 14 file edges / 18 declarations, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks both owner injection paths and the reduced baseline, and the expanded complete suite passes 415/415 tests. `MD-R05` remains open; Phase 130 must audit the next coordinated bounded cluster.
- Completed Phase 130 by turning the AI response modal into a configured widget boundary. The public `createAiResponseModalComponent` factory accepts lower-layer structural `CharacterCard`, `LocationCard`, `MonsterStatBlock`, and `MonsterEditorModal` slots while keeping the feature-owned `AiResponseModalComponent` contract unchanged. `MainContent` and `EncounterPage` each create one stable module-scope configured response component; both direct Encounter/Bestiary response consumers reuse the same page-owned symbol. The raw modal renderer is private and no longer imports sibling widgets. This preserves the route assistant key and mount guard, the Encounter overlay guard and Bestiary forwarding, raw `useState` → draft-controller → generated-prompt null-guard order, card/stat-block branches and conditional callbacks, editor state, and `AiResponseModalView` before the editor.
- Phase 130 removed exactly the three `AiResponseModal.tsx` sibling-widget allowances. Widget debt fell from 3 importer files / 6 directed pairs / 6 file edges / 6 declarations to 2 / 3 / 3 / 3. Features remain unchanged at 10 importer files / 12 directed pairs / 14 file edges / 18 declarations, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks the factory, stable owner bindings, slot inventory, render order, and reduced baseline, and the expanded complete suite passes 416/416 tests. `MD-R05` remains open; Phase 131 must audit the remaining coordinated rules-reference/editor widget cluster.
- Completed Phase 131 by configuring the remaining rules-reference/editor widget chain at its app and page owners. Public `createRulesReferenceModalContentComponent` binds the stable `MonsterStatBlock` and `SpellsBrowser` symbols into owner-specific rules-reference content in `MainContent` and `EncounterPage`, while `App` passes those same symbols directly to the public `RulesReferenceModalHost`. The configured content then feeds owner-specific monster editors, and each owner reuses its configured editor in every direct, Bestiary, and AI-response path. Raw rules content/view and monster-editor renderers remain private and no longer import sibling widgets. Host request gating, content loading/navigation/history, empty details, selected-Bestiary-only stat rendering, spells-tab-only browser rendering, exact embedded-selection projection, fresh render options, rule-picker gating, selection cloning, and editor prop precedence remain unchanged.
- Phase 131 removed all three remaining widget importer-file allowances. Widget debt fell from 2 importer files / 3 directed pairs / 3 file edges / 3 declarations to zero across all four measures. Features remain unchanged at 10 importer files / 12 directed pairs / 14 file edges / 18 declarations, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks the factories, stable owner bindings, render gates, adapter behavior, and empty widget baseline, and the expanded complete suite passes 417/417 tests. `MD-R05` remains open for feature debt; Phase 132 must audit the next coordinated feature cluster.
- Completed Phase 132 by moving the cross-feature Bestiary AI edit/draft modal shell from `features/ai-edit-monster` to the encounter page composition owner, superseding the earlier Phase 2 and strict-TypeScript UI-ownership wording. `EncounterPage` owns one stable `EncounterBestiaryAiModals` symbol, reuses it in the direct encounter path, and injects it into the sole `BestiaryBrowser` consumer through the widget-owned callable structural slot contract. The feature retains AI-edit policies and `MonsterAiActionModal`; the widget retains its state and modal orchestration without importing the page implementation. Edit-before-draft order, null guards, `general` campaign scope, model and attachment controls, active-request cancellation, response composition, and resource-level apply/undo forwarding remain unchanged.
- Phase 132 removed the three obsolete AI-edit importer-file allowances. Feature debt fell from 10 importer files / 12 directed pairs / 14 file edges / 18 declarations to 7 / 10 / 10 / 11. Widget debt remains zero across all four measures, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks the page owner, callable slot, public policy export, modal order, critical behavior, and reduced baseline; the expanded complete suite passes 418/418 tests. `MD-R05` remains open; Phase 133 must audit the coordinated Notes composition cluster.
- Completed Phase 133 by replacing the raw public `NoteCard` with the Notes-owned `createNoteCardComponent` composition boundary. The factory accepts minimal callable `EditableField` and `renderMentionText` slots without importing sibling-feature types; private `NoteCard`/parts retain rendering ownership. `SessionPage`, `CampaignNotesSection`, `CampaignEntityCardNotes`, and `AiResponseModal` each configure exactly one named component at module scope, preventing Lexical editor remounts while keeping page/widget-to-feature dependency direction. Header/simplified/body order, collapse/null gates, title and textarea modes, history/campaign forwarding, event target callbacks, raw preview slicing, mention rendering, ellipsis order, highlight classes, and delete propagation remain unchanged.
- Phase 133 removed the sole Notes importer-file allowance and both `notes → editor` and `notes → rich-content` edges. Feature debt fell from 7 importer files / 10 directed pairs / 10 file edges / 11 declarations to 6 / 8 / 8 / 9. Widget debt remains zero across all four measures, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks the private raw renderer, callable public contracts, four stable owners, slot precedence, behavior, and reduced baseline; the expanded complete suite passes 419/419 tests. `MD-R05` remains open; Phase 134 must audit the coordinated rich-content/rules-reference/dice/entity-link composition cluster.
- Completed Phase 134 by moving `renderMentionText` unchanged from rich-content to entity-link and replacing the raw public rich-content and rules-link renderers with callable composition factories. The mention helper now owns its private raw `EntityLink` implementation, whose obsolete facade export was removed. `createRulesLinkComponent` accepts the structural `RollDice` slot while keeping raw `RulesLink` private; `createRichContentRenderers` accepts structural `RollDice` and configured `RulesLink` slots while keeping raw `parseRollsAndSpells` and `renderRecursiveContent` private. `widgets/monster-stat-block`, `widgets/spells-browser`, and `widgets/rules-reference-modal` each configure stable module-scope rules-link and rich-content symbols through feature public entries. Falsy mention coercion, bracket splitting/trimming, numeric keys and link children, recursive content behavior, token-handler order, tooltip preview rendering, dice interactivity, consumer render options, and React component identity remain unchanged.
- Phase 134 removed the remaining `rich-content → dice`, `rich-content → entity-link`, `rich-content → rules-reference`, and `rules-reference → dice` allowances without adding widget debt. Feature debt fell from 6 importer files / 8 directed pairs / 8 file edges / 9 declarations to 4 / 4 / 4 / 5. Widget debt remains zero across all four measures, and the widget catalog remains at 11 slices. Permanent source-inventory coverage locks helper ownership, runtime/type facade parity, private raw renderers, callable structural contracts, stable owners, slot precedence, preserved behavior, and the reduced baseline; the expanded complete suite passes 420/420 tests. `MD-R05` remains open; Phase 135 must audit the remaining AI/editor/images/entity-link/settings cluster.
- Completed Phase 135 with a hybrid explicit-composition boundary for the remaining AI/editor/images/entity-link/settings cluster. Raw AI attachment and prompt renderers are private behind `createAiAttachmentControlsComponent` and `createAiPromptComposerComponent`: `widgets/ai-assistant` configures stable attachment and prompt components at module scope, while the encounter owner configures its own stable attachment component. Raw settings modal content is private behind `createSettingsModalContentComponent`, and `widgets/sidebar` binds the public `EditableField` once at module scope. Structural callable contracts keep AI and settings independent of sibling-feature types while preserving attachment state, prompt callbacks, controller identity, modal behavior, render order, and Lexical component identity.
- Phase 135 removed editor's entity-link edge through the narrow app-injected `EditableFieldEntityLinkProvider` runtime rather than turning static UI composition into a global service locator. `App` passes one frozen runtime containing the two entity-link Context objects, `EntityModal`, and `openEntityLinkModal`; `EditableField` obtains that runtime but reads both Context values locally at its own render position, so nested campaign/session resolver providers retain their existing scope and precedence. The production same-layer baselines are now empty for both features and widgets: zero importer files / zero directed pairs / zero file edges / zero declarations. The exported `createFsdSameLayerFileEdgeRule` seam exercises synthetic approved and stale allowances while the production baseline remains empty. Permanent source-inventory coverage locks factory ownership, runtime/type parity, stable owners, provider order, local Context reads, and the empty production graph; the expanded complete suite passes 421/421 tests. `MD-R05` is closed. Phase 136 is next and must establish typed app-owned store composition without claiming the wider migration complete.
- Completed Phase 136 by moving the actual global store composition from `shared` to `src/app/model/appStore.ts`. The app owner now contains initial state, listeners, thunk dispatch, language side effect, `useSyncExternalStore`, modal resolver map, rules-reference commands, router adapter, and browser navigation; its local `appStateReducer.ts` performs the typed ordered root-reducer composition over lower shared policies. `src/shared/model/appStore.ts` becomes a temporary effect-free typed compatibility facade for lower layers. `App`, `main`, realtime, routing, and the message-box host import the app public entry directly; remaining lower-layer consumers retain the facade without importing upward until Phase 159 retires it.
- Phase 136 adds the narrow `appStoreRuntime.ts` registration port and `fsd-boundaries/app-store-runtime-owner` enforcement. Only the app composition owner and the single shared facade may reference that private port; the rule covers static imports and re-exports, dynamic imports, `require`, Vite-root paths, extensionless paths, and Windows case variants. The facade deliberately throws until the app owner configures it, so app/root entry code must import `app/model` before invoking a shared compatibility API. Permanent source-inventory coverage locks the state/effect ownership, reducer order, public facade parity, app-consumer migration, runtime behavior, and port boundary. The expanded complete suite passes 422/422 tests. This is a historical transitional rule: Phase 159 deletes the facade/runtime and removes the checker after migration, closing `MD-R02`; the wider FSD migration remains active.
- Completed Phase 137 by replacing Settings' direct global-store dependency with the explicit typed `SettingsModalRuntime` contract. `features/settings/ui/settingsModalComposition.ts` owns the read/command surface, `SettingsModalContent` passes it into the private controller, and `ColorThemeSwitcher` is controlled-only. `useSettingsModalController` retains API orchestration, effects, local save state, normalization, and fallback policy, but no longer selects, dispatches, or imports the shared store facade. `widgets/sidebar/ui/sidebarSettingsComposition.ts` configures the Settings component once at module scope and mounts a live runtime-owning adapter inside the modal, so delayed modal rendering subscribes to current store state rather than receiving a click-time snapshot.
- Phase 137 adds scoped `fsd-boundaries/settings-store-facade` enforcement for `src/features/settings/**/*.{js,jsx,ts,tsx}`. It blocks every `app/model` and `shared/model` reference across imports, re-exports, dynamic imports, `require`, Vite glob calls, and TypeScript module references. Source-inventory and lint-rule tests lock the runtime contract, controlled theme ownership, sidebar adapter, relative/Vite-root/filesystem/extensionless/Windows variants, glob patterns, and rule scope. The expanded complete suite passes 423/423 tests. `MD-R02` remains in progress because other lower-layer consumers still use the compatibility facade; `MD-R04` remains blocked only on the missing lint/typecheck tooling.
- Completed Phase 138 by moving the one live simplified-note preference read out of `features/notes` and into `App` composition. `SimplifiedNotesProvider` is a deliberately scalar feature-owned Context contract: it takes a required boolean, preserves `false`, and fails fast if a Note renderer is mounted outside the provider. The app root wraps its full rendered subtree, including route, modal, and widget trees, so the four stable module-scope NoteCard factory owners retain live preference updates without a stale factory closure or wider component-prop threading. `NoteCard`, campaign/session note presentation, campaign graph notes, and character/location cards consume the public `useSimplifiedNotesEnabled` hook; the latter two no longer import the global-store facade at all.
- Phase 138 factors the existing injected-store check and adds scoped `fsd-boundaries/notes-store-facade` enforcement for `src/features/notes/**/*.{js,jsx,ts,tsx}`. The Settings rule remains intact, while both rules reject every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory coverage locks the nullable-context false/missing-provider behavior, public provider surface, app-root order, presentation consumers, and relative/Vite-root/filesystem/glob bypass matrix. The expanded complete suite passes 424/424 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 139 by replacing Player Questions' direct dice store dependency with the explicit typed `PlayerQuestionsRuntime` contract. The feature keeps list virtualization, scrolling, result-ID deduplication, exact context/target filtering, roll-formula construction, and debounce behavior; it now receives only the current opaque dice result, an already-normalized debounce boolean, and a narrow request command. `widgets/sidebar/ui/sidebarPlayerQuestionsComposition.tsx` owns the selectors and action dispatch, then mounts a live adapter as the delayed modal child so a result published after opening reaches the rendered modal without a click-time snapshot.
- Phase 139 adds scoped `fsd-boundaries/player-questions-store-facade` enforcement for `src/features/player-questions/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and lint-rule coverage lock the public type surface, live Sidebar adapter, false-only debounce policy, result-ID initialization, modal mount, and relative/Vite-root/filesystem/glob bypass matrix. The expanded complete suite passes 425/425 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 140 by removing Campaign Entity's embedded `refreshEntitiesAction` dependency. `submitCreateEntity` retains its public feature-owned orchestration but now receives one narrow `onRefreshEntities` command from the two campaign-entity-card widget owners. An awaited custom `onCreate` still returns without fallback API work or refresh; the default branch still awaits the campaign API creation before exactly one refresh, and rejected creation never refreshes.
- Phase 140 adds scoped `fsd-boundaries/campaign-entity-store-facade` enforcement for `src/features/campaign-entity/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory, action-order, mocked-create, custom-create, and failure-path coverage lock the callback boundary and both host bindings. The expanded complete suite passes 426/426 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 141 by removing Encounter Editor's direct modal/reload facade dependency from the Add Monster to Encounter modal. The feature keeps campaign/session/encounter discovery, active-campaign fallback, mounted-state guards, focused `addEncounterMonster` command, instance construction, and single-submit state; the Monster Stat Block widget supplies only typed error notification, campaign reload, and modal-close commands. The existing click-time filtered campaign snapshot and API success -> reload -> close ordering remain unchanged.
- Phase 141 adds scoped `fsd-boundaries/encounter-editor-store-facade` enforcement for `src/features/encounter-editor/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the public runtime contract, mounted-only load errors, add success/failure ordering, host mapping, override short-circuit, and bypass matrix. The expanded complete suite passes 427/427 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 142 by removing Rules Reference's direct global navigation/error facade. `RulesLink` retains its resolver, preview, tooltip lifecycle, and stable factory API while consuming a typed feature-owned `RulesReferenceRuntime`; `App` maps that contract to the existing navigation action and alert dispatch. The Sidebar now dispatches the typed navigation action itself, so the thin `openRulesReferenceModal` helper is retired without changing close-before-navigation behavior.
- Phase 142 adds scoped `fsd-boundaries/rules-reference-store-facade` enforcement for `src/features/rules-reference/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the nullable provider, public types, resolver/error order, App mapping, Sidebar action dispatch, deleted helper, and bypass matrix. The expanded complete suite passes 428/428 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 143 by removing AI attachment controls' direct alert facade dependency. The controls retain native input clearing, no-file/no-slot exits, asynchronous validation/preparation, functional attachment merge-before-notice ordering, gallery lifecycle, and their two module-scope factories; a narrow `AiAttachmentAlertRuntime` only publishes validation alerts from the app root. The unrelated AI generation request identity is now the feature's own numeric lifecycle value instead of a global-store `RequestId` type import.
- Phase 143 adds scoped `fsd-boundaries/ai-store-facade` enforcement for `src/features/ai/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the nullable provider, UI-only public surface, App mapping/scope, attachment ordering, lifecycle identity, factory preservation, and bypass matrix. The expanded complete suite passes 429/429 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 144 by removing Editor's three direct mention-picker facade consumers. `Input` retains its target snapshot, asynchronous selection, change, and cursor-restoration order; `EditableField` retains its selected-text immediate path plus picker -> eligibility -> focus -> update path. A narrow `EditorMentionPickerRuntime` carries only the global picker request from `App`, while the feature-owned `requestMentionSelection` continues to own the selected/cancelled promise contract separately from the entity-link runtime.
- Phase 144 adds scoped `fsd-boundaries/editor-store-facade` enforcement for `src/features/editor/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public provider/type surface, direct App action mapping, preserved controller order, separate entity-link contract, and bypass matrix. The expanded complete suite passes 430/430 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 145 by removing Dice's direct global-facade imports. A narrow `DiceRequestRuntime` carries only stable roll requests from `App`; the app-local `DiceCalculatorHost` alone selects the live request and injects the result-publish callback. This keeps dice request updates out of the root App render path while preserving request-ID deduplication, two-frame animation, local result/history/open state before publication, Player Questions auto-close, manual/Enter/history requests, and all timer/listener ownership.
- Phase 145 adds scoped `fsd-boundaries/dice-store-facade` enforcement for `src/features/dice/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private provider hook, public runtime/type surface, narrow app selector host, stable App request adapter, result callback, controller order, and bypass matrix. The expanded complete suite passes 431/431 tests. `MD-R02` remains in progress because the compatibility facade still has other lower-layer consumers.
- Completed Phase 146 by removing Images' five direct global-facade consumers. The feature-owned `ImageGalleryRuntime` provides live debounce/campaign/ignored-source inputs and narrow alert, confirmation, and prompt commands; `app/ui/ImageGalleryRuntimeHost.tsx` alone selects and maps them while wrapping the whole App tree. Location retains feature-owned ignored-source derivation and debounce timing, mutations retain bulk-delete confirmation normalization/loading/finally behavior, and the private item/dropzone UI retains prompt/upload ordering.
- Phase 146 adds scoped `fsd-boundaries/images-store-facade` enforcement for `src/features/images/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public runtime/type surface, separately selected host inputs, alert/prompt/confirmation mappings, closed-gallery scope, controller order, and bypass matrix. The expanded complete suite passes 432/432 tests. `MD-R02` remains in progress because the compatibility facade still has page/widget consumers outside the completed feature slices.
- Completed Phase 147 by removing Campaign Entity Card's two direct global-facade consumers. The widget-owned `CampaignEntityCreationRuntime` provides only error-notification and entity-refresh commands; `app/ui/CampaignEntityCreationRuntimeHost.tsx` maps those commands and wraps the existing App tree. The card widget retains draft validation, payload construction, feature-owned create orchestration, custom-create no-refresh behavior, success close, failure notification, and finally unlock behavior without receiving raw dispatch or state.
- Phase 147 adds scoped `fsd-boundaries/campaign-entity-card-store-facade` enforcement for `src/widgets/campaign-entity-card/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public provider/type surface, app host mapping/scope, creation ordering, and bypass matrix. The expanded complete suite passes 433/433 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 148 by removing Campaign Search's direct active-campaign selector and navigation facade. The widget-owned `CampaignSearchRuntime` carries only the active campaign value and ordinary target navigation; `app/ui/CampaignSearchRuntimeHost.tsx` selects/maps them and wraps only `MainContentLayout`'s persistent route `<Outlet />`. The widget retains malformed-campaign narrowing, abortable index loading, query/filter state, cancel-before-navigation, target coercion, URL/hash construction, delayed scrolling, and conditional hash cleanup without receiving raw app state, dispatch, replace, or new-tab controls.
- Phase 148 adds scoped `fsd-boundaries/campaign-search-store-facade` enforcement for `src/widgets/campaign-search/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public provider/type surface, single selector/fixed-navigation host mapping, stable Outlet scope, controller cancellation, navigation/hash ordering, and bypass matrix. The expanded complete suite passes 434/434 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 149 by removing Campaign Entity Modal's direct confirmation and entity-refresh facade access. The widget-owned `CampaignEntityModalRuntime` carries only a confirmation payload request and a refresh command; App maps them to the existing confirmation and refresh actions while the widget retains localized copy, persistence, scope ownership, parent-resolver precedence, and deletion close behavior.
- Phase 149 adds scoped `fsd-boundaries/campaign-entity-modal-store-facade` enforcement for `src/widgets/campaign-entity-modal/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the public type surface, localized confirmation ownership, update/rename/delete ordering, parent-resolver precedence, App action mapping, and bypass matrix. The expanded complete suite passes 435/435 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 150 by removing Rules Reference Modal's direct navigation/history selectors plus modal, error, and history facade access. The widget-owned `RulesReferenceModalRuntime` exposes only typed state snapshots and commands; App maps the live state, modal request, alerts, and workflow actions while the widget retains its stable content factories, request de-duplication, loading lifecycle, history behavior, and tab/selection orchestration.
- Phase 150 adds scoped `fsd-boundaries/rules-reference-modal-store-facade` enforcement for `src/widgets/rules-reference-modal/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public type/provider surface, stable App commands, factory compatibility, host request behavior, content lifecycle, App scope, and bypass matrix. The expanded complete suite passes 436/436 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 151 by removing Spells Browser's direct debounce/campaign/source selectors plus campaign replacement, ignored-source patch, and error facade access. The widget-owned `SpellsBrowserRuntime` exposes only the live values and narrow commands; App maps current state and actions while the widget retains API loading, source derivation, persistence branching, localization, local selection, and abortable loader behavior.
- Phase 151 adds scoped `fsd-boundaries/spells-browser-store-facade` enforcement for `src/widgets/spells-browser/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public type/provider surface, stable App commands, debounce/source/persistence ordering, factory-slot compatibility, App scope over route and modal rendering, and bypass matrix. The expanded complete suite passes 437/437 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 152 by removing Monster Stat Block's direct campaigns selector plus modal, error, reload, and dice facade access. The widget-owned `MonsterStatBlockRuntime` exposes only the live campaign snapshot and narrow commands; App maps current state and actions while the widget retains custom add override precedence, filtered campaign snapshot, localized modal config, feature runtime adapter, and all stat-block behavior.
- Phase 152 adds scoped `fsd-boundaries/monster-stat-block-store-facade` enforcement for `src/widgets/monster-stat-block/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public type/provider surface, stable App commands, add-to-encounter ordering/filtering, both dice bindings, stable Bestiary/AI/Rules slots, App scope over route and modal rendering, and bypass matrix. The expanded complete suite passes 438/438 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 153 by removing Sidebar's three direct facade consumers: navigation/modal/error effects in `Sidebar.tsx`, delayed Player Questions dice state/requests, and delayed Settings state/commands. The widget-owned `SidebarRuntime` retains its public `SidebarProps` while `app/ui/SidebarRuntimeHost.tsx` maps the live selectors/actions and covers both Sidebar and root modal rendering.
- Phase 153 adds scoped `fsd-boundaries/sidebar-store-facade` enforcement for `src/widgets/sidebar/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public type/provider surface, app-host mapping, delayed-modal scope, import/export error/finally ordering, close-before-open/navigation behavior, `forceTab`, raw Settings and false-only Player Questions debounce policy, public-prop stability, and bypass matrix. The expanded complete suite passes 439/439 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 154 by removing Bestiary Browser's direct selectors plus campaign/settings, alert, and confirmation facade access. The widget-owned `BestiaryBrowserRuntime` exposes only live language/debounce/campaign/source/sync values and narrow replacement/message/confirmation commands; `app/ui/BestiaryBrowserRuntimeHost.tsx` maps them around `MainContentLayout`'s routed content while the browser retains raw sync parsing, transport, local selection/history/abort state, localized copy, and stable slots.
- Phase 154 adds scoped `fsd-boundaries/bestiary-browser-store-facade` enforcement for `src/widgets/bestiary-browser/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public type/provider surface, route-host mapping/scope, raw sync identity, false-only debounce, campaign/global source-save ordering, delete confirmation, public-prop stability, and bypass matrix. The expanded complete suite passes 440/440 tests. `MD-R02` remains in progress because the compatibility facade still has other page/widget consumers.
- Completed Phase 155 by removing AI Assistant's direct campaign/session/encounter, prompt/language/navigation, selection, reload/refresh, sync, confirmation, and message facade access. The widget-owned `AiAssistantRuntime` exposes only those live values and narrow commands; `app/ui/AiAssistantRuntimeHost.tsx` maps them once around both `MainContentLayout`'s routed content and route-level assistant while the widget retains route projection, local loading/context, generation/retry, history restore/reload, localized copy, and response-modal composition.
- Phase 155 adds scoped `fsd-boundaries/ai-assistant-store-facade` enforcement for `src/widgets/ai-assistant/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and regression coverage lock the private Context hook, public type/provider surface, host scope across route and injected Bestiary assistants, stable action mapping, AI sync event construction, history confirmation/reload, generation/retry errors, public-prop stability, and bypass matrix. The expanded complete suite passes 441/441 tests. `MD-R02` remains in progress because the compatibility facade still has page consumers.
- Completed Phase 156 by removing Session Page's direct active campaign/session/sync selectors plus navigation, active-session selection, campaign reload, entity refresh, confirmation, prompt, and message facade access. The page-owned `SessionPageRuntime` exposes only those live values and narrow commands; `app/ui/SessionPageRuntimeHost.tsx` maps them inside `SessionRoute` after its campaign guard while the page retains loading/normalization, realtime-sync decisions, editing/history/persistence, scope movement, encounter creation, keyboard/hash behavior, and quick-access projection.
- Phase 156 adds scoped `fsd-boundaries/session-page-store-facade` enforcement for `src/pages/session/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. One added static regression plus source-inventory coverage lock the private Context hook, public type/provider surface, route-host scope, stable action mapping, active-session publication, sync discard/reload policy, rename and scope-move ordering, confirmation/prompt semantics, quick-access new-tab navigation, runtime-free page API, and bypass matrix. The expanded complete suite passes 442/442 tests. `MD-R02` remains in progress because the compatibility facade still has remaining page consumers.
- Completed Phase 157 by removing Encounter Page's direct campaign/session/encounter/sync, dice, theme/language, and encounter-view settings selectors plus navigation, active-selection, reload/refresh, dice, prompt, UI-settings, and message facade access. The page-owned `EncounterPageRuntime` exposes only those live values and narrow commands; `app/ui/EncounterPageRuntimeHost.tsx` maps them inside `EncounterRoute` after its campaign guard while the page retains encounter load/save, active selection, sync/AI updates, participant editing/creation, HP roll/dedupe behavior, settings persistence, and localized error handling.
- Phase 157 adds scoped `fsd-boundaries/encounter-page-store-facade` enforcement for `src/pages/encounter/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and static regression coverage lock the private Context hook, public type/provider surface, route-host scope, stable action mapping, active-session/encounter publication, raw sync and dice-result handling, UI-settings update-before-API ordering, page API stability, and bypass matrix. The expanded complete suite passes 443/443 tests; `MD-R02` remains in progress while the compatibility facade has remaining page consumers.
- Completed Phase 158 by removing Campaign Page's direct active-campaign, entity-refresh/sync, theme/language selectors plus campaign-list/renamed-campaign/session navigation, graph-note modal, campaign reload, confirmation, prompt, and message facade access. The page-owned `CampaignPageRuntime` exposes only those live values and narrow commands; `app/ui/CampaignPageRuntimeHost.tsx` maps them inside `CampaignRoute` after its campaign guard while the page retains campaign/session/entity loading and persistence, history, sync/AI flows, graph layout/note editing, archive/import/export, and localized workflow copy.
- Phase 158 adds scoped `fsd-boundaries/campaign-page-store-facade` enforcement for `src/pages/campaign/**/*.{js,jsx,ts,tsx}` through the common injected-runtime checker. It rejects every `app/model` and `shared/model` reference across static/re-export/dynamic/require/Vite-glob/TypeScript forms. Source-inventory and static regression coverage lock the private Context hook, public type/provider surface, route-host scope, stable selector/action/navigation/modal mapping, graph session navigation and note-modal behavior, runtime-free page API, and bypass matrix. The expanded complete suite passes 444/444 tests; architecture, performance, and Ukrainian encoding checks pass. Phase 159 then performs the planned retirement of the typed compatibility facade and closes `MD-R02`.
- Completed Phase 159 by deleting the former transitional `shared/model/appStore.ts` facade and `shared/model/appStoreRuntime.ts` port. `app/model` is now the sole stateful store, selector, modal, and navigation owner; `shared/model` contains global action contracts/creators, action/state types, and lower-level reducer policies only. `SessionPageRuntimeHost` now imports navigation from `app/model`.
- Phase 159 removes the obsolete app-store-runtime-owner enforcement together with its retired target. Existing scoped page/widget facade rules continue to block lower-layer `app/model` and `shared/model` store access; the completed source/import inventory supports closing `MD-R02`. One added static regression brings the complete suite to 445/445 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 160 by moving the global mention-picker request subscription, active-campaign entity reads, callback validation, option projection, and modal lifecycle from root `App` to `app/ui/MentionPickerModalHost.tsx`. `App` retains only the narrow `EditorMentionPickerRuntime` action adapter, and the host is mounted within that provider so the existing editor modal content keeps its injected runtime.
- Phase 160 keeps editor-owned promise/selection behavior, `Input` and `EditableField` interactions, and the public editor UI boundary intact while concentrating app-wide store/API/modal orchestration in an app host. One added static regression brings the full suite to 446/446 tests; architecture, performance, and Ukrainian encoding checks pass. `MD-R02` remains closed and complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 161 by extracting the Rules Reference Modal's mounted/controller/requested-tab/loading/error lifecycle from `RulesReferenceModalContent.tsx` into private `ui/useReferenceTabLoading.ts`. The widget retains seven-tab API aggregation and `AbortController` effects while content retains navigation/history, search, selection, scrolling, and rendering.
- Phase 161 keeps the Node-safe `widgets/rules-reference-modal/model.js`, public widget entry, injected runtime, and stable content factory unchanged. Active-request identity, abort cleanup, retry release, mounted result/error/finalization guards, localized errors, custom-Bestiary abort propagation, and existing selection defaults remain private UI behavior. One added static regression brings the full suite to 447/447 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 162 by moving the AI Response Modal's creature-field draft editing state coordination from `AiResponseModal.tsx` to private `ui/aiResponseCreatureFieldEditing.ts`. The raw renderer retains its state-before-draft-controller-before-prompt-guard order, configured factory, injected card/editor slots, preview tree, and final editor placement.
- Phase 162 keeps draft/applied/object guards, identity-field preservation, encounter-participant replacement, nested draft-resource resolution, draft update, and close-after-success ordering private to the widget UI. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 448/448 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 163 by moving AI Response Modal JSON-diff markup from `AiResponseModal.tsx` to private `ui/AiResponseJsonDiff.tsx`. The raw renderer retains configured factory/composition, state/controller/guard order, injected card/editor slots, preview tree, and the `AiResponseModalView` boundary.
- Phase 163 keeps resource/field/line keys, localized labels, resource-state lookup, diff-line classes and markers, and number/text fallbacks widget-private. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 449/449 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 164 by moving AI Response Modal generic snapshot-diff presentation from `AiResponseModal.tsx` to private `ui/AiResponseGenericDiff.tsx`. The raw renderer retains configured factory/composition, state/controller/guard order, injected card/editor slots, preview tree, and the `AiResponseModalView` boundary.
- Phase 164 keeps single-snapshot selection, metadata exclusion, value fallback, ordered field projection, note-array delegation, resource/field keys, localized Before/After labels, formatted values, and CSS classes widget-private. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 450/450 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 165 by moving AI Response Modal card-diff presentation from `AiResponseModal.tsx` to private `ui/AiResponseCardDiff.tsx`. The raw renderer retains configured factory/composition, state/controller/guard order, every injected card/editor slot tag, preview tree, and the `AiResponseModalView` boundary.
- Phase 165 keeps new/deleted/changed routing, snapshot selection, localized labels, card-surface classes, draft/apply edit eligibility, highlight construction, callback argument order, and the outer resource wrapper/header/key widget-private. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 451/451 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 166 by moving AI Response Modal encounter participant-list presentation from `AiResponseModal.tsx` to private `ui/AiResponseEncounterParticipantList.tsx`. The raw renderer retains configured factory/composition, state/controller/guard order, encounter-side panels, encounter MonsterStatBlock slots, encounter editing/resource composition, preview tree, and the `AiResponseModalView` boundary.
- Phase 166 keeps before/after entry derivation and maps, empty-state copy, participant list keys, ordered before/after/mutated class routing, participant-name rendering, and both metadata evaluations widget-private. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 452/452 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 167 by moving Bestiary Browser header-action presentation/action dispatch, menu-local state, and DOM refs from `BestiaryBrowser.tsx` to private `ui/BestiaryHeaderActions.tsx`. The raw browser retains custom-monster import parsing/persistence/messages, JSON export, undo/redo operations and stacks, and its existing `BestiaryContent` header-actions composition.
- Phase 167 keeps the always-mounted menu structure, hidden `.json` input, functional toggle, classes/titles/icons/copy, close-before-action dispatch, and disabled capability flags widget-private. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 453/453 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 168 by moving the behaviorally identical open-gated header `pointerdown` dismissal from Campaign Page, Session Page, and private Bestiary Header Actions to public `shared/ui/usePointerDownOutsideDismissal.ts`. Each consumer retains its own state, root ref, menu presentation, and commands.
- Phase 168 preserves the `Node` containment guard, inside-root no-op, outside/non-Node close, listener cleanup, functional toggles, and close-before-action behavior. Encounter and Dice retain their distinct dismissal policies, while Select/MultiSelect keep their existing `mousedown` portal lifecycle. One added static regression brings the full suite to 454/454 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 169 by moving AI Response Modal note-diff presentation from `AiResponseModal.tsx` to private `ui/AiResponseNoteDiff.tsx`. The raw renderer retains configured factory/composition, state/controller/guard order, injected card/editor slots, `AiResponseNoteCard` factory, `renderNoteCard` draft-update callback, twice-resolved resource flow, nested note-array ID/label/list-index synthesis, preview classification, and the `AiResponseModalView` boundary.
- Phase 169 keeps single new/deleted and changed Before/After routing, snapshot selection, note-surface classes, localized labels, draft/apply edit eligibility, highlight construction, outer resource key/header, and callback argument order widget-private. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 455/455 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 170 by moving Campaign Page header-action presentation, menu-local state/ref, and shared pointer-dismissal composition from `CampaignPage.tsx` to private `ui/components/CampaignHeaderActions.tsx`. `CampaignHeader` retains campaign title/rename/created metadata and supplies only narrow search, partial-archive, undo/redo, export, and deletion callbacks. The Fallow-detected duplicate undo-redo pair is now public shared `ui/UndoRedoButtons.tsx`, used by Campaign and Session headers.
- Phase 170 keeps the always-mounted menu, classes/titles/icons/localized copy, open-only `pointerdown` containment lifecycle, functional toggle, direct search/undo/redo behavior, capability/saving-disabled flags, and close-before-export/partial-archive/delete order intact. `UndoRedoButtons` remains a fragment-only button pair with an optional disabled gate, not a menu/workflow owner. Page public/runtime entries remain unchanged. One added static regression brings the full suite to 456/456 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 171 by moving AI Response Modal encounter monster-change presentation from `AiResponseModal.tsx` to private `ui/AiResponseEncounterMonsterChange.tsx`. The raw renderer retains configured factory/composition, state/controller/guard order, `renderEncounterMonsterCard` MonsterStatBlock/field-edit slot, participant entry/map/change detection, draft/apply eligibility, highlight construction, localized labels, resource keys, and encounter composition.
- Phase 171 keeps precomputed card/label slot evaluation in the raw renderer while the private component preserves single/paired stack/column/frame/title markup and ordering. The public widget runtime/type/model entries remain unchanged. One added static regression brings the full suite to 457/457 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 172 by moving Monster Field Edit Modal action-section presentation from `MonsterFieldEditModal.tsx` to private `ui/MonsterActionSections.tsx`. The raw feature modal retains draft/JSON synchronization, functional action mutations, rule-picker lifecycle, parsing, save, and error ownership.
- Phase 172 forwards original change/keyboard events to raw callbacks while the private Fragment-only component preserves section order, keys, classes, localized copy, empty/add/remove/name/text behavior, and no public-entry expansion. One added static regression brings the full suite to 458/458 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Completed Phase 173 by moving Monster Field Edit Modal fields-mode layout from `MonsterFieldEditModal.tsx` to private `ui/MonsterFieldSections.tsx`. The raw feature modal retains draft/JSON synchronization, raw field renderers and select fallback, update/rule-picker callbacks, action composition, parsing, save, error, and modal lifecycle ownership.
- Phase 173 precomputes basic, ability, text, and action slots in the raw renderer's original evaluation order while the private Fragment-only component preserves only the four wrapper groups, order/classes, fields/keys, disabled source, custom select option, and textarea presentation. One added static regression brings the full suite to 459/459 tests; architecture, performance, and Ukrainian encoding checks pass. Complete lint/typecheck remains tracked under `MD-R04`.
- Apply the typed API results to focused feature models as those modules migrate; avoid repository-wide component conversion.
- Keep repository ports and HTTP payload types type-only until their owning runtime modules can migrate independently.

## Selective recovery after Phase 119

This recovery track does not renumber or rewrite the Phase 0-119 history above.
Phase 119 passed its full regression gate at 398 tests, Phase 120 completed
the editor root presentation split at 403 tests, Phase 121 completed the
recovery-hardening gate at 404/404 tests, Phase 122 completed the
public-entry/entity-isolation gate at 405/405 tests, and Phase 123 completed
the exact same-layer baseline plus first reduction gate at 409/409 tests.
Phase 124 completed the modal-ownership correction and lowered feature
same-layer gate at 410/410 tests, Phase 125 completed the first
widget-composition reduction at 411/411 tests, and Phase 126 absorbed the
redundant sole-consumer spell-card slice at 412/412 tests. Phase 127 moved
Bestiary content composition to its page owner and lowered the widget gate at
413/413 tests. Phase 128 moved campaign-entity modal card composition to the
app root and lowered the widget gate at 414/414 tests. Phase 129 moved AI
response-modal composition to the assistant's owners and lowered the widget
gate at 415/415 tests. Phase 130 configured the raw response renderer at stable
app/page composition roots and lowered the widget gate at 416/416 tests. Phase
131 configured the remaining rules-reference/editor chain, reduced the
same-layer widget baseline to zero, and passed its expanded gate at 417/417
tests. Phase 132 moved Bestiary AI modal composition to the encounter page,
reduced feature debt to 7 / 10 / 10 / 11, and passed its expanded gate at
418/418 tests. Phase 133 configured Notes rendering at four stable owners,
reduced feature debt to 6 / 8 / 8 / 9, and passed its expanded gate at 419/419
tests. Phase 134 moved unchanged mention rendering to entity-link, configured
private rules-link and rich-content renderers at three stable widget owners,
reduced feature debt to 4 / 4 / 4 / 5, and passed its expanded gate at 420/420
tests. Phase 135 completed the hybrid AI/settings factories plus the narrow
app-injected editor entity-link runtime, reduced both production same-layer
baselines to zero across importer files, directed pairs, file edges, and
declarations, and passed its expanded gate at 421/421 tests. Phase 136 moved
the actual configured store to the app layer, temporarily left a typed
lower-layer compatibility facade, enforced the private runtime port, and
passed 422/422 tests. Phase 159 removes that facade and port after the final
lower-layer migration. Phase 137 removes Settings' direct global-store facade dependency
through a live sidebar-provided runtime, makes theme switching controlled-only,
enforces the focused Settings store boundary, and passes 423/423 tests. Phase
138 gives Notes a live app-root simplified-preference provider, removes its
direct facade read plus duplicate note-render reads from higher owners,
enforces the focused Notes store boundary, and passes 424/424 tests. Phase 139
gives Player Questions a live Sidebar dice runtime, removes its direct store
facade dependency, enforces the focused Player Questions store boundary, and
passes 425/425 tests. Phase 140 gives Campaign Entity an injected widget refresh
command, removes its embedded facade action, enforces the focused Campaign
Entity store boundary, and passes 426/426 tests. Phase 141 gives Encounter
Editor an injected Monster Stat Block modal runtime, removes its direct facade
effects, enforces the focused Encounter Editor store boundary, and passes
427/427 tests. Phase 142 gives Rules Reference an App-provided navigation/error
runtime, removes its direct facade dependency, enforces the focused Rules
Reference store boundary, and passes 428/428 tests. `MD-R05` is closed;
Phase 143 gives AI attachment controls an App-provided validation-alert runtime,
removes their direct facade dependency, enforces the focused AI store boundary,
and passes 429/429 tests. Phase 144 gives Editor an App-provided mention-picker
runtime, removes its direct facade dependency, enforces the focused Editor store
boundary, and passes 430/430 tests. Phase 145 gives Dice an App-provided stable
request runtime plus an app-local live request host, removes its direct facade
dependency, enforces the focused Dice store boundary, and passes 431/431 tests.
Phase 146 gives Images an app-local live gallery-runtime host plus a
feature-owned provider, removes every direct feature facade dependency, enforces
the focused Images store boundary, and passes 432/432 tests. Phase 147 gives
Campaign Entity Card a widget-owned creation runtime plus an app-local host,
removes both direct widget facade dependencies, enforces the focused widget
store boundary, and passes 433/433 tests. Phase 148 gives Campaign Search a
widget-owned runtime plus a route-layout host, removes both direct widget
facade dependencies, enforces the focused widget store boundary, and passes
434/434 tests. Phase 149 gives Campaign Entity Modal an App-supplied runtime
for confirmation and refresh effects, keeps localized copy and entity
persistence widget-owned, enforces the focused widget store boundary, and
passes 435/435 tests. Phase 150 gives Rules Reference Modal a widget-owned
runtime with stable App-mapped navigation/history state and modal/history/error
commands, preserves its factories and UI lifecycle, enforces the focused widget
store boundary, and passes 436/436 tests. Phase 151 gives Spells Browser a
widget-owned runtime with live App-mapped source settings and persistence/error
commands, preserves its factory slot and browser lifecycle, enforces the
focused widget store boundary, and passes 437/437 tests. `MD-R02` remains in
progress and the broader migration remains active. Phase 152 gives Monster Stat
Block a widget-owned runtime with live App-mapped campaigns and
modal/error/reload/dice commands, preserves its factory slots and encounter
modal/roll behavior, enforces the focused widget store boundary, and passes
438/438 tests. Phase 153 gives Sidebar a widget-owned runtime with live
app-host-mapped navigation, modal/error, settings, and dice contracts; it keeps
its public props and delayed modal children live, enforces the focused widget
store boundary, and passes 439/439 tests. Phase 154 gives Bestiary Browser a
widget-owned runtime with live preferences/campaign/source/sync state and
replacement/message/confirmation commands; it keeps browser-local parsing,
transport, selection/history state, and composition slots, enforces the focused
widget store boundary through a route-layout host, and passes 440/440 tests.
Phase 155 gives AI Assistant a widget-owned runtime with live route state,
prompt/language/navigation inputs, and selection/reload/refresh/sync/
confirmation/message commands; one app-local host covers both the ordinary
route assistant and the injected Bestiary assistant, preserves its public
props/slots and widget-owned workflow state, enforces the focused widget store
boundary, and passes 441/441 tests. Phase 156 gives Session Page a page-owned
runtime with live campaign/session/sync state and narrow navigation, selection,
reload, refresh, confirmation, prompt, and message commands; a `SessionRoute`
host scopes it after the existing campaign guard, preserves page-owned session
workflows, enforces the focused page store boundary, and passes 442/442 tests.
Phase 157 gives Encounter Page a page-owned runtime with live campaign,
session/encounter, sync/dice, theme/language, and encounter-settings state plus
narrow navigation, active-selection, reload/refresh, dice, prompt, settings,
and message commands; an `EncounterRoute` host scopes it after the existing
campaign guard, preserves page-owned encounter workflows, and enforces the
focused page store boundary, and passes 443/443 tests. Phase 158 gives
Campaign Page a page-owned runtime with live active-campaign,
entity-refresh/sync, theme, and language state plus campaign-list,
renamed-campaign/session navigation, graph-note modal, reload, confirmation,
prompt, and message commands; a `CampaignRoute` host scopes it after the
existing campaign guard, preserves page-owned campaign workflows, enforces the
full page store boundary, and passes 444/444 tests.
Phase 159 then deletes the temporary shared facade/runtime, makes `app/model`
the sole stateful store/selector/modal/navigation owner, and closes `MD-R02`
at 445/445 tests. Phase 160 moves global mention-picker request/entity/modal
orchestration out of root `App` into `app/ui/MentionPickerModalHost.tsx` while
retaining the Editor's injected runtime and feature-owned selection policy at
446/446 tests. Phase 161 extracts the private Rules Reference Modal tab loader
without widening its Node-safe model, public widget entry, runtime, or stable
content-factory contracts, at 447/447 tests. Phase 162 isolates AI Response
Modal creature-field draft editing in a private widget controller at 448/448
tests. Phase 163 isolates JSON-diff presentation in the same widget at 449/449
tests. Phase 164 isolates generic snapshot-diff presentation in that widget at
450/450 tests. Phase 165 isolates card-diff presentation in that widget at
451/451 tests. Phase 166 isolates encounter participant-list presentation in
that widget at 452/452 tests. Phase 167 isolates Bestiary Browser header-action
presentation and menu-local lifecycle in its private widget UI at 453/453
tests. Phase 168 centralizes the identical Campaign, Session, and Bestiary
header pointer-dismissal lifecycle in shared UI while retaining local menus and
commands at 454/454 tests. Phase 169 isolates AI Response Modal note-diff
presentation in private widget UI while retaining raw resource, draft, and
editor composition at 455/455 tests. Phase 170 isolates Campaign Page
header-action presentation and menu-local lifecycle in private page UI while
consolidating its duplicated Campaign/Session undo-redo controls in shared UI,
at 456/456 tests. Phase 171 isolates AI Response Modal encounter monster-change
presentation in private widget UI while retaining raw participant mapping,
change detection, card/editor, and draft composition at 457/457 tests.
Phase 172 isolates Monster Field Edit Modal action-section presentation in
private feature UI while retaining raw draft synchronization, action mutations,
and rule-picker lifecycle at 458/458 tests.
Phase 173 isolates its fields-mode layout in private feature UI while retaining
raw field renderers, draft/JSON lifecycle, action composition, update/rule-picker
callbacks, and save/error behavior at 459/459 tests.

### Provenance and transfer rule

Accidental commit `25b5ccc40f57f104343a4ff4d3a1d6fd13c3883e` and the current
`fsd` branch share pre-migration base
`0f93e77a1d35cd097dda7d457d253ca5fecc166c`, but are sibling histories.
The accidental commit was reverted on `main` by `8daaf4e`; it is not an
ancestor of `fsd`. Its tree also predates the current branch's strict
TypeScript/FSD and backend application-module work.

Therefore the recovery rule is:

- compare behavior and contracts, not file paths;
- reimplement useful deltas in current owners;
- do not cherry-pick the commit or copy its stale JavaScript/backend tree;
- preserve all Phase 0-119 contracts and keep unrelated redesigns as explicit
  debt.

The detailed decisions are recorded in `docs/adr/0001-0004`; remaining work is
tracked in `docs/migration-debt.md`.

### Disposition summary

| Side-commit area | Decision on `fsd` |
| --- | --- |
| Cancellation, bounded campaign search, filter/query performance, performance gate | Adapt to current strict TypeScript owners |
| Campaign lookup and rules-reference endpoint ownership | Adapt through current entity public APIs |
| Canonical Bestiary AI history and mutation request validation | Adapt through current backend modules, repositories, storage facade, and routes |
| FSD/import enforcement, ADRs, recovery phases, debt register, agent rules | Recover and align with the current tree |
| App-owned configured store | Phase 136 owns configured store composition in `app`; Phase 137 removes the Settings UI's direct facade dependency through a sidebar-provided runtime; Phase 138 gives Notes a live app-root preference provider; Phase 139 gives Player Questions a live Sidebar dice runtime; Phase 140 gives Campaign Entity a widget-provided refresh command; Phase 141 gives Encounter Editor a Monster Stat Block-provided modal runtime; Phase 142 gives Rules Reference an App-provided navigation/error runtime; Phase 143 gives AI attachment controls an App-provided validation-alert runtime; Phase 144 gives Editor an App-provided mention-picker runtime; Phase 145 gives Dice an App-provided request runtime plus an app-local live request host; Phase 146 gives Images a feature-owned gallery runtime supplied by a narrow app-local host; Phase 147 gives Campaign Entity Card a widget-owned creation runtime supplied by a narrow app-local host; Phase 148 gives Campaign Search a widget-owned runtime supplied by a narrow route-layout host; Phase 149 gives Campaign Entity Modal an App-supplied confirmation/refresh runtime while keeping localized copy widget-owned; Phase 150 gives Rules Reference Modal a widget-owned runtime supplied through the existing App root while keeping its stable content factories unchanged; Phase 151 gives Spells Browser a widget-owned runtime supplied through the same App-root scope while keeping its stable factory slot unchanged; Phase 152 gives Monster Stat Block a widget-owned runtime supplied through that App-root scope while keeping its stable Bestiary, AI Response, and Rules Reference slots unchanged; Phase 153 gives Sidebar a widget-owned runtime supplied by an app-local host that covers delayed root-modal children while keeping `SidebarProps` unchanged; Phase 154 gives Bestiary Browser a widget-owned runtime supplied by a narrow route-layout host while keeping raw sync parsing and public composition slots unchanged. Phase 159 retires the shared facade/runtime: `app/model` alone owns stateful store/selectors/modal/navigation, `shared/model` retains action/type/reducer policy, and `MD-R02` is closed. Phase 160 moves app-wide mention-picker request/entity/modal orchestration from root `App` into an app-local host while preserving the Editor's injected runtime and feature-owned selection policy. |
| AI Assistant facade migration | Phase 155 gives AI Assistant a widget-owned runtime supplied by one app-local host across both its ordinary route mount and its injected Bestiary mount. Keep route projection, generation/retry, history, and public composition contracts widget-owned until Phase 159 retires the facade/runtime (`MD-R02` closed). |
| Session Page facade migration | Phase 156 gives Session Page a page-owned runtime supplied by `app/ui/SessionPageRuntimeHost.tsx` only inside `SessionRoute` after the active-campaign guard. Keep session load/normalization, realtime sync, editing/history/persistence, scope movement, encounter creation, keyboard/hash behavior, and public `SessionPage` contracts page-owned until Phase 159 retires the facade/runtime (`MD-R02` closed). |
| Encounter Page facade migration | Phase 157 gives Encounter Page a page-owned runtime supplied by `app/ui/EncounterPageRuntimeHost.tsx` only inside `EncounterRoute` after the active-campaign guard. Keep encounter load/save, active selection, sync/AI handling, participant workflows, HP dice dedupe, UI-settings persistence, localized error behavior, and public `EncounterPage` contracts page-owned until Phase 159 retires the facade/runtime (`MD-R02` closed). |
| Campaign Page facade migration | Phase 158 gives Campaign Page a page-owned runtime supplied by `app/ui/CampaignPageRuntimeHost.tsx` only inside `CampaignRoute` after the active-campaign guard. Keep campaign/session/entity loading and persistence, history, sync/AI flows, graph layout/note editing, archive/import/export, localized workflow copy, and public `CampaignPage` contracts page-owned while Phase 159 retires the now-unused facade/runtime (`MD-R02` closed). |
| Store facade retirement | Phase 159 deletes the now-unused shared facade/runtime and moves `SessionPageRuntimeHost` navigation to `app/model`. `app/model` alone owns stateful store/selectors/modal/navigation; `shared/model` retains only action/type/reducer policy. `MD-R02` is closed; complete lint/typecheck remains `MD-R04`. |
| Side commit's `server/domains` replacement, stale JS/JSX slices, graph ownership, and whole-file configuration/test rewrites | Skip because current `fsd` already has newer typed/application-module owners and stricter contracts |

### Recovery R0 — Divergence audit

Status: **Complete**

- [x] Proved that `25b5ccc` and `fsd` diverged from `0f93e77`.
- [x] Classified the side commit's changes as recover, adapt, defer, or skip.
- [x] Rejected a wholesale cherry-pick because it would replace current typed
  slices, tests, and backend commands.

### Recovery R1 — Read lifecycle and performance

Status: **Complete**

- [x] Forward native request options through Bestiary, spell, reference,
  campaign, and session read APIs.
- [x] Abort unmounted or superseded Bestiary, spell, rules-reference, and
  campaign-search requests; suppress native abort failures and guard state
  commits.
- [x] Hydrate campaign sessions with an ordered concurrency limit of `6`.
- [x] Cap campaign-search presentation at `80` results.
- [x] Optimize Bestiary filtering and campaign-search early termination without
  changing result order.
- [x] Add `npm run check:performance` with executable Bestiary, spells,
  campaign-search, and encounter budgets defined in ADR 0004.

### Recovery R2 — Endpoint and model ownership

Status: **Complete**

- [x] Move API-backed campaign entity resolution to
  `entities/campaign/api/resolveEntityByName.ts`.
- [x] Keep name matching and entity-selection policy pure in
  `entities/campaign/model/entityLookup.ts`.
- [x] Move conditions, diseases, variant rules, skills, and senses to
  `entities/reference/api/referenceApi.ts`.
- [x] Keep the spell client limited to spell endpoints and update
  rules-reference consumers through current public facades.

### Recovery R3 — Canonical Bestiary AI history

Status: **Complete**

- [x] Read the canonical Bestiary AI-history path first.
- [x] If only legacy history exists, normalize it and attempt one canonical
  write while retaining the legacy source.
- [x] Deduplicate concurrent migration attempts.
- [x] Serialize canonical Bestiary writes behind the same per-slug operation
  queue so migration cannot overwrite a live write.
- [x] Return normalized in-memory history when a canonical write fails and
  retry on a later read.
- [x] Keep non-Bestiary AI-history path behavior unchanged.

### Recovery R4 — Validation before mutation

Status: **Complete**

- [x] Add shared validation/error mechanics and domain-owned campaign, session,
  and archive request schemas.
- [x] Attach schemas to all mutation routes listed in ADR 0003.
- [x] Parse uploaded archives once, validate the schema-governed payload, and
  only then invoke an import command; `images` is an optional array container,
  while individual rows remain intentionally tolerant under restoration/storage
  policy.
- [x] Prove that invalid or empty wipe-and-replace archives cannot reach
  persistence.
- [x] Return one `400 / INVALID_REQUEST / details` contract for request-schema
  and uploaded-archive validation failures.
- [x] Validate campaign entity-move source type before reading/validating the
  body, while retaining target-type and valid-pair checks plus exact errors in
  the campaign command.

Focused contracts cover schema rejection, route middleware order, campaign and
session guards, a strict archive-image array container with tolerant rows,
source-before-body entity movement, UTF-8 archive content, and the empty
wipe-and-replace boundary.

### Recovery R5 — Enforced ownership and verification

Status: **In progress**

- [x] Keep Fallow's
  `app -> pages -> widgets -> features -> entities -> shared` direction.
- [x] Correct the Fallow app-zone entry paths to current `.tsx` owners.
- [x] Enforce the recovered campaign/reference public APIs through lint rules
  and focused import scans, including the JavaScript/JSX
  `entities/reference/api/*` and `entities/reference/model/*` group alongside
  the current TypeScript/TSX restrictions.
- [x] Apply one shared `FSD_PUBLIC_API_PATTERNS` baseline and synchronized
  `FSD_SLICE_NAMES` registry to JavaScript/JSX and TypeScript/TSX, redirect all
  10 audited private/deep imports, and expose campaign state utilities through
  the minimal `features/campaign` public facade.
- [x] Discover entity slices independently with Fallow `autoDiscover`, reduce
  entity sibling edges to zero, and move cross-entity preview/resolution
  orchestration into `features/rules-reference` while retaining the five
  public entity-owned reference cache/normalizer modules.
- [x] Freeze the exact importer-file → target-slice baseline for features and
  widgets with a local ESLint rule that rejects new edges and stale allowances,
  cover every supported static module-reference form, and lock the live source
  inventory in `npm test`.
- [x] Eliminate the first bounded dependency cluster in Phase 123 by replacing
  five plain editor `Input` dependencies with `shared/ui/TextInput`; reduce
  feature debt to 18 directed pairs / 25 file edges / 29 declarations while
  widgets remain at 13 / 13 / 13.
- [x] Correct the modal cluster in Phase 124 by moving generic modal UI and
  policies to `shared/ui`, moving the store-backed host to `app/ui`, deleting
  the dead imperative API and `features/modal`, and lowering feature debt to
  12 directed pairs / 14 file edges / 18 declarations while widgets remain at
  13 / 13 / 13.
- [x] Lower the first bounded widget-composition cluster in Phase 125 by
  injecting `ResponseModal` and `MonsterEditorModal` from `EncounterPage` into
  `BestiaryBrowser`; reduce widget debt to 7 importer files / 11 directed pairs
  / 11 file edges / 11 declarations while features remain at 10 / 12 / 14 / 18.
- [x] Absorb the redundant sole-consumer `widgets/spell-card` slice unchanged
  into private `widgets/spells-browser/ui/SpellCard.tsx` in Phase 126; delete
  its public barrels, catalog entry, and stale lint allowance, reduce the widget
  catalog from 12 to 11 slices, and lower widget debt to 6 importer files / 10
  directed pairs / 10 file edges / 10 declarations while features remain at
  10 / 12 / 14 / 18.
- [x] Move Bestiary assistant/stat-block composition to `EncounterPage` in
  Phase 127, forward the required stable component slots through
  `BestiaryBrowser` to private `BestiaryContent`, remove the two stale
  same-layer allowances, and lower widget debt to 5 importer files / 8 directed
  pairs / 8 file edges / 8 declarations while features remain at
  10 / 12 / 14 / 18.
- [x] Move campaign-entity modal card composition to `App` in Phase 128,
  forward the required stable `CharacterCard` and `LocationCard` slots through
  `CampaignEntityModalProvider` to its private card composition, remove the
  stale same-layer allowance, and lower widget debt to 4 importer files / 7
  directed pairs / 7 file edges / 7 declarations while features remain at
  10 / 12 / 14 / 18 and the widget catalog remains at 11 slices.
- [x] Move AI response-modal composition to the assistant's owners in Phase
  129: inject the stable public `AiResponseModal` from `MainContent` for route
  assistants, forward the existing Bestiary `ResponseModal` through
  `BestiaryBrowser`/`BestiaryContent`, remove the stale same-layer allowance,
  and lower widget debt to 3 importer files / 6 directed pairs / 6 file edges /
  6 declarations while features remain at 10 / 12 / 14 / 18 and the widget
  catalog remains at 11 slices.
- [x] Configure the AI response modal at stable app/page composition roots in
  Phase 130: expose `createAiResponseModalComponent`, inject the public
  `CharacterCard`, `LocationCard`, `MonsterStatBlock`, and
  `MonsterEditorModal` symbols once per owner, keep the raw renderer private,
  remove its three stale same-layer allowances, and lower widget debt to 2
  importer files / 3 directed pairs / 3 file edges / 3 declarations while
  features remain at 10 / 12 / 14 / 18 and the widget catalog remains at 11
  slices.
- [x] Configure the remaining rules-reference/editor widget cluster at stable
  app/page composition roots in Phase 131: bind `MonsterStatBlock` and
  `SpellsBrowser` into owner-specific rules content through
  `createRulesReferenceModalContentComponent`, pass both slots directly to the
  public app-owned global host, configure owner-specific monster editors, keep
  raw content/view/editor renderers private,
  remove the final three stale widget allowances, and lower widget debt to zero
  across importer files, directed pairs, file edges, and declarations while
  features remain at 10 / 12 / 14 / 18 and the widget catalog remains at 11
  slices.
- [x] Move the coordinated Bestiary AI modal cluster to its encounter page
  composition owner in Phase 132: retain AI-edit policy/action ownership in the
  feature, inject one stable callable modal slot into `BestiaryBrowser`, remove
  all three stale AI-edit allowances, keep widget debt at zero, and lower
  feature debt from 10 / 12 / 14 / 18 to 7 / 10 / 10 / 11.
- [x] Configure the coordinated Notes cluster at stable page/widget owners in
  Phase 133: replace the raw public card with `createNoteCardComponent`, retain
  raw rendering in private Notes UI, remove both sibling-feature edges and the
  stale allowance, keep widget debt at zero, and lower feature debt from
  7 / 10 / 10 / 11 to 6 / 8 / 8 / 9.
- [x] Configure the coordinated rich-content/rules-reference/dice/entity-link
  cluster in Phase 134: move unchanged `renderMentionText` to entity-link, keep
  raw `RulesLink` and rich renderers private behind callable factories,
  configure stable module-scope symbols in monster-stat-block, spells-browser,
  and rules-reference-modal, keep widget debt at zero, and lower feature debt
  from 6 / 8 / 8 / 9 to 4 / 4 / 4 / 5.
- [x] Complete the remaining AI/editor/images/entity-link/settings cluster in
  Phase 135: configure private AI and settings renderers through stable
  page/widget-owned factories, inject only the narrow editor entity-link
  runtime from `App`, preserve local scoped Context reads, remove the final
  production allowances, and reduce both feature and widget debt to zero
  across importer files, directed pairs, file edges, and declarations.
- [x] Migrate the first bounded lower-layer store consumer in Phase 137: keep
  Settings API/effect/save behavior feature-owned, inject a live typed runtime
  from the sidebar modal owner, make the theme switcher controlled-only, and
  enforce that Settings cannot regain direct app/shared-store facade access.
- [x] Migrate the next bounded lower-layer store read in Phase 138: keep the
  simplified-note preference live through one App-root `SimplifiedNotesProvider`,
  preserve all stable NoteCard factory identities and `false` behavior, move
  note-render consumers to the public hook, and enforce that Notes cannot
  regain direct app/shared-store facade access.
- [x] Migrate Player Questions in Phase 139: retain feature-owned result
  targeting, debouncing, scrolling, and request construction; inject its
  minimal live dice runtime from the Sidebar modal owner; mount that adapter as
  delayed modal content rather than take a click-time snapshot; and enforce
  that Player Questions cannot regain direct app/shared-store facade access.
- [x] Migrate Campaign Entity in Phase 140: retain feature-owned creation
  sanitization and custom-create short-circuiting, replace its embedded refresh
  action with a narrow widget-provided callback, preserve default create then
  refresh order and failed-create propagation, and enforce that Campaign Entity
  cannot regain direct app/shared-store facade access.
- [x] Migrate Encounter Editor in Phase 141: retain feature-owned campaign and
  session discovery plus focused add-monster orchestration, inject only modal
  error/reload/close commands from Monster Stat Block, preserve the existing
  campaign snapshot and command/error ordering, and enforce that Encounter
  Editor cannot regain direct app/shared-store facade access.
- [x] Migrate Rules Reference in Phase 142: retain its resolver, preview,
  tooltip lifecycle, and stable factories; inject only navigation and error
  commands through the App-provided runtime; let Sidebar dispatch its own
  navigation action; retire the thin helper; and enforce that Rules Reference
  cannot regain direct app/shared-store facade access.
- [x] Migrate AI attachment controls in Phase 143: retain their input,
  preparation, merge, gallery, and stable-factory behavior; inject only
  validation alerts through the App-provided runtime; keep its hook out of the
  feature root; detach the lifecycle request-ID type; and enforce that AI
  cannot regain direct app/shared-store facade access.
- [x] Migrate Editor in Phase 144: retain feature-owned selected/cancelled
  promise behavior plus Input and EditableField ordering; inject only mention
  picker requests through the App-provided runtime; keep its hook private and
  its entity-link runtime separate; and enforce that Editor cannot regain
  direct app/shared-store facade access.
- [x] Migrate Dice in Phase 145: retain feature-owned request decoding, result
  timing, animation, and local panel state; inject stable roll requests through
  the App-provided runtime; keep the only live request selector and result
  publishing callback in the narrow app-local calculator host; and enforce that
  Dice cannot regain direct app/shared-store facade access.
- [x] Migrate Images in Phase 146: retain feature-owned gallery preference
  derivation, mutations, prompt guards, and local UI state; inject only live
  preference values and modal commands through the App-wide runtime host; keep
  the Context hook private; and enforce that Images cannot regain direct
  app/shared-store facade access.
- [x] Migrate Campaign Entity Card in Phase 147: retain widget-owned draft
  validation, payload construction, creation orchestration, custom-create
  no-refresh behavior, success close, error notification, and finally unlock;
  inject only error-notification and entity-refresh commands through the
  app-local host; keep the Context hook private; and enforce that the widget
  cannot regain direct app/shared-store facade access.
- [x] Migrate Campaign Search in Phase 148: retain widget-owned malformed
  campaign narrowing, abortable loading, query/filter state, close-before-
  navigation, target coercion, and URL/hash/timer lifecycle; inject only the
  active campaign value and ordinary target navigation through the route-layout
  host around the persistent Outlet; keep the Context hook private; and enforce
  that the widget cannot regain direct app/shared-store facade access.
- [x] Migrate Campaign Entity Modal in Phase 149: retain widget-owned localized
  confirmation copy, entity validation/persistence, parent-resolver precedence,
  campaign-scope ownership, cancellation semantics, and deletion close; inject
  only confirmation and entity-refresh effects through the existing App-owned
  provider seam; and enforce that the widget cannot regain direct
  app/shared-store facade access.
- [x] Migrate Rules Reference Modal in Phase 150: retain stable content
  factories, modal opening, navigation-request de-duplication, mounted-only
  loading/error behavior, history, and tab/selection orchestration; inject only
  live navigation/history state and modal/error/history commands through the
  App-root provider; keep the hook private; and enforce that the widget cannot
  regain direct app/shared-store facade access.
- [x] Migrate Spells Browser in Phase 151: retain API loading, debounce/source
  derivation, campaign-vs-global persistence branching, localization, local
  selection, and abortable loaders; inject only live debounce/campaign/source
  state plus campaign replacement, ignored-source patch, and error commands
  through the App-root provider; keep the hook private and stable factory slot
  unchanged; and enforce that the widget cannot regain direct app/shared-store
  facade access.
- [x] Migrate Monster Stat Block in Phase 152: retain custom add override
  precedence, click-time campaign filtering, localized fallback modal config,
  feature-owned add-to-encounter adapter, token/favorite/spell behavior, and
  both raw formula roll bindings; inject only live campaigns plus modal/error/
  reload/dice commands through the App-root provider; keep the hook private and
  stable factory slots unchanged; and enforce that the widget cannot regain
  direct app/shared-store facade access.
- [x] Migrate Sidebar in Phase 153: retain campaign grouping/reordering,
  session/encounter-aware selection, gallery/import/export behavior, `forceTab`
  rules navigation, and delayed Settings/Player Questions modal content; inject
  only live navigation, modal/error, settings, and dice contracts through a
  private widget provider supplied by `app/ui/SidebarRuntimeHost.tsx` around
  both Sidebar and root modal rendering; keep `SidebarProps` unchanged; and
  enforce that the widget cannot regain direct app/shared-store facade access.
- [x] Migrate Bestiary Browser in Phase 154: retain API loading, raw sync-event
  parsing, source derivation and campaign-vs-global persistence, localization,
  custom selection/history/edit/AI behavior, delete confirmation, and stable
  composition slots; inject only live language/debounce/campaign/source/sync
  state plus campaign replacement, global-source patch, message, and
  confirmation commands through a private widget provider supplied by
  `app/ui/BestiaryBrowserRuntimeHost.tsx` around routed content; keep public
  props unchanged and enforce that the widget cannot regain direct
  app/shared-store facade access.
- [x] Migrate AI Assistant in Phase 155: retain route projection, local
  context/loading, generation/retry lifecycle, history restore/reload,
  localized copy, AI sync-event construction, and response-modal composition;
  inject only live campaign/session/encounter, prompt/language/navigation state
  plus selection/reload/refresh/sync/confirmation/message commands through a
  private widget provider supplied by `app/ui/AiAssistantRuntimeHost.tsx`
  around both routed content and the ordinary route assistant; keep
  `AiAssistantPanelProps` and Bestiary slots unchanged and enforce that the
  widget cannot regain direct app/shared-store facade access.
- [x] Migrate Session Page in Phase 156: retain session loading/normalization,
  realtime-sync decisions, editing/history/persistence, scope movement,
  encounter creation, keyboard/hash/header behavior, and quick-access
  projection; inject only live campaign/session/sync state plus narrow
  campaign/session/encounter navigation, active-session selection, campaign
  reload, entity refresh, confirmation, prompt, and message commands through a
  private page provider supplied by `app/ui/SessionPageRuntimeHost.tsx` inside
  `SessionRoute` after its campaign guard; keep `SessionPage` runtime-free and
  enforce that the page cannot regain direct app/shared-store facade access.
- [x] Migrate Encounter Page in Phase 157: retain encounter load/save,
  active-session/encounter publication, realtime sync and AI updates,
  participant workflows, HP-roll de-duplication, encounter-settings persistence,
  and localized error behavior; inject only live campaign/session/encounter,
  sync/dice, theme/language, and view-settings state plus narrow navigation,
  active-selection, reload/refresh, dice, prompt, settings, and message
  commands through a private page provider supplied by
  `app/ui/EncounterPageRuntimeHost.tsx` inside `EncounterRoute` after its
  campaign guard; keep `EncounterPage` runtime-free and enforce that the page
  cannot regain direct app/shared-store facade access.
- [x] Migrate Campaign Page in Phase 158: retain campaign/session/entity
  loading and persistence, history, sync/AI flows, graph layout/note editing,
  archive/import/export, and localized workflow copy; inject only live active
  campaign, entity-refresh/sync, theme/language state plus campaign-list,
  renamed-campaign/session navigation, graph-note modal, reload, confirmation,
  prompt, and message commands through a private page provider supplied by
  `app/ui/CampaignPageRuntimeHost.tsx` inside `CampaignRoute` after its
  campaign guard; keep `CampaignPage` runtime-free and enforce that the entire
  page cannot regain direct app/shared-store facade access.
- [x] Run focused recovery tests, performance budgets, architecture checks,
  syntax/diff hygiene, and UTF-8/replacement-character checks.
- [ ] Run the complete lint and typecheck gates after the declared local
  tooling dependencies are available.
- [x] Run the full `npm test` gate; Phase 119 closed at 398 tests, Phase 122
  passed 405/405, Phase 123 passed 409/409, Phase 124 passed 410/410, and Phase
  125 passed 411/411, Phase 126 passed 412/412, Phase 127 passed 413/413, Phase
  128 passed 414/414, Phase 129 passed 415/415, Phase 130 passed 416/416, Phase
  131 passed 417/417, Phase 132 passed 418/418, Phase 133 passed 419/419, and
  Phase 134 passed 420/420; Phase 135 passed 421/421; Phase 136 passed
  422/422; Phase 137 passes 423/423; Phase 138 passes 424/424; Phase 139
  passes 425/425; Phase 140 passes 426/426; and the expanded Phase 141 suite
  passes 427/427 tests; Phase 142 passes 428/428 tests; Phase 143 passes
  429/429 tests; Phase 144 passes 430/430 tests; Phase 145 passes 431/431
  tests; Phase 146 passes 432/432 tests; Phase 147 passes 433/433 tests; Phase
  148 passes 434/434 tests; Phase 149 passes 435/435 tests; Phase 150 passes
  436/436 tests; Phase 151 passes 437/437 tests; Phase 152 passes 438/438
  tests; Phase 153 passes 439/439 tests; Phase 154 passes 440/440 tests; Phase
  155 passes 441/441 tests; Phase 156 passes 442/442 tests; Phase 157 passes
  443/443 tests; Phase 158 passes 444/444 tests; Phase 159 passes 445/445
  tests; Phase 160 passes 446/446 tests; Phase 161 passes 447/447 tests; Phase
  162 passes 448/448 tests; Phase 163 passes 449/449 tests; Phase 164 passes
  450/450 tests; Phase 165 passes 451/451 tests; Phase 166 passes 452/452
  tests; Phase 167 passes 453/453 tests; Phase 168 passes 454/454 tests; Phase
  169 passes 455/455 tests; Phase 170 passes 456/456 tests; Phase 171 passes
  457/457 tests; Phase 172 passes 458/458 tests; Phase 173 passes 459/459
  tests.

### Recovery R6 / Phase 136 — Typed app-owned store composition

Status: **Completed**

- [x] Move the configured store's stateful and root-reducer composition into
  `src/app/model/appStore.ts` and its local `appStateReducer.ts`, while keeping
  the typed lower-level reducer policies in `shared` without an upward
  dependency.
- [x] Historical Phase 136 transition: keep `shared/model/appStore.ts` as a
  typed delegation-only compatibility boundary, migrate app/root consumers to
  `app/model/index.js`, and preserve lower-layer consumers behind that facade
  during incremental migration.
- [x] Historical Phase 136 transition: enforce the private runtime registration
  port so only the app composition owner and its one shared facade can import
  it; preserve reducer, realtime, modal, navigation, settings, selector,
  dispatch, and browser-history behavior.
- [x] Phase 159 removes both transitional files and the port checker after the
  final lower-layer migration; the `app/model` public entry is the sole store,
  selector, modal, and navigation boundary.

The accidental commit's untyped JavaScript store was not a migration candidate.
Phase 136 preserves the current typed reducer and consumer contracts, passes
422/422 tests, and changes `MD-R02` from planned to in progress rather than
claiming the compatibility facade is already removable. Phase 159 completes
that temporary transition by deleting the facade/runtime and closing `MD-R02`.

### Recovery R7 / Phase 137 — Settings injected store runtime

Status: **Completed**

- [x] Define and publicly type the minimal `SettingsModalRuntime` and
  `SettingsModalUiPatch` contracts without exposing raw Settings UI.
- [x] Move Settings' global-store reads and commands into the live
  sidebar-owned modal adapter; retain feature ownership of API calls,
  synchronization, saving, notifications, scope recovery, and fallback policy.
- [x] Make `ColorThemeSwitcher` controlled-only and prevent direct Settings
  access to `app/model` or `shared/model` with the scoped boundary rule.

Phase 137 preserves empty prompt fallback, `undefined`-only image-prompt
defaulting, empty ignored-source fallback, and false-only disabling for
auto-apply/search debounce. It passes 423/423 tests. Complete lint/typecheck
remain tracked under `MD-R04`; this phase does not claim that the shared
compatibility facade is removable.

### Recovery R8 / Phase 138 — Notes simplified preference provider

Status: **Completed**

- [x] Define the browser-only public `SimplifiedNotesProvider` and
  `useSimplifiedNotesEnabled` contract while keeping its nullable Context
  implementation private to `features/notes/ui`.
- [x] Select the one live `ui.simplifiedNotes` value at `App` and provide it
  around routes, modal hosts, and widgets; preserve valid `false` and fail fast
  when a note renderer is outside the provider.
- [x] Move NoteCard plus campaign/session/graph/entity-card presentation reads
  onto the public Notes hook, retain module-scope card factories, and prevent
  direct Notes access to `app/model` or `shared/model` with the scoped rule.

Phase 138 avoids both stale factory snapshots and wider NoteCard prop-threading
while preserving the exact simplified/classic presentation policy. It passes
424/424 tests. Complete lint/typecheck remain tracked under `MD-R04`; the
shared compatibility facade remains in progress under `MD-R02`.

### Recovery R9 / Phase 139 — Player Questions injected dice runtime

Status: **Completed**

- [x] Define and publicly type the minimal `PlayerQuestionsRuntime`, request,
  and modal-props contracts without exposing the Sidebar adapter to the feature.
- [x] Move Player Questions' global dice result read, debounce preference read,
  and request dispatch into a live Sidebar-owned modal adapter; retain feature
  ownership of result targeting, refs, virtual-list scrolling, and local state.
- [x] Prevent direct Player Questions access to `app/model` or `shared/model`
  with the scoped boundary rule while preserving the existing DiceCalculator
  request/publish pipeline and delayed modal rendering behavior.

Phase 139 preserves initial result-ID capture, exact `playerQuestions` context
and bounded integer target validation, false-only debounce behavior, one-time
result handling, and two-frame scrolling. It passes 425/425 tests. Complete
lint/typecheck remain tracked under `MD-R04`; the shared compatibility facade
remains in progress under `MD-R02`.

### Recovery R10 / Phase 140 — Campaign Entity injected refresh command

Status: **Completed**

- [x] Define and publicly type the narrow `SubmitCreateEntityOptions`
  `onRefreshEntities` command without exposing the global-store action to the
  feature.
- [x] Move refresh action construction to the two campaign-entity-card widget
  owners while retaining feature ownership of payload cleanup, client
  delegation, awaited custom creation, and fallback creation.
- [x] Prevent direct Campaign Entity access to `app/model` or `shared/model`
  with the scoped boundary rule, preserving custom-create short-circuiting,
  default create-then-refresh order, and failed-create propagation.

Phase 140 passes 426/426 tests, including mocked successful and failed default
creation plus custom-create behavior. Complete lint/typecheck remain tracked
under `MD-R04`; the shared compatibility facade remains in progress under
`MD-R02`.

### Recovery R11 / Phase 141 - Encounter Editor injected modal runtime

Status: **Completed**

- [x] Define and publicly type the narrow `AddMonsterToEncounterModalRuntime`
  error, reload, and close commands without exposing action creators or the
  global store to Encounter Editor.
- [x] Move the alert, campaign-reload action, and active-modal close mapping to
  the Monster Stat Block widget while retaining feature-owned discovery,
  focused mutation, mounted loading guard, and local submit locking.
- [x] Prevent direct Encounter Editor access to `app/model` or `shared/model`
  with the scoped boundary rule, preserving the custom add override,
  click-time campaign snapshot, success order, and failure unlock behavior.

Phase 141 passes 427/427 tests. Complete lint/typecheck remain tracked under
`MD-R04`; the shared compatibility facade remains in progress under `MD-R02`.

### Recovery R12 / Phase 142 - Rules Reference injected navigation runtime

Status: **Completed**

- [x] Define and publicly type the browser-only `RulesReferenceRuntime` and
  provider while keeping its nullable Context implementation private to
  `features/rules-reference/ui`.
- [x] Map typed navigation and alert commands in `App`, then provide them
  around route, modal, and widget trees so Rules Link factories retain live
  behavior without importing the global-store facade.
- [x] Move Sidebar to its own typed navigation dispatch, retire the thin
  `openRulesReferenceModal` helper, and prevent Rules Reference access to
  `app/model` or `shared/model` with the scoped boundary rule.

Phase 142 preserves the existing resolver-before-navigation order, failed-load
console/error-notice behavior, tooltip lifecycle, stable composition factories,
and Sidebar close-before-navigation ordering. It passes 428/428 tests.
Complete lint/typecheck remain tracked under `MD-R04`; the shared compatibility
facade remains in progress under `MD-R02`.

### Recovery R13 / Phase 143 - AI attachment injected alert runtime

Status: **Completed**

- [x] Define and publicly type the UI-only `AiAttachmentAlertRuntime` provider
  while keeping its nullable Context hook private to `features/ai/ui` and out
  of the Node/model-safe feature root.
- [x] Map the alert command once in `App` around route, modal, and widget trees
  so both existing attachment-control factories receive live behavior without
  a dispatch import or factory-signature change.
- [x] Keep attachment preparation/merge/error ordering unchanged, make AI
  generation request IDs feature-owned numbers, and prevent all AI code from
  importing `app/model` or `shared/model` with the scoped boundary rule.

Phase 143 preserves native input clearing, no-file/no-slot exits, async
attachment validation, functional merge-before-non-awaited-notice ordering,
gallery state, factory identity, and generation lifecycle stale-request checks.
It passes 429/429 tests. Complete lint/typecheck remain tracked under `MD-R04`;
the shared compatibility facade remains in progress under `MD-R02`.

### Recovery R14 / Phase 144 - Editor injected mention picker runtime

Status: **Completed**

- [x] Define and publicly type the UI-only `EditorMentionPickerRuntime`
  provider while keeping its nullable Context hook private to `features/editor/ui`
  and separate from the entity-link runtime.
- [x] Map `openMentionPicker(request)` once in `App` directly to the existing
  `openMentionPickerAction(request)` around the editor consumers without
  nesting the feature's promise adapter.
- [x] Keep selected/cancelled promise policy and Input/EditableField selection,
  insertion, focus, update, and timer ordering feature-owned; prevent all
  Editor code from importing `app/model` or `shared/model` with the scoped
  boundary rule.

Phase 144 preserves synchronous picker opening, callback/promise behavior,
Input target snapshots and cursor restoration, EditableField's selected-text
short circuit and focus-before-update picker path, and independent entity-link
resolution. It passes 430/430 tests. Complete lint/typecheck remain tracked
under `MD-R04`; the shared compatibility facade remains in progress under
`MD-R02`.

### Recovery R15 / Phase 145 - Dice injected runtime and app-owned request host

Status: **Completed**

- [x] Define and publicly type the command-only `DiceRequestRuntime` provider
  while keeping its nullable Context hook private to `features/dice`.
- [x] Map stable request actions once in `App`, while the app-local calculator
  host alone selects `state.dice.rollRequest` and injects the memoized
  result-publish callback.
- [x] Keep Dice request decoding, request-ID deduplication, rolling animation,
  timer/listener cleanup, local result/history/open ordering, and Player
  Questions auto-close behavior feature-owned; prevent all Dice code from
  importing `app/model` or `shared/model` with the scoped boundary rule.

Phase 145 preserves global request/result reducer transport, request identity,
manual/Enter/history rolls, source-aware result context, outside-click guards,
and a root App that does not subscribe to dice request state. It passes 431/431
tests. Complete lint/typecheck remain tracked under `MD-R04`; the shared
compatibility facade remains in progress under `MD-R02`.

### Recovery R16 / Phase 146 - Images injected gallery runtime

Status: **Completed**

- [x] Define and publicly type the narrow `ImageGalleryRuntime` provider in the
  Images model while keeping its nullable Context hook private to the slice.
- [x] Let the app-local host separately select the live debounce preference,
  active campaign, and ignored-source list, then map alert, confirmation, and
  prompt actions without exposing a raw store or dispatch object to Images.
- [x] Keep ignored-source derivation, debounce timing, gallery mounted-state,
  rename prompt guards, upload completion/error ordering, and bulk-delete
  confirmation normalization/loading/finally behavior feature-owned; prevent
  all Images code from importing `app/model` or `shared/model` with the scoped
  boundary rule.

Phase 146 preserves the 250/0 debounce policy, campaign source precedence,
closed-gallery controller mounting, alert/prompt/confirmation promise behavior,
folder and file rename ordering, upload completion before file clearing, and
bulk-delete partial/error lifecycle. It passes 432/432 tests. Complete
lint/typecheck remain tracked under `MD-R04`; the shared compatibility facade
remains in progress under `MD-R02`.

### Recovery R17 / Phase 147 - Campaign Entity Card injected creation runtime

Status: **Completed**

- [x] Define the narrow `CampaignEntityCreationRuntime` provider in widget UI,
  expose only its provider/types through the widget root, and keep its Context
  hook private to the slice.
- [x] Let the app-local host map only error notification and entity refresh,
  then wrap every existing card creation-button render site without exposing raw
  app state or dispatch.
- [x] Keep draft validation, payload construction, feature-owned creation,
  custom-create no-refresh behavior, success close, failure notification, and
  finally unlock widget-owned; prevent all Campaign Entity Card code from
  importing `app/model` or `shared/model` with the scoped boundary rule.

Phase 147 preserves invalid-draft notice/return, default API create followed by
exactly one refresh, custom create without fallback refresh, success close,
error notification, and submitting-state finalization. It passes 433/433 tests.
Complete lint/typecheck remain tracked under `MD-R04`; the shared compatibility
facade remains in progress under `MD-R02`.

### Recovery R18 / Phase 148 - Campaign Search injected runtime

Status: **Completed**

- [x] Define the narrow `CampaignSearchRuntime` provider in widget UI, expose
  only its provider/types through the widget root, and keep its Context hook
  private to the slice.
- [x] Let the app-local host own the sole active-campaign selector and map only
  ordinary target navigation, then mount it around the persistent
  `MainContentLayout` Outlet shared by Campaign and Session routes.
- [x] Keep malformed-campaign narrowing, index loading/cancellation, query and
  filter state, close-before-navigation, target coercion, URL/hash construction,
  80 ms scroll, and 2400 ms conditional cleanup widget-owned; prevent all
  Campaign Search code from importing `app/model` or `shared/model` with the
  scoped boundary rule.

Phase 148 preserves current campaign identity without cloning, the null-
campaign no-load path, abort suppression after every await, ordinary navigation,
and the exact hash lifecycle. It passes 434/434 tests. Complete lint/typecheck
remain tracked under `MD-R04`; the shared compatibility facade remains in
progress under `MD-R02`.

### Recovery R19 / Phase 149 - Campaign Entity Modal injected mutation runtime

Status: **Completed**

- [x] Define the narrow `CampaignEntityModalRuntime` and confirmation payload
  in widget UI, expose only their types and the existing provider through the
  widget root, and keep modal content private.
- [x] Let App map only confirmation and entity-refresh effects through the
  existing provider seam without exposing raw store state or dispatch.
- [x] Keep localized confirmation copy, entity validation, update/rename/delete
  persistence, parent-resolver precedence, campaign-scope guarding, rename
  cancellation, and close-after-delete widget-owned; prevent all Campaign
  Entity Modal code from importing `app/model` or `shared/model` with the
  scoped boundary rule.

Phase 149 preserves the ordinary update -> refresh order, localized
confirmation -> cancellation -> mention-update order, rename state replacement,
and delete -> refresh -> close order. It passes 435/435 tests. Complete
lint/typecheck remain tracked under `MD-R04`; the shared compatibility facade
remains in progress under `MD-R02`.

### Recovery R20 / Phase 150 - Rules Reference Modal injected runtime

Status: **Completed**

- [x] Define the narrow `RulesReferenceModalRuntime` provider in widget UI,
  expose only its provider/types through the widget root, and keep the Context
  hook private to the slice.
- [x] Let App select only the current navigation request, history, and open
  state; map modal, error, open-state, and history commands once with stable
  identities; and scope the provider over the existing factory consumers and
  modal host.
- [x] Keep stable content factories, request de-duplication, mounted-only
  load/error guards, history navigation, tab/selection behavior, and modal
  opening widget-owned; prevent all Rules Reference Modal code from importing
  `app/model` or `shared/model` with the scoped boundary rule.

Phase 150 preserves the existing modal-open request order, active-request
identity checks, history update -> local selection order, modal open/cleanup
effects, and localized load-error behavior. It passes 436/436 tests. Complete
lint/typecheck remain tracked under `MD-R04`; the shared compatibility facade
remains in progress under `MD-R02`.

### Recovery R21 / Phase 151 - Spells Browser injected runtime

Status: **Completed**

- [x] Define the narrow `SpellsBrowserRuntime` provider in widget UI, expose
  only its provider/types through the widget root, and keep the Context hook
  private to the slice.
- [x] Let App select only the debounce policy, active campaign/source settings,
  and global ignored sources; map campaign replacement, ignored-source patch,
  and error commands once with stable identities; and scope the provider over
  route, factory, and modal rendering.
- [x] Keep API loading, source derivation, campaign-vs-global persistence,
  localization, local selection, initial scroll, and abortable loader behavior
  widget-owned; prevent all Spells Browser code from importing `app/model` or
  `shared/model` with the scoped boundary rule.

Phase 151 preserves the `useSearchDebounce !== false` 250/0 policy, raw active
campaign source normalization, campaign update -> list -> replacement ordering,
global settings update -> ignored-source patch ordering, and localized
console-error -> alert behavior. It passes 437/437 tests. Complete
lint/typecheck remain tracked under `MD-R04`; the shared compatibility facade
remains in progress under `MD-R02`.

### Recovery R22 / Phase 152 - Monster Stat Block injected runtime

Status: **Completed**

- [x] Define the narrow `MonsterStatBlockRuntime` provider in widget UI,
  expose only its provider/types through the widget root, and keep the Context
  hook private to the slice.
- [x] Let App reuse its live campaign snapshot and map modal, error, reload,
  close, and dice commands once with stable identities; scope the provider over
  route, factory, and modal rendering.
- [x] Keep custom add override precedence, click-time campaign filtering,
  localized fallback modal config, feature-owned add-to-encounter adapter,
  token/favorite/spell behavior, and both dice call sites widget-owned; prevent
  all Monster Stat Block code from importing `app/model` or `shared/model` with
  the scoped boundary rule.

Phase 152 preserves the custom add early return, fallback modal config and
filtered campaign snapshot, feature API success -> reload -> close(true) and
failure -> notice -> unlock behavior, and both formula requests. It passes
438/438 tests. Complete lint/typecheck remain tracked under `MD-R04`; the
shared compatibility facade remains in progress under `MD-R02`.

### Recovery R23 / Phase 153 - Sidebar injected runtime

Status: **Completed**

- [x] Define the widget-owned `SidebarRuntime` provider with navigation,
  modal/error, Rules Reference, Settings, and Player Questions dice contracts;
  expose only its provider/types through the widget root and keep the Context
  hook private to the slice.
- [x] Let `app/ui/SidebarRuntimeHost.tsx` own the live selectors and stable
  action mapping, and scope its provider around both Sidebar and root modal
  rendering so delayed modal children retain live reads after opening.
- [x] Keep Sidebar's public props, campaign grouping/reorder/selection,
  import/export and error/finally ordering, close-before-open/navigation,
  `forceTab`, Settings factory, and false-only Player Questions debounce policy
  unchanged; prevent all Sidebar code from importing `app/model` or
  `shared/model` with the scoped boundary rule.

Phase 153 preserves numeric encounter IDs, delayed Settings/Player Questions
modal updates, import strategy -> close -> deferred file-click order, and
Rules Reference `forceTab` forwarding. It passes 439/439 tests. Complete
lint/typecheck remain tracked under `MD-R04`; the shared compatibility facade
remains in progress under `MD-R02`.

### Recovery R24 / Phase 154 - Bestiary Browser injected runtime

Status: **Completed**

- [x] Define the widget-owned `BestiaryBrowserRuntime` provider with live
  language, false-only debounce, active campaign/source settings, and raw sync
  event state, plus campaign replacement, global-source patch, message, and
  confirmation commands; expose only provider/types through the widget root and
  keep the Context hook private to the slice.
- [x] Let `app/ui/BestiaryBrowserRuntimeHost.tsx` own the live selectors and
  stable action mapping, and scope the provider around `MainContentLayout`'s
  routed `<Outlet />` without threading runtime props through `EncounterPage`.
- [x] Keep Bestiary API loaders, raw sync parsing/effect dependency,
  campaign-versus-global selected-source persistence order, localized message
  copy, confirmation-before-delete, custom history/edit/AI behavior, and all
  public composition slots unchanged; prevent every Bestiary Browser file from
  importing `app/model` or `shared/model` with the scoped boundary rule.

Phase 154 preserves the raw sync event identity before parsing, `false`-only
search-debounce policy, source-save update -> list -> replacement behavior,
confirmation-before-delete order, and runtime-free public props. It passes
440/440 tests. Complete lint/typecheck remain tracked under `MD-R04`; the
shared compatibility facade remains in progress under `MD-R02`.

### Recovery R25 / Phase 155 - AI Assistant injected runtime

Status: **Completed**

- [x] Define the widget-owned `AiAssistantRuntime` provider with live active
  campaign/session/encounter, language, global/campaign prompt, and navigation
  state plus narrow selection, campaign reload, entity refresh, AI sync,
  confirmation, and message commands; expose only provider/types through the
  widget root and keep the Context hook private to the slice.
- [x] Let `app/ui/AiAssistantRuntimeHost.tsx` own the live selectors and stable
  action mapping, and scope the provider around both `MainContentLayout`'s
  routed `<Outlet />` and route-level `<AiAssistantPanel />` so the injected
  Bestiary assistant remains covered without threading runtime props through
  `EncounterPage` or Bestiary composition.
- [x] Keep route projection, local context/loading, generation/retry lifecycle,
  history confirmation/restore/reload, localized error copy, raw AI sync-event
  construction, and response-modal composition widget-owned; prevent every AI
  Assistant file from importing `app/model` or `shared/model` with the scoped
  boundary rule.

Phase 155 preserves sync payload ordering, confirmation Boolean coercion,
reload-before-conditional-refresh behavior, generation/retry error ordering,
route-state identity, and runtime-free `AiAssistantPanelProps`/Bestiary slots.
It passes 441/441 tests. Complete lint/typecheck remain tracked under `MD-R04`;
the shared compatibility facade remains in progress under `MD-R02`.

### Recovery R26 / Phase 156 - Session Page injected runtime

Status: **Completed**

- [x] Define the page-owned `SessionPageRuntime` provider with live active
  campaign/session/sync state plus narrow campaign/session/encounter navigation,
  active-session selection, campaign reload, entity refresh, confirmation,
  prompt, and message commands; expose only provider/types through the page
  root and keep the Context hook private to the slice.
- [x] Let `app/ui/SessionPageRuntimeHost.tsx` own the live selectors and stable
  action mapping, and scope the provider around `<SessionPage />` in
  `SessionRoute` only after its active-campaign guard without threading a
  runtime prop through the public page API.
- [x] Keep session loading/normalization, realtime-sync decisions,
  editing/history/persistence, scope movement, encounter creation,
  keyboard/hash/header behavior, localized modal copy, and quick-access
  projection page-owned; prevent every Session Page file from importing
  `app/model` or `shared/model` with the scoped boundary rule.

Phase 156 preserves active-session publication, truthy-version compatible-sync
gating, AI discard-before-reload behavior, rename replace-then-reload order,
scope-move refresh-then-reload order, confirmation/prompt coercion, and
ordinary/new-tab encounter navigation. One added static regression brings the
complete suite to 442/442 tests. Complete lint/typecheck remain tracked under
`MD-R04`; the shared compatibility facade remains in progress under `MD-R02`.

### Recovery R27 / Phase 157 - Encounter Page injected runtime

Status: **Completed**

- [x] Define the page-owned `EncounterPageRuntime` provider with live active
  campaign, session/encounter identity, sync event, dice result, theme,
  language, and encounter-view/grid settings state plus narrow session
  navigation, active-selection, campaign reload, entity refresh, dice, prompt,
  UI-settings patch, and message commands; expose only provider/types through
  the page root and keep the Context hook private to the slice.
- [x] Let `app/ui/EncounterPageRuntimeHost.tsx` own the live selectors and
  stable action mapping, and scope the provider around `<EncounterPage />` in
  `EncounterRoute` only after its active-campaign guard without threading a
  runtime prop through the public page API.
- [x] Keep encounter loading/saving, active-session/encounter publication,
  realtime sync and AI update handling, participant editing/creation, HP dice
  request/de-duplication, UI-settings persistence, and localized error/finally
  behavior page-owned; prevent every Encounter Page file from importing
  `app/model` or `shared/model` with the scoped full-form boundary rule.

Phase 157 preserves saved-session publication before reload, raw sync-event and
dice-result identity, numeric-zero encounter identity, AI update then campaign
reload ordering, local settings update before API persistence, HP-roll context
and de-duplication, participant refresh behavior, and the runtime-free
`EncounterPage` API. One added static regression brings the complete suite to
443/443 tests. Complete lint/typecheck remain tracked under `MD-R04`; the
shared compatibility facade remains in progress under `MD-R02`.

### Recovery R28 / Phase 158 - Campaign Page injected runtime

Status: **Completed**

- [x] Define the page-owned `CampaignPageRuntime` provider with live active
  campaign, entity-refresh version, sync event, theme, and language state plus
  narrow campaign-list, renamed-campaign, and session navigation; graph-note
  modal; campaign reload; confirmation; prompt; and message commands. Expose
  only provider/types through the page root and keep the Context hook private
  to the slice.
- [x] Let `app/ui/CampaignPageRuntimeHost.tsx` own the live selectors and
  stable action/navigation/modal mapping, and scope the provider around
  `<CampaignPage />` in `CampaignRoute` only after its active-campaign guard
  without threading a runtime prop through the public page API.
- [x] Keep campaign/session/entity loading and persistence, history, sync/AI
  flows, graph layout and note editing, archive/import/export, and localized
  workflow copy page-owned; prevent every Campaign Page file from importing
  `app/model` or `shared/model` with the scoped full-form boundary rule.

Phase 158 preserves campaign/session navigation argument order, rename reload
before replace-navigation, deletion navigation then reload, session-creation
append → navigation → reload, confirmation/prompt semantics, entity refresh
and sync behavior, graph theme/language identity, graph-note modal behavior,
and runtime-free `CampaignPage` contracts. One added static regression brings
the complete suite to 444/444 tests; architecture, performance, and Ukrainian
encoding checks pass. Complete lint/typecheck remain tracked under `MD-R04`.
Phase 159 performs the planned retirement of the typed compatibility facade
and closes `MD-R02`.

### Recovery R29 / Phase 159 - Shared store-facade retirement

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Delete `src/shared/model/appStore.ts` and
  `src/shared/model/appStoreRuntime.ts`, and remove their barrel/runtime
  registration plus the transitional app-store-runtime-owner checker.
- [x] Keep `app/model` as the sole public owner of the stateful store,
  selectors, modal coordination, and navigation; move
  `SessionPageRuntimeHost` navigation to that entry.
- [x] Restrict `shared/model` to global action contracts/creators, action/state
  types, and lower-level reducer policies. Lower slices stay behind their
  injected runtimes and existing scoped facade-boundary rules.

Phase 159 closes `MD-R02`: no shared global store facade/runtime remains, and
the current source/import inventory leaves app-owned hosts on `app/model` while
lower layers have no direct global-store access. Do not recreate the facade,
runtime port, or retired checker. One added static regression brings the full
suite to 445/445 tests; architecture, performance, and Ukrainian encoding
checks pass. `MD-R04` remains open for complete lint/typecheck.

### Recovery R30 / Phase 160 - App-owned mention-picker modal host

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move the global mention-picker request subscription, active-campaign
  entity reads, callback validation, option projection, and modal
  open/close/cancel/error lifecycle out of `App.tsx` into
  `app/ui/MentionPickerModalHost.tsx`.
- [x] Keep `App` as the narrow adapter that maps
  `EditorMentionPickerRuntime.openMentionPicker(request)` to the existing
  `openMentionPickerAction(request)`, and mount the modal host inside that
  provider so editor UI continues to receive its injected runtime.
- [x] Keep `requestMentionSelection`, `Input`, `EditableField`, and
  `MentionPickerModalContent` feature-owned. The host may coordinate app state,
  entity reads, and modal effects but must not move editor promise or selection
  policy upward or give the feature direct store access.

Phase 160 continues app-shell consolidation after the Phase 159 closure of
`MD-R02`. It removes a bounded global workflow from root composition without
recreating a shared store facade: `app/model` remains the sole stateful
store/selector/modal/navigation owner and `shared/model` remains
action/type/reducer policy only. Static source-inventory coverage protects the
host/provider placement and preserved editor boundary. One added static
regression brings the full suite to 446/446 tests; architecture, performance,
and Ukrainian encoding checks pass. `MD-R04` remains open for complete
lint/typecheck.

### Recovery R31 / Phase 161 - Private Rules Reference tab loader

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move the modal's mounted state, requested-tab/controller refs, tab-load
  effect, API/`AbortController` lifecycle, loaded-selection application,
  localized failure reporting, retry release, and loading finalization into
  private `widgets/rules-reference-modal/ui/useReferenceTabLoading.ts`.
- [x] Keep `RulesReferenceModalContent.tsx` responsible for local
  navigation/history, search, selection, scroll, and view composition, passing
  only its setters and injected runtime commands to the private hook.
- [x] Preserve active-request identity, abort cleanup and silence, custom
  Bestiary abort propagation, mounted result/error/finalization guards,
  selection defaults, and the existing error-before-loading-finalization
  sequence.
- [x] Keep API/abort effects out of the Node-safe modal model and keep the
  hook outside the public widget entry; the runtime provider and stable content
  factory remain unchanged.

Phase 161 is a widget-local complexity split, not a new shared loader or a
public API expansion. The Rules Reference Modal remains the sole owner of its
seven-tab aggregation and browser lifecycle, while `model.js` retains pure
policy only. `MD-R02` remains closed and `MD-R04` remains open for complete
lint/typecheck. The full `npm test` gate passes 447/447 tests; architecture,
performance, and Ukrainian encoding checks also pass.

### Recovery R32 / Phase 162 - Private AI response creature-field editing

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move creature-field editor state coordination, applied/draft/object
  eligibility, identity preservation, encounter-participant replacement, and
  draft update/close sequencing into private
  `widgets/ai-response-modal/ui/aiResponseCreatureFieldEditing.ts`.
- [x] Keep `AiResponseModal.tsx` as the private raw renderer/composition owner:
  its `useState` -> draft-controller -> generated-prompt guard ordering,
  configured factory, injected slot contracts, card/stat-block rendering, and
  final monster-editor placement remain unchanged.
- [x] Keep the controller outside widget public runtime/type/model entries and
  retain its internal-only import from the raw renderer.

Phase 162 isolates a widget-local editing controller; it neither creates a
shared editing policy nor widens the configured response-modal API. The full
`npm test` gate passes 448/448 tests; architecture, performance, and Ukrainian
encoding checks also pass. `MD-R02` remains closed and `MD-R04` remains open
for complete lint/typecheck.

### Recovery R33 / Phase 163 - Private AI response JSON-diff presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move JSON-diff resource, field-summary, and line presentation into
  private `widgets/ai-response-modal/ui/AiResponseJsonDiff.tsx`.
- [x] Preserve resource/field/line keys, localized labels, resource-state
  lookup, exact line-state CSS class and marker routing, and falsy number/text
  fallbacks.
- [x] Keep the raw renderer's configured factory, state/controller/guard
  ordering, injected slots, preview tree, editor placement, and the View's
  modal/diff-switch/draft-editor composition unchanged.
- [x] Keep the component outside widget public runtime/type/model entries and
  retain its internal-only import from the raw renderer.

Phase 163 is private presentation ownership only, not a new public response
format or a shared diff renderer. The full `npm test` gate passes 449/449
tests; architecture, performance, and Ukrainian encoding checks also pass.
`MD-R02` remains closed and `MD-R04` remains open for complete lint/typecheck.

### Recovery R34 / Phase 164 - Private AI response generic snapshot-diff presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move generic single-snapshot and before/after field presentation into
  private `widgets/ai-response-modal/ui/AiResponseGenericDiff.tsx`.
- [x] Preserve snapshot selection, metadata exclusion, fallback field values,
  field ordering, nested notes delegation, resource/field keys, localized
  Before/After labels, formatted values, and CSS classes.
- [x] Keep the raw renderer's configured factory, state/controller/guard
  ordering, injected slots, preview tree, editor placement, and the View's
  modal/diff-switch/draft-editor composition unchanged.
- [x] Keep the component outside widget public runtime/type/model entries and
  retain its internal-only import from the raw renderer.

Phase 164 is private presentation ownership only, not a new public response
format or a shared diff renderer. The full `npm test` gate passes 450/450
tests; architecture, performance, and Ukrainian encoding checks also pass.
`MD-R02` remains closed and `MD-R04` remains open for complete lint/typecheck.

### Recovery R35 / Phase 165 - Private AI response card-diff presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move generic card new/deleted/changed presentation into private
  `widgets/ai-response-modal/ui/AiResponseCardDiff.tsx`.
- [x] Preserve branch routing, snapshot selection, localized labels,
  card-surface classes, draft/apply edit eligibility, highlight construction,
  and callback argument order.
- [x] Keep the raw renderer's configured factory, state/controller/guard
  ordering, all injected card/editor JSX slots, outer resource wrapper/header/
  key, preview tree, editor placement, and the View's modal/diff-switch/
  draft-editor composition unchanged.
- [x] Keep the component outside widget public runtime/type/model entries and
  retain its internal-only import from the raw renderer.

Phase 165 is private presentation ownership only, not a new public response
format or a shared card renderer. The full `npm test` gate passes 451/451
tests; architecture, performance, and Ukrainian encoding checks also pass.
`MD-R02` remains closed and `MD-R04` remains open for complete lint/typecheck.

### Recovery R36 / Phase 166 - Private AI response encounter participant-list presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move encounter participant-list presentation into private
  `widgets/ai-response-modal/ui/AiResponseEncounterParticipantList.tsx`.
- [x] Preserve before/after entry derivation and maps, empty-state copy,
  participant keys, ordered before/after/mutated class routing, participant
  names, and both metadata evaluations.
- [x] Keep the raw renderer's configured factory, state/controller/guard
  ordering, encounter-side panels, encounter MonsterStatBlock slots, encounter
  editing/resource composition, preview tree, editor placement, and the View's
  modal/diff-switch/draft-editor composition unchanged.
- [x] Keep the component outside widget public runtime/type/model entries and
  retain its internal-only import from the raw renderer.

Phase 166 is private presentation ownership only, not a new public response
format or a shared encounter renderer. The full `npm test` gate passes 452/452
tests; architecture, performance, and Ukrainian encoding checks also pass.
`MD-R02` remains closed and `MD-R04` remains open for complete lint/typecheck.

### Recovery R37 / Phase 167 - Private Bestiary header-action presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move Bestiary Browser header-action presentation/action dispatch and its
  menu-local state, refs, and pointer-close lifecycle into private
  `widgets/bestiary-browser/ui/BestiaryHeaderActions.tsx`.
- [x] Keep custom-monster import parsing/persistence/messages, JSON export,
  undo/redo operations and stacks, and `BestiaryContent` composition in
  `BestiaryBrowser`.
- [x] Preserve the always-mounted menu, hidden `.json` input, functional
  toggle, pointer guard/listener cleanup, classes/titles/icons/copy,
  close-before-import click/export/undo/redo order, and disabled capability
  flags.
- [x] Keep the component outside widget public runtime/type/model entries and
  retain its internal-only import from the raw browser.

Phase 167 is private presentation/action-dispatch ownership only, not a new
public Bestiary workflow. The full `npm test` gate passes 453/453 tests;
architecture, performance, and Ukrainian encoding checks also pass. `MD-R02`
remains closed and `MD-R04` remains open for complete lint/typecheck.

### Recovery R38 / Phase 168 - Shared header pointer-dismissal lifecycle

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Extract the identical open-gated document `pointerdown` dismissal into
  public `shared/ui/usePointerDownOutsideDismissal.ts`.
- [x] Migrate Campaign Page, Session Page, and private Bestiary Header Actions
  while keeping each consumer's state, root ref, menu presentation, and
  commands local.
- [x] Preserve the `Node` containment guard, inside-root no-op,
  outside/non-Node close, listener cleanup, functional toggles, and
  close-before-action behavior.
- [x] Export only the hook through the shared UI JavaScript/type barrels; do
  not change the distinct Encounter/Dice dismissal policies or the Select/
  MultiSelect `mousedown` portal lifecycle.

Phase 168 is shared lifecycle reuse only, not shared header/menu presentation
or a new Bestiary workflow. The full `npm test` gate passes 454/454 tests;
architecture, performance, and Ukrainian encoding checks also pass. `MD-R02`
remains closed and `MD-R04` remains open for complete lint/typecheck.

### Recovery R39 / Phase 169 - Private AI response note-diff presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move AI Response Modal note-diff presentation into private
  `widgets/ai-response-modal/ui/AiResponseNoteDiff.tsx`.
- [x] Keep the configured raw renderer, state/controller/guard order, injected
  card/editor slots, `AiResponseNoteCard` factory, `renderNoteCard` draft
  callback, twice-resolved resource flow, nested note-array synthesis, preview
  classification, and editor placement in `AiResponseModal`.
- [x] Preserve single new/deleted and changed Before/After routing, snapshot
  selection, note-surface classes, localized labels, draft/apply eligibility,
  highlight construction, resource key/header, and callback argument order.
- [x] Keep the component outside widget public runtime/type/model entries and
  retain its internal-only import from the raw modal.

Phase 169 is private note-diff presentation only, not a new public AI Response
workflow. The full `npm test` gate passes 455/455 tests; architecture,
performance, and Ukrainian encoding checks also pass. `MD-R02` remains closed
and `MD-R04` remains open for complete lint/typecheck.

### Recovery R40 / Phase 170 - Private Campaign header-action presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move Campaign Page header-action presentation, open state/root ref, and
  shared pointer-dismissal composition into private
  `pages/campaign/ui/components/CampaignHeaderActions.tsx`.
- [x] Retain CampaignHeader's campaign title/rename/created metadata composition
  and pass only narrow search, partial-archive, undo/redo, export, and deletion
  commands to the private component.
- [x] Consolidate the duplicate Campaign/Session undo-redo button pair in public
  `shared/ui/UndoRedoButtons.tsx`, preserving each caller's capability and
  saving-disabled flags without adding a DOM wrapper or workflow ownership.
- [x] Preserve the always-mounted menu, classes/titles/icons/localized copy,
  shared pointer dismissal, functional toggle, direct search/undo/redo
  callbacks, and close-before-action order; keep the page component private and
  free of new page-root/runtime/store access.

Phase 170 is page-local header presentation/lifecycle ownership plus focused
shared button reuse, not a new public campaign workflow. The full `npm test`
gate passes 456/456 tests; architecture, performance, and Ukrainian encoding
checks also pass. `MD-R02` remains closed and `MD-R04` remains open for complete
lint/typecheck.

### Recovery R41 / Phase 171 - Private AI response encounter monster-change presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move AI Response Modal encounter monster-change presentation into private
  `widgets/ai-response-modal/ui/AiResponseEncounterMonsterChange.tsx`.
- [x] Keep the configured raw renderer, state/controller/guard order,
  `renderEncounterMonsterCard` MonsterStatBlock/field-edit slot, participant
  entries/maps/change detection, single/paired routing, draft/apply eligibility,
  highlight construction, localized labels, resource keys, and encounter
  composition in `AiResponseModal`.
- [x] Pass only precomputed card/label slots to the private component while
  preserving single/paired stack/column/frame/title markup and render order.
- [x] Keep the component outside widget public runtime/type/model entries and
  retain its internal-only import from the raw modal.

Phase 171 is private encounter monster-change presentation only, not a new
public AI Response workflow. The full `npm test` gate passes 457/457 tests;
architecture, performance, and Ukrainian encoding checks also pass. `MD-R02`
remains closed and `MD-R04` remains open for complete lint/typecheck.

### Recovery R42 / Phase 172 - Private Monster action-section presentation

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move Monster Field Edit Modal action-section presentation into private
  `features/edit-monster/ui/MonsterActionSections.tsx`.
- [x] Keep draft/JSON synchronization, `updateAction`, functional action
  mutations, Ctrl+K shortcut detection/selection state, parsing, save, and
  error lifecycle in the raw `MonsterFieldEditModal`.
- [x] Forward original change/keyboard events to raw callbacks so it continues
  to read event values and construct `{ type: "action", section, index }` rule
  targets under the existing functional-update path.
- [x] Preserve Fragment-only output, section order, `${section.key}-${index}`
  keys, class hierarchy, localized copy, empty/add/remove/name/text behavior,
  and private-only feature ownership.

Phase 172 is feature-local action presentation only, not a new public Monster
editing workflow. The full `npm test` gate passes 458/458 tests; architecture,
performance, and Ukrainian encoding checks also pass. `MD-R02` remains closed
and `MD-R04` remains open for complete lint/typecheck.

### Recovery R43 / Phase 173 - Private Monster fields-mode layout

Status: **Completed** (complete lint/typecheck remains tracked under `MD-R04`)

- [x] Move Monster Field Edit Modal fields-mode layout into private
  `features/edit-monster/ui/MonsterFieldSections.tsx`.
- [x] Keep draft/JSON synchronization, raw field renderers and reads, select
  fallback, update and Ctrl+K rule-picker callbacks, action composition,
  parsing, save, error, and modal lifecycle in the raw `MonsterFieldEditModal`.
- [x] Precompute basic, ability, text, and action slots in raw evaluation order;
  preserve the four existing Fragment-only wrapper groups, field order/keys,
  classes, disabled source, custom select option, and textarea presentation.
- [x] Keep the component outside feature public runtime/type/model entries and
  retain its internal-only import from the raw modal.

Phase 173 is feature-local fields-mode presentation only, not a new public
Monster editing workflow. The full `npm test` gate passes 459/459 tests;
architecture, performance, and Ukrainian encoding checks also pass. `MD-R02`
remains closed and `MD-R04` remains open for complete lint/typecheck.

## Validation required for every phase

Run:

```text
npm test
npm run lint
npm run check:architecture
npm run check:performance
npm run typecheck
```

Do not use the production build as routine validation. Check changed Ukrainian text for valid UTF-8 and replacement characters.
